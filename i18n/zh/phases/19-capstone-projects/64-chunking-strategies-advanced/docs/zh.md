# 分析策略,比较

> 碎决定你的回升器能出现什么, 错误地划界限, 没有嵌入模型, 没有重排器, 没有LLM可以修复下游的损坏.

> **【中文解读】**分块决定检查器"能挽到什么"――边界切错了,下游再强的嵌入模型、重排序器、LLM 都救不回来――本课并排实现五种分块策略固定窗户、句子、递归切分、语义聚类、结构化 标签下标题在带金标答案区间的固定语料上跑回忆@k对比,让你亲眼看看不同的语料形态下谁赢谁输,以及每个策略的各自注入的失败模式――

> **【拓展：分块在 RAG 产业链中的位置】**分块是RAG流水线的第一站,也是被低估最深的站点:LongRAG(2024)实测仅分块策略就能造成35个百分点的检索召回绝对摆动;人类的上下文检索(上文检索,2025)给每个块补充下文头条也只是收收窄而未消除差距.本课是RAG深入路线的基:65 课的混合检索,66 课的重排,68 课的评测线束全消耗了这里出来的块.

>  **【前置】**学本课前请先掌握:阶段11 · 04(嵌入) 、06(RAG 基础)、07(进阶 RAG) 嵌入与检索的上下游关系;阶段19 轨道B 基础(20-29 课) DenseIndex的形状来自那里──术语约定:chunking=分块、recall@k=前 k 命中率、金间跨度区──

**Type:** Build | **类型:** 构建
**Languages:** Python | **语言:** Python
**Prerequisites:** Phase 11 lessons 04 (embeddings), 06 (RAG), 07 (advanced RAG); Phase 19 Track B foundations (lessons 20-29) | **前置知识:** Phase 11 · 04（嵌入）、06（RAG）、07（进阶 RAG）；Phase 19 Track B 基础（20-29 课）
**Time:** ~90 minutes | **时间:** 约 90 分钟

## 学习目标
- 从零开始实施五种分断策略:固定窗口,句子,递归分断,语义集群和结构分类标题.
  中文翻译:从零实现五种分块策略:固定窗口、句子、递归切分、语义聚类和结构化 标题切分──
- 测量以黄金标记的答案范围为止的固定材料体内 recall@k,并解释为什么一个策略在散文上获胜,而另一种策略在技术文件上获胜.
  中文翻译:在带金标答案区间的固定语料上度回忆@k,并解释为什么一种策略在散文体中赢"",另一种在技术文档中赢──
- 阅读一段时间分布,并识别每个策略所注入的失败模式:孤儿句子,中标的切割,仅标题的部分,语义漂移.
  中文翻译:读懂块长度分布,识别每条策略注入的失败模式:孤儿句、符号中途截断、纯标题块、语义漂移──
- 通过检查三个属性来选择一个新的体积的默认,而不运行基准:文档类型,平均段子长度,以及格式是否具有明确的结构.
  中文翻译:不跑基准,只需检查三个属性文档类型、平均段落长度、格式是否自带显式结构为新语料选择默认策略──

## 问题 问题引入

> **【中文解读】**本节立论:切在哪里不是超参数,而是检查器召回的上限.一个"预算停止值是多少"的问题,只有当包含值的块完整才能成功固定窗口把数值从下文切开的那一刻,嵌入漂移、BM25 降分、重排序器看到噪声,LLM的答案随出错.

每个RAG管道都开始切割源文档成足够小的部分,以使嵌入模型适合它们,并且足够大,以使每个部分都具有独立的想法.切割地点的选择不是一个超参数.这是检索器能回归的上限.

> 每条RAG流水线都从把源文档分块开始:块必须小到嵌入模型装载,再要大到每个块都承载一个自足的观点――在哪里切不是超参数,而是检查器所能回归内容的上限――

问"预算中断门是什么样子"的查询只能成功, 如果固定窗口分区器从周围的环境中切除了门值,嵌入将转移到不同的集群,BM25分数下降,重排查器看到噪音,LLM生成的答案是错误的. 2024年"LongRAG:通过长文本LLM增强回收生成"的论文仅仅是从分量选择中测量了回收回收的绝对转变率为35%. 后续工作在2025年对文本部分标题缩小了差距,但并没有缩小它.

> 询问"预算停止值长什么样"的查询,只有在持有停止值的内容块中才可达成才可能成功. 如果固定窗口切割器把值数值从上下文中切开,嵌入就漂移到另一个聚类,BM25得分下降,重排查器看到的是噪音,LLM 生成的答案随之出错.2024年论文"LongRAG:增强复苏-增强的代子与长文下LLM"实践:仅分块策略的选择就会带来35个百分点的检查索对摆摆.2025年上下文的后续块收到了狭窄差距,但没有消除.

这一课将五种策略放在一边, 运行它们与金标记的答案范围的固定组合,

> 本课程并排列构建五种策略,运行在带金标答案区间的固定语料上,让你亲自阅读召回数字.

## 概念的核心概念

> **【中文解读】**五种策略是一条"从盲到聪明"光谱:固定窗户按字符硬切(对照组);句子切分按句界打包;递归切分按分隔层级逐级降低;语义聚类按句嵌入质心漂移切;结构化 标题层级切点 越往右越尊重语义,但对语料形态的要求也越苛刻――评测口径是 recall@k:金标答案区间与任一前的 k块有重叠记号 1,否则记号 0――

```mermaid
flowchart LR
  Doc[Source Document] --> S1[Fixed Window]
  Doc --> S2[Sentence]
  Doc --> S3[Recursive Split]
  Doc --> S4[Semantic Cluster]
  Doc --> S5[Structural Markdown]
  S1 --> Chunks1[Chunks]
  S2 --> Chunks2[Chunks]
  S3 --> Chunks3[Chunks]
  S4 --> Chunks4[Chunks]
  S5 --> Chunks5[Chunks]
  Chunks1 --> Index[Embedding Index]
  Chunks2 --> Index
  Chunks3 --> Index
  Chunks4 --> Index
  Chunks5 --> Index
  Index --> Eval[Recall@k vs Gold Spans]
```

### 固定窗口

粗力基线.切除每一个N字符.可选地重叠,因此在N位置切断的句子在N位置开始的部分内出现完整.在边界快速,确定性,可怕.用它作为控制,而不是默认.

> 暴力基线──每 N个字符切一刀──可重叠:在位置 N处被切断的句子将完整地出现现在从位置 N - 叠加开始的那块里──快、确定性、边界质量糟糕──把它当对照组用,别当默认值──

### 判决

通过Regex或简单的状态机划分句子边界.将一个或多个句子包装成一个部分,达到目标字符预算.停止切割中文字.仍然切割中段和中段.许多早期RAG管道中的默认和没有其他结构的散文的合理选择.

> 用正则或简单状态机按句子边界切分. 把一个或多个句子包装成块,直到目标字符预算.不会把词切成两半,但仍然会在段落中间切断.这是许多早期的RAG流水线的默认选择,对没有其他结构的散文是合理的方案.

### 复发分

根据"二次新线"的定义,在一个区别中,一个区别是"二次新线" (二次新线,一段),然后是"单一新线",然后是"字符".当部分适合预算时,复制结束.在文件中,有不一致的结构,因为它适应每个地区.

> 首先尝试按最强分隔符,退而求其次,再到句子,最后到字符,归归于块装入预算时终止,对结构不一致的文档很强,因为它根据区域自适应.

### 语义集群

嵌入每个句子. 集结连接句子,共享一个主题中位数. 切除每当与中位数的运行相似性下降到门. 边界反映了意义,而不是字符. 缓慢构建,依赖嵌入模型,但对在段落内更换主题的文档具有弹性.

> 给每个句子嵌入. 把共享主题质心的连续句子聚合一. 一旦与质心的运行相似度下降,就切一刀.边界反映了意义而不是字符. 构建更慢,依赖于嵌入模型,但对段内切换主题的文档有性.

### 结构性标记标题

对于包含明确结构的文件 (标记,重构文本,RFC式编号部分),切断标题边界.每个部分成为标题加上下面的所有内容,并将其降至下一个标题,以相同或更高的水平.每个主题的最小部分,但只有在体积形成良好时才可用.

> 按标题边界切片,每个块 = 标题 + 其下直到同级或更高级标题之前的全部内容.

### 如何测量边界选择

标有金色的查询包含源文档内部的答案跨度的确切字符抵消. 碎后,你问: 取器回来的任何一块是否覆盖了黄金跨度? 如果是,那么对此查询的 recall@k 为 1. 如果没有,则是0. 查询组中平均值 对于每个策略进行相同的评估, 扩散显示了您在您的结构中存活的边界政策.

> 金标查带答案区间在源文档中精确字符偏移. 分块后你问:检查器返回的前 k 块中是否有任何块与金标区间重叠? 有则该查询回忆@k 记 1,无则记 0,在查询集中平均取.对每个策略的运行同样的评价,差距分布会告诉你哪些边界策略可以活跃在你的手头语料上.

```figure
ci-chunk-boundaries
```

## 建立它,实现它.

> **【中文解读】** `code/main.py`实现五种策略,以实现同签名的分分函数,配合一个哈希式确定性模拟,`DenseIndex`沿线B 混合检索课程的形状,`eval_recall`转换成一个"策略 × k"的召回表:句子策略在结构化语料上输输,结构化 标记下降 在标记下降语料上赢,归归切分靠自适应在混合语料上守住,语义聚类在无结构线索的散文语料上胜出.

`code/main.py`执行:

- `fixed_window(text, size, overlap)`- 基本线.
  翻译: 中文`fixed_window(text, size, overlap)`基线――
- `sentence_chunks(text, target)`- - 简单的句子包装.
  翻译: 中文`sentence_chunks(text, target)`简单的句子打包器――
- `recursive_split(text, separators, target)`- 层次回归.
  翻译: 中文`recursive_split(text, separators, target)`层级递归――
- `semantic_chunks(text, similarity_threshold)`- 基于中心位的集群,在确定性模拟嵌入的顶部.
  翻译: 中文`semantic_chunks(text, similarity_threshold)`在确定性模拟上嵌入做质量聚类
- `structural_markdown(text)`- 标题意识的分区器.
  翻译: 中文`structural_markdown(text)`标题感知切分器
- `mock_embed(text, dim)`- 基于哈希的嵌入,所以循环运行离线.
  翻译: 中文`mock_embed(text, dim)`哈希式嵌入,让整条回路离线运行.
- `DenseIndex`- 类似于19期B轨道混合物检索课程中使用的形状.
  翻译: 中文`DenseIndex`与19期B轨道混合检查课程相同的形状.
- `eval_recall(strategy, corpus, queries, k)`- - 比较循环.
  翻译: 中文`eval_recall(strategy, corpus, queries, k)`对比循环.
- `main()`运行每个策略在固定器件体内,打印一个回调@k表.
  中文翻译:一个`main()`运行在固定语料上每种策略并印记回忆@k 表――

运行它:

> 运行:

```bash
python3 code/main.py
```

输出表是一个小表,每个策略有一个行,每个k一个列.句子在结构式固定上输掉.结构性划分在划分固定上获胜.回复性保持在混合固定上,因为回复性适应.在没有有用的结构线索的情况下,语义聚合在散文固定上获胜.

> 输出是一个小表:每条策略一行,每个一列. 句子策略在结构化语料上输出;结构化Markdown在Markdown语料上赢得;递归切分靠递归的自适应性在混合语料上保持;语义聚类在没有可用的结构线索的散文语料上胜出.

## 失败模式表格不住的失败模式表格

> **【中文解读】**召回表只给数字,这五种失败模式解释数字背后的机理:孤儿句让嵌入指向错误聚类;符号中途切断让两个半截识符各自嵌入噪音;纯标题块是零信息块;语义漂移让一个5000字符的块装入太多答案、嵌入变得散布;过期嵌入提醒你换嵌入模型但不重建索引,块和检索不再配套了;;

**Orphan sentences.**语句包装产生错过主题句子的部分. 嵌入后指向错误的集群.

> **孤儿句。**句子打包产生的块会漏掉主题句子.嵌入随着指向错误的聚类.

**Mid-symbol cuts.**固定窗口内代码或YAML将识别符分为两半.

> **符号中途截断。**固定窗口在代码或YAML里将标识符切成两半――两半各自嵌入于噪音――

**Header-only chunks.**结构性值发出一个部分,只包含`## Title`过这些或附上下一个部分的第一段.

> **纯标题块。**结构化 标记会出出只包含`## Title`它们被过掉,或者把下一个块的第一段挂起来.

**Semantic drift.**语义集群在一个主题上均时,下切割.一个5000字符的部分将许多具体答案包装成一个分散的嵌入.将语义与硬字符盖合在一起.

> **语义漂移。**语料通篇 语义聚类会少切. 一块5000字符把许多具体答案塞进一个散布的嵌入式中.

**Stale embeddings.**语义集群使用嵌入模型.如果你改变模型,你也会改变块. 按单元模型与检索模型分开,或者重新构建索引.

> **过期嵌入。**语义聚类依赖嵌入模型――换模型,块也跟随变化――把分块使用的模型与检索使用的模型分开锁定版本,或一起重建索引――

## 选择默认策略,没有运行基准.

> **【中文解读】**三个属性定默认:文档类型、段落长度、格式是否自带显式结构──无结构散文选归归归归归分分(目标800字符);Markdown/RFC/API 文档选归归结构化分分;代码走 AST 感知(超纲);长而单一主题的段落选句子切分(目标500);短而混杂主题的段落选语义切分(值0.6)──拿不准就选归归分它是最强大的单一策略基线──

三个属性决定了新的体积的默认分数.

> 三个属性决定新语料的默认分块器.

| Property | Value | Default |
|----------|-------|---------|
| Document type | Prose with no structure | Recursive split, target 800 |
| Document type | Markdown / RFC / API docs | Structural markdown |
| Document type | Code | AST-aware (out of scope; see Phase 19 lesson 02) |
| Paragraph length | Long, single topic | Sentence, target 500 |
| Paragraph length | Short, mixed topics | Semantic, threshold 0.6 |

只有一个战略的基础.

> 拿不准备时时选择递归切分.

## 用它实现框架

> **【中文解读】**三条生产纪律:上新流水线前跑评测,别盲信库的默认策略;每次换嵌入模型或语料配比后重跑评测,赢家随语料而变;把策略名称持久化到每个块的元数据中,将来返回才能归因.

生产模式:

- 在运送新管道之前,运行评估;不要相信您的图书馆默认的策略.
  中文翻译:上新流水线前先跑评测;不要盲信你的库默认的策略──
- 每当你改变嵌入模型或体组混合时,再运行评估;获胜者是体组依赖的.
  中文翻译:每次换嵌入模型或语料配比都重跑评测;赢家随语料而变.
- 保持每个部分的元数据中战略名称,以便您可以稍后归因回归.
  中文翻译:把策略名字持久化成每个块的元数据,便于日后归归归因.

## 运送它.

在第69课中,F轨道端到端RAG系统使用了这里选定的克作为其第一阶段.在第68课中,评估带写出 recall@k 从相同的形状`eval_recall`选择一个胜利的策略,然后把它推进.

> 通过"深入路线"的方法,我们将这些分块器作为第一级的工具.`eval_recall`选择你的语料中赢的策略,把它传递到后面.

## 练习题

1. 添加第六个策略:使用代币窗口`tiktoken`根据相同的装置的固定窗口进行比较.
   中文翻译:加第六种策略:用 `tiktoken`按符号数而不是字符数的符号窗──与固定窗相比,在同一固定语料上──
2. 给表格重新运行,解释为什么除了结构性划分之外的每个策略都失去了回忆.
   中文翻译:向散文语料注入30%的代码块──重跑表格──解释为什么除了结构化标记外每条策略都掉召回──
3. 取代确定性嵌入式与您的项目真正提供商的嵌入式.测量语义集群回忆 delta.报告战略之间的差距是否扩大或缩小.
   中文翻译:把确定性嵌入换成你项目真实供应商嵌入.
4. 添加一个`summary`单片段的字段:一个句子的中心形容. 再次运行评估,并附加总结到单片体内. 测量召回升.
   中文翻译:给每块加一个`summary`字段:一句话质心描述――把摘要附加到块正文后重跑评测――度量召回提升――

## 关键词 快速查找表

| Term | What people say | What it actually means |
|------|-----------------|------------------------|
| Recall@k | "Did we get the right chunk?" | Fraction of queries where any of the top-k chunks overlaps the gold answer span |
| Chunk overlap | "Sliding window" | Re-include the last N characters of the previous chunk in the next chunk |
| Structural splitter | "Header-aware chunks" | Cut at H1/H2/H3 boundaries; the heading text is part of the chunk |
| Semantic chunker | "Topic-aware chunks" | Embed sentences, cluster by centroid similarity, cut on drift |
| Centroid drift | "Topic shift" | Cosine similarity between the running mean and the next sentence drops past a threshold |

## 继续阅读 继续阅读

- [LongRAG: Enhancing Retrieval-Augmented Generation with Long-context LLMs (arXiv 2406.15319)](https://arxiv.org/abs/2406.15319)
  中文翻译:LongRAG论文 分块策略造成35个百分点召回摆动的实测来源.
- [Anthropic, Contextual Retrieval](https://www.anthropic.com/news/contextual-retrieval)
  中文翻译:人类上下文检索用上下文块头收窄分块差距的后续工作
- [LlamaIndex, Chunking strategies for production RAG](https://docs.llamaindex.ai/en/stable/optimizing/production_rag/)
  中文翻译:LlamaIndex 生产RAG 分块策略文档。
- 第十一阶段第六课 - RAG基本面
  中文翻译:第11阶段 · 06RAG 基础。
- 阶段11课07 - 高级RAG
  中文翻译:阶段11 · 07进阶 RAG。
- 第19阶段课程65:混合采集,
  中文翻译:阶段19 · 65对本课产出的块做排序的混合检索──
- 第19阶段课程68 - 评估杆,以评分生产中的战略选择
  中文翻译:19期 · 68 在生产中给策略选择打分的评测线束──
