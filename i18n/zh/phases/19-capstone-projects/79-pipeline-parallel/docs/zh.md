# 管道平行和泡分析.

> 数平行式分开矩阵乘以各行.管道平行式分开模型跨行,每个行各分别分开一个阶段.微分组通过管道流动.开始和结束的空时间是泡;最小化它是整个工艺.

> **【中文解读】**本课讲与张量并行、数据并行正交的第三条并行轴流水线并行:把模型按深切切成N个阶段(阶段),每个阶段都有一个阶段,微批次(微批次) 如流水线上的产品从阶段0流到最后阶段,反传再倒着流回来.流水线开头等"第一个微批次到最后阶段"和结尾等"最后微批次排空"期间,部分阶段的事情可以做这是气泡 (泡) .气泡分数的闭式解解是 (N-1) /M+N-1),压力是流水线调低的全部手工.

> **【拓展：三条并行轴→三维混合并行】**大模型训练的显存压导致了三条正交的并行轴:数据并行:数据并行:每卡完整模型、不同数据) 流水线并行:按层切模型) 张量并行:按矩阵乘层切) 梅加特龙-LM 将三者叠加成3D并行:DP×PP×TP) 跑千亿模型:本课专注于水轴的两大调度:GPipe(先填排后) 与 1F1B(一前后交数错,Megatron/PipeDream的分选),以及气泡的推与课程 78的 Ze.RO的不同状态:Ze:Z 切"生产,流水线是"切计算本身""

>  **【前置】**学本课前请先掌握:(1) 阶段19 · 76发送/recv 与集合通信原语,双级真流水线演示 建立在它上;(2) 前向/反传与激活值的生命周期概念气泡分析的核心束是"激活值必须存活到它的反传";(3) 第78课 ZeRO的两条轴正交常和组合(ZeRO-1+管道) 〔后续接:第80课 把每个阶段的参数片写入检查点,第81课 将DDP+ZeRO演示组装成端到端.

**Type:** Build | **类型:** 动手构建
**Languages:** Python | **语言:** Python
**Prerequisites:** Phase 19 Track C lessons 42-49 | **前置知识:** Phase 19 Track C 课程 42-49
**Time:** ~90 min | **时间:** 约 90 分钟

## 学习目标

- 分开一个连续模型成N阶段,并模拟N行间的前进管道.
  中文翻译:把一个顺序模型切成N个阶段,并在N个阶段上模拟前向流水线.
- 使用GPipe时间表 (仅前面填充,然后倒退) 通过管道进行M微洗,并计算泡分数.
  中文翻译:用GPipe调度 (先全部前向填充,再反传) 把M个微批次排进流水线,并计算气泡分数.
- 根据Megatron-LM和PipeDream所使用的1F1B时间表进行泡比较.
  中文翻译:把气泡与Megatron-LM 和 PipeDream 使用的交错式 1F1B 调度做对比
- 防御阶段分配:每阶段的等式计算比每阶段的等式参数数数更重要.
  中文翻译:论证阶段划分:每阶段计算量相等比每阶段参数相等更重要.

## 问题 问题引入

> **【中文解读】**本节回答"为什么还需要流水线"──70B 模型光参数需要140GB,单卡放不下;ZeRO-3 虽然能分参数,但每次前向每层都必须整层的结合,付出日期.

只有在fp16中的70B参数模型需要140GB的参数. 没有消费者GPU可以控制它. 泽罗-3在各层中分断参数,但仍然需要每个层来收集每一步的全部层次,每层支付 log ((N) 跳. 管道平行采用不同的路径:将模型切成N阶段,并将每个阶段设置为一个阶段. 阶段1的前进在0级完成,并将激活子交给1级;阶段1运行2级,手交给2级等等. 倒流向后,倒流向后. 记忆线性下降,因为每个级别只包含一个阶段;计算是序列的,这是泡问题.

> 一个fp16的70B参数模型光参数就需要140GB──没有消费级GPU装下──ZeRO-3 把参数分分成各级,但每次前向步每级 仍需要整层集合,为每层支付日志(N) 跳──流水线并行走另一条路:把模型切成N个阶段,每个阶段放一个──第1层的前向下降在0级上完成后交给1级激活张量交付; 排列1 跑第二层再交付排列2;根据此类推──反传反流动──因为每个阶段只持有一阶段,存线性下;计算是串行就是这个气泡问题──

泡是管道开始时的置时间 (等待第一批微洗到最后阶段),以及最后 (等待最后一批微洗回流). 对于M微洗和N阶段,每个阶段的泡分数为 (N-1)/(M+N-1). 在M=8,N=4是27%. 在M=64时,N=4是4.5%. 泡会缩小,当你每步都有很多微型洗器,这意味着每微型洗器的批量量很小,

> 气泡是流水线开端的空时间 ((等第一批到最后一段) 和结尾的空时间 ((等最后一批排空回流) ⋅M个微批次、N个阶段时,每阶段气泡分数是 (N-1) / M+N-1) ・M=8、N=4 时是27%──M=64、N=4 时是4.5%──每批次越来越多气泡越小,这意味着每个批次的批次要小,这正是驱动微批设计的约束──

## 概念的核心概念

```mermaid
flowchart LR
  R0[rank 0: stage 0 / layer 0] --> R1[rank 1: stage 1 / layer 1]
  R1 --> R2[rank 2: stage 2 / layer 2]
  R2 --> R3[rank 3: stage 3 / loss]
  R3 -.backward.-> R2
  R2 -.backward.-> R1
  R1 -.backward.-> R0
```

### 轮时间表

> **【中文解读】**是"先填后排":所有M个微批次先全部跑完前向,再倒序全部跑反传――代价是激活值 每个微批次激活必须活到它自己的反传,所以激活显存与M 线性增长相伴――时间账:前向M+N-1 周期、反传再M+N-1 个周期;每阶段有用功 2M 个周期、气泡 2(N-1) 周期,气泡分数 = (N-1) /M+N-1) ─让M远大于N能把气泡藏藏在住里――

在向后开始之前,将M微洗手机填充到前方,然后向后排水. 任何微型的激活必须保持到它向后,所以记忆以线性方式增长与M. 前进需要M+N-1周期,后退需要另一个M+N-1周期. 每阶段有用的工作为2M周期;每阶段泡为2 ((N-1) 周期. 泡分数是 (N-1)/(M+N-1) 每个向前和向后都需要一个时间单位. 选择M比N大得多,就隐藏了泡.

> 先让全部M个微批次填满前向流水线,再倒序排空反传――每个微批次的激活值必须保持到它自己的反传,所以显存随着M 线性增长――前向花 M+N-1个周期,反向再花 M+N-1个周期――每个阶段有用功是2M个周期;每个阶段气泡是2(N-1)个周期――当前向和反向各个时间单位时,气泡分数是 (N-1) / M+N-1) ─选M 远于大 N 就能藏气泡――

### 时间表1F1B

> **【中文解读】**1F1B是"一前一后"交错:某个微批次的前向一到最后阶段,立即开始反传并让它流回去. 每个阶段交换执行一次前向一次反传.气泡仍然是N-1周期,但活跃显存只受流线深度约束,不再随着微批次数的增长而成.这是Megatron、PipeDream等生产流线的原因.

间歇:一旦微型的前行达到最后阶段,就开始向后流,然后让它回流. 每个阶段,时间表交换一次向前,一次向后. 泡仍然是N-1,但激活记忆由管道深度限制,而不是微量. 生产管道使用1F1B (Megatron, PipeDream). 课程首先将GPipe执行,因为它更简单,

> 交错执行:某个微批次的前向一到最后一阶段,就启动它的反传并让它流回去.调度在每个阶段上交换一次前向一次反传.气泡仍然是N-1,但活跃显存由流水线深度界定,而不是微批次数.

### 为什么每个阶段的等式计算是重要的

> **【中文解读】**阶段分的第一原则是"每阶段计算量相等"而不是"参数相等":若阶段0 花50 ms、阶段1 花100 ms,每周期都被阶段1卡住,其他阶段每周期白等50 ms──参数是错误的轴变压器 每层的计算由注意力+MLP主导,而嵌层参数多、计算少──划分应平衡每阶段的FLOP,而不是每阶段权重字节数量.

如果阶段0需要50ms,阶段1需要100ms,每个周期都被关在阶段1.其他阶段每周期停滞50ms等待阶段1释放.等等参数数数量是错误的轴:变压器的计算由注意力加上每层MLP主导,嵌入层具有许多参数,但计算很少.阶段分配应该等同于每个阶段的FLOP,而不是每个阶段的重量.

> 如果阶段0花50ms而阶段1花100ms,每个周期都卡在阶段1 上下.其他阶段每周期空等50ms等阶段1放行.参数相等是错误的轴:变压器的计算由每个层的注意力 + MLP 主导,而嵌层参数很多但计算很少.

### 微分批量对批量

管道运行B尺寸M微.有效批量大小为M*B.管道步骤结束时的梯度是结合M*B示例的梯度.泡分数取决于M;优化器看到M*B.调整M意味着交易泡 (低于M高) 与每微存储器 (GPipe的高于M高的更高的激活存储).

> 流水线一次运行 M 个大小为 B 的微批次。有效批次 大小是 M*B。流水线步骤结束时的梯度是合并的 M*B 个样本上的梯度。气泡分数取决于 M;优化器看到的是 M*B。调 M 意味着气泡(M 大则低) 与每微批次显存(GPipe 下 M 大则活存高) 之间进行交易显――

```figure
cd-pipeline-bubble
```

## 动手构建

> **【中文解读】**代码分两层:纯调度层`gpipe_schedule`生成 (周期,阶段,微批次, 前向/反传) 事件表,`render_gantt`画成甘特图,`measure_bubble`气泡的确实测试,与闭式解`bubble_fraction`对照;真实通信层`PipelineStage`两级光环 进程, 级 0 持阶段 0、 级 1 持阶段 1, 激活值经发/恢复 接力、梯度反传回流,证明"线上"真的能跑――demo 还打印M=1.64的气泡对照表,直观呈现"M越大气泡越小"的和曲线――

`code/main.py`执行:

- `PipelineStage`子`nn.Module`具有一个阶段的参数和暴露`forward(activation)`现在,我们要去.
- `Pipeline(stages, num_microbatches)`通过模拟每阶段的墙钟,调整GPipe的时间表.
- `bubble_fraction(num_stages, num_microbatches)`:封闭式 (N-1) /(M+N-1).
- 通过4个阶段的演示, 打印每微分量的痕迹和测量的泡分数.

运行它:

```bash
python3 code/main.py
```

输出:一个阶段按微批次的甘特图表和与封闭形式预测相比的泡百分比.

> 输出:一张阶段×微批次的甘特图,以及实测气泡百分比与闭式解预测的对照.

## 产品的实际战斗模式

> **【中文解读】**三条实战经验:(1) 激活值检查点与流水线是天生一对GPipe 下 M 个微批次在飞,激活显存储是单微批次的 M 倍,激活检查点使用反传时重量预转换显存,两者结合才让长序列的流水线可行;(2) 阶段平衡实验不要假设生产团队先运行配置量 每个层真实耗时再划分,Megatron 的`--num-layers-per-stage`接受列表以支持不均等层次数; 3) 发送/回收 顺序必须防死锁所有阶段都"先发后收"会在网上死锁,标准修法是偶数排名先发后收、奇数排名先发后收──

两种模式使管道硬化,

> 三条模式让流水线并行足以投入生产.

**Activation checkpointing pairs with pipeline.**通过GPipe飞行中的M微分钟,激活内存为M乘以1微分钟.激活检查点重新计算前进时间,对内存进行交易.

> **激活值检查点与流水线配对。**轮上同时有M个微批次在飞行时,激活显存是单个微批次的M倍――激活值检查点在反传时重算前向,使用计算换显存;两者结合才让长序列的流水线变得可行――

**Stage balance is measured, not assumed.**制作团队运行一个配置文件通过,测量目标硬件的实际每层计算 (FLOP和墙钟),然后按此测量进行分区.`--num-layers-per-stage`标志接受列表,以便在各阶段的成本不同时允许不均的层计数.

> **阶段均衡靠实测，不靠假设。**生产团队先跑一次概况,在目标硬件上量出每层真实计算量(FLOPs 和墙钟时间),再按测量结果划分――Megatron-LM 的`--num-layers-per-stage`标志接受一列表,允许在每个层次成本不同时使用不均等的层次数量.

**Send-recv schedule must avoid deadlock.**管道,每个阶段都在收到电线上的门之前发送.标准的修正是交互:平排阶段先发送,然后回复,奇排阶段先回复,然后发送.课程时间表明确排列,所以模式可见.

> **send/recv 调度必须避免死锁。**每个阶段都先发后收收流线将在网络上死锁.标准修法是交错:偶数级阶段先发后收,奇数级阶段先收后发.

## 用它实现框架

> **【中文解读】**三个生产框架与手工实现的应对:Megatron-LM是大规模流水线并行的参考实现(1F1B调度、张量+流水线+数据三并行混合);DeepSpeed管道与ZeRO集成,ZeRO-1+管道是最大的开源模型的常见组合;PyTorch管道是PyTorch原生流水线封装;;

生产模式:

- **Megatron-LM.**参考管道平行尺度. 使用1F1B,支持子+管道+数据平行组合.
  翻译: 中文**Megatron-LM**大规模流水线并行参考实现,使用1F1B并支持张量 + 流水线 + 数据并行组合――
- **DeepSpeed Pipeline.**与 ZeRO 集成; ZeRO-1 + 管道是最大的开放模型的常见组合.
  翻译: 中文**DeepSpeed Pipeline**与ZERO 集成;ZRO-1+流水线是最大的开源模型的常见组合.
- **PyTorch Pipe.**管道包装, 基于`torch.distributed.pipeline.sync.Pipe`现在,我们要去.
  翻译: 中文**PyTorch Pipe**PyTorch 原生流水线封装,构建在 `torch.distributed.pipeline.sync.Pipe`之上.

## 运送它.

第80课节将每个阶段参数的分片存储在分片检查点. 第81课节将DDP + ZeRO +管道组合到端到端演示中 (精神上;演示保持管道模拟运行时间).

> 第80课 把每个阶段的参数分片存储进分片检查点―― 第81课 在端到端演示 上组 DDP + ZeRO + 流水线(取其精神;演示为控制运行时保留流水线的模拟形态)

## 练习题

1. 执行1F1B,验证泡分数与GPipe相匹配,但激活内存是有限的.
   中文翻译:实现1F1B,验证气泡分数与GPipe相似但活跃显现有界.
2. 在更深层次的模型上,按测量的墙钟进行分析,并重新平衡阶段的实时阶段.
   中文翻译:在更深的模型上剖析每阶段真实耗时,并按实测墙钟时间重新平衡阶段.
3. 通过管道微分组加上梯度积累,检查梯度等于相当的全批次向前梯度.
   中文翻译:跨流水线微批次加梯度累积,检查梯度等等等等价全批前向的梯度──
4. 通过激活检查点对配管道进行测量,测量内存下降与计算成本.
   中文翻译:给流水线配上激活值检查点,测量显存下降与计算成本的比较.
5. 结合管道与DDP (每个管道排名在数据平行组中复制) 并通过2D时间表进行推理.
   中文翻译:把流水线与DP组合 (中文翻译:把流水线与DP组合)

## 关键词 快速查找表

| Term | What people say | What it actually means |
|------|----------------|------------------------|
| Pipeline | "Model parallel along depth" | One stage per rank, activations flow stage to stage |
| Bubble | "Pipeline idle time" | (N-1) steps at start + end where some stages have no work |
| Microbatch | "Slice of the batch" | One forward/backward unit; bubble shrinks as M grows |
| GPipe | "Fill then drain" | All M forwards before any backward; high activation memory |
| 1F1B | "Interleaved schedule" | One forward one backward per stage; bounded activation memory |

## 继续阅读 继续阅读

- [Huang et al, GPipe: Efficient Training of Giant Neural Networks](https://arxiv.org/abs/1811.06965)
  中文翻译:GPipe论文原典先填后排调与气泡推导
- [Narayanan et al, PipeDream: Generalized Pipeline Parallelism for DNN Training](https://arxiv.org/abs/1806.03377)
  中文翻译:管梦论文1F1B 类交错调度的来源
- [Megatron-LM pipeline parallel docs](https://github.com/NVIDIA/Megatron-LM)
  中文翻译:Megatron-LM 流水线并行文档生产级 1F1B 与 3D并行
- 第19阶段 第76课 - 时间表使用的发送/回收原始函数
  中文翻译:第19阶段 第76课调度使用的发送/回复 原语
- 阶段19课程78 - ZeRO与管道直角,通常是结合的
  中文翻译:第19阶段 第78课ZeRO与流水线正交并经常组合使用
