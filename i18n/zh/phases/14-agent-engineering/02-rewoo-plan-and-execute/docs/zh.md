# 计划和执行:解规划

> 雷亚克特将思想和行动交织成一个流.雷亚克特分开它们:一个大计划前面,然后执行. 5倍少于代币,HotpotQA上的准确度为4%,你可以将计划器化为7B模型.计划和执行将其概括;计划和行动将其扩展到网络导航.

> **【中文解读】**反应在一个流中交替思考和行动――ReWOO将它们分开:先一次制定完整计划,然后执行――标消耗减少了5倍,HotpotQA准确率提高了4%,还可以将规划器蒸到7B模型――计划和执行是其泛化版本,计划和行动将扩展到网页导航――

> **【拓展：ReWOO → 现代 Agent 架构】**在人类的"建设有效代理"五种工作流模式中,计划执行模式是核心之一.

>  **【前置】**精通本节前请先掌握:阶段14·01(代理循环/反应循环) 你必须理解 ReAct 的"思考行动观察"交换模式,因为 ReWOO 就是为了了解 ReAct 的代币第二次增长问题;以及基础图论概念 ((DAG、拓排序),不熟悉 DAG 会卡在"依赖解析"那一步.

**Type:** Build | **类型:** 构建
**Languages:** Python (stdlib) | **语言:** Python (标准库)
**Prerequisites:** Phase 14 · 01 (Agent Loop) | **前置知识:** Phase 14 · 01 (Agent 循环)
**Time:** ~60 minutes | **时间:** ~60 分钟

## 学习目标

- 解释为什么ReWOO的规划者/工人/解决器分区节省了代币,并提高了ReAct的互联循环的强度.
  中文翻译:解释为什么ReWOO的规划器/工作器/求解器分离能节省代币并比ReAct的交换循环更强大.
- 实现一个计划DAG,一个依赖命令执行器,以及一个构成员工输出的解决器.
  中文翻译:实现计划 DAG、依赖顺序执行器和组合工作器输出求解器全部使用标准库──
- 决定一个任务应该在2026年"五个工作流程模式"框架 (Anthropic) 中运行时,将计划然后执行与交互 ReAct进行.
  中文翻译:使用2026年"五种工作流模式"框架,决定任务是应该使用计划执行或交换 ReAct.
- 识别在长远网络或移动任务中需要什么时候使用 Plan-and-Act 的合成计划数据.
  中文翻译:识别何时需要计划和行动的合成计划数据来处理长程网页或移动任务.

## 问题 问题引入

雷亚克的互联思考-行动-观察循环简单而灵活,但每个工具调用必须携带完整的先前文本,包括每一个先前思想.代币使用随着深度的增长而增长.更糟糕的是:当工具中期失败时,模型必须从错误观察中重新衍生整个计划.

> 反应的交换思考行动观察循环简单灵活,但每次调用都需要携带完整的前置上下文包括之前的每一个思考.

雷沃 (Xu et al., arXiv:2305.18323,2023年5月) 注意到这一点,并投注:提前计划整个事情,并行地收集证据,在最后编写答案.一个LLM调用计划,N工具要求证据 (可以并行),一个LLM调用解决.交易更少的灵活性 (计划是静态的) 获得更好的代币效率和更清晰的失败模式.

>  **【类比】**像装修房子前出施工图:设计师一次性画完所有工序 (规划器) 工人按图并行施工互不打扰 (工作器) 工人按图并行施工互不打扰 (工作器) 工人按图 验收时把所有工序结果拼在一起结算 (求解器) ⋅ 复制 则像边施工边设计,每个工人可以翻阅前面所有工人的笔记动手,所以10步后笔记就堆成山──

> 李 (李) 指出这一点并提出了一个方案:先规划整个任务,并行获取证据,最后组合答案――一次LLM调用规划、N次工具调用获取证据 (可并行) 、一次LLM调用求解――代价是灵活性降低的(计划是静态的),但换来更好的标志效率和更清晰的失败模式――

## 概念的核心概念

### 三个角色

```
Planner:  user_question -> [plan_dag]        # 规划器：将用户问题转为计划 DAG
Workers:  [plan_dag]     -> [evidence]        # 工作器：执行工具调用获取证据（可并行）
Solver:   user_question, plan_dag, evidence -> final_answer  # 求解器：组合证据生成最终答案
```

规划器生成DAG. 每个节点都会命名一个工具,它的参数,以及它取决于哪些早期节点 (如`#E1`现在`#E2`工人按拓顺序执行节点.

> 规划器生成一个DAG――每个节点指定一个工具、其参数以及依赖的先前节点(如`#E1`,我知道.`#E2`等引用) ・工作器按拓顺序执行节点――求解器将所有内容拼接在一起――

### 为什么5倍少的代币

反应随步数线性增长.在步骤10时,提示包含1加行动1加观察1加思考2加行动2加观察2等.每个中间步骤也含有原始提示.

> 反应的提示长度随着线数的增长而长――到第十步时,提示包含思考1 + 行动1 + 观察1 + 思考2 + 行动2 + 观察2,依此类推――每个中间步骤还有余余的包含原始提示――

在HotpotQA上,纸质测量比5倍少的代币,同时获得+4绝对精度.

> 只有一个规划器提示 (较大) 个小工作器提示 (每个只包含调用工具,无链式历史) 和一个求解器提示.

### 为什么它更强

如果 ReAct 中工作者3失败,循环必须在误差中流中推理出来.在 ReWOO 中,工作者3返回一个错误字符串;解决器将其视为与原始计划的背景,并且可以优雅地降级.故障定位是每个节点,而不是每个步骤.

> 如果工作器3在ReAct中失败,循环必须在流程中从错误中推理.在ReWOO中,工作器3返回一个错误字符串;求解器在原始计划上下文中看到它并可以优雅降级.故障定位是按节点,不是按步骤的.

### 调制剂蒸

由于规划者不看到观察,所以你可以从175B老师对规划者输出进行细节调整7B模型.小模型处理规划;在推断时,大模型不需要.现在这是标准的.

> 论文的第二个成果:因为规划器看不到观察结果,你可以在175B教师模型的规划输出微调一个7B模型――小模型处理规划;大模型在推理时不需要――现在是标准做法许多2026年生产代理使用小规划器和大执行器,反之亦然――

> **【拓展：规划器蒸馏 → 成本优化】**因为规划器不需要观察结果,可以将175B教师模型的规划输出蒸到7B模型.2026年生产代理普遍使用"小模型规划 +大模型执行"的混合架构来优化成本.

### 计划和执行 (长链, 2023)
### 计划和执行 (2023)

兰格链团队在2023年8月的帖子将REWOO整体化为模式名称:计划和执行.前面规划器发布一步列表,执行器运行每个步骤,可选的重组规划器可以在观察结果后修改.这更接近ReAct比REWOO (重组规划器将观察恢复到规划中),但保留了代币节省.

> 长链团队2023年8月的文章将将REWOO 泛化为一个模式名称:计划和执行.前置规划器发出步骤列表,执行器运行每一步,可选的重新规划器可以在观察结果后修改.

### 计划和法案 (埃尔多根等人, arXiv:2503.09572, ICML 2025)

计划和行动将模式扩展到长视野网络和移动代理.主要贡献是合成计划数据:标记轨迹生成器生成了计划明确的训练数据.用于在一个ReAct轨迹失去了一致性的情况下继续在WebArena类任务上工作3050步后调整规划器模型.

> 计划和行动将该模式扩展到长程网页和移动代理. 关键贡献是合成计划数据:标记轨迹生成器产生训练数据,其中计划是显而易见的.

### 什么时候选择哪个

| Pattern | When | 适用场景 |
|---------|------|----------|
| ReAct | Short tasks, unknown environment, need reactive exception handling | 短任务、未知环境、需要响应式异常处理 |
| ReWOO | Structured tasks with known tools, token-sensitive, parallelizable evidence | 结构化任务、已知工具、Token 敏感、可并行 |
| Plan-and-Execute | Like ReWOO but with replanning after partial execution | 类似 ReWOO 但支持执行后重新规划 |
| Plan-and-Act | Long-horizon (>30 steps), web/mobile/computer-use | 长程任务(>30步)、网页/移动端/计算机使用 |
| Tree of Thoughts | Search is worth paying for (Lesson 04) | 值得付出搜索成本的场景 |

根据"人类学"的2024年12月指导:从最简单的开始. 如果任务是一个工具调用加上一个总结,不要构建ReWOO. 如果任务是40步的研究任务,不要单独做ReAct.

>  **【困惑】**问:既然REWOO节省了5倍的代币还准确,为什么不全部使用REWOO?A:因为REWOO的计划是"静态"一旦发出就不再根据观察调整.如果第3步发现"这个问题根本问错了",ReAct可以立即转向,REWOO只能在解决器阶段"硬拼"......所以动态环境(探索性研究、未知的API) 使用REAct,结构化任务(多跳问答、ETL) 使用REWOO──

> 人类2024年12月的指导:从最简单的开始. 如果任务是一个工具调用加一个摘要,不要构建REWOO.

## 建立它,实现它.

> ️ **【易错点】**实现ReWOO 时新手最常踩的坑:忘记计划 DAG做环检.`#E1`根据`#E2`现在`#E2`依赖`#E1`, 排序会无限循环卡死.**后果**执行人 挂起或溢出**一行修复**排序后检查已排序节点数是否等于总节点数,不等于抛`CycleDetectedError`,我知道.
```figure
rewoo-plan
```

## 建立它

`code/main.py`实现玩具ReWOO:

> `code/main.py`实现一个玩具:

- `Planner`一个编写的政策,从提示中发出计划DAG.
  翻译: 中文`Planner`从提示生成计划 DAG的脚本策略──
- `Worker`通过注册表发送每个节点的工具调用.
  翻译: 中文`Worker`通过注册表分派每个节点的工具调用.
- `Solver`编写的作文,阅读证据并产生最终答案.
  翻译: 中文`Solver`读取证据并生成最终答案的脚本组合器.
- 依赖性决议 如`#E1`工人产量更换为以前的工人产量.
  中文翻译:依赖解析如`#E1`的引用被替换为前面的工作器输出.

演示题回答"法国首都人口是多少,总数为数百万?"

> 演讲回答"法国首都的人口是多少,四舍五入到百万?"使用两步计划:

运行它:

> 运行:

```
python3 code/main.py
```

追踪首先显示了完整的计划,然后是工人结果,然后是解决器组合.将代币数量 (我们打印粗略的字符数量) 与ReAct式的交叉运行进行比较.

> 轨迹首先显示完整计划,然后是工作器结果,最后是求解器组合――比较符号数量 (我们打印粗略字符数量) 与ReAct风格的交换运行ReWOO在这种结构化任务中获胜――

## 用它实现框架

兰格拉夫公司作为配方 (`create_react_agent`对于ReAct,定制图为计划执行).CrewAI的流程直接编码模式:您先定义任务,流程DAG执行它们.计划和行动的合成数据方法仍然主要是研究;运行时间模式 (明确计划DAG) 通过LangGraph和CrewAI流程进行生产.

> 作为配方提供将计划和执行`create_react_agent`用于 ReAct,自定义图用于计划执行) ・CrewAI的流程直接编码该模式:你预先定义任务,Flow DAG 执行它们――计划和行动的合成数据方法仍然主要在研究阶段;运行时模式(显式计划 DAG) 通过LangGraph 和 CrewAI流程在生产环境中使用――

## 运送它.

`outputs/skill-rewoo-planner.md`在执行器交付之前,它验证该计划 (循环,每个参考已解决,每个工具都存在).

> `outputs/skill-rewoo-planner.md`根据用户请求和工具目录生成ReWOO 计划DAG──它在移交到执行器之前验证计划(无环、每个引用已解析、每个工具存在)──

## 练习题

1. 通过一个6节点 DAG,两个平行组,你会得到什么?
   中文翻译:将独立计划节点工作机并行化. 在 6 节 DAG 中 2 组并行的情况下有什么收益?
2. 如果任何员工返回错误,添加一个重组节点,如果任何员工返回错误,它会被执行.
   中文翻译:添加一个在工作器返回错误时触发的重新规划节点.
3. 取代`Planner`具有小型型号 (7B类) 和保持`Solver` 哪里是分断失败?
   中文翻译:用小模型 ((7B 级) 替换规划器,保留前沿模型作为求解器――对比端到端质量分裂在哪里失败?
4. 阅读REWOO关于计划器蒸的论文第4节. 概念上复制175B -> 7B结果:您需要哪些培训数据,以及如何评分计划质量?
   中文翻译:阅读ReWOO论文 第4节关于规划器蒸的内容――概念上重现 175B→7B 结果:需要什么训练数据?如何评估计划质量?
5. 运输玩具到计划和行动的轨迹形状:计划是序列,而不是DAG.
   中文翻译:将玩具系统移植为计划和行动的轨迹形式:计划是序列而不是DAG.

## 关键词 快速查找表

| Term | What people say | What it actually means |
|------|-----------------|-----------------------|
| 术语 | 通俗说法 | 实际含义 |
| ReWOO | "Reasoning without observations" / "无观察推理" | Plan, then fetch evidence in parallel, then solve — no observations in the planning prompt / 先规划，再并行获取证据，最后求解——规划提示中不包含观察 |
| Plan-and-Execute | "LangChain's plan-execute pattern" / "LangChain 的计划-执行模式" | ReWOO with an optional replanner node after execution / 带可选重新规划节点的 ReWOO |
| Plan-and-Act | "Scaled plan-execute" / "扩展版计划-执行" | Explicit planner/executor split with synthetic plan training data for long-horizon tasks / 使用合成计划训练数据的长程任务显式规划器/执行器分离 |
| Evidence reference | "#E1, #E2, ..." / "证据引用" | Plan-node placeholder substituted with prior worker output at dispatch time / 计划节点占位符，在分派时替换为工作器输出 |
| Planner distillation | "Small planner, big executor" / "小规划器，大执行器" | Fine-tune a small model on planner traces from a large teacher / 用大模型的规划轨迹微调小模型 |
| Token efficiency | "Fewer round trips" / "更少往返" | 5x fewer tokens on HotpotQA vs ReAct in the paper / 论文中 HotpotQA 上比 ReAct 减少 5 倍 token |
| DAG executor | "Topological dispatcher" / "拓扑分派器" | Runs plan nodes in dependency order; parallel at each level / 按依赖顺序运行计划节点；每层可并行 |

## 继续阅读 继续阅读

- [Xu et al., ReWOO: Decoupling Reasoning from Observations (arXiv:2305.18323)](https://arxiv.org/abs/2305.18323)法典论文
  中文翻译:ReWOO 经典论文将推理与观察解──
- [Erdogan et al., Plan-and-Act (arXiv:2503.09572)](https://arxiv.org/abs/2503.09572) 具有合成计划的规模规划者执行者
  中文翻译:计划和行动使用合成计划的扩展版规划器-执行器──
- [LangGraph Plan-and-Execute tutorial](https://docs.langchain.com/oss/python/langgraph/overview)框架配方
  中文翻译:长度图计划和执行教程框架配方──
- [Anthropic, Building Effective Agents](https://www.anthropic.com/research/building-effective-agents)选择最简单的模式,
  中文翻译:关于构建有效代理的指导选择最简单的可行模式
