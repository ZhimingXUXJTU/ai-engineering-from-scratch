# 导演/管家/工作者模式

> 一个主要代理人计划和代表;专业工作者在平行环境中执行并报告. 这就是安特罗皮克研究系统 (Claude Opus 4作为, Sonnet 4作为子基) 背后的模式,在内部研究评估中,测量为+90.2% 据Anthropic的工程文章报道,BrowseComp上的80%的差异仅仅是通过代币使用解释的. 这一课从原始人构建了监督模式,并涵盖了2026年的生产部署工程课程.

> **【中文解读】**专业化工作器在并行上下文中执行并汇报.这是人类研究系统背后的模式.

> **【拓展：Supervisor 模式 → Claude DevFleet】**克劳德代码的多代理编排工具 克劳德·德夫飞就是监督者模式的实现 一个主代理 分解任务,发送到多个隔离的工作树中子代理并行工作,最后汇总结果――人类的生产研究系统也使用这种模式――

**Type:** Learn + Build | **类型:** 学习 + 构建
**Languages:** Python (stdlib, `threading`) | **语言:** Python (标准库, `threading`)
**Prerequisites:** Phase 16 · 04 (Primitive Model) | **前置知识:** Phase 16 · 04 (原语模型)
**Time:** ~75 minutes | **时间:** ~75 分钟

>  **【前置】**学本节前请先掌握:阶段16·04(4个原语) 、阶段14·01(代理循环) ――监督者模式 = 多 代理中最常用的一种一个主管+多个工作器──
>  **【类比】**监督者模式 = "项目经理 + 工程师团队"――主管(Opus) 分解任务+审查,工作器(Sonnet) 各干一摊――人类数据:浏览Comp 80% 方差由代币 使用解释多 代理 赢是因为每个子 代理有独立上下文窗口(新文本),不是协调本身魔法――

## 问题 问题引入

研究是单代理系统失败的原型任务.你问"2023年至2026年间多代理系统发生了什么变化?" 一个单代理在连续阅读五篇论文,将其文本填写到一半,然后必须一起考虑它们.在达到第五篇时,它忘记了第一篇论文.它不能平行化.

> 研究是单个代理系统失败的典型任务. 你问"2023年到2026年间多个代理系统发生了什么变化?"单个代理 顺序阅读五篇论文,用它们的文本填满下文的一半,然后必须一起推理它们.

单机失败是结构性的,不能通过更好的提示来解决.不管系统提示有多好,文本窗口都会填满.合成所需的信息 (五篇论文的关键发现) 从物理上来说不适合论文的原始文本.

> 单代理 失败是结构性的,无法使用更好的提示修复――不管系统提示多么好,上下文窗口都会填满――综合需要的信息――所有五篇论文的关键发现)

监督模式解决了这一问题:一个领导代理计划搜索,委托每个子问题给一个工人,并合成.每个工人获得了自己的200k代币窗口,以满足一个狭窄的问题.领导从来没有看到原材料 只有工人的摘要.

> 监督者模式修复了这个问题:一个主导代理 规划搜索,将每个孩子的问题委托给一个工作器,然后综合――每个工作器获得自己的200万代币 窗口用于一个狭窄的问题――主导永远不会看到原始论文只看到工作器摘要――

信息流程是设计:原始数据在工人背景下留下;只有压缩的发现才能达到头.头的背景是专注于合成,而不是数据加载.这是建筑的胜利 数据重的工作与合成重工作的分离.

> 信息流是设计:原始数据留在工作器下文中;只有压缩的发现到达主导者.主导者的上下文专注于综合而不是数据加载.

报告显示,在内部研究评估和单个Opus4中,Anthropic的生产研究系统报告了90.2%的数据. 同一篇文章指出,80%的BrowseComp变化仅仅是通过使用代币来解释的.

> 报告在内部研究评估中提升了90.2%以上. 同篇文章指出,BrowseComp的差异80%仅由*代币使用量*解释.

对于研究人员的表现,如果您想要更好的研究代理性能,在调整提示之前,花费更多的代币 (更多的子代码,更大的背景).成本,而不是聪明,是杆.

> 如果您想更好研究代理性能,在调整提示之前花更多的代币 (更多子代理更大上下文) ⋅成本不是聪明,是杆──

## 概念的核心概念

### 模式

```
                 ┌──────────────┐
                 │   Lead       │  plans, decomposes,
                 │  (Opus 4)    │  synthesizes
                 └──┬────┬───┬──┘
                    │    │   │
            ┌───────┘    │   └───────┐
            ▼            ▼           ▼
      ┌─────────┐  ┌─────────┐  ┌─────────┐
      │ Worker1 │  │ Worker2 │  │ Worker3 │
      │(Sonnet) │  │(Sonnet) │  │(Sonnet) │
      └─────────┘  └─────────┘  └─────────┘
         fresh       fresh        fresh
         context     context      context
```

子从来没有读到原材料. 工人从来没有看到彼此的工作,直到子合成. 每个箭头都是一个狭窄的文物.

> 主导者永远不读取原材料――工作器在主导者综合之前永远不见彼此的工作――每个箭头都是带有狭窄的工件的交接――

信息隔离是核心设计选择. 的背景窗口始终集中在规划和合成上. 从来没有被200万个原始搜索结果的代币污染. 工人每个人都为他们的狭窄问题获得了200万的清洁预算.

> 这种信息隔离是核心设计选择――主持者的下文窗口保持专注于规划和综合永远不会受到200万代币的原始搜索结果污染――工作器每个得到针对其狭窄问题干净200万预算――

### 为什么它赢得

三个机制:

> 三个机制:

1. **Fresh context per subagent.**调查"FIPA-ACL遗产"的工人没有带上40万代币,
   翻译: 中文**每个子 Agent 的清新上下文。**探索"FIPA-ACL遗产"的工作器不携带主导用于规划的40k代币――它获得了一个用于一个问题的200k窗口――
2. **Specialization via prompt.**领导的提示是"分解和合成",而不是"研究".每个工作者的提示是狭窄的:"找到X变化的东西".
   翻译: 中文**通过提示专业化。**主导的提示是"分解和综合",而不是"研究"――每个工作器的提示是狭窄的:"找出X发生了什么变化――"聚焦的提示产生聚焦的输出――.
3. **Parallelism.**工人同时运行.`max(worker_times) + plan + synthesis`没有`sum(worker_times)`现在,我们要去.
   翻译: 中文**并行性。**工作器并发运行.`max(worker_times) + plan + synthesis`没有什么.`sum(worker_times)`,我知道.

### 工程课程 (人类学2025年)

对于2026年,这份"人类"文章列出了一些仍然与2026年相关的生产课程:

> 人类的文章列出了几条仍然适用于2026年的生产经验:

- **Scale effort to query complexity.**简单的查询:一个代理,3-10个工具调用.复杂的查询:10个代理. 领先者必须估计这一点,而不是调用者.
  翻译: 中文**按查询复杂度缩放工作量。**简单查询:一个代理,3-10次调用工具──复杂查询:10+个代理──主管必须估计这一点,而不是调用者──
- **Broad then narrow.**首先将其分解成广泛的子问题,然后在答案要求深度时,每次子问题就会产生更多的工人.
  翻译: 中文**先宽后窄。**首先分解为广泛的子问题,如果答案需要深度,则为每个子问题生成更多工作机.
- **Rainbow deployments.**机器人是长期的,充满状态的.传统的蓝绿色不起作用.人类使用彩虹:新版本逐步推出,而旧版本会耗尽.
  翻译: 中文**彩虹部署。**作为一个长时间运行和状态的代理人.
- **Token usage dominates.**单代理的代币大约是15倍. 只有任务值证明成本时运行.
  翻译: 中文**Token 使用量占主导。**多代理是单个代理的15倍的代币.

### 图表原生转折

拉格格拉夫最初发送了一个`langgraph-supervisor`具有高水平的图书馆`create_supervisor`帮助人. 在2025年,兰格链将推转移到直接通过工具调用实现监督模式,因为工具调用对监督者看到的东西给予更多控制 (文本工程).图书馆仍然运行; 文档现在建议使用工具调用形式.

> 兰格拉夫最初发布了一个带着高级`create_supervisor`助手的`langgraph-supervisor`库──2025年 兰格链将建议通过工具调用直接实现监督者模式,因为工具调用对*监督者看什么*(上下文工程) 提供更多控制.库仍然有效;文档现在推工具调用形式──

转变反映了2025-2026年的见解:文本工程比管弦工程更重要.监督者所看到的决定了他能规划的内容.通过原始工人文本会杀死领导的规划能力;通过狭窄的总结会保存它.

> 这种转变反映了2025-2026年的洞察:上下文工程比编排工程更重要.监督者看到什么决定它能规划什么.传递原始工作器上下文杀主导人的规划能力.传递狭窄摘要保留它.

### 失败模式

- **Lead hallucinates the plan.**如果引擎引发了不解解答真正的问题的子问题,
  翻译: 中文**主导者幻觉计划。**如果主导生成的子问题没有解决真正的问题,工作机会在错误的目标上做精确的研究.
- **Workers over-explore.**没有明确的范围限制,工人超越了分配给他们的子问题,
  翻译: 中文**工作器过度探索。**工作器将漂移超出分配的子问题并污染综合步骤.
- **Synthesis conflicts.**两个工人返回矛盾的事实. 领先者要么重新问 (添加一轮) 或明确指出分歧. 默默选择一边是最严重的失败:用户永远不知道分歧发生了.
  翻译: 中文**综合冲突。**两个工作器回复矛盾的事实――主持人必须重新询问(增加一轮) 或明确记录分歧――静默选择一方是最糟糕的失败:用户永远不知道分歧发生了――

### 当监督员错误时

- **Sequential tasks.**如果步骤2实际上需要步骤1的输出,并行性就不会买到任何东西.使用管道 (CrewAI序列,LangGraph线形图).
  翻译: 中文**顺序任务。**如果步骤2确实需要步骤1的输出,并行性没有帮助.
- **Simple queries.**单个代理人处理它们更快,更便宜.
  翻译: 中文**简单查询。**单代理更快更便宜地处理它们. 在生成工作器之前使用主导的"缩放工作量"检查.
- **Strict determinism.**监督者使用LLM选定的代表. 静态图表在审计/重播更重要时比适应性更好.
  翻译: 中文**严格确定性。**监督者使用 LLM 选择的委派──当审计/回放比适应性更重要时,静态图更好──

## 建立它,实现它.
```figure
supervisor-hierarchy
```

## 建立它

`code/main.py`执行一个由三个平行工人组成的监督员工,使用`threading`将查询分解成子问题,工人同时对每个子问题进行运行,合成.没有真正的LLM工人编写以模拟搜索和总结.

> `code/main.py`使用 `threading`实现一个三个并行工作器的监督者――主导将查询分解为子问题,工作器在每个子问题上发行并运行,主导综合――没有真正的LLM  工作器被编写成模拟获取和总结――

关键结构:

> 关键结构:

- `Lead.plan(query)`问题分为3个子问题.
  翻译: 中文`Lead.plan(query)`查询分为三个问题.
- `Worker.run(sub_q)`返回假摘要 (可能是生产中的任何使用工具的代理).
  翻译: 中文`Worker.run(sub_q)`返回一个假摘要 (在生产中可以是任何工具的代理)
- `Lead.run(query)`它们将工人分为线程,结合和合成.
  翻译: 中文`Lead.run(query)`在线中启动工作器,等待,然后综合――

运行:

```
python3 code/main.py
```

输出显示了计划,并行工作者跟踪了开始/结束时间标签,以及最终合成.你可以看到墙钟的胜利:三名0.3秒的工人在0.35秒内跑,而不是0.9.

> 输出显示计划、带有开始/结束时间的并行工作器跟踪和最终综合――你可以看到挂钟时间优势:三个0.3秒的工作器在约0.35秒内运行,而不是0.9秒――

## 用它实现框架

`outputs/skill-supervisor-designer.md`通过使用者查询,生成一个监督模式设计:导向系统提示,工作者角色,子问题分解规则和合成模板. 在构建新的研究式代理系统之前,使用此.

> `outputs/skill-supervisor-designer.md`接收用户查询并生成监督者模式设计:主导系统提示、工作器角色、子问题分解规则和综合模板──在构建新的研究风格 代理系统之前使用──

## 运送它.

在部署监督模式之前,检查列表:

> 部署监督者模式之前的检查清单:

- **Model pairing.**在推理层模型上 (Opus类,`o3`工人在更快,更便宜的模型上 (Sonnet, `o4-mini`)
  翻译: 中文**模型配对。**主导者使用推理级模型(Opus类`o3`类) ・工作器使用更快,更便宜的模型`o4-mini`
- **Worker timeout.**任何超过2倍的平均运行时间的工人都会被杀害; 领先者要么以更窄的范围重新生长,要么没有它.
  翻译: 中文**工作器超时。**超过2倍的运行时间的任何工作器都被终止;主导者要么以更窄的范围重新生成,要么不继续使用它.
- **Token cap per worker.**强制限制 (例如,预期的合成输入的10倍) 阻止逃离工人
  翻译: 中文**每个工作器的 Token 上限。**硬限制 (例如预期综合输入的10倍) 防止失控工作器耗尽预算.
- **Observability.**追踪领导的计划,每个员工的工具调用,以及合成. 这是任何后期调试的基础.
  翻译: 中文**可观测性。**随着导师的计划,每个工作器的工具调用和综合.
- **Rainbow rollout.**长期经营的代理人需要逐步的版本转换,而不是热交换.
  翻译: 中文**彩虹推出。**需要逐步版本转让,而不是热交换.

## 练习题

1. 跑步`code/main.py`现在,我们可以看到一个新的方法,然后修改引擎到5个工人而不是3.观察墙钟效果.在哪个工人数量上,在这个演示中,产生的总成本超过了平行节省?
   中文翻译:运行 `code/main.py`观察时间效果. 在这个演示中,工作器数量达到多少时时的生产开销超过并行节省?
2. 执行员工时间限制:杀死超过0.5秒的员工,让领先者合成剩余的结果.
   中文翻译:实现工作器超时:终止任何运行超过0.5秒的工作器,让主导者综合剩余结果――你需要什么可观测性来知道工作器被截断?
3. 另外,如果两个工人回复矛盾的答案,领导者会注意到不同意见,而不是选择一个.
   中文翻译:在主导者综合中添加冲突检测步骤:如果两个工作器返回矛盾的答案,主导记录分歧而不是选择其中一个.
4. 列出这款玩具演示需要采用的三种做法,才能在生产中运行.
   中文翻译:阅读Anthropic的研究系统工程文章.
5. 比较兰格拉夫的情况`create_supervisor`什么让你更好地控制监督者看到的东西?为什么人类明确地将仅次答案而不是原始工人背景合成?
   中文翻译:比较 长图的`create_supervisor`什么让你更好地控制监督者看到什么?为什么人类明确只传递子答案而不是原始工作器上下文到综合中?

## 关键词 快速查找表

| Term | What people say | What it actually means |
|------|----------------|------------------------|
| Supervisor / 监督者 | "Lead agent" / "主导 Agent" | An orchestrator agent that plans, delegates, and synthesizes. Does not do the work itself. / 规划、委派和综合的编排 Agent。不做实际工作。 |
| Worker / 工作器 | "Subagent" / "子 Agent" | A focused agent invoked by the supervisor with narrow scope and its own context window. / 由监督者调用的聚焦 Agent，具有狭窄范围和自己的上下文窗口。 |
| Orchestrator-worker / 编排器-工作器 | "Supervisor pattern" / "监督者模式" | Same thing, different name. The 2026 literature uses both. / 同一事物，不同名称。2026 年文献两者都用。 |
| Fresh context / 清新上下文 | "Clean window" / "干净窗口" | A worker's context starts from its system prompt and assigned question, not the lead's history. / 工作器的上下文从其系统提示和分配的问题开始，而不是主导者的历史。 |
| Rainbow deployment / 彩虹部署 | "Gradual rollout" / "渐进推出" | Long-running stateful agents need versioned drain-and-replace, not blue-green. / 长时间运行的有状态 Agent 需要版本化的排空和替换，而不是蓝绿部署。 |
| Token dominance / Token 主导 | "Context is the variable" / "上下文是变量" | 80% of research-eval variance comes from total tokens used, not model choice, per Anthropic. / 80% 的研究评估方差来自使用的总 token，而不是模型选择，据 Anthropic。 |
| Scale effort / 缩放工作量 | "Match agent count to complexity" / "按复杂度匹配 Agent 数量" | Lead estimates query difficulty, spawns 1 vs 10+ workers accordingly. / 主导者估计查询难度，相应地生成 1 个或 10+ 个工作器。 |
| Synthesis conflict / 综合冲突 | "Workers disagree" / "工作器不一致" | Two workers return contradictory facts; the lead must surface disagreement, not silently pick one. / 两个工作器返回矛盾的事实；主导者必须揭示分歧，而不是静默选择一方。 |

## 继续阅读 继续阅读

- [Anthropic engineering — How we built our multi-agent research system](https://www.anthropic.com/engineering/multi-agent-research-system)监管模式的生产参考
  中文翻译:人类工程  我们如何构建多代理研究系统 监督者模式的生产参考
- [LangGraph workflows and agents](https://docs.langchain.com/oss/python/langgraph/workflows-agents) 工具调用监督者现在是建议的形式
  中文翻译:长图工作流和代理  工具调用监督者现在是推的形式
- [LangGraph supervisor reference](https://reference.langchain.com/python/langgraph-supervisor)传统的辅助器,仍在2026年生产中使用
  中文翻译:长图 监督者参考  旧版助手,仍用于2026年生产
- [OpenAI cookbook — Orchestrating Agents: Routines and Handoffs](https://developers.openai.com/cookbook/examples/orchestrating_agents)基于转让的监管变体
  中文翻译:OpenAI 手册  编排 代理:例程和交接  基于交接的监督者变体
