# 随机/群众/网络架构

> 与监管者相比:没有中央决定者. 代理人阅读了共享活动的公共汽车, 随时进行工作, 写回结果. 拉格格拉夫明确支持"群众架构"用于分散,动态环境. 矩阵 (arXiv:2511.21686) 代表了控制和数据流程,作为串行信息通过分布式排队来消除乐队主持人瓶. 交易是明确的:确定性和可追溯性 群集适应许多独立子问题的任务;它不适应需要单一一致的计划的任务.

> **【中文解读】**本节介绍了通过共享状态并行工作的组织模式.

> **【拓展：parallel swarm networks→具体应用】**集团网络让大量的代理同时处理子任务,然后集成结果――适合可并行任务――如多文件编辑,多数数据源查询)――关键挑战是集成结果――如何合并可能发生冲突的部分结果――地图减少是常见的集成模式――2026年实践表明,5-10个并行代理是最优范围――


**Type:** Learn | **类型:** 学习
**Languages:** Python | **语言:** Python
**Prerequisites:** Phase 16 · 05 (Supervisor Pattern), Phase 16 · 04 (Primitive Model) | **前置知识:** Phase 16 · 05 (Supervisor Pattern), Phase 16 · 04 (Primitive Model)
**Time:** ~75 minutes | **时间:** ~75 分钟

>  **【前置】**学本节前请先掌握:阶段16·04-05(原语+监督) ⋅本节是监督的反面无中心协调器的群体网络。
>  **【类比】**集团对监督者 (Swarm vs Supervisor) = "去中心化"对 "层级制"――监督者 = 公司(CEO调度);集团 = 开源社区(每个人看问题 板自己领取) ――集团 适合独立子任务(多文件编辑、多源查询),不适合需要单一计划的任务──5-10 个代理是最优太多会聚合时打架──

## 问题 问题引入

监督员工只能达到几名工人.如果有数百人,怎么办?监督员本身就会成为瓶:每一个决定谁做什么,都通过一个代理. 一个缓慢的计划步骤,会阻碍整个系统.

> 监督者可以扩展到几个工作器――那几百个?监督者本身就变成瓶:每个关于谁做什么的决定都通过一个代理――一个缓慢的计划步骤会停滞整个系统――

监督员本身就是一个LLM电话. 在数百名工人身上,监督员只做数百次LLM电话,每次电话都是秒钟的,发送费用占主导地位.Swarm完全取消监督员.

> 监督者本身就是 LLM调用. 在数百个工作器中,监督者只调用就进行了数百次 LLM调用. 每次调用几秒钟;调度开销占主导.

群众架构翻转了设计.而不是一个中央规划者发送工作,工人从共享队列中选择工作."协调"被入事件巴士语义中.没有管弦器;系统在队列完成之前进行了扩展.

> 群体架构翻转了设计――不是中央规划者分发工作,而是工作器从共享队列中获取工作――"协调"被嵌入事件总线语义中――没有编排器;系统扩展直到队列成为瓶──

建筑的逆转是显著的:瓶从"决定该做的事情的LLM"转变为"路线运行的消息经纪人".LLC是缓慢和昂贵的;消息经纪人是快速和便宜的.Swarm交易LLM瓶对经纪人瓶几乎总是获胜.

> 结构反转重大:从"决定做什么的LLM"转移到"路由工作的消息代理"――LLM 慢而昂贵;消息代理快而便宜――群体用代理瓶换LLM瓶几乎总是赢──

## 概念的核心概念

### 形状

```
                ┌──── shared queue ────┐
                │                      │
       ┌────────┼────────┐  ◄──────┬───┘
       ▼        ▼        ▼         │
     Worker  Worker  Worker   Worker
      A       B       C        D
       │        │        │         │
       └────────┴────────┴─────────┘
                 │
                 ▼
            results pool
```

没有调整器. 每个员工都重复:拉出任务,处理,写出结果 (选择性地排列后续).

> 没有编排器.每个工作器重复:拉取任务,处理,写入结果.

没有中央决策者是定义特征. 工人不等待指示;他们自我组织在队列周围.这是适用于 LLM的演员模式.

> 缺乏中央决策者是定义特征.工作器不等待指令;它们围绕着队列自组织.

### 当群众相应时

- **Many independent tasks.**扫描,转换,分类,任务不依赖于彼此.
  翻译: 中文**许多独立任务。**抓取,转换,分类.任务之间不相互依赖.
- **Variable-duration work.**如果一些任务需要100ms,而其他任务需要10ms,一群平衡负载自动快速的工人将下一个工作拉动.
  翻译: 中文**可变持续时间的工作。**如果某些任务需要100ms,而其他需要10ms,群体会自动平衡负载快速工作器拉取下一个任务――监督者必须预测持续时间――
- **Throughput over determinism.**你关心的是完成时间,而不是严格的订单.
  翻译: 中文**吞吐量优先于确定性。**你关心总完成时间,而不是严格排序.

### 当群众失败时

- **Ordered workflows.**如果步骤3需要步骤2的输出,一群在步骤2完成之前会冒着步骤3的风险.
  翻译: 中文**有序工作流。**如果步骤3需要步骤2的输出,群体有步骤3 在步骤2完成之前触发风险.
- **Global-plan tasks.**复杂的研究问题从一个规划者中获益. 一群研究人员会提供独立的事实,而不是一致的报告.
  翻译: 中文**全局计划任务。**复杂研究问题从规划者中受益. 一群研究人员产生独立的事实,而不是连贯报告.
- **Debugging.**没有中央日志和异步工作,
  翻译: 中文**调试。**没有中央日志和异步工作,复现 bug 的成本很高.

### 矩阵 (arXiv:2511.21686)

矩阵是2025年的论文,将群众带到自然结论:控制流和数据流都是分布式排列上的串行信息.没有中央协调员.错误容忍性来自于消息持久性.可扩展性是消息经纪人的问题,而不是系统的.

> 矩阵是2025年将群体推向自然结论的论文:控制流和数据流都是分布式队列上的序列化消息――没有中央协调器――容错来自消息持久性――可扩展性是消息代理问题,而不是系统的――

通过使经纪人 (卡夫卡,雷迪斯流,NATS) 成为规模瓶,Matrix完全回避了LLM作为管弦乐队员的瓶.

> 通过使代理 (Kafka、Redis Streams、NATS) 成为扩展瓶,Matrix 完全避开了LLM作为编排器的瓶. 如果代理可以,系统可以扩展到数千个代理;LLM是纯工作器,永远不是协调器.

贡献:一个多代理协调模式是"该代理订阅什么信息主题?"而不是"监督者接下来选择哪个代理?" 这使系统看起来像一个公寓/子活动网.

> 贡献:一个编程模型,多代理 协调是"这个代理订阅什么消息主题?"而不是"监督者下一个选择哪个代理?"这使系统看起来像一个发布/订阅事件网格.

### 兰格拉夫的群众建筑
### 图表框架中的群体

兰格拉夫 2025 文件明确描述"群众架构"为多代理模式之一:代理是节点,但边缘形成一个有周期的导向图表,任何节点都可以从池中激活.一个工人根据条件选择可用的工作,而不是监督员的任务.

> 张图 2025 文档明确将"群体架构"描述为多个代理模式之一:代理是节点,但边缘形成有环有向图,任何节点都可以从池中激活.

兰格拉夫的贡献:相同的基于图的心理模型现在支持群体动态. 节点根据条件而不是固定边缘激活. 这将静态图和纯群体世界桥接.

> 拉格格拉夫的贡献:相同的图像基于的心智模型现在支持群体动态――基于条件而不是固定边的激活节点――这桥梁了静态图和纯群体世界――

### 失效模式:饥饿和热点

如果所有工人都能完成最快的任务, 长期的任务就不会被选中,

> 如果所有工作器都能抓住最快可用的任务,长时间运行的任务永远不会被选中,直到它们成为唯一剩下的.

饥饿是群众的标志性失败模式.没有明确的老化 (等待时间增加优先级) 或专业的长任务工作者,10秒任务永远在100ms任务的流后等待.生产群众必须围绕这一点进行工程.

> 饥饿是群体的标志性失败模式――没有明显的老化――优先级随着等待时间增加) 或专业化长任务工作器,10秒任务永远在100ms 任务流后等待――生产群体必须围绕这个工程设计――

减轻:
- 优先排队,显着老化 (随着等待时间增加优先级).
  中文翻译:带显式老化的优先队列 (随着等待时间增加优先级)
- 工人专业化:有些工人只承担"长时间"的任务.
  中文翻译:工作器专业化:一些工作器只接受"长"任务──
- 逆压力:限制排列中进入多少个快速任务.
  中文翻译:背压:限制多少快速任务进入队列──

### 基于内容的路由链接

随着内容基础的路由 (课 22) 进行自然的群组组.而不是一个通用队列,每个消息类型都有一个队列.专业人员只订阅他们的类型.这是以数千个代理为基础的消息巴士架构.

> 群体与内容基础的路由 (课 22) 自然配对――不是通用队列,而是每个类型的消息类型是一个队列――专业化工作器只订阅其类型――这是扩展到数千个代理的消息总线架构的基础――

基于内容的路由加上群群给你提供了 pub/sub事件网格:一个基板,任何代理可以发布任何信息类型,只有感兴趣的代理收到它.这是Matrix,CA-MCP和大多数2026生产多代理系统的基础.

> 基于内容的路由加群给你发布/订阅事件网格:任何代理都可以发布任何类型的消息,只有感兴趣的代理才能接收它.

## 建立它,实现它.
```figure
sw-work-stealing
```

## 建立它

`code/main.py`通过使用4个工人线程的工具,`queue.Queue`任务的持续时间可变 (有些快,有些慢).

> `code/main.py`实现了4个共享`queue.Queue`拉取工作线程──任务有可变持续时间,一些快,一些慢)──演示对比:

对于三方面比较,教育价值是相同的任务,相同的工人,只有调度策略的变化. 序列 = 缓慢. 固定 = 浪费. 群量 = 最佳. 墙钟数字使情况 empirically.

> 三方对比是教育价值:相同任务,相同工作器,只有调度策略变化.

- **Sequential baseline:**一个工人将所有任务进行序列处理.
  翻译: 中文**顺序基线：**一个工作器处理所有任务.
- **Fixed assignment:**每个事先分配给特定的工人 (监督员类型).
  翻译: 中文**固定分配：**每个任务都预先分配给特定的工作器.
- **Swarm:**工人从共享队列中拉下来.
  翻译: 中文**群体：**工作器从共享队列拉取.

积的平衡自动加载; 固定的任务让快速的工人放松,

> 群体自动平衡负载;固定分配在分配任务慢时让快速工作器空──

工作者在50ms内完成任务,则再拉出三个,而一个工作者在2秒任务上仍然在第一任务. 总墙钟由最慢的单个任务,而不是总数限制.

> 完成任务工作器在2秒任务工作器仍然在第一任务上取三个更多任务.

输出显示每位工人任务数量 (群群分布不均但最佳) 和墙钟时间.

> 输出显示每个工作器的任务计数,群体分布不均,但最优) 和挂钟时间.

## 用它实现框架

`outputs/skill-swarm-fit.md`评估任务是否应该使用群与监督者.输入:任务独立性,持续时间差异,订单要求,可调试需求.

> `outputs/skill-swarm-fit.md`评估任务应由群体或监督者使用.

## 运送它.

检查列表:

> 检查清单:

- **Priority queue with aging.**防止长期的饥饿.
  翻译: 中文**带老化的优先队列。**防止长任务饥饿.
- **Worker idempotency.**工人在运行中遇到事故,工作可能会一次以上被拖累.
  翻译: 中文**工作器幂等性。**如果工作器中途崩,任务可能会被拉取多次.
- **Durable queue.**通过使用卡夫卡,Redis流或数据库支持的排队进行制作. `queue.Queue`只有记忆.
  翻译: 中文**持久队列。**生产环境使用卡夫卡、Redis流或数据库支持的队列──`queue.Queue`只有内存中.
- **Observability per task.**每个任务都有一个追踪身份证,每个员工都会使用它开始/结束日志.
  翻译: 中文**每个任务的可观测性。**每个任务都有跟踪身份证; 每个工作器都记录它开始/结束.
- **Back-pressure.**如果排队增长得比工人排水更快,
  翻译: 中文**背压。**如果队列增长速度快于工作器排空速度,减缓生产者.

## 练习题

1. 跑步`code/main.py`变量时间工作负载上的群群比顺序速度快多多?
   中文翻译:运行 `code/main.py`群体在可变持续时间工作负载上比顺序快多少?比固定分配快多少?
2. 添加优先排列变量 (使用 `queue.PriorityQueue`按任务"重要"字段分配优先级. 观察是否在持续负载下,低优先级任务会饿死.
   中文翻译:添加优先队列变体(使用 `queue.PriorityQueue`根据任务"重要"字段分配优先级.
3. 实现热点检测器:记录当任何工人处理比最慢工人多的任务3倍时. 这表明任务时间分布如何?
   中文翻译:实现热点检测器:当任何工作器处理任务时记录的速度比最慢的工作器多3倍.
4. 阅读Matrix论文 (arXiv:2511.21686) 摘要和第3节. 确定一个特定的交易Matrix接受 (可扩展性收益) 和一个它放弃 (可追溯性,确定性).
   中文翻译:阅读矩阵论文 ((arXiv:2511.21686) 摘要和第3节──识别矩阵 接受一个具体权衡(可扩展性收益) 和一个放弃的(可追溯性、确定性)──
5. 转换群体演示器使用一个`queue.Queue`工作人员只会订阅特定类型. 任务异性时,哪些路由规则是有意义的?
   中文翻译:将群体演示转换为使用 (任务类型,有效载荷) 元组的 `queue.Queue`工作器只订阅特定类型.当任务构建时,什么路由规则是合理的?

## 关键词 快速查找表

| Term | What people say | What it actually means |
|------|----------------|------------------------|
| Swarm architecture / 群体架构 | "Decentralized agents" / "去中心化 Agent" | Workers pull from shared queue; no central orchestrator. / 工作器从共享队列拉取；没有中央编排器。 |
| Event bus / 事件总线 | "Agents subscribe to topics" / "Agent 订阅主题" | Message broker that routes tasks to workers by type or content. / 按类型或内容将任务路由到工作器的消息代理。 |
| Starvation / 饥饿 | "Task never runs" / "任务永远不运行" | Low-priority task never gets picked because higher-priority work arrives continuously. / 低优先级任务因为高优先级工作持续到达而永远不被选中。 |
| Hot-spotting / 热点 | "One worker drowns" / "一个工作器淹没" | Load imbalance where one worker gets most tasks. / 一个工作器获得大部分任务的负载不均衡。 |
| Back-pressure / 背压 | "Slow down the producer" / "减慢生产者" | Mechanism that signals upstream to stop producing when the queue fills up. / 当队列填满时向上游发出停止生产的信号机制。 |
| Idempotent worker / 幂等工作器 | "Safe to re-run" / "安全重新运行" | A task processed twice produces the same result. Required because workers may crash mid-run. / 任务处理两次产生相同结果。因为工作器可能中途崩溃所以需要。 |
| Durable queue / 持久队列 | "Survives crashes" / "崩溃后存活" | Queue backed by disk or replicated storage; tasks are not lost when a worker crashes. / 由磁盘或复制存储支持的队列；工作器崩溃时任务不丢失。 |
| Matrix framework / Matrix 框架 | "Full message-passing swarm" / "全消息传递群体" | Both data and control flow are serialized messages on distributed queues. / 数据流和控制流都是分布式队列上的序列化消息。 |

## 继续阅读 继续阅读

- [LangGraph workflows and agents — Swarm Architecture](https://docs.langchain.com/oss/python/langgraph/workflows-agents)明确的群群支持
  中文翻译:长度图 工作流和代理  群体架构  明确的群体支持
- [Matrix — A Decentralized Framework for Multi-Agent Systems](https://arxiv.org/abs/2511.21686) 完整的传递信息群
  中文翻译:矩阵  多代理 系统的去中心化框架  全消息传递群体
- [Anthropic engineering — why supervisor not swarm in Research](https://www.anthropic.com/engineering/multi-agent-research-system)为什么一个特定的生产系统明确选择监管者而不是群众
  中文翻译:人类工程 为什么研究系统选择监督者而不是群体 为什么一个特定的生产系统明确选择监督者而不是群体
- [AutoGen v0.4 actor-model docs](https://microsoft.github.io/autogen/stable/)事件驱动演员重写,比 v0.2 的群体聊天更接近群体
  中文翻译:AutoGen v0.4演员 模型文档  事件驱动演员 重写,比 v0.2 的群体聊天更接近群体
