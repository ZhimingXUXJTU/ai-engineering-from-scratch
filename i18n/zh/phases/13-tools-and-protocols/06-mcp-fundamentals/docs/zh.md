# 基本的MCP:无状态请求与JSON-RPC

> 现代MCP没有握手和协议会议.每个请求必须包含足够的元数据,才能被理解,授权,路由和重新尝试.

> **【中文解读】**现代MCP 没有握手,也没有协议会话――每个请求必须自带足够的元数据,才能独立理解,授权,路由和重试――这是2026-07-28 规范相对旧版(2025-11-25 及更早的初始化握手模型) 最根本的范式转变:协议核心从"连接级状态"变成"请求级自描述"――

> **【拓展：MCP→协议演进时间线】**据"人类"公司发布的MCP 于2024年11月首发,现由Linux基金会下的Agentic AI基金会管理.旧版:到2025-11-25 为止) 是"连接 →初始化握手 →操作"的三阶段生命周期模型.2026-07-28 版把协议核心改为无状态:每个请求在`params._meta`里自带协议版本,客户端能力与身份,`initialize`降级为旧版兼容路径──本课是这个MCP系列的基基,后续07(服务器) 、08(客户端) 都基于这个无状态模型──

>  **【前置】**学本节前请先掌握:(1) 阶段13 · 01-05工具接口、函数调用、方案设计;(2) JSON-RPC 2.0 基础(请求/响应/通知 三种信封);(3) 对旧版MCP的初始化 握手有整体认识(本课会解释为什么它被废弃为兼容分支)

**Type:** Learn | **类型:** 学习
**Languages:** Python | **语言:** Python
**Prerequisites:** Phase 13, Lessons 01 through 05 | **前置知识:** Phase 13, Lessons 01 through 05
**Time:** ~55 minutes | **时间:** ~55 分钟

## 学习目标

- 区分MCP的服务器原始功能与客户端功能.
  中文翻译:区分 MCP的服务器原语与客户端特性──
- 建立有效的JSON-RPC 2.0请求和响应`2026-07-28`现在,我们要去.
  中文翻译:为 MCP `2026-07-28`构建合法的JSON-RPC 2.0 请求与响应
- 添加协议版本,客户端功能和客户端身份到每个请求.
  中文翻译:为每一个请求附加协议版本、客户端能力和客户端身份──
- 使用`server/discover`手`UnsupportedProtocolVersionError`没有握手.
  中文翻译:在无手的前提下使用`server/discover`并处理`UnsupportedProtocolVersionError`,我知道.
- 追踪一个独立的请求从验证到完整的结果.
  中文翻译:追踪一个独立请求从校验到完整结果的全程──

## 问题 问题引入

如果服务器记住前一个请求声明了什么,它可以应用错误的权限或返回错误的电线形状.

> 一个MCP服务器可能在同一进程或HTTP工作者上接收了来自不同客户端的两个请求,具有不同的能力.如果服务器记住上述请求声明了什么,就可能使用错误的权限,或返回错误的线程格式.

股`2026-07-28`服务器必须从当前请求中决定如何处理当前请求,而不是从连接历史中决定.

> 股`2026-07-28`消除了这种差异.协议的核心是无状态的.服务器必须根据"当前这个请求"本身来决定如何处理它,而不是根据连接历史.

这改变了心理模型.旧的序列是连接第一,握手第二,操作第三.现代的序列更简单:

> 这改变了心智模型.旧版本的顺序是:先建连接,再握手,后操作.

1. 客户向客户发出自定义请求.
   中文翻译:客户端发送一个自描述的请求――
2. 服务器验证了请求的版本和功能.
   中文翻译:服务器校验该请求自带的版本与能力──
3. 服务器处理该方法.
   中文翻译:服务器处理这个方法.
4. 服务器返回输入结果或JSON-RPC错误.
   中文翻译:服务器返回一个类型化结果或一个JSON-RPC 错误.

接下来的请求从零开始重复相同的过程.

> 下一个请求从头重复同样的流程.

> **【中文解读】**旧模型的隐患在于"服务器记忆":当同一进程前后服务多个客户端时,上一个请求声明的功能会污染下一个请求的处理.`params._meta`里),服务器处理完成即便被遗忘. 这既适合无共享多工部署,也让重试和等变得简单.

>  **【类比】**旧版MCP 像银行柜员办理业务:先取号(建连) 、再出示身份证登记(初步握手) 、之后每笔业务都默认"还是你这个号"――新版MCP 像微信扫码支付:每笔支付请求都自带完整证凭 (订单号、金额身份),任何一台收银机、任何一班都能独立核销售,不需要"记住你是谁"――收银机换班(进程重启、换工) 对业务零影响.

## 概念的核心概念

### 服务器原始

> **【中文解读】**服务器原语仍有三个:工具(模型可调用动作) 资源(按URI 寻址的数据) 提示(可复用模板) 注意新版的重大变化:roots/sampling/logging 这三个旧客户端原语在2026-07-28计划中虽然保留但已废弃;要求改走"多轮往返请求"(服务器返回输入_要求,客户端补充输入后重试) 现代服务器不再主动发起任何独立的 JSON-RPC 请求──

 MCP 服务器暴露了三个主要的原始性:

1. **Tools**它们是模型控制的行动,`tools/list`,,,,,,,,,,,,,,,,,,,,,,,,,,,,,.`tools/call`现在,我们要去.
   翻译: 中文**Tools**是由模型控制的动作,用`tools/list`发现,使用`tools/call`调用.
2. **Resources**发现的数据是 URI-  адресат的数据`resources/list`子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子,子`resources/read`现在,我们要去.
   翻译: 中文**Resources**是根据URI寻址的数据,用`resources/list`发现,使用`resources/read`读取:
3. **Prompts**它们是可重复使用的模板,`prompts/list`字母为`prompts/get`现在,我们要去.
   翻译: 中文**Prompts**是可复用模板,用`prompts/list`发现,使用`prompts/get`染色.

根源,样本采集和伐木仍然存在`2026-07-28`它们是过时的. 新的实施应使用明确的工具或资源输入,用于根源,直接的模型提供商API用于采样,以及用于记录的 stderr或 OpenTelemetry. 通过多次回路请求,服务器返回输入请求,客户端重复初始操作. 现代服务器从来没有启动独立的JSON-RPC请求.

> 根,样本和伐木`2026-07-28`模式中为兼容而保留,但已被废弃. 新实现应:roots 用显式的工具或资源输入替代;样本直接调用模型提供商API;登录用 stderr或 OpenTelemetry。发出 仍然可通过"多轮往返请求" (多轮往返请求) 通过服务器返回一个输入请求,客户端补齐后重试原操作。现代服务器绝不主动发起独立的 JSON-RPC 请。

>  **【困惑】**问: 旧版引以为傲的样本测试 (服务器借户端模型) 怎么说废弃就废弃? A: 实践中它把"谁付代币费用哪个模型"的决策权了,还要求服务器能反向调用客户端,使网关、鉴定权和缓存都变得复杂.`input_required`了解这段"原语兴衰史"比背六种原语更重要:协议词表会变化,"请求自定义+服务器不主动发起"这两个不变式才是核心.

### 其他类型的文件

采用JSON-RPC 2.0的MCP:

- 要求:`{jsonrpc, id, method, params}`
  中文翻译:请求:`{jsonrpc, id, method, params}`,我知道.
- 答案:`{jsonrpc, id, result}`或`{jsonrpc, id, error}`
  中文翻译:响应:`{jsonrpc, id, result}`或`{jsonrpc, id, error}`,我知道.
- 通知:`{jsonrpc, method, params}`没有`id`
  中文翻译:通知:`{jsonrpc, method, params}`没有`id`,我知道.

要求`id`没有创建协议会议.

> 请问`id`只是为了关联一个响应.

### 要求的请求元数据

每个现代要求都包含一个`_meta`内部的物体`params`其他:

> 每个现代要求都在`params`带一个`_meta`标题:

```json
{
  "jsonrpc": "2.0",
  "id": 7,
  "method": "tools/list",
  "params": {
    "_meta": {
      "io.modelcontextprotocol/protocolVersion": "2026-07-28",
      "io.modelcontextprotocol/clientCapabilities": {},
      "io.modelcontextprotocol/clientInfo": {
        "name": "course-client",
        "version": "1.0.0"
      }
    }
  }
}
```

需要协议版本和客户端功能. 客户端身份是建议的. 它是自主报告的显示和调试数据,而不是安全凭证.

> 协议版本和客户端能力是必需的.客户端身份是建议提供的.

服务器不得从之前的请求,工作室进程,HTTP连接或单独的运输标题中推断这些值.

> 服务器必须从更早的请求,工作室进程,HTTP连接或单个传输层头部推断这些值.

> ️ **【易错点】**场景:从旧版本迁移到实现的仅仅在第一条请求里带来`_meta`后续请求"省略"以图省事 / 后果:服务器根据规则必须拒绝`-32602`其他坑只依赖HTTP头条版本号,不经验机构规则要求对头部和身体的边界处分,不一致返回`-32020`/ 修复:用统一的请求构建函数为每条请求盖全`_meta`三件套(版本、能力、身份),并发送前检查序列化后的最终报文.

### 完整的结果和服务器身份

每一个成功的现代结果都包括`resultType`正常的最终结果使用`"complete"`服务器还应在结果的元数据中识别自己:

> 每一个成功的现代结果都包含`resultType`△普通最终结果用`"complete"`△服务器还应在结果元数据中自我识别:

```json
{
  "jsonrpc": "2.0",
  "id": 7,
  "result": {
    "resultType": "complete",
    "tools": [],
    "ttlMs": 30000,
    "cacheScope": "public",
    "_meta": {
      "io.modelcontextprotocol/serverInfo": {
        "name": "notes-server",
        "version": "1.0.0"
      }
    }
  }
}
```

`tools/list`现在`resources/list`现在`prompts/list`现在`resources/templates/list`现在`resources/read`其他`server/discover`它们包括:`ttlMs`其他`cacheScope`安全的默认是`ttlMs: 0`其他`cacheScope: "private"`列表项应有确定性排序,因此相当的响应产生稳定的缓存键和稳定的模型背景.

> `tools/list`,我知道.`resources/list`,我知道.`prompts/list`,我知道.`resources/templates/list`,我知道.`resources/read`和 `server/discover`它们包含`ttlMs`和 `cacheScope`△安全默认值是`ttlMs: 0`加  `cacheScope: "private"`△列表项应有确定性排序,这样等价的响应才能产生稳定的缓存键和稳定的模型下文.

### 没有握手的发现

> **【中文解读】** `server/discover`取代旧版本初始化 握手的"探路"角色:客户端可以先使用它获得服务器支持的版本集合、能力、使用说明和身份──但它是便利而不是门因为每个请求本来就基于自带的版本和能力,客户端跳过发现直接发发`tools/list`也完全合法. 版本不匹配时返回.`-32022`客户端换一个双方都支持的版本重试即可.

每个现代服务器都必须实现`server/discover`客户可以在其他方法之前调用:

- `supportedVersions`
  翻译: 中文`supportedVersions`(支持的版本集合)
- 服务器`capabilities`
  中文翻译:服务器 `capabilities`(能力)
- 选择性使用`instructions`
  中文翻译:可选的使用 `instructions`(说明)
- 结果是服务器身份`_meta`
  中文翻译:结果`_meta`中的服务器身份
- 缓存提示
  中文翻译:缓存提示

发现是有用的,但它不是一个门户.`tools/list`首先,因为该请求已经包含了协议版本和功能.

> 发现很有用,但它不是门.`tools/list`因为这个请求已经带来了自己的协议版本和能力.

如果未支持请求版本,服务器将返回JSON-RPC代码 `-32022`含有:

> 如果请求版本不支持,服务器返回JSON-RPC 错误码`-32022`附带:

```json
{
  "requested": "2027-01-01",
  "supported": ["2026-07-28"]
}
```

客户端选择一个互助的现代版本,并再次尝试使用新的JSON-RPC请求ID.

> 客户端选择一个双方都支持的现代版本,使用一个新的JSON-RPC 请求ID重试──

### 一个请求生命周期

追踪一个现代要求的顺序:

> 按下列顺序追踪一个现代请求:

1. 分析一个JSON-RPC封面.
   中文翻译:解析一个JSON-RPC 信封──
2. 确认`jsonrpc`是`"2.0"`其他`id`存在的`method`是一个字符串,`params`是一个物体.
   中文翻译:确认 `jsonrpc`是 `"2.0"`存在`id`,我知道.`method`是字符串,`params`是象象.
3. 要求版本字符串和功能对象在 `params._meta`错误的或缺失的元数据`-32602`现在,我们要去.
   中文翻译:要求 `params._meta`中有版本字符串和能力对象;元数据形或缺失返回 `-32602`,我知道.
4. 在HTTP边界,将版本,方法和适用的名称标题与体格进行比较.`-32020`即使两个版本值中的一个不支持.
   中文翻译:在HTTP边界处比对版本、方法和适用的名称头部与体──不一致返回 `-32020`即使两个版本值中的一个本身都不受支持.
5. 通过" 支持不支持的版本"来拒绝相匹配的版本.`-32022`现在,我们要去.
   中文翻译:确认一致后,再拒绝"一致但不受支持"的版本,返回 `-32022`,我知道.
6. 检查所需的能力,然后通过路线`method`验证特定方法的参数.
   中文翻译:检查必需能力,然后按 `method`路由并校验方法级参数
7. 在操作器运行之前,验证和授权混凝土操作.
   中文翻译:在操作之前对具体操作做认证与授权.
8. 返回一个完整的结果,提供服务器身份.
   中文翻译:返回带服务器身份的完整结果──
9. 忘记请求范围的协议元数据.
   中文翻译:忘掉请求级的协议元数据――

> **【中文解读】**这条序列本身就是安全设计:先验证信封、再验证元数据、再比对头部和身体(防"头条笔记.阅读、身体却是笔记.删除"的走私) 、然后才授权和执行──每一步的失败都有独立的错误码(-32600/-32602/-32020/-32022/-32001等),排查时能确定哪个层的问题,而不是得到一个的"400个坏请求"――

通过该命令,两个组件无法解释不同的通话.`Mcp-Name: notes.read`在原始执行时`params.name: notes.delete`它还将错误输入,标题混,版本谈判,能力故障,授权和处理器故障作为明显的证据.

> 为了防止两个组件对"同一个调用"做不同的解读.`Mcp-Name: notes.read`同时,源站执行`params.name: notes.delete`                                                                                                                                                                                                                                                              

关闭STDIN或HTTP响应结束了运输活动.它不会终止协议会议,因为现代MCP没有协议会议.

> 关闭 站或返回 HTTP 响应只是结束传输层活动――它并非终止协议会话,因为现代MCP根本没有协议会话――

### 显而易见的遗产兼容性

通过版本`2025-11-25`使用`initialize`现在`notifications/initialized`由于这种行为仍然是相关的,当一个双代客户端与旧服务器交谈时.

> 到了`2025-11-25`为止版本使用`initialize`,我知道.`notifications/initialized`、连接级别能力,以及早期流媒体HTTP上可选的协议会话.

保持时代分开.现代请求由每请求所需的元数据识别.只通过记录后退路径选择旧连接.`initialize`作为一个 `2026-07-28`服务器.

> 把两个时代分开.现代请求由必需的单个请求元数据识别.`initialize`当作发给`2026-07-28`服务器的默认行为――

无国籍因此具有特定时代的意义.`2026-07-28`任何普通请求都可以独立解释,没有MCP会议.`2025-11-25`双代实现不是一个允许状态的机器.它是一个独立的传统适配器旁边的无状态现代核心,在任何解析器运行之前,它明确选择决定.

> 因此"无状态"有时有特定的含义.`2026-07-28`中它是协议不变式:每个普通请求都可独立解读,没有MCP会话.`2025-11-25`为止版本中,初始化和协商的能力属于连接,因此兼容适配器可以保留旧版本连接状态.双时代实现不是一个"什么都能容纳"的状态机,而是:一个无状态的现代内核,旁边挂着一个隔离的旧版本适配器,在任何解析器运行之前先做一次显而易见的时代选择.

任何一个意思都禁止持久的应用状态.一个工作流程,任务或草案可以在共享存储器中的不透明的手柄背后生活.客户端将该手柄作为普通输入,每个复制都验证并授权其使用.协议文本不得作为删除的会议的替代品泄露到该存储器中.

> 两种含义都不禁止持久的应用状态――工作流、任务或草稿可以在共享存储中的一个不透明的句柄后面生活――客户端将这个句柄视为普通输入发送,每个副本都对其使用进行认证和授权――协议上下文必须作为被移除的会话的替代泄漏到该存储中――

> **【拓展：双时代并存的现实】**"无状态"绝对不等于"不能有业务状态"这是最常见的误读.笔记,任务,工作流照常存储库;被禁止的只是"把协议上下文当隐式会话使用" (例如通过"上述链接下载的声明过什么能力"来解码下一条请求) 分清"应用状态(可以持久) "与"协议会话状态(已移除"),就抓住了2026-07-28 版的精神.

```figure
mcp-tool-call
```

## 用它实现框架

`code/main.py`建立,验证,追踪和发送现代MCP消息,而无需框架.

> `code/main.py`在不借任何框架的情况下构建,校验,追踪并分发代MCP消息──运行:

```bash
python3 code/main.py
python3 -m unittest discover code/tests -v
```

检查输出中的三个不变:

> 在输出中关注三个不变式:

- 每个请求都重复了自己的要求.`_meta`其他地方
  中文翻译:每个请求都重复携带自己的`_meta`字段.
- 每个成功的结果都是`resultType: "complete"`包含服务器身份.
  中文翻译:每个成功的结果都是`resultType: "complete"`且包含服务器身份──
- 列表结果是确定性排序的,并且有明确的缓存提示.
  中文翻译:列表结果有确定性排序并带显式缓存提示──

## 运送它.

这一课是很好的.`outputs/skill-mcp-handshake-tracer.md`历史文件名仍然稳定,但该文物现在已成为一个无状态请求追踪器. 它独立审计每个消息,并且只有当它真正存在时才标记旧的握手流量.

> 本课交付 `outputs/skill-mcp-handshake-tracer.md`◎文件名沿用历史名称,但产品现在是一个无状态请求追踪器:它独立审计每条消息,只有当手动流量真正出现时才标记为遗产──

## 练习题

1. 改变一个请求的协议版本为 `2027-01-01`确认错误代码是`-32022`数据宣传支持版本.
   中文翻译:把某个请求的协议版本改成 `2027-01-01`确认错误码是`-32022`且数据中公布了受支持的版本.

2. 删除`io.modelcontextprotocol/clientCapabilities`确认服务器不使用第一请求的功能.
   中文翻译:从第二请求中移除 `io.modelcontextprotocol/clientCapabilities`❖确认服务器不会复用第一个请求的能力声明――

3. 转换内存工具登记器. 确认`tools/list`仍然返回相同的确定性顺序.
   中文翻译:反转内存中的工具注册表――确认 `tools/list`仍返回相同的确定性顺序.

4. 改变`cacheScope`其他`public`为了`private`解释哪些授权环境可以在每个情况下重复使用响应.
   中文翻译:把 `cacheScope`从`public`改成`private`解释两种情况下文中的授权可以复用该响应

5. 添加一个可选的`clientInfo`由于客户身份是建议的,而不是要求的,请求应保持有效.
   中文翻译:添加一个可选的`clientInfo`缺省测试――该请求应仍然合法,因为客户端身份是建议项而不是必填项――

## 关键词 快速查找表

| Term | Meaning |
|------|---------|
| Stateless protocol | Every request supplies the metadata needed to interpret it |
| Request metadata | Version, client capabilities, and recommended client identity in `params._meta` |
| `server/discover` | Mandatory server method for versions, capabilities, instructions, and identity |
| `resultType` | Discriminator on every successful modern result |
| Cacheable result | Result that includes required `ttlMs` and `cacheScope` hints |
| Protocol era | Modern per-request metadata or legacy connection-scoped initialization |
| Transport lifetime | Process, connection, or response-stream lifetime, not protocol session state |
| `-32022` | Unsupported protocol version error with requested and supported versions |

> 术语中文对照:无状态协议=无状态协议;请求元数据=请求元数据(`params._meta`中文版本、客户端能力与建议的客户端身份);服务器发现/发现=服务器发现(必选方法,公示版本、能力、说明与身份);结果Type=结果类型(每个成功现代结果上的判别字段);可缓存结果=可缓存结果(必含`ttlMs`与`cacheScope`提示);协议时代 (协议时代) 现代逐请求元数据与旧版连接级初始化;运输寿命=传输层生命周期 (进程/连接/响应流的存活期,不是协议会话状态);-32022=不支持的协议版本错误 (附请求与支持) ]]

## 继续阅读 继续阅读

- [MCP Architecture](https://modelcontextprotocol.io/specification/2026-07-28/architecture)
  中文翻译:MCP 架构文档(2026-07-28 版)
- [MCP Base Protocol](https://modelcontextprotocol.io/specification/2026-07-28/basic)
  中文翻译:MCP 基础协议(JSON-RPC 信封、元数据、生命周期)
- [MCP Server Discovery](https://modelcontextprotocol.io/specification/2026-07-28/server/discover)
  中文翻译:服务器/发现 方法规范
- [MCP 2026-07-28 Changelog](https://modelcontextprotocol.io/specification/2026-07-28/changelog)
  中文翻译:2026-07-28 版变更日志
