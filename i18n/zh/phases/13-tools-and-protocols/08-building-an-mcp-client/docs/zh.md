# 建立一个MCP客户端:发现,路由,和双代倒退. 构建MCP客户端:发现,路由与双时代回归.

> 现代MCP客户端每次请求都会重复其合同. 它最难的兼容性决定是知道什么时候一个旧服务器真的老了,什么时候一个现代服务器正在报告一个可纠正的错误.

> **【中文解读】**现代MCP客户端在每次请求上重复携带自己的契约 (?? 版本、能力、身份) ⋅最难的决定是兼容性判断:对端到底是"真旧" (??) 只有初始化握手的老服务器),还是"现代服务器在报告可修正的错误"?本课构建多服务器客户端:现代发现探测"",白名单授权的旧版本探测"",确定性工具合并与路由,全程不发明协议会话.

> **【拓展：MCP 客户端→Agent 编排核心】**客户端是代理主人的核心──Claude Desktop、Cursor等都同时加载多个MCP 服务器──文件系统、Postgres、GitHub......),把工具列表合并后交给模型──2026-07-28 让稳态更简单(每一个请求自含),让启动更微妙四种对端形态──现代/报版错/没听到发现/沉默等) 必须依赖"运营图+正向协议证据"来区分,而不是把一切探测失败都在旧版中.

>  **【前置】**学本节前请先掌握:(1) 第13阶段 · 07(构建MCP 服务器) `server/discover`、每一个请求的数据校验、`resultType`与缓存提示;(2) 第13期 · 06期的无状态请求模型与JSON-RPC 信封;(3) Python可调用对象/回调模拟传输层的方式(本课用进程内同行函数代替真实子进程) ――多服务器合并与路由是本课新增量──

**Type:** Build | **类型:** 构建
**Languages:** Python | **语言:** Python
**Prerequisites:** Phase 13, Lesson 07 | **前置知识:** Phase 13, Lesson 07
**Time:** ~85 minutes | **时间:** ~85 分钟

## 学习目标

- 建立每一个MCP`2026-07-28`要求包含当前的元数据.
  中文翻译:为每一个MCP`2026-07-28`请构建携带当前元数据的报文.
- 检查工作室服务器`server/discover`选择一个相互支持的版本.
  中文翻译:用`server/discover`探测工作室 服务器并选择了双方都支持的版本.
- 允许仅限于被明确允许的同龄人进行有限遗产探测.
  中文翻译:仅对显式加入白名单的对端授权一次有界的旧版探测──
- 接受一个遗产时代,只有经过验证一个积极的时代.`initialize`支持的修订结果.
  中文翻译:只有在验证到受支持修订版的正向`initialize`结果后,才接受旧版时代.
- 没有默默地覆盖碰撞的确定性工具列表.
  中文翻译:合并确定性工具列表而不静默覆盖命名冲突。
- 通过调用每个工具的同行,而没有发明协议会议.
  中文翻译:把调用路由到拥有该工具的对端,且不发明协议会话.

## 问题 问题引入

代理主机通常与多个MCP服务器交谈.它必须发现每个服务器,将工具目录结合起来,解决重复名称,路线调用,并恢复运输故障.

> 一个代理主机通常与多个MCP服务器通信――它必须发现每个服务器、合并工具目录、解决重名、调用路由,并从传输层故障中恢复――

其他`2026-07-28`复制使稳定状态更简单,因为每个请求都是自主化的.兼容性使启动更微妙.

> `2026-07-28`修订让稳态更简单,因为每个请求包含自己.

- 支持首选版本的现代服务器;
  中文翻译:支持首选版本的现代服务器;
- 现代服务器,返回已识别的版本或标题错误;
  中文翻译:返回可识别的版本或头部错误的现代服务器;
- 一个从未听说过的遗产服务器`server/discover`其他
  中文翻译:从未听说过`server/discover`旧版服务器;
- 一个遗留服务器,直到收到`initialize`现在,我们要去.
  中文翻译:收到`initialize`之前一直沉默的旧版本服务器.

> **【中文解读】**四种端形态中最容易踩踏的混是后两种与故障的混:形式错误的现代请求,过载的服务器,死掉的进程和旧服务器都可能产生相同的超时或连接关闭.

处理每一个探测器错误都是遗产的.一个错误的现代请求,一个过载的服务器,一个死进程,和一个旧的服务器都能产生相同的时间过关或连接关闭.这些信号是模糊的.客户端必须在选择遗产时代之前,将运营商的明确意图与积极的协议证据结合起来.

> 把每个检测错误都视为旧版本是危险的. 形式错误的现代请求,过载的服务器,死掉的进程和旧服务器都可能产生相同的超时或连接关闭. 这些信号是歧义的. 客户端必须将"明显的运维者意图"和"正向协议证据"结合在一起,才能选择旧版本时代.

>  **【类比】**时代判断像医院急诊分诊:病人(对端服务器) 送来时,你不能因为"叫不应"就断定他是外国人(旧版服务器) 他可能只是昏睡(过载) 耳背(请求格式错) 或已心跳停止(进程死了) `server/discover`),听懂并规范应答 → 现代病人;听懂但纠正你的用语(-32022/-32020/-32021)→ 还是现代病人,改口即可;完全叫不醒 → 也不能直接按外国人处理,除非病历上写"确认是外国人,允许换方言再喊一次"(允许 授权的有界初始化 探测),并且换方言后得到清醒的规范应答(正向初始化 证据) 才确诊.

## 概念的核心概念

### 一个同行,而不是协议会议

> **【中文解读】**客户端为每个服务器进程或端点保留一条"对端记录" (对端记录):传输句柄、选定的时代与版本、最近发现的能力、最近确定性工具列表、待关联的请求 id、传输健康度──注意分寸:这是客户端自己的账户,不是协议会话状态现代MCP下,服务器仍然在每个请求上收到当前版本和能力──

保持每个服务器进程或终端点的一个运输同行记录:

- 运输手柄或发送功能;
  中文翻译:传输句柄或发送函数;
- 选择的协议时代和版本;
  中文翻译:选定的协议时代与版本;
- 最后发现的服务器功能;
  中文翻译:最近发现的服务器能力;
- 最后确定性工具列表;
  中文翻译:最近的确定性工具列表;
- 暂未申请相关性身份证;
  中文翻译:用于关联的待处理请求 id;
- 交通健康.
  中文翻译:传输健康度。

在现代MCP上,服务器仍然在每次请求中接收到当前版本和功能.

> 在现代MCP上,服务器仍然在每个请求上收到当前版本和能力.

### 创建每一个现代要求从零开始

```python
def modern_request(request_id, method, params, version, capabilities):
    return {
        "jsonrpc": "2.0",
        "id": request_id,
        "method": method,
        "params": {
            **params,
            "_meta": {
                "io.modelcontextprotocol/protocolVersion": version,
                "io.modelcontextprotocol/clientCapabilities": capabilities,
                "io.modelcontextprotocol/clientInfo": CLIENT_INFO,
            },
        },
    }
```

连接对象不要连接一次的元数据,假设它已经到达电线. 盖章并检查最终串行请求.

> 不要把元数据连接对象上一次挂在线,默认它已经上线.

### 现代发现

`server/discover`返回支持版本,服务器功能,说明,缓存提示和推的服务器身份. 客户端选择最高的互助现代版本.

> `server/discover`返回支持版本"",服务器能力"",使用说明"",缓存提示和建议的服务器身份――客户端选择双方都支持的最新现代版本――

发现是当代客户端的选项,但在studio上建议.一些传统服务器在启动之前接受操作,所以发送`tools/list`首先可以产生模糊的成功.`server/discover`创造了一个清洁的时代界限.

> 对于纯现代客户端来说,发现是可选的,但在工作室上建议做.`tools/list`可能产生差异的成功.`server/discover`制造一个干净的时代边界.

### 工作室兼容性探测器

> **【中文解读】**收到的发现结果 → 现代对端,选定版本后继续;[2]收到可识别的现代错误(-32020/-32021/-32022)→ 仍是现代对端-32022 就从数据.支持的选定版本重试,其他错误修改请求,绝不发动;[3] 歧义信号未识别的JSON-RPC错误、超时、连接关闭、空响)→ 不判定时间,除非该端配置旧版本兼容就失败关闭――关键不变式:一旦对端证明认识现代错误表,即使在白名单里也不允许降级.

两代工作室客户发送`server/discover`需要在任何其他请求之前使用其首选的现代化元数据.

1. **DiscoverResult.**服务器是现代化的. 选择一个相互支持的版本,然后继续按请求进行元数据.
   翻译: 中文**DiscoverResult。**服务器是现代的. 选择双方支持的版本,以继续请求数据.
2. **Recognized modern error.**服务器是现代化的.`-32022`选择一个`data.supported`对于标题或功能错误,请纠正请求.不要发送`initialize`现在,我们要去.
   翻译: 中文**可识别的现代错误。**服务器是现代的.`-32022`根据`data.supported`中选择并使用新请求 id 重试;头部或能力错误就修正请求――不要发送`initialize`,我知道.
3. **Ambiguous signal.**没有识别的JSON-RPC错误,截止时间,连接关闭或空响应不识别一个时代.除非该精确的同行配置为遗留兼容性,否则失败关闭.
   翻译: 中文**歧义信号。**无法识别的 JSON-RPC 错误,超时,连接关闭或空响应不能确定时代.

已识别的现代协议错误包括:

- `-32020`标题不匹配
  翻译: 中文`-32020`头部不一致 (头部不一致)
- `-32021`缺失要求 客户能力
  翻译: 中文`-32021`缺失必需的客户端能力(缺失所需的客户能力)
- `-32022`无支持协议版本
  翻译: 中文`-32022`不支持的协议版本(不支持的协议版本)

认可的现代错误仍然是现代的,即使同行在遗留的允许列表上. 一旦服务器证明它理解现代错误词汇,发送`initialize`这将是降级.

> 可识别的现代错误即使来自白名单上的对端也被判定为现代.`initialize`这就是降级.

不要治疗`-32601`只有一个被明确允许的同行才有资格接受一个被遗留的探测.同样的规则适用于时间过期,连接关闭或空响应.

> 不要把`-32601`当旧版本正向证据一样,它只是让显然白名单上的端子获得一次旧版本探测的资格.

### 允许列表是操作员的意图,而不是证据

遗产兼容性必须是一个固定同行配置的明确属性:

> 旧版兼容必须是固定对端配置的显式属性:

```python
client.add_server("archive", archive_transport, allow_legacy=True)
```

没有任何选择,请将其绑定到配置命令或终端点.不要使用一个允许任意服务器选择更弱的语义符号的野生卡.`allow_legacy=True`经过模糊的发现结果失败,`initialize`现在,我们要去.

> 把这个选择绑定到配置命令或端点上. 不要使用通配符让任意服务器自行选择更弱的语义.`allow_legacy=True`结果是直接失败,永远不收到.`initialize`,我知道.

允许者允许探测,而不是选择时代.`initialize`在运输的强制期限内,则要求所有以下内容:

> 白名单授予的是探测许可,不是时代选择.`initialize`要求下列条件全部成立:

- 一个JSON-RPC `2.0`答案与匹配请求ID;
  中文翻译:带匹配请求 id 的 JSON-RPC `2.0`响应
- 完全是一个.`result`没有`error`其他
  中文翻译:恰好一个`result`且没有`error`其他
- 其他`protocolVersion`在客户端配置的传统修改集中;
  翻译: 中文`protocolVersion`在客户端配置的旧版本修订集合中;
- 值对象`capabilities`领域;
  翻译: 中文`capabilities`字段是对象值;
- 其他`serverInfo`无空字符串的对象`name`其他`version`其他地方
  翻译: 中文`serverInfo`象带非空字符串的`name`与`version`字段.

时间过关,连接关闭,错误响应,错误的结果,不匹配的ID或未支持的修改未能关闭.只有结构有效的正确结果才能选择遗产时代.代码通过`legacy_probe_timeout_ms`实际的STDIO或HTTP适应器必须执行该截止日期,而不是仅仅记录它.

> 超时、连接关闭、错误响应、形结果、id 不匹配或修改不受支持,都会失败即关闭――只有结构合法的正向结果才选择旧版时代――代码把`legacy_probe_timeout_ms`传给传输适配器;真实工作室或HTTP适配器必须强制执行这个时间限,而不是仅仅记录它.

预备选定的时代,不要在每次电话之前再次查询.

> 选择时代缓存到传输对端.

> ️ **【易错点】**场景:把`-32601`(方法未找到) 或超时直接当成"对端是旧版"证据,随即发送初始化 / 后果:现代服务器上这是降级握手;恶意或故障服务器借机把客户端拖入更弱的语义;无界等待挂死启动流程 / 修复:坚持"白名单只给探测资格 + 五条正向验证全部通过才选旧版",并让传输适配器真正强制.`legacy_probe_timeout_ms`时限;任何一条不满足就失败关闭,对端保持不可用.

### 遗产是一个兼容之分支

一旦边界探测器返回有效的正确遗产证据,客户端使用所选的遗产版本,正如该修订所定义的那样:

1. 检查响应包裹和相关性ID.
   中文翻译:校验响应信封与关联 id──
2. 检查谈判修订在配置的遗产集中.
   中文翻译:确认协商出的修订在配置的旧版集合内──
3. 记录验证的功能和服务器身份.
   中文翻译:记录已验证的能力与服务器身份──
4. 发送`notifications/initialized`只有经过所有支票之后.
   中文翻译:所有检查通过后才发送 `notifications/initialized`,我知道.
5. 使用已旧的请求形状,用于运输寿命.
   中文翻译:在该传输层生命周期中使用旧版本请求形状──

运输系统的使用方式是: 运输系统的使用方式是: 运输系统的使用方式是: 运输系统的使用方式是: 运输系统的使用方式是: 运输系统的使用方式是: 运输系统的使用方式是: 运输系统的使用方式是: 运输系统的使用方式是: 运输系统的使用方式是: 运输系统的使用方式是: 运输系统的使用方式是: 运输系统的使用方式是: 运输系统的使用方式是: 运输系统的使用方式是: 运输系统的使用方式是: 运输系统的使用方式是: 运输系统的使用方式是: 运输系统的使用方式是: 运输系统的使用方式是: 运输系统的使用方式是: 运输系统的使用方式是: 运输系统的使用方式是: 运输系统的使用方式是: 运输系统的使用方式是: 运输系统的使用方式是: 运输系统的使用方式是: 运输系统的运输方式是: 运输系统的运输方式是: 运输系统的运输方式是: 运输方式是: 运输方式是: 运输方式是: 运输方式是: 运输方式是: 运输方式是: 运输方式是: 运输方式是: 运输方式是运输方式是: 运输方式是运输的运输方式是: 运输方式是: 运输的运输方式是:

> 这条分支是与已知端端互联的存在.它不是新服务器或新请求的默认设计.如果传输层重启或端点变化,则丢弃端时缓存并重新协商.

### 发现和缓存工具

对于每位活跃的同龄人,请拨打`tools/list`现代化结果包括`resultType`现在`ttlMs`其他`cacheScope`根据正确的授权背景, 根据订阅过期或订阅过的变更事件, 重新收购.

> 对于每一个活跃对端调用`tools/list`现代结果包含`resultType`,我知道.`ttlMs`和 `cacheScope`在正确授权下文中尊重新鲜度提示在订阅变更事件发生后重新拉取订阅或收到的列表

客户必须处理一个失踪者`resultType`从一个旧服务器中`"complete"`对于一个早期的谈判时代的响应,不需要现代缓存字段.

> 客户端必须把旧版本服务器缺失`resultType`应作`"complete"`应对现代缓存要求的回应应应应对更早协商时代的回应应应应对现代缓存要求

服务器应该返回确定性排序. 客户端也应该在合并之前排序,以便本地注册表排序不取决于进程启动时间.

> 服务器应返回确定性排序――客户端在合并前也应排序,这样本地注册表的顺序就不依赖于启动时序――

### 无碰撞名字空间的合并

> **【中文解读】**两个服务器可能都叫`search`〔三种声明的策略:(1) 冲突加前保留第一规范名,后来的冲突暴露为 `<server>/<tool>`冲突拒绝不加载重复项并报告清晰的配置错误; 3) 静默覆盖永远不要使用,它隐藏"模型中的动作实际发送给哪个服务器"──同时存储规范名和本地名:模型看规范名,发出`tools/call`使用该工具的服务器声明的本地名称.

两个服务器可能都会暴露`search`选择声明的政策:

1. **Prefix on collision.**保存第一名,并将后来的碰撞暴露为`<server>/<tool>`现在,我们要去.
   翻译: 中文**冲突加前缀。**保持第一条规范, 暴露后来的冲突.`<server>/<tool>`,我知道.
2. **Reject on collision.**不要加载复制文件,并出现明显的配置错误.
   翻译: 中文**冲突拒绝。**不加载重复项,抛出清晰的配置错误.
3. **Silent overwrite.**永远不要使用这个,它隐藏了哪个服务器接收了模型选择的操作.
   翻译: 中文**静默覆盖。**永远不要使用. 它隐藏了哪个服务器收到模型中动作.

保存既正规名称和当地名称.模型看到正规名称.`tools/call`使用了所有者服务器声明的本地名称.

> 同时存储规范名与本地名.`tools/call`使用该工具的服务器声明的本地名称.

### 调用通话

路由是纯粹的搜索:

> 路由是一次纯查表:

```text
canonical tool name
  -> peer name + local tool name
  -> new JSON-RPC request id
  -> modern request metadata or explicit legacy shape
  -> matching response id
```

没有可用的运输时不要发电,重新连接或重新启动运输,然后重新执行发现和`tools/list`现代飞行过程中在故障的运输中丢失的请求可在操作的安全政策允许时再使用新的JSON-RPCID.

> 当拥有该工具的传输层不可用时,不要发送调用――重连或重启传输层,然后重运发现与`tools/list`△ 断开传输层上丢失的现代中途请求,在操作的安全策略允许时可以使用新的JSON-RPCID重试──

### 通知和订阅

现代的列表和资源变更只会在客户端开放时发生`subscriptions/listen`客户端发送通知过器,等待`notifications/subscriptions/acknowledged`并且与通知元数据中的听取请求ID相关.

> 现代的列表和资源变化只能到达客户端开放`subscriptions/listen`流上. 客户端发送通知器,等待.`notifications/subscriptions/acknowledged`没有用通知元数据里听 请求ID 关联事件

在断开电话时,打开一个新的听取请求,重新调整相关列表或资源.`Last-Event-ID`现在,我们要去.

> 断连时,打开一个新的听力 请并重新拉取相关列表或资源.`Last-Event-ID`恢复

### 没有服务器启动的请求

现代服务器不会用独立的JSON-RPC请求调用客户端进行样本采集,发出或根源.`input_required`客户端在完成嵌入式输入请求后重新尝试原始请求.

> 现代服务器不会使用独立的JSON-RPC 请调用客户端来采样,发明或根.`input_required`客户端补充内嵌的输入请求后重试原请求.

保持相关性,并创建一个新的JSON-RPCID,以便重新尝试.

> 补齐输入时不要阻端的响应读取器──保持关联性,并为重试创建新的JSON-RPC id──

> **【拓展：与旧版客户端课的对照】**旧版本节课的主角是"子进程管理 + 启动 握手 + 样本回调";新版把它们全部让位给"时代协商":样本/征集 已被废弃为多轮往返请求,握手只剩下一条白名单授权有界探测分支,进程管理抽象成对端记录 (记) ⋅变化最大的也最体会的一个点:客户端的复杂性从"运行时会话编排"转移到"启动时代判断和失败关闭策略"――

```figure
tp-client-merge
```

## 用它实现框架

`code/main.py`通过使用在进程中的同行功能,使协议决策保持可见.它连接到两个现代同行和一个故意允许的遗产同行,然后合并并并路由他们的工具.运输调用器获得时间限期预算,因此兼容分支不能隐藏无限探测器.

> `code/main.py`通过使用进程中的端函数,好让协议决策保持可见. 它连接两个现代端和一个故意加入白名单的旧版本端,然后合并并通过它们的工具.

```bash
cd code
python3 main.py
python3 -m unittest discover tests -v
```

测试证明了正常演示所错过的界限:

> 这些测试证明了普通演示会错过的边界:

- 现代请求重复元数据;
  中文翻译:现代请求重复携带元数据;
- `-32022`试图重新进行现代发现,而不需要初始化;
  翻译: 中文`-32022`重试现代发现而不做初始化;
- 已认可的现代错误,甚至对于被允许的同行,从来没有降级;
  中文翻译:可识别的现代错误绝不降级即使在端在白名单上;
- 时间停止,连接关闭,空置响应,未识别错误不会触发`initialize`没有允许的;
  中文翻译:超时、连接关闭、空响应和未识别的错误在没有白名单时不会触发`initialize`其他
- 允许的同行仅在有效,支持后成为继承者`initialize`结果;
  中文翻译:白名单对端只有在接受合法和受支持的`initialize`结果后才成为旧版;
- 错误的和未支持的遗产结果使同行无法获得;
  中文翻译:形或不支持的旧版本结果使端保持不可用;
- 已成功选择的时代将为运输寿命存储在缓存中.
  中文翻译:成功选定的时代会缓存整个传输层生命周期.

## 运送它.

这一课是很好的.`outputs/skill-mcp-client-harness.md`它提供了现代的请求邮票,工作室时代的谈判,确定性命名空间的合并,路由和一个故障关闭的传统兼容分支.

> 本课交付 `outputs/skill-mcp-client-harness.md`△它建立现代请求盖章、studio 时代协商、确定性命名空间合并、路由,以及一条失败关闭的旧版本兼容分支──

## 练习题

1. 让一个假的服务器返回`-32022`确认客户端失败,而不是发送`initialize`现在,我们要去.
   中文翻译:让一个假服务器返回`-32022`且没有双方支持的版本. 确认客户端直接失败而不是发送.`initialize`,我知道.

2. 允许一个假的遗产服务器,使其有限`initialize`探测时间,证明同龄人留下了`unknown`没有任何东西.
   中文翻译:把一个假旧版服务器加入白名单,让它有界限的 `initialize`探测超时,证明该对端保持`unknown`且不可用.

3. 加入`cacheScope: "private"`确认客户端从来没有与另一个文本共享一个缓存结果.
   中文翻译:为两个授权上下文添加 `cacheScope: "private"`确认客户端绝对不会把一个上下文的缓存结果共享给另一个.

4. 改变碰撞政策到拒绝,使启动失败,
   中文翻译:把冲突策略改为拒绝,让启动失败,带上两个对端的名字.

5. 添加一个有限的`subscriptions/listen`在流失时,再用新的请求身份和重复工具来听.
   中文翻译:添加一个有限的`subscriptions/listen`模拟器──流断开时,使用新请求ID 重新监听并重新拉取工具──

## 关键词 快速查找表

| Term | Meaning |
|------|---------|
| Peer | Client-side record for one server transport and its discovered data |
| Protocol era | Modern per-request metadata or legacy initialization semantics |
| Discovery probe | Initial `server/discover` used to identify the stdio era |
| Recognized modern error | Error that proves modern behavior and forbids legacy fallback |
| Legacy allowlist | Operator configuration permitting one bounded compatibility probe for a pinned peer |
| Positive legacy evidence | Valid, correlated `initialize` result for an explicitly supported legacy revision |
| Merged namespace | Canonical tool names across all active peers |
| Collision policy | Prefix or reject rule for duplicate tool names |
| Era cache | Selected modern or legacy behavior stored for one transport peer |
| Transport recovery | Restart or reconnect, rediscover, relist, and retry safely with a new id |

> 术语中文对照:Peer=对端(客户端侧对服务器传输层及其发现数据的记录);协议时代 (Protocol era=协议时代) 现代逐请求元数据或旧版初始化语义);发现探测 (Discovery probe=发现探测) 用于判断工作室时代的首次`server/discover`); 认可现代错误=可识别的现代错误(证明端是现代的并禁止旧版回退); 遗产权限列表=旧版白名单(允许一个固定端进行一次有界兼容探测的运维配置); 积极的遗产证据=正向旧版证据(对显然受支持的旧版修改的合法和关联`initialize`结果); 合并名字空间=合并命名空间(跨所有活跃对端的规范工具名); 冲突政策=冲突策略(重名工具的前或拒绝规则); 时代缓存=时代缓存(用于一个传输对端存下选定的现代或旧行为); 运输恢复=传输层恢复(重启/重连、重新发现、重新列表、使用新 id 安全重试) ⋅

## 继续阅读 继续阅读

- [MCP Specification 2026-07-28](https://modelcontextprotocol.io/specification/2026-07-28/)
  中文翻译:MCP 2026-07-28 规范全文
- [MCP Server Discovery](https://modelcontextprotocol.io/specification/2026-07-28/server/discover)
  中文翻译:服务器/发现 方法规范
- [MCP stdio Transport](https://modelcontextprotocol.io/specification/2026-07-28/basic/transports/stdio)
  中文翻译:stdio 传输层规范
- [MCP Versioning](https://modelcontextprotocol.io/specification/2026-07-28/basic/versioning)
  中文翻译:MCP 版本协商机制
- [MCP Tools](https://modelcontextprotocol.io/specification/2026-07-28/server/tools)
  中文翻译:工具 原语规范
