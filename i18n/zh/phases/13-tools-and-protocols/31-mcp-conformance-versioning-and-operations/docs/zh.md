# 技术支持: 版本化,证据和运营

> 服务器不符合,因为其快乐路径通过一个SDK工作. 符合性在线,版本边界,通过中间人,以及在滚动过程中.

> **【中文解读】**一个服务器不会因为"快乐路径在某个SDK 里跑通了"就算一致 (conformant) 了. 一致性活在四个地方:线格式 (线格式) 上,版本边界上,穿越中间设备时以及回滚过程中. 本课是MCP 2026-07-28规范系列的结尾课:把规范性规则变成可执行的黄金/负转录 (转录)语料库,把版本纪元,头部完整性,SDK归纳到差分,代理证据,脱敏,健康窗口和回滚证据,全部进入一个发布门口) 了.

> **【拓展：MCP→质量工程与 SRE】**它们是HTTP/TLS的经典项目,与SRE的经典项目 (http/TLS的统一套件,降级保护,金丝雀的发布都是同构问题) 它们的特殊性在于将它们与准备MCP的具体契约:`resultType`判斷器、镜像路由头、通知不變量、以及2026-07-28的自含元数据──它与第13期 · 29(可靠性) 和第13期 · 30(注册中心准入) 首尾相接:29管"出事怎么活",30管"进门前怎么审",本课管"凭什么说它一直对"──

>  **【前置】**学本课前请先掌握:第13阶段 · 09(MCP 传输可流的HTTP与头部) 第13阶段 · 17期网关与注册中心中间设备与镜像头) 第13阶段 · 30期(注册中心准入pin、证据摘要与回滚目标) 

**Type:** Build | **类型:** 动手实践
**Languages:** Python | **语言:** Python
**Prerequisites:** Phase 13 · 09 (transports), Phase 13 · 17 (gateways), Phase 13 · 30 (registry admission) | **前置知识:** Phase 13 · 09（传输）、Phase 13 · 17（网关）、Phase 13 · 30（注册中心准入）
**Time:** ~100 minutes | **时间:** 约 100 分钟

## 学习目标

- 转换规范性MCP规则成金色和负线转录.
  中文翻译:把规范性的MCP规则变成黄金与负面线格式转录 (转录) ]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]
- 保持严格的态度`2026-07-28`行为与传统的背后行为是不同的.
  中文翻译:让严格的 `2026-07-28`行为与有界的旧版本回归 (旧版本回归) 保持分离.
- 区分添加未知的字段与无效未知的字段`resultType`现在,我们要去.
  中文翻译:区分"增量式未知字段"与"非法的未知 `resultType`"──
- 进行原始JSON-RPC证据与SDK正常化的视图的比较.
  中文翻译:把原始JSON-RPC证据与SDK归结后的视图做差分比较.
- 通过一个真正的代理界限证明头部和身体的完整性.
  中文翻译:穿越真实的代理边界证明头部与主体的完整性.
- 关口的发行文件,有编辑的转录,健康和反弹证据.
  中文翻译:用脱敏后的转录、健康与回滚证据发布关.

## 问题 问题引入

> **【中文解读】**"SDK 调调"`tools/list`得到了工具,集成测试绿色了"这个结果回答了不真正的问题:请求带不带现代的单次请求元数据?头部与JSON-RPC主体一致吗?`resultType`是否是线上现实还是SDK 合成的?未来新增字段会保留吗?一个现代错误会不会意外触发旧版本的手掌吗?代理保留源站状态码和错误吗?通知序列化器是否发出禁止响应?运维能不能不存储秘密就证明一次发布或回滚的理由?一致性就是一组可观察的不变量 在生产流量替代问题之前,先制造一个能够捕获这些不变量的测试架 (() ).

你的客户打电话`tools/list`通过 SDK,获得工具.

> 通过 SDK 调用`tools/list`没有得到工具,集成测试通过了.

这样,我们仍然没有回答重要问题:

> 这导致许多没有答案的重要问题:

- 要求是否包含了现代的每次请求协议的元数据?
- 确实`MCP-Protocol-Version`现在`Mcp-Method`其他`Mcp-Name`能否与JSON-RPC的体格相匹配?
- 答案是否包含有效的`resultType`电线上,或者SDK合成了一个?
- 客户是否会保留未来的添加剂领域?
- 现在的错误会导致一个遗产的握手吗?
- 代理是否保存了源状态和JSON-RPC错误?
- 通知序列化器发出了禁止响应吗?
- 没有保密的释放或推迟?

合规性是观察到的不变元件的集合. 在生产流量必须发现它们之前,建立一个捕获不变元件的带.

> 一致性是一组可观察的不变量. 在生产流量不得不发现它们之前,首先构建一个能够捕获这些不变量的测试架.

```figure
mcp-conformance-operations
```

## 从版本纪元开始

> **【中文解读】**两元两套规则,绝不做"一个宽松验证器同时吃两种形态"――现代纪元`2026-07-28`):自包含的单个请求元数据`params._meta.io.modelcontextprotocol/protocolVersion`与`.../clientCapabilities`裸体的`protocolVersion`别名是形的;镜像路由头在场时必须与 JSON-RPC 主体一致;成功结果带`resultType`〔旧纪元〕截至`2025-11-25`):早期初始化时代,客户端选择定旧纪元之后,没有`resultType`结果才被解释为完整的分支防止"现代的形状对端而获得更弱的验证".`server/discover`或被识别的现代错误),绝不因`-32020/-32021/-32022`降级; 回退模式先做一次有界现代探测,超时/空响应/断连只是"无结论"、不证明旧版本,只有显然允许的端点才可以做有界旧版本探测,并且必须验证它.`initialize`结果才选定旧分支. 选定纪元记录在每条转录旁边.

>  **【类比】**版本纪元像充电口的"全程USB-C"和"备用转接"──默认只走USB-C(严格模式);转接(旧版回退) 只给登记在白名单上的旧设备使用,而且首先要确认那台设备是真正的旧设备(合法遗产`initialize`证据) 不能因为"新线没插上" (超时/无响应) 默认对方是老设备――否则一个干扰你的中间人就能靠"弄丢现代响应"把整个连接拖回旧协议,这正是降级攻击 (降级攻击) 的套路――

股`2026-07-28`现代请求包含了`params._meta.io.modelcontextprotocol/protocolVersion`其他`params._meta.io.modelcontextprotocol/clientCapabilities`确切的名称间隔的密钥是重要的;`protocolVersion`或`clientCapabilities`标题标题是错误的.当镜像路由标题在HTTP边界存在时,它们的值必须与JSON-RPC体一致.现代成功结果带来了`resultType`现在,我们要去.

> 股`2026-07-28`使用自含的单个请求元数据.`params._meta.io.modelcontextprotocol/protocolVersion`和 `params._meta.io.modelcontextprotocol/clientCapabilities` 精确的命名空间键很重要:裸的`protocolVersion`或`clientCapabilities`别名是形的──镜像路由头现在HTTP边界时,其值必须与JSON-RPC 主体一致──现代成功结果携带`resultType`,我知道.

通过版本`2025-11-25`没有任何数据的遗产结果.`resultType`只有客户选择了早期时代后才被解释为完整.

> 截至`2025-11-25`版本使用更早的初始化纪元──一个没有`resultType`结果只有在客户端已经选择了更早的纪元之后才会被解释为完整的.

不要创建一个允许验证器,同时接受两个形状. 使用两个分支:

> 不要做一个"同时接受两种形式"的宽松验证器.

| Branch | Entry evidence | Missing `resultType` | Initialization |
|---|---|---|---|
| Modern | Successful `server/discover` or recognized modern response | Invalid | Not the default path |
| Legacy | Configured allowlist plus a valid legacy `initialize` result after an inconclusive modern probe | Interpreted as complete | Required by that era |

> 现代分支的入口证据是成功的`server/discover`或被识别的现代反应,缺失`resultType`视为非法,初始化不是默认路径;旧版分支的入口证书是"已配置的允许者+一次无结论的现代探测后有效的旧版 `initialize`结果",缺失`resultType`解释为完整,初始化是该纪元所需的.

隔离使得一个错误的现代同龄人不会得到更弱的验证.

> 这种分离防止一个形状的现代对端而得到更弱的证据.

### 严格的模式.

严格模式需要证明现代行为.`server/discover`现在,我们已经发现了一个新的版本,它可以证明现代分支.一个已知的现代JSON-RPC错误也证明了这一点.`-32020`现在`-32021`其他`-32022`现在,我们要去.

> 严格模式要求现代行为正面证明:一次成功`server/discover`证明现代分支,一个被识别的现代 JSON-RPC 错误同样证明它.修改请求或直接停止绝不因为服务器回归.`-32020`,我知道.`-32021`或`-32022`降级.

### 倒退模式

倒退模式执行一个有限的现代探测器.时间过关,空答,关闭连接或未识别的响应是不确定性的.它并不证明同行是遗产.只有一个明确配置或配置的终点才能接收一个有限的遗产探测器,客户端只选择了验证该探测器的遗产分支后.`initialize`结果和谈判的遗产修订.

> 回退模式先做一次有界的现代探测.超时、空响应、断连或无法识别的响应都是"无结论"它们不证明端是旧版本.只有被显然配置或列入兼容容容容权的端点才能接收一次有界的旧版本探测;而且客户端只在验证那个探测的`initialize`结果与协商的旧版本修订后,才确定旧分支.

错误后,反弹不是被遗留的. 已认可的现代错误包含有用的纠正信息. 降级后,可能隐藏标题不匹配,缺失功能声明或不支持版本.

> 回退不是"任何错误后都试试旧版"――一个被识别的现代错误带有有用的修改信息;在它之后降级,可能掩盖头部不匹配、能力声明缺失或版本不支持――

这样,攻击者,中断或过代理不会通过放弃现代响应强加降级.记录终点政策,无可推断的现代观察,确切的积极遗产证据和选定的时代.

> 为了防止攻击者,故障或过类型的代理通过"弄丢现代响应"强迫降级.

没有这个事实,一个缺失的字段可能在一个测试过程中看起来是可接受的,而另一个则看起来是不有效的.

> 没有这样的事实,同一个缺失的段落可能是这次的测试运行看起来合法,下一次就变成非法.

## 建立一个转录库

> **【中文解读】**转录(转录) 固定记录是"真实跨越边界的东西"头部"",请求体"",响应状态码"",响应体"",选择纪元"而不是"SDK调用回归了什么"――语料库分类:黄金转录证明"接受行为"元数据与头部匹配"",`resultType`合法、通知无响应等);负面转录证明"拒绝行为" ((头部与主体不匹配、未知判别器、代理吞错等) .`-32020`虽然我说失败,但讲给运维的故事完全不同.

转录器记录了跨越界限的内容,而不仅仅是SDK调用:

> 一条转录装置记录了跨界内容,而不仅仅是使用了SDK:

```json
{
  "name": "golden-modern-list",
  "era": "modern",
  "headers": {
    "MCP-Protocol-Version": "2026-07-28",
    "Mcp-Method": "tools/list"
  },
  "request": {
    "jsonrpc": "2.0",
    "id": 1,
    "method": "tools/list",
    "params": {
      "_meta": {
        "io.modelcontextprotocol/protocolVersion": "2026-07-28",
        "io.modelcontextprotocol/clientCapabilities": {}
      }
    }
  },
  "responseStatus": 200,
  "responseBody": {
    "jsonrpc": "2.0",
    "id": 1,
    "result": {
      "resultType": "complete",
      "tools": []
    }
  }
}
```

保持两个类的灯具.

> 保留两类固定式

### 黄金转录

黄金的转录证明了被接受的行为:

> 黄金转录证明"被接受行为":

黄金的转录是精确的,不是大. 保持可变的身份证和时间标签确定性或在比较之前将它们正常化.

> 黄金转录要精确,不要大. 随时变化的ID,或在比较前规范化.

- 现代发现或方法请求,配合的元数据和标题
- 要求的字段的完整结果
- `input_required`结果是,当方法可以要求更多输入时
- 扩展结果仅在相应的功能被广告后
- 没有遗产结果`resultType`只有在选择的遗产时代
- 没有JSON-RPC响应的通知处理

黄金的转录是精确的,不是大. 保持可变的身份证和时间标签确定性或在比较之前将它们正常化.

### 负面转录

负面的转录证明拒绝行为:

> 负面转录证明"被拒绝行为":

- 头部和身体的不匹配
- 缺少按要求的能力
- 支持不到的匹配协议版本
- 没有现代`resultType`
- 未知或未公告`resultType`
- 反应`jsonrpc`其他`2.0`或是值或JSON类型不同的ID
- 包含两者之间的反应`result`其他`error`没有一个
- 没有整数的错误`code`子`message`
- 已知协议错误被错误的HTTP状态映射
- 发出通知的回应
- 错误的JSON-RPC封面
- 协议错误的代理崩

对于每一个负例,请确认拒绝边界和稳定错误代码. 呼叫失败太弱.`-32020`虽然它们都能看起来像失败,但却却会讲述完全不同的故事.

> 对于每一个负面的使用例,断言拒绝边界和稳定错误码.`-32020`它们看起来像失败,但它们给运维的故事完全不同.

标题不匹配配器件必须包含服务器的实际HTTP 400 JSON-RPC响应,并包含匹配请求ID和错误代码`-32020`当地方验证器观察时,自动执行.`HeaderMismatch`对于一个 HTTP 500 系统,即使当地拒绝代码是正确的,也会出现故障. 通过自己的请求验证器投射后停止的链接只测试了自己,而不是服务器的电线行为.

> 头部不匹配的装置 必须包含服务器真实的HTTP 400 JSON-RPC 响应带匹配的请求ID 和错误码 `-32020`,只要本地验证器观察`HeaderMismatch`开关. 一个"HTTP 500 且无响应体"的使用例,即使本地拒绝代码是对的也算失败. 一个在自己的请求验证器抛出异常后就停下来测试架,只测试了自己,没有测试服务器的线上行为.

官方MCP合规项目作为外部套件和版本参考很有用. 保存您的本地转录.它们捕获您的代理,SDK,身份验证,扩展和发布路径,而一般套件无法知道.

> 官方MCP 一致性项目适合作外部套件和带版本的参照.但本地转录也应该保留:它们捕获的是你的代理,SDK、认证、扩展和发布路径.

## 标题值必须与PC体相匹配

> **【中文解读】**现代流通 HTTP 允许中间设备使用镜头进行路由或策略执行,但JSON-RPC 主体才是协议的事实来源不匹配是完整性故障,不是"二选一挑一个"提示――验证顺序:先解析校验JSON-RPC 信封与元数据类型,再比较对`MCP-Protocol-Version`它们的名称和空间键`Mcp-Method`与`method`有路由名时比对`Mcp-Name`只有在相等的确立之后才能判断版本与能力是否得到支持.`-32020`"与"版本不支持`-32022`"区分开,也住了"网关按头部名字授权"",源站按另一个主体名字执行"的攻击──HTTP 字段名大小写不敏感而值大小写敏感;`Mcp-Name`含不安全字符时走 Base64 UTF-8 哨兵解码,解码失败或首尾空白都以`-32020`拒绝了.

在现代流式HTTP中,中间人可以通过镜头标题进行路由或执行政策.JSON-RPC的机体仍然是协议的真相来源.不匹配是完整性失败,而不是选择一个值的暗示.

> 在现代流式HTTP中,中间设备可以使用镜头进行路由或执行策略,但JSON-RPC 主体仍然是协议的事实来源.

在此顺序进行验证:

> 按以下顺序验证:

1. 分析和验证JSON-RPC封装和元数据类型.
2. 比较`MCP-Protocol-Version`随着`params._meta.io.modelcontextprotocol/protocolVersion`现在,我们要去.
3. 比较`Mcp-Method`随着`method`现在,我们要去.
4. 如果该方法有路由名称,则比较`Mcp-Name`具有相应的体值.
5. 确定平等后,决定是否支持匹配的版本和功能集.

这种顺序区分了不匹配`-32020`没有支持的版本`-32022`通过此,一个网关可以使用一个不同的机体名称.

> 这个顺序不匹配`-32020`与版本不支持`-32022`区分开,也阻止了"网关按主题名字授权"",而源站执行另一个主体名字"的情况.

HTTP 字段名称是不敏感的,而它们的值仍然是敏感的. 在搜索之前,正常化头条名称,拒绝矛盾的重复.对于不安全的,非ASCII或领先或后续的白色空间.`Mcp-Name`解码了精确的`=?base64?{Base64EncodedValue}?=`拒绝一个不完整的哨兵,不有效的Base64,不有效的UTF-8,或原始的不安全值`-32020`虽然机身包含相同的字符,但原料周围的白色空间是无效的,因为该值需要在运输前进行哨兵编码.

介绍一个错误的HTTP,在请求到达MCP服务器之前可以拒绝错误的HTTP,因此其失败可能是没有JSON-RPC的HTTP错误. 捕获是否拒绝来自介绍者或来源. 当处理有效的JSON-RPC请求时,原始MCP服务器应该使用协议错误合同.

## 无知字段不等于未知结果

> **【中文解读】**前向兼容需要两条不同的规则──增量式未知字段:结果对象和`_meta`映射可以新增字段透明代理通常应保留(转发),应用客户端可以忽略,但要靠差分测试让"SDK 丢掉它"变成一个明显的有意的决定――未知.`resultType`客户端不知道自己会丢弃什么样的生命周期,必须拒绝. 同样的原始反应可以同时包含"可接受的未知字段"和"不可接受的未知结果类型",两种情况都必须测试.`tools/list`需要工具 数组且描述符合法,`task`结果需要`taskId`时间/`ttlMs`完成 结果需要合法的`completion`象征

未来兼容性需要两个不同的规则.

> 前向兼容需要两条不同的规则.

### 增加式未知字段

结果对象和`_meta`根据其作用,验证者应保留或忽略添加值的字段,除非该字段违反了保留合同.`futureHint`除了已知结果之外.

> 结果对象与`_meta`映射可以新增字段. 验证器应根据自己的角色保留或忽略增量字段,除非该字段违反保留协议. 举例把完整的原始结果留在证据里,并接受已知结果旁边.`futureHint`,我知道.

如果您是透明代理,保存一个未知的字段通常比剥夺它更安全.如果您是应用程序客户端,忽略它可能是有效的.您的差异测试仍然应该显示SDK遗漏它,因此行为是故意的.

> 如果是透明代理,保留未知字段通常比剥离它更安全;如果是应用客户端,忽略它可以合法.

### 没有人知道`resultType`没有人知道的结果Type

`resultType`核心现代结果使用`complete`或`input_required`扩展只能在其功能被广告时添加另一个值.`task`在谈判能力的背景下.

> `resultType`是一个判别器.`complete`或`input_required`扩展只有在应对能力已被声明的情况下才能增加其他值例如任务 扩展在协商能力下文可以增加`task`,我知道.

客户不知道它会丢弃的生命周期. 拒绝它.

> 客户端不知道自己将如何丢弃其生命周期.

因此,相同的原始反应可以包含一个可接受的未知的字段和一个不可接受的未知的结果类型.

> 因此,相同的原始反应可以包含可接受的未知字段,也包含不可接受的未知结果类型.

区分器只是第一层. 验证后的方法特定的有效载荷.`tools/list`结果需要一个`tools`配列的描述符具有独特的非空名,有用的描述和对象根`inputSchema`值.`task`结果仅适用于符合条件的`tools/call`具有任务能力和要求`taskId`已知状态,创建和更新时间标签,`ttlMs`另外一个有效的选项选项间隔.`completion/complete`结果需要一个`completion`具有不超过100个字符串值的对象,可选的非负整数 `total`且可选的布尔式值`hasMore`一个好拼写的`resultType`无法制造一个错误的有效载荷的符合性.

> 判别器只是第一层,后面还需要进行检验方法`tools/list`结果需要`tools`描述符必须具有唯一的空域名称,有用的描述和对象根.`inputSchema`其他`task`结果只有在带有任务能力的合规`tools/call`里才合法,需要`taskId`已知状态 创建与更新时间 及`ttlMs`法律外可选轮询间隔;完整的`completion/complete`结果需要`completion`对象的字符串值不超过100个,可选的非负整数`total`没有更小的返回值数量,可选的布尔`hasMore`拼写正确的`resultType`没有了形的载荷.

## 通知不变量

没有JSON-RPC通知`id`接收器不得发送一个成功或错误的JSON-RPC响应.

> 一条 JSON-RPC 通知没有`id`△接收方必须发送JSON-RPC 成功或错误响应──

对于被接受的HTTP通知形状,带期望一个HTTP`202`没有任何东西.`2026-07-28`定义没有基于 Streamable HTTP 的核心客户端到服务器通知.样本仅使用一个名字空间的进程扩展通知来测试单向串联器不变量.不要将其作为一个新的核心方法.

> 对于接受的HTTP通知形式,测试架期望HTTP`202`和空主体──MCP `2026-07-28`没有定义核心的客户端到服务器 流式HTTP通知――例如使用一个命名空间的课程扩展通知,仅用于测试单向序列化器不变量;不要把它当作新的核心方法――

测试连续化器,不仅是操作器.`None`通过中文软件将其包裹成一个JSON成功对象.

> 测序列化器,不仅仅是处理器.处理器可能返回.`None`中间件将其包装成一个JSON成功对象.

## 增加一个 SDK 差分

> **【中文解读】**SDK 常将线上对象转换为方便的语言类型,这有用,但归化后的对象证明不"得到什么"――对每条高风险固定抓四样东西:SDK 解码前的原状态,头部、响应体;SDK归化回值或异常;选择纪元的期望语义投影;SDK 升级,合成,剥离或改变的段子――例如允许SDK 专门移除已知的线上账本段段`resultType`,我知道.`_meta`,我知道.`ttlMs`,我知道.`cacheScope`)并同时比较应用载荷,但`futureHint`由于这些数据的数据和数据的不同,因此,在数据的数据的数据中,数据的数据和数据的不同程度是很大.

 SDK 经常将线程对象转化为方便的语言类型. 这很有用,但一个正常化的对象不能证明收到的内容.

>  SDK 常将线上对象转化为方便的语言类型.

对于每一个高风险装置,捕获:

1. 在 SDK 解码之前,原始状态,标题和响应器官.
2.  SDK正常化回报值或例外
3. 预期的选择时代的语义投影.
4.  SDK 提升,合成,剥离或更改的场地.

样本允许仅使用SDK去除已知电线账本,如`resultType`现在`_meta`现在`ttlMs`其他`cacheScope`应用程序的有效载荷.`futureHint`因为这个未知的语义领域消失了.

您的组件是否是一个应用程序终端点,可以忽略添加字段,或者一个透明的中间体,该组件应该保存它.

> 不要假设每个区别都是SDK bug. 区别的意义是让转换可见. 首先想清楚你的组件是"可以忽略增量字段的应用端点",还是"应该保留它的透明中间人".

如果两个SDK以不同的方式正常化相同的转录,则发布政策应该说明哪种行为是可接受的,而不是选择最方便的输出.

> 如果两个 SDK 对同一转录的归化不同,发布策略应明确哪些行为可以接受,而不是后面选择最顺利的输出.

## 抓住代理证据

产品MCP失败的大部分发生在多个过程中.记录三个视图:

> 大多数生产MCP故障跨越不止一个进程.

| View | Minimum evidence |
|---|---|
| Ingress | request headers, JSON-RPC body, content type, authenticated route, receive time |
| Origin | forwarded headers and body digest, origin status, response headers and body |
| Egress | client-visible status, headers, body, and send time |

> 三个视角表:入口(Ingress) 至少记号请求头部、JSON-RPC 主体、内容类型、已认证路由与接收时间;源站(原始) 至少记号转发头部与主体摘要、源站状态码、应对头部与主体;出口(Egress) 至少记号客户端可见的状态码、头部、主体与发送时间──

样本检测到两个常见的变化:

> 示例检测两类常见变换:

- 源 HTTP 400 或 404 JSON-RPC 错误成为通用代理 500
- 出口JSON-RPC体与原体不同

> 两类变化:源站的HTTP 400或404 JSON-RPC 错误变成代理的通用500;出口 JSON-RPC 主体与源站主体不一致.

添加内容类型的部署特定声明,`Accept`检查TLS终止的两侧,当政策允许时.永远不要记录凭证,只是为了证明路径.

> 为了内容类型,`Accept`、压缩、请求级 SSE、缓存头和痕迹关联加上部署特定断言――策略允许时捕获TLS 终止的两侧――绝对不要为了证明路径而记录凭证――

## 在证据离开记忆之前,再编写.

> **【中文解读】**脱敏是连贯运维的一部分,不是事后清理工作在序列化、哈希、日志、测试工件或失败上传之前完成.`Authorization`,我知道.`Cookie`,我知道.`accessToken`,我知道.`clientSecret`,我知道.`token`,我知道.`password`,我知道.`secret`,我知道.`api_key`等键下值;规范化与 denylist 必须使用相同的形态,防止驼案例、连字符、下划线、点号变体相互绕过──生产采集器还应加方法 特定参数策略像`query`摘要证明,哪个脱敏包驱动了决策,但不会泄露被移除的价值;原始抓包只留在批准的短生命周期系统中,只需要特定的调查.

编辑是符合性操作的一部分,而不是后续清理工作. 在串行,哈希,日志,测试文物或失败上传之前应用它.

> 脱敏是连贯性运维的一部分,不是后续清理工作. 在序列化,哈希,日志,测试工件或失败上传之前完成它.

样本案例将关键名称折叠,在匹配之前删除分区,然后再次次替换关键下的值,如`Authorization`现在`Cookie`现在`Set-Cookie`现在`X-Api-Key`现在`accessToken`现在`clientSecret`现在`registrationAccessToken`现在`token`现在`password`现在`secret`其他`api_key`纳化和丹尼尔单必须使用相同的形式,因此马,字符串,强调和点子变体不能绕过彼此的政策.`query`仍可能包含个人或受监管的数据.

> 示例先把键名做大小写折叠并移除分隔符再匹配,然后递归替换 `Authorization`,我知道.`Cookie`,我知道.`Set-Cookie`,我知道.`X-Api-Key`,我知道.`accessToken`,我知道.`clientSecret`,我知道.`registrationAccessToken`,我知道.`token`,我知道.`password`,我知道.`secret`,我知道.`api_key`等键下值――规范化与丹尼尔主义 必须使用相同的形态,这样驼案例、连字符、下划线和点号变体就无法绕过对方的策略――生产采集器也应增加方法特定参数策略,因为像`query`无害的键仍然可能携带个人或监管数据.

除了这些数据,还可以将其保存在已批准的短暂系统中,只需要进行特定调查. 化证明了哪些数据驱动了决定;它不显示删除的值.

> 只有当确实需要特定调查时,才能将原始抓包保留在批准的短生命周期系统中.摘要证明了哪个脱敏包驱动了决策;它不会泄露被移除的价值.

## 让健康和回滚成为门的一部分.

> **【中文解读】**协议一致是发布的必要条件,不是充分的条件 完全一致的候选版本仍然可能超时;泄漏内存或压依赖.发布前定义健康窗口 ((最小样本数量;最大错误率;最大延迟分位;和或资源上限;观察时间长度;与已准入基线的比较);同样在发布前定义回滚证书的准确前版本;进入证书摘要;SHA-256 产品与描述符;现存注册表;现状;现存健康结果;路由恢复程序;以及可信控制器身份认证的以上部分) 恢复目标在升级前验证健康,并且没有完成等候版本失败后;补充候选版本失败后才完成回滚证书的缺失,保持流量,不值,不值,不值,不值,不值,不值,不值,不值.`healthy: "yes"`、任意证据字符串): 示例要求精确类型、活性 状态、三个 SHA-256 摘要、可信签署者和对完整回滚载有效的 HMAC-SHA-256 认证;使用确定性密钥非机密装置的示范,生产要在发布边界注入受保护的密钥、KMS 验证结果或公钥认证器.

协议合规性是必要的,但不够的释放. 合规候选人仍然可以过时,泄漏内存或过度负载依赖性.

> 协议一致是发布的必要条件而不是充分条件.

在推出之前定义健康窗口:

> 发布之前先定义健康窗口:

- 样本数量最低
- 错误率最高
- 延迟最大百分比
- 和资源限制
- 观察时间
- 与被允许的基线的比较

在推出之前也定义反弹证据:

> 发布之前同样定义回滚证据:

- 确切的前版本
- 录取证据消化
- 石器和描述器SHA-256
- 目前的登记处状态
- 现状健康结果
- 航线恢复程序
- 证据证明这些精确的领域来自可靠的释放控制器身份

要求提升前,不仅需要候选人失败后,还要验证并健康的反弹目标.

> 要求回滚目标在晋升之前就完成验证并健康,而不是仅等候选人失败后才查查.

如果候选人失败,而反弹目标缺乏这些证据, 停止交通,而不是猜测.

> 如果候选人失败,然后回滚目标缺少证据,那就稳住流量,不要去猜.

不要减少对未空版本等真实性检查的准备,`healthy: "yes"`样本需要准确的类型,活跃状态,三个SHA-256字段,一个可信的签名器和一个有效的HMAC-SHA-256证明,在完整的反弹有效载荷上.其确定性演示密钥是一个非秘密的装置.在生产的释放边界注入一个保护密钥,KMS验证结果或公钥验证证证.

> 不要把现绪检查退化为真价值判断如非空的版本号,`healthy: "yes"`或任意证据字符串. 示例要求精确类型,活跃状态,三个SHA-256摘要可信签名人,以及对完整回滚载有效的HMAC-SHA-256认证.

发放门也拒绝空格转录,SDK差异或代理证据.每个来源都必须携带有效的证据消化.绿色健康窗口不能填补未被观察过的边界.

> 发行门也拒绝空的转录、SDK差分或代理证据:每个来源都必须携带有效的证据摘要――一个绿色的健康窗口,不填满一个从未观察过的边界――

## 建立它,实现它.

运行标准图书馆带:

> 运行标准库测试架:

```bash
cd phases/13-tools-and-protocols/31-mcp-conformance-versioning-and-operations
python3 code/main.py
```

演示程序运行了15个黄金和负面的转录,包括有效和错误的完成结果,将原始结果与SDK视图进行比较,检查一个失败的代理,评估健康,验证反弹证据,并选择目标.

> 试图运行十五条黄金与负面转录 (含合法与形的完成结果),把原始结果与SDK视图进行比较,检查一个吞掉了源站错误的代理,评估健康,认证回滚证据,并选择该目标.

预期的形状:

> 预期输出形状:

```json
{
  "transcriptsPassed": 15,
  "transcriptsTotal": 15,
  "sdkDroppedFields": ["futureHint"],
  "proxyIssues": [
    "proxy collapsed a protocol error into HTTP 500",
    "proxy changed the origin JSON-RPC body"
  ],
  "releaseAction": "rollback",
  "evidenceDigest": "..."
}
```

阅读`code/main.py`在此顺序:

> 按以下顺序阅读 `code/main.py`其他:

1. `validate_request()`执行特定时代的请求和标题规则.
2. `validate_result()`区分失踪的传统歧视者,有效的现代价值观,扩展和未知的价值观.
3. `select_era()`实施严格的反弹政策.
4. `run_transcript()`评估黄金和负光.
5. `compare_sdk_view()`报告显示了正常化差异.
6. `inspect_proxy()`进行了进口,出口和出口证据的比较.
7. `redact()`在证据被除之前,它会删除明显的秘密.
8. `rollback_evidence_ready()`验证准确的针字段和可靠的释放证书.
9. `ReleaseGate.evaluate()`加入非空格合规性,SDK,代理,健康和反弹证据.

## 用它实现框架

运行四个点:

> 在四个时间点运行测试架:

1. 在每次实施变更时,使用在过程中测试适配器.
2. 针对实际运输的客户端和服务器二进制.
3. 通过部署的代理或门户在一个舞台环境中.
4. 在加拿大鱼发射期间,有现实健康和反弹证据.

保持相同的稳定案例名称在各层.`negative-header-body-mismatch`据悉,在""中,该指数的数量是1个,且在"端到端",代理和可查报告中,该指数的变量不相同.

> 在各层之间保持相同的稳定用例名:`negative-header-body-mismatch`在单元,端到端,代理和金丝雀报告里必须指同一个不变量.证据摘要会不同,因为边界变化;但要求不能变化.

存储版本控制中的固定方案. 在发布系统中存储编辑的运行证据. 仅在事件访问控制下存储短暂的原始捕获.

> 固定方案 存储版本控制;脱敏后运行证据存储到发布系统;短生命周期的原始抓包只被置于事件访问控制之下――

> 四个运行点对应四层边界:过程内实现转变,真传输的成品二进制,阶段中的代理或网关,带真健康和回滚证据的金丝雀.

## 互动实验室

### 实验室A:证明时代的边界

关于`code`开放字符串:

> 从`code`现在打开Python:

```bash
cd phases/13-tools-and-protocols/31-mcp-conformance-versioning-and-operations/code
python3 -q
```

运行:

```python
from main import *
validate_result({"tools": []}, "legacy")
validate_result({"tools": []}, "modern")
```

遗产的呼叫是无效的`complete`现代的呼唤引起了`ProtocolViolation`现在试试倒:

> 旧版调用推断出`complete`现代调用抛出`ProtocolViolation`接着测回退:

```python
select_era({"kind": "timeout"}, "fallback")
select_era(
    {"kind": "timeout"},
    "fallback",
    legacy_allowed=True,
    legacy_evidence={"kind": "initialize_success", "protocolVersion": LEGACY_VERSION},
)
select_era({"kind": "jsonrpc_error", "code": -32021}, "fallback")
```

由于沉默不是遗产证据,所以第一时间停止运行.第二次调用仅因为配置允许,所以选择遗产,并且观察到有效的遗产初始化结果.

> 第一个超时失败闭合 (失败闭合),因为沉默不是旧版本证据. 第二个调用之所以能选定旧版本,只是因为配置允许,并且观察到有效的旧版本初始化结果.

### 实验室B:增量字段与判定器

```python
validate_result({"resultType": "complete", "tools": [], "futureHint": True}, "modern")
validate_result({"resultType": "future_mode", "tools": []}, "modern")
```

首先,结果保持了`futureHint`另一种原因是因为生命周期的差异是未知的.

> 第一个结果保留了`futureHint`由于生命周期的判定器是未知的,

### 实验室C:检查一个 SDK转换.

```python
compare_sdk_view(
    {"resultType": "complete", "tools": [], "futureHint": {"mode": "new"}},
    {"tools": []},
)
```

决定是否可以忽略你的组件`futureHint`您可以将此选项写入释放政策中.

> 决定你的组件可以被忽略`futureHint`现在,我们必须转发它.

### 实验室D:修复代理

修改演示表格,以保证出口的原始状态和体质.`python3 main.py`现在,我们需要一个新的方法来实现.`futureHint`在 SDK 视图中,并观察行动变化到`promote`当一切证据都过去了.

> 修改演示 交换,让出口保留源站状态码与主体――再运行一次`python3 main.py`现在,我们需要一个新的解决方案.`futureHint`加入SDK 视图,观察当每一个证据来源都通过时,动作变成`promote`,我知道.

## 实验室 进步实验

加入要求测量的SSE转录到带上.

> 给测试架增加请求级 SSE 转录.

> 进阶练习将语料库从"一请求一响应"扩展到"一请求一流":捕获响应状态"",内容类型"",有序SSE事件与流终止;证明每个JSON-RPC事件都有合法的纪元特定结果或错误;加"代理把整个流缓冲完再转发"和"SSE事件的JSON-RPC id与请求符不"两个负面使用例;写证据前先脱敏事件数据;将流长时间,事件延迟和事件计数纳入健康窗口;只在流失败时发布门选有证据的滚动目标.成功标准是相同条例可以直接运行、也可以穿越代理运行,报告能精确指出改变行为层的界限.

要求:

- 捕获响应状态,内容类型,订单的SSE事件和流程终止.
- 证明每个JSON-RPC事件具有有效的时代特定结果或错误.
- 添加一个负案例,为代理,在转发之前缓冲整个流.
- 添加一个与请求不同的JSON-RPCID的SSE事件的负案例.
- 在写证据之前,重新编写事件数据.
- 包含流程时间,第一次事件延迟和事件数量在健康窗口中.
- 让释放门选择只有一个证明的反弹目标,当流失败.

成功意味着同一案例直接通过代理运行, 报告确定了改变行为的确切边界.

> 成功的标准:相同的使用例既能直连运行也能穿代理运行,报告能指出改变行为的精确边界.

## 运送的艺术品产品

这一课是很好的.`outputs/skill-mcp-conformance-release-gate.md`通过它将服务器,客户端,门户或SDK更改成版本的兼容矩阵和发布决定.该文物需要原始线索证据,负案例,明确时代选择,SDK差异,代理证明,编辑,健康门和反弹证据.

> 本课附带 `outputs/skill-mcp-conformance-release-gate.md`△使用它将一个服务器,客户端,网关或SDK的变化转化为带版本的一致性矩阵和发布决策.

## 检查一下.

> **【中文解读】**验收清单逐条过:所有黄金和负面转录都到期结局;现代要求精确的命名空间元数据键;HTTP 头部名大小写不敏感地匹配、编码的`Mcp-Name`值被精确解码;头部与主体不匹配回归现代不匹配码;响应版本、ID、结果/错误 互斥、错误形态与HTTP 映射都被校验;方法特定的工具列表/任务/完成 载荷要求被执行;每个观察到的`HeaderMismatch`都要求真实的HTTP 400 JSON-RPC `-32020`响应;原始`Mcp-Name`空白被拒绝,哨兵编码的空白精确往返;缺失 `resultType`仅在选定的旧纪元合法;增量字段活着而未知结果类型失败;扩展结果类型要求其能力已声明;被识别的现代错误绝不会触发旧版回归;通知不会产生JSON-RPC 响应;SDK 簿记移动与语义字段丢失被区分;代理错误缩被检测到;凭证被归归敏在驼案例与分隔符变体上;晋升要求非空转录;SDK、代理与健康运维证;晋升与回滚都需要认证、已 pin、active、健康的回滚目标.

运行演示和确定性套件:

> 运行演示与确定性测试套件:

```bash
cd phases/13-tools-and-protocols/31-mcp-conformance-versioning-and-operations
python3 code/main.py
python3 -m unittest discover -s code/tests -v
```

验证应证明:

- 每个包含的黄金和负面的转录都达到预期的结果
- 现代要求需要精确的名称空间的元数据密钥
-  HTTP 标题名称是不敏感的,并且编码.`Mcp-Name`值被解码准确
- 标题和机身不匹配返回现代不匹配代码
- 答案版本,ID,结果或错误独占性,错误形状和HTTP映射被验证
- 执行方法特定的工具列表,任务和完成有效载荷要求
- 每个观察到的`HeaderMismatch`需要一个实际的HTTP 400 JSON-RPC `-32020`反应
- 原料`Mcp-Name`时被拒绝,而精确的哨兵编码的白空间回来旅行
- 失踪的`resultType`仅在选择的遗产时代有效
- 添加式字段存活原始验证,而未知的结果类型失败
- 扩展结果类型需要其广告的能力
- 现代错误从来没有导致遗产倒退
- 通知没有产生JSON-RPC响应
-  SDK 会计删除和语义字段损失分别
- 检测到代理错误崩,并且在camelCase和分离器变体中恢复性删除凭证
- 促进需要非空格转录,SDK,代理和健康的运营证据
- 促进和推翻都需要一个认证,固定,活跃,健康的推翻目标

## 产品失败模式

> 下表三列:失败、弱测试会报告什么、测试架必须证明什么──最贵的三行:SDK 合成了缺失的判别器("工具/列表 通过了",实际上原始现代结果缺失`resultType`、非法的);代理授权一个工具而执行另一个站`Mcp-Name`必须在每跳都等于主体路由名称; 雀零样本却显示健康; 最少的样本数必须被强制)

| Failure | What the weak test reports | What the harness must prove |
|---|---|---|
| SDK synthesizes a missing discriminator | “tools/list passed” | Raw modern result lacked `resultType` and is invalid |
| Client downgrades after `-32021` | “legacy retry worked” | Recognized modern error forbids fallback |
| Unknown result type treated as complete | “response parsed” | Unadvertised lifecycle discriminator is rejected |
| Proxy authorizes one tool and origin executes another | “request reached server” | `Mcp-Name` equals the body routing name at every hop |
| Harness throws before reading the server response | “header mismatch test passed” | HTTP 400 and JSON-RPC `-32020` response are captured and validated |
| Proxy turns origin 400 into generic 500 | “upstream error” | Origin and egress statuses and JSON-RPC bodies are preserved |
| Notification middleware emits `{result: null}` | “handler returned none” | Final egress body is empty and no JSON-RPC response exists |
| SDK strips an additive field | “typed objects match” | Raw and normalized views show the exact dropped field |
| Failure artifact leaks a bearer token | “debug bundle uploaded” | Redaction occurred before hashing, logging, or upload |
| Credential key style bypasses redaction | “denylist contains api_key” | CamelCase and separator variants share one canonical denylist form |
| Canary has no samples but appears healthy | “zero errors” | Minimum sample count is enforced |
| Rollback selects an unknown build | “previous deployment restored” | Target version, admission digest, pins, status, and health are present |

## 运营规则

测试你发送的字节,每个中间件的字节向前,每个SDK暴露的语义,以及在压力下使用的证据操作.兼容性是明确的分支.滚动是基于证据的释放行动.任何一个不能是允许解析器的意外副作用.

> 测试你发出的字节"",每个中间设备转发的字节"",每个SDK 暴露的语义,以及运维在压力下所需的证据――兼容是一个显而易见的分支;回滚是一个有证据的支的发布动作――两者都不应该是宽松解析器意外产生的副作用――

> 一句总结全课:一致性不是"一次跑通",而是"每个边界都有可观察的可复现的证据"",宽松解析器将"兼容"和"回滚"都变成意外的副作用;显然分支加证据化决策,才把它们变成受控的工程行为――

## 继续阅读 继续阅读

- [MCP 2026-07-28 base protocol](https://modelcontextprotocol.io/specification/2026-07-28/basic)
- [MCP version negotiation](https://modelcontextprotocol.io/specification/2026-07-28/basic/versioning)
- [MCP Streamable HTTP](https://modelcontextprotocol.io/specification/2026-07-28/basic/transports/streamable-http)
- [Official MCP conformance project](https://github.com/modelcontextprotocol/conformance)
