# 展示和分辨散的统一模型

> 输血混合连续和分离的表现. 显示 (Xie等同,2024年8月) 则是另一种方式:文字代币使用因果下一个代币预测,图像代币使用面具的分离传播, 两人都坐在一个变压器里, 结果将VQA,文字到图像,涂料和混合模式生成在一个背骨上统一,每个模式每一个代币,一个损失式 (下一个代币扩展到掩盖预测). 这一课标介绍了"Show-o"设计,为什么隐形分离散射是平行,几步的图像生成器,与转和Emu3有着对比.

> **【中文解读】**显示-o(2024年8月)走另一条路:文本代币 用因果下一代币 预测,图像代币 用掩码离散扩散(MaskGIT风格) ・・・两者共用一个变压器,用混合注意力掩码──结果是一个检查点 同时支持VQA、文本生成图像和图像修复──

> **【拓展：并行解码的速度优势】**显示生成图像只需要16步,而马龙/Emu3则需要1024-4096步.

**Type:** Learn  | **类型:** 学习
**Languages:** Python (stdlib, masked-discrete-diffusion sampler) | **语言:** Python（标准库，掩码离散扩散采样器）
**Prerequisites:** Phase 12 · 13 (Transfusion) | **前置知识:** Phase 12 · 13（Transfusion）
**Time:** ~120 minutes | **时间:** ~120 分钟

>  **【前置】**学本节前请先掌握:阶段12·13(转 双损失) 阶段12·11-12(马里昂/Emu3 离散代币) 阶段8(MaskGIT 离散扩散概念) ◎Show-o = 全离散 + 图像使用MaskGIT 风格并行解码,速度比Emu3 快 60 倍──
>  **【类比】**显示-o = "并行开锁"──Emu3 = 一把钥匙开 1024 把锁(自归单个代币);显示-o = 16 步内同时尝试所有锁(掩码扩散并行解码)──代价:图像质量略差(VQ 量化损失),但推理快得多──

## 学习目标

- 解释掩盖的分离散射:掩盖代币的时间表,然后要求变压器恢复它们.
  > 解释掩码离散扩散:均掩码代币 然后让变压器恢复它们的调度.
- 根据速度和质量,比较平行图像解码 (Show-o, MaskGIT) 与自动降低图像解码 (Chameleon, Emu3).
  > 比较并行图像解码(Show-o、MaskGIT) 与自归图像解码(Chameleon、Emu3) 在速度和质量上的差异──
- 举个名单,在一个检查点上,Show-o处理的三个任务:T2I,VQA,图像涂料.
  > 列举 在一个检查点中支持三种任务:T2I、VQA、图像修复──
- 选择一个掩盖时间表 (,线性,缩短) 并考虑其对样品质量的影响.
  > 选择掩码调度 (余弦,线性,截断) 并分析对采样质量的影响.

## 问题背景

输血的两损失训练工作,但具有更棘手的动态. 持续扩散损失在与分离NTP损失不同的数值尺度上生活.平衡损失权重是超参数搜索. 建筑是有效的,但复杂的.

> 输血双损训练可行但动态更复杂 连续扩散损失与离散NTP 损失在数值尺度上不同――平衡损失权重是一个超参数搜索――架构有效但复杂――

展示-o的答案:保持两种模式分离 (如马里昂),但通过掩饰分离散射而不是连续生成平行图像.训练目标成为一个单一的掩饰代币预测,自然将下一个代币预测概括.

> 展示的答案:保持两种模态都是散散的,但通过掩码散散并行生成图像,而不是顺序生成.

## 概念的核心概念

> **【中文解读】**展示 统一多模态理解和生成,使用离散扩散替代传统连续扩散――离散扩散直接在代币级操作中,将遮盖预测 (理解任务) 和去噪音 (生成任务) 统一在同一框架下――

> **【拓展：离散扩散的统一优势】**散发散发将文本生成和图像生成统一到同一个数学框架 (掩码代币预测),使多模态联合训练更简单――这是2025年多模态AI的趋势方向――


### 面具的离散传播 (MaskGIT)

格IT的原始技巧是优雅的.从一个完全蒙面的图像开始 (每个代币都是特殊的图像).`<MASK>`在每一步,预测所有面具代币并行,然后保持最自信的预测,再重新掩盖其余. ~ 8-16 个代之后,所有代币都填写.每步需要揭开多少代币的时间表调整.

> 开始从完全掩盖的图像开始,每个标志都是特殊的.`<MASK>`通过8-16次代,所有代币都被填充. 每步解掩码多少代币调节需要调调优余弦调节效果好.

训练简单:从 [0, 1] 样本模拟一个掩盖比率,将其应用到图像的VQ代币,训练变压器恢复掩盖的代币. 正如BERT对文本所做的,扩展到图像生成.

> 训练很简单:从 [0, 1] 均采样掩码比例,应用到图像的VQ代币,训练变压器恢复被掩码的代币――就是BERT对文本做,扩展到图像生成――

### 展示:一个变压器,混合面具

展示将MaskGIT放进一个因果语言模型变压器.

> 展示将MaskGIT 放入因果语言模型变压器 中.

- 文字代码:因果性 (标准的LLM).
  中文翻译:文本代币:因果(标准法学) 』
- 图像代币:图像区块内完全双向 (因此,掩盖的代币可以在预测过程中看到其他图像代币).
  中文翻译:图像代币:图像块内全双向(掩码代币可以在预测时看到所有其他图像代币) 』
- 文字对图像:文字与以前的图像相结合,图像与以前的文字相结合.
  中文翻译:文本到图像:文本关注之前的图像,图像关注之前的文本──

培训的替代者:
1. 标准NTP在文本序列上.
   中文翻译:文本序列上的标准NTP──
2. 标签: 标签: 标签: 标签: 标签: 标签: 标签: 标签: 标签: 标签: 标签: 标签: 标签: 标签: 标签: 标签: 标签: 标签: 标签: 标签: 标签: 标签: 标签: 标签: 标签: 标签: 标签: 标签: 标签: 标签: 标签: 标签: 标签: 标签: 标签: 标签: 标签: 标签: 标签: 标签: 标签: 标签: 标签: 标签: 标签: 标签
   中文翻译:T2I 样本:文本→带掩码图像代币的图像,掩码代币 预测损失──
3. 面具的文本代码 (实际上只是NTP) 的图像 →文本.
   中文翻译:VQA 样本:图像→带掩码文本代币的文本(实际上就是NTP) 』

统一的损失是交叉化.`<MASK>`代币,涵盖文字NTP (仅最后一个代币是"掩盖") 和图像掩盖传播 (随机子集是掩盖).

> 统一损失是`<MASK>`标志上的交叉,同时覆盖文本 NTP(只有最后一个标志是"掩码"的) 和图像掩码扩散(随机子集被掩码) ⋅

### 并行采样

显示-o在~1000 (每代币自行降低) 或~20 (传播) 代码的位置上生成16个步骤.在每个步骤上,并行预测所有隐藏代币; 提交顶部K自信; 重复.

> 显示在16步内生成图像,而不是1000步 (每个代币自归) 或20步 (扩散) ⋅每步并行预测所有掩码代币;提交顶级K有信心;重复──

进行比较:
- 马里昂/EMU3 (自行降低代币):N_代币前进传递,通常每张图像为1024-4096.
  中文翻译:Chameleon / Emu3(逐符号自归):N_tokens 次前向传播,通常每张图像 1024-4096──
- 输血 (持续扩散):~20个步骤,每个步骤都通过一个完整的变压器.
  中文翻译:转化 (连续扩散):约20步,每步一次完整转化器传播――
- 显示 (掩盖的分离散射):~16个步骤,每个步骤都通过一个完整的变压器.
  中文翻译:Show-o(掩码离散扩散):约16步,每步一次完整变压器传播──

显示-o比相似规模模型的马里昂更快,大致与输血步骤数量相匹配,每步成本更低 (微妙的语音符与持续的MSE损失).

> 转移的速度比马龙更快,步数大致匹配,但每步成本更低.

### 在一个检查点的任务

通过快速格式选择的推断支持四项任务:

> 通过提示形式选择:

- 文字生成:标准的自动退缩文字输出.
  中文翻译:文本生成:标准自归文本输出.
- 图像进来,短信出.
  中文翻译:VQA:图像输入,文本输出.
- 通过掩盖的分离传播输入文字,输出图像.
  中文翻译:T2I:文本输入,通过掩码离散扩散输出图像.
- 涂料:有一些隐藏的代币的图像,填写.
  中文翻译:图像修复:带部分掩码代币的图像,填充缺失部分──

面具预测训练免费提供了涂料功能. 面具预测网格的一个区域, 输送其余部分加上文本提示, 预测面具预测.

> 图像修复能力从掩码预测训练中免费获得──掩码VQ代币网格的一个区域,进入其余部分加上文本提示,预测被掩码的代币──

### 掩盖时间表

按每一步揭开多少代币的时间表,决定了质量.

> 每步解掩码多少代币的调度影响质量――Show-o 推余弦调度:

```
mask_ratio(t) = cos(pi * t / (2 * T))   # t = 0..T
```

在步骤0时,所有代币都被掩盖 (比率1.0).在步骤T时,没有一个被掩盖. 科西因集中质量在中范围的比率上,预测最有信息性. 线性时间表也更快地工作,但高原速度更快.

> 第0 步,所有符号被掩盖了(比例1.0) ――第T 步,无掩码――余弦调度将质量集中在中程比例上,此时预测信息量最大――线性调度也可用但更早和――

### 展示2

展示2 (2025后续, arXiv 2506.15564) 尺度展示:更大的LLM基础,更好的标记,更好的面具时间表.相同的建筑模式.

### 在Show-o坐的地方

在2026年分类:

> 在2026年分类中:

- 微观代币+NTP:马龙,Emu3. 简单但缓慢的推断.
  中文翻译:离散代币 + NTP:Chameleon、Emu3──简单但推理慢──
- 微观代币+隐藏的传播:Show-o,MaskGIT,LlamaGen,Muse.并行采样,仍然被代币器输掉.
  中文翻译:离散代币 + 掩码扩散:Show-o、MaskGIT、LlamaGen、Muse。并行采样,仍受分词器损失限制──
- 持续+扩散:输血,MMDiT,DiT. 质量最高,训练更复杂.
  中文翻译:连续 + 扩散:转、MMDiT、DiT──最高质量,训练更复杂──
- 在VLM中连续+流量匹配:JanusFlow,InternVL-U. 最新.
  中文翻译:VLM 中连续 + 流匹配:JanusFlow、InternVL-U──最新──

按任务选择:当您想要一个开放型号的T2I+涂料+VQA时,请选择;当质量至关重要时,并且您可以负担两损失的管道时,请输血.

> 按任务选择:需要一个开放模型同时做T2I + 修复 + VQA 且速度合理时选 显示-o;质量至上且能承担双损失复杂性时选转血――


> **【拓展：Show-o 的离散扩散方法】**展示的离散扩散使用掩码预测:随机遮盖部分标记,模型预测被遮盖的标记――理解任务遮盖部分答案,生成任务从全遮盖开始逐步到噪音――数学上等于多项式扩散――


## 运用它 动手实践
```figure
masked-diffusion-unmask
```

## 用它

`code/main.py`模拟显示样本:

> `code/main.py`模拟 展示

- 一个玩具网,包含16个VQ代币.
  中文翻译:一个16个VQ代币的玩具网格.
- 根据提示和目前未掩盖的代币预测登录的假变压器.
  中文翻译:一个模拟"变形器",基于提示和当前未掩码代币预测 logits。
- 通过8个步骤进行并行面具样本采集,
  中文翻译:余弦调度下 8 步并行掩码采样。
- 打印中间状态 (面具模式演变) 和最后的代币.
  中文翻译:打印中间状态 (掩码模式演化) 和最终标志.

运行它,看面具一步一步溶解.

> 运行它,观察掩码逐步消解.

## 发射上线

这一课产生了`outputs/skill-unified-gen-model-picker.md`由于需要理解 (VQA,字幕) 和生成 (T2I,涂料) 的产品,需要对开放权重的限制,

> 本课产出发 `outputs/skill-unified-gen-model-picker.md`△给定需要理解 (VQA、描述) 和生成 (T2I、修复) 和受限开放权重的产品,在 Show-o、Transfusion/MMDiT 和 Emu3/Chameleon 家族间选择,附具体权衡──

## 练习题

1. 假设在步骤0中,你将全部解开什么?
   中文翻译:掩码离散散散散在约16步内采样.为什么不是1步?如果在第0步解掩码所有代币会怎么样?

2. 涂料是免费的,如果涂料被掩盖的传播. 提出一种产品使用情况 (真实或假设),其中Show-o的涂料比专业模型更好.
   中文翻译:掩码扩散图像修复是免费的――提出一个展示修复能力胜过专业模型的产品用例――

3. 随机时间表与线性时间表:按T=8的步骤追踪每步未掩盖的代币数量.
   中文翻译:余弦调度与线性调度:追踪T=8 时每步解掩码的符号 数――哪个更平衡?

4. 显示图像为512x512显示图像为1024个代币.在词汇K=16384,模型发射1024 * log2(16384) = 14,336位 (~1.75 KiB) 的数据.稳定扩散输出512*512*24位 = 6,291,456位 (~768 KiB) 的原始像素.压缩比是什么,它购买什么质量?
   中文翻译:512x512 显示图像是1024个标志――词汇表 K=16384 下,模型输出约1.75KB 数据――SD 输出约768KB 原始像素――压缩比是多少?换来什么质量?

5. 如何使LlamaGen的类条件自动降低图像模型与Show-o的掩饰方法不同?
   中文翻译:阅读LlamaGen(arXiv:2406.06525) ――LlamaGen的类别条件自归图像模型与Show-o的掩码方法有什么区别?

## 关键词 关键词

| Term | What people say | What it actually means | 中文含义 | 中文释义 |
|------|-----------------|------------------------|---------|
| Masked discrete diffusion | "MaskGIT-style" | Training to predict masked tokens; at inference, iteratively unmask the most-confident predictions | 训练预测掩码 token；推理时迭代解掩码最有信心的预测 |
| Cosine schedule | "Unmask schedule" | Decay of mask ratio over inference steps; concentrates confidence growth at mid-range | 推理步骤中掩码比例的衰减；将信心增长集中在中程 |
| Parallel decoding | "All tokens at once" | Every step predicts the full sequence of masked tokens in one forward pass, then commits top-K | 每步在一次前向传播中预测所有掩码 token，然后提交 top-K |
| Hybrid attention | "Causal + bidirectional" | Mask that is causal over text tokens and bidirectional within image blocks | 文本 token 因果、图像块内双向的掩码 |
| Inpainting | "Fill-in generation" | Condition on an image with some tokens masked, predict the missing ones; free from the training objective | 以部分掩码图像为条件，预测缺失 token；从训练目标免费获得 |
| Commitment rate | "Top-K per step" | How many tokens are declared "done" per iteration; controls inference vs quality trade-off | 每次迭代声明"完成"的 token 数；控制推理与质量的权衡 |

## 继续阅读 继续阅读

- [Xie et al. — Show-o (arXiv:2408.12528)](https://arxiv.org/abs/2408.12528)
  中文翻译:展示论文
- [Show-o2 (arXiv:2506.15564)](https://arxiv.org/abs/2506.15564)
  中文翻译:展示2 后续论文──
- [Chang et al. — MaskGIT (arXiv:2202.04200)](https://arxiv.org/abs/2202.04200)
  中文翻译:MaskGIT论文,掩码离散扩散的原始工作──
- [Sun et al. — LlamaGen (arXiv:2406.06525)](https://arxiv.org/abs/2406.06525)
  中文翻译:LlamaGen自归图像生成──
- [Chang et al. — Muse (arXiv:2301.00704)](https://arxiv.org/abs/2301.00704)
  中文翻译:Muse 掩码图像生成──
