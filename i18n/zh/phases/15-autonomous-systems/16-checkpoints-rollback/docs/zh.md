# 检查点和回滚点

> 图形状态的每一个转变都持续. 工人车时,租期限会过期, 云耐用物体保持状态数小时或数周. 建议后承诺 (课 15) 定义每项行动的反弹计划. 后行动验证结束了循环. 根据欧盟人工智能法第14条,高风险系统必须有有效的人类监督. 严重故障模式:没有无效密钥和先决条件检查,过渡故障后再次尝试可以双重执行已经批准的操作. 行动后的验证是发现的.

> **【中文解读】**每个图形状态转换持久化――工人 崩时其租过期,另一个工人在最新检查点上起――云耐用物体 跨数小时或数周持有状态――建议-然后-承诺(第15课) 为每一个动作定义回滚计划――动作后验证闭包循环――欧盟AI法第14条使高风险系统的有效人类监督强制实践中意味着检查点必须查查,回滚必须练习,审计活跃必须跨部署存储――尖失败模式:没有等键和前置条件检查,即时失败后重复可能执行两倍证已批准的动作――动作后验证被捕获――它.

> **【拓展：幂等+前置条件+验证+回滚四件套】**仅等不够:考虑"当余额 > $1000 时从 A 转 $执行中崩后恢复,仅等检查会通过,但如果A 余额在崩和恢复间通过另一流下降至500美元,前置条件检查失败没有它就发出通过支.每个后置性动作需要四件套件:等键防双) +前置条件 状态仍然与批准一致) + 动作后验证 副作用真的发生) + 验证失败时回滚.

**Type:** Learn | **类型:** 学习
**Languages:** Python (stdlib, checkpoint and rollback state machine) | **语言:** Python（标准库，检查点和回滚状态机）
**Prerequisites:** Phase 15 · 12 (Durable execution), Phase 15 · 15 (Propose-then-commit) | **前置知识:** Phase 15 · 12（持久执行），Phase 15 · 15（propose-then-commit）
**Time:** ~60 minutes | **时间:** ~60 分钟

>  **【前置】**学生节前请先掌握:阶段15·12(可持续执行) 阶段15·15(提出后提交) 数据库事务 (ACID) ◎本节把"持久执行"+"提议提交"组合成完整安全网。
>  **【类比】**检查点回滚 = "游戏的档案与读档"――检查点 = 自动档案(每次关存一次);回滚 = 读档(这关打错了回到上关)――后果性动作四件套 = 等键(防止重启双重执行) + 前置条件(重启后世界状态仍符合预期) + 动作后验证(确认真的副作用发生) + 失败回滚(恢复到动作前) ─ 这四件套缺失不可避免,类似数据库事务的ACID──
> ️ **【易错点】**只有等键没有前置条件检查 → 重启时余额已被其他流程改改仍执行 → 通过支──修复:恢复时必须重检"批准时的世界状态"是否还成立(如"余额 > 1000美元"是否还存在恢复时)

## 问题 问题引入

> **【中文解读】**检查点和回滚机制允许代理在执行过程中保存状态快照,出错时恢复到之前的良好状态. 这类似于数据库的事务和 Git 的版本控制.检查点保持关键节点 (如修改文件前),回滚在检查到错时执行.

> **【拓展：checkpoints rollback】**检查点-回滚是可靠的 代理系统基础设施――实现选择: 1) 文件系统级使用 Git 或快照保存文件状态; 2) 数据库级使用事务保证数据一致性; 3) 应用级 代理自我管理检查点(如LangGraph) ――关键权衡是检查点粒度太细会增加开销,太粗会丢失更多工作――

持续执行 (课 12) 使一个失败的代理重新启动. 提出后承诺 (课 15) 使一个批准的行动可审计.

> 持久执行 (第12课) 使崩 代理可恢复;;建议-然后-承诺 (第15课) 使批准动作可审计;;

经过此,我们将会发现, 经过此次审批, 经过此次审批, 经过此次审批, 经过此次审批, 经过此次审批, 经过此次审批, 经过此次审批, 经过此次审批, 经过此次审批, 经过此次审批, 经过此次审批, 经过此次审批, 经过此次审批, 经过此次审批, 经过此次审批, 经过何次审批, 经过何次审批, 经过何次审批?

> 什么会发生当批准动作部分执行,崩,恢复?回滚何时运行,针对什么状态?

实际系统的方法是不同的:

> 真实系统连接方式不同:

> **【中文解读】**本节介绍了AI代理的核心概念和实现方法. 代理是由LLM驱动的自主系统,能够观察环境,思考决策,执行行动和循环代直到完成目标.

- **LangGraph**在每次转移到PostgreSQL的时间内,租合同会被释放,另一个工人在最后的检查点恢复.`interrupt()`现在,我们已经知道,
  翻译: 中文**LangGraph**将每个图状态转换检查点到PostgreSQL――工人 崩时租释放另一个工人 在最新检查点恢复――工作流在`interrupt()`暂停,本身持久化.
- **Cloudflare Durable Objects**按键状态保持几个小时或几周. 合并计算与批准行动的存储.
  翻译: 中文**Cloudflare Durable Objects**跨数小时或数周持有每个键状态――将与已批准的存储和存储地址计算――
- **Microsoft Agent Framework**暴露`Checkpoint`工作流 API中的原始性;重播加上无能性涵盖重试.
  翻译: 中文**Microsoft Agent Framework**在工作流 API 中暴露`Checkpoint`原语;重放加等覆盖重试──

在每种情况下,实际上有效的组合是:无效密钥 + 预先条件检查 + 后行动验证 + 验证失败的反转.

> 实际有效组合是:等键+前置条件检查+动作后验证+验证失败时回滚――

## 概念的核心概念

### 每次转变都持续.

图形状态转型是从一个命名状态到另一个工作流动的任何步骤. 简单的实现只存在于特定的承诺点上;生产实现在每个转型上持续.成本 (一些额外的写作) 与可靠性增长相比较小 (重播到处都会降落,租恢复是精确的).

> 图状态转换是将工作流从一个命名状态转移到另一个命名状态的任何步骤. 简单实现只在特定提交点持久化;生产实现持久化每转换. 成本几次额外写) 相对可靠性收益.

### 租恢复

工人失败时,工作流程不会丢失;租 (即该工人执行这次运行的短暂声明) 简单地过期.另一个工人接到最新的检查点并恢复.租机制是让生产系统在运行中生存的,而不会失去飞行工作.

> 工人 崩时工作流不丢失;租(此工人正执行此运行的短暂声明) 只是过期――另一工人 拾起最新检查点恢复――租机制让生产系统在不丢失的情况下进行工作中存活滚部署――

### 无能能力加上先决条件

考虑一下:一个工作流程被批准以"转移"$100 from A to B when balance > $1000. "工作流已提交,执行中崩,然后恢复.如果只检查无效率密钥,然后执行恢复,转移运行一次 (正确).但考虑到在崩和恢复之间,A的余额通过不同的工作流程下降到500美元.无效率检查仍然通过;先决条件没有.没有先决条件检查,我们发送过账.

> 仅等不够.考虑:工作流被批准"当余额 > $1000 时从 A 转 $转账运行一次 (正确) 但考虑崩和恢复间 A 余额通过另一条工作流降至500美元.

每个后果行动都需要:

> 每次性动作需要两件事:

- **Idempotency key**防止双重执行.
  翻译: 中文**幂等键**防止两次执行.
- **Precondition check**证实国家仍与批准的内容一致.
  翻译: 中文**前置条件检查**确认状态仍与批准一致.

### 行动后验证

实际验证重新读取目标状态并确认副作用实际发生.

> 实际发生的副作用:

- 数据库更新:`UPDATE ... RETURNING *`然后确认返回的行匹配的预期状态.
  中文翻译:数据库更新:`UPDATE ... RETURNING *`然后断言返回行匹配预期状态.
- 发送电子邮件:在发送后检查发送文件查询消息身份.
  中文翻译:邮件发送:提交后检查发送文件 中的消息 ID──
- 文件写:读取文件并将其加密.
  中文翻译:文件写:回读文件并哈希。
- 接下来的应用程序`GET`目标资源.
  中文翻译:API调用:对目标资源的后续`GET`,我知道.

如果验证失败,工作流程已知坏状态.

> 验证失败时工作流处于已知坏状态.

### 滚动计划

建议后承诺 (课 15) 的每一个后续行动都包含了反弹计划.

> 建议后承诺 (第15课) 中每后果性动作带回滚计划:类型:

- **In-band rollback**直接扭转副作用 (`DELETE`之后`INSERT`现在`Send-correction-email`在发送后).
  翻译: 中文**带内回滚**直接反转副作用`INSERT`后`DELETE`、发送后发送更正邮件) ⋅
- **Compensating transaction**:一种新的行动,它可以消除原始的 (标准SAGA模式).
  翻译: 中文**补偿事务**抵消原始动作的新动作
- **Out-of-band rollback**警报人类,暂停工作流程,离开坏状态进行调查.
  翻译: 中文**带外回滚**警报人类,暂停工作流,留下坏状态进行调查.

没有反弹的行动需要在承诺时间上加强HITL (课题15挑战和反应).

> 无回滚作文在提交时需要更强的HITL 第15课挑战-响应)

### 欧盟人工智能法第14条 运营解读

执行者将其运用为以下方式:

> 第14条要求高风险系统的有效人类监督.

- 检查点可以由审计师查询.
  中文翻译:检查点可被审计者查询.
- 轮反弹进行了练习 (至少一次进行了端到端测试).
  中文翻译:回滚演练 ((至少端到端测试一次) ⋅
- 审计轨迹存活着部署 (检查点后台不是短暂的).
  中文翻译:审计追踪跨部署存活(检查点后端非临时) 』
- 失败的验证会被警报,而不是默默记录.
  中文翻译:失败验证被警报而非静默记录.

工作流程在执行中崩,恢复并完成副作用,而没有验证+反弹路径,不会经过第14条测试.

> 提交中崩、恢复、无验证+回滚路程完成副作用工作流不通过第 14条测试──

### 两次执行模式:双倍执行

在这个领域中最常见的生产事件: 行动批准,提交启动,返回200,工作流失误在持续状态之前,恢复和重新执行.

> 工作流在持久化前崩,恢复并重新执行.

1. 行动批准,无权关键 k.
   中文翻译:动作批准,等键 k。
2. 承诺开始,执行,返回200.
   中文翻译:提交开始、执行、返回200。
3. 在"承诺"状态持续之前,工作流失效.
   中文翻译:工作流在持久化"已提交"状态前崩。
4. 工作流程恢复;查看"批准但未承诺";重新执行.
   中文翻译:工作流恢复;看到"批准但未提交";重新执行──
5. 副作用两次发生.
   中文翻译:副作用触发两次.

减轻:在执行之前坚持"在飞行"的意图,使用无效密钥执行,然后仅在后操作验证成功后标记"承诺".如果操作执行和状态写失败,你知道要验证和 (如果必要) 重复执行.如果状态写成功和操作失败,你通过恢复路径检查和执行确切一次.

> 缓解:执行前持久化"在飞行"意图,用等键执行,仅在动作后验证成功后标记"已提交"――如动作触发而状态写失败,你知道要验证(如必要) 重触发――如状态写成功而动作失败,你验证并通过恢复路径精确触发一次――

## 用它实现框架
```figure
checkpoint-replay
```

## 用它

`code/main.py`驾驶员模拟四种情况:清洁运行,事故后重新尝试 (无效捕获),预先条件失败 (工作流失无需开火),验证失败 (滚动火灾).

> `code/main.py`实现带等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等等

## 运送它.

`outputs/skill-rollback-rehearsal.md`设计一个拟议的工作流程的反弹试验,并对检查轨迹持续性进行检查.

> `outputs/skill-rollback-rehearsal.md`为提议工作流设计回滚演练测试并审计检查点后端审计追踪持久性.

## 练习题

1. 跑步`code/main.py`检查四种情况, 确认一次性行动,
   中文翻译:运行 `code/main.py`证实四场景,对提交中崩案例,确认动作在重试间精确触发一次.

2. 修改"先标记完成,然后做"模式,以便状态在操作后写火灾. 重复崩情况.测量多次重复操作火灾.
   中文翻译:修改"先标记完成再做"模式使状态写在动作后触发──重跑崩场景──测量多少重动作触发──重动作触发──重动作触发──重动作触发──重动作触发──重动作触发──重动作触发──重动作触发──重动作触发──重动作触发──重动作触发──重动作触发──重动作触发──重动作触发──重动作触发──重动作触发──重动作触发──重动作触发──重动作触发──重动作触发──重动作触发──重动作触发──重动作触发──重动作触发──重动作触发──重动作触发──重动作重动作触发──重动作重动作触发

3. 设计一个特定生产行动的反弹计划 (例如"将其转载到Slack频道"). 归类为带内,补偿或带外. 理由选择.
   中文翻译:为特定生产动作 (例如"发到 Slack 频道") 设计回滚计划──分类为带内、补偿或带外──论证选择──

4. 确定每个状态转变. 标记每个状态转变的耐用性要求 (持续/不持续). 计算你目前没有持续的转变.
   中文翻译:取一个你了解的工作流――识别每个状态转换――标记各个持久性要求――计数你当前不持久化――

5. 复制反转测试:设计一个端到端测试,运行一个真正的工作流程,崩它,并确认反转路径的火灾.测试声称什么?
   中文翻译:演练回滚测试:设计端到端测试运行真实工作流、崩、确认回滚路径触发──测试断言什么?

## 关键词 快速查找表

| Term | What people say | What it actually means |
|---|---|---|
| 术语 | 通俗说法 | 实际含义 |
| Checkpoint | "Save point" | Every graph-state transition persists to a durable store |
| 检查点 | "保存点" | 每个图状态转换持久化到持久存储 |
| Lease | "Worker claim" | Short-lived claim that a worker is executing a run; expires on crash |
| 租约 | "Worker 声明" | worker 正在执行运行的短暂声明；崩溃时过期 |
| Precondition | "State gate" | Assertion that the state is still consistent with the approved action |
| 前置条件 | "状态门" | 状态仍与批准动作一致的断言 |
| Post-action verify | "Re-read check" | Confirm the side effect actually happened in the target system |
| 动作后验证 | "回读检查" | 确认副作用在目标系统中实际发生 |
| In-band rollback | "Direct undo" | Reverse the side effect with the inverse operation |
| 带内回滚 | "直接撤销" | 用逆操作反转副作用 |
| Compensating transaction | "SAGA undo" | A new action that neutralizes the original |
| 补偿事务 | "SAGA 撤销" | 抵消原始动作的新动作 |
| Mark-as-done-first | "Status write order" | Persist the committed status before returning from commit |
| 先标记完成 | "状态写顺序" | 从提交返回前持久化已提交状态 |
| Article 14 | "EU AI Act human oversight" | Operational: queryable checkpoints, rehearsed rollbacks, auditable trail |
| 第 14 条 | "EU AI 法案人类监督" | 运营：可查询检查点、演练回滚、可审计追踪 |

## 继续阅读 继续阅读

- [Microsoft Agent Framework — Checkpointing and HITL](https://learn.microsoft.com/en-us/agent-framework/workflows/human-in-the-loop)检查点原始和租回收.
  中文翻译:检查点原语和租约恢复。
- [Cloudflare Agents — Human in the loop](https://developers.cloudflare.com/agents/concepts/human-in-the-loop/) 作为状态基板的持久物体.
  中文翻译:可持续的物体 作为状态基板──
- [EU AI Act — Article 14: Human oversight](https://artificialintelligenceact.eu/article/14/)监管基准.
  中文翻译:监管基线――
- [Anthropic — Measuring agent autonomy in practice](https://www.anthropic.com/research/measuring-agent-autonomy)可靠性框架长远工作流程.
  中文翻译:长程工作流的可靠性框架.
- [Anthropic — Claude Code Agent SDK: agent loop](https://code.claude.com/docs/en/agent-sdk/agent-loop) 克劳德代码程序工作流程形状.
  中文翻译:Claude Code 工作流形态.
