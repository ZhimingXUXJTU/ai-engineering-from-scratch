# القواعد والمسافات

> وظيفتك المسافة تعريف ما تعنيه "مثل" اختر خطأ وكل شيء أسفل التيار ينكسر.
> 距离函数定义了"相似"的含义──选错了,下游一切都崩──

**Type:** Build | **类型:** 动手
**Language:**" بايثون "**语言:**بايثون
**Prerequisites:** Phase 1, Lessons 01 (Linear Algebra Intuition), 02 (Vectors, Matrices & Operations) | **前置知识:** Phase 1, Lessons 01-02
**Time:** ~90 minutes | **时间:** ~90 分钟

## أهداف التعلم

- تنفيذ L1، L2، كوسين، مهالانوبي، جاكارد، وتحرير وظائف المسافة من الصفر
  من صفر تحقيق L1、L2、余弦、马氏、 جاكارد 和 تحرير وظيفة المسافة
- حدد مقياس المسافة المناسبة لمهمة ML معينة وشرح لماذا تفشل البدائل
  لمحدد ML  المهمة اختيار مناسبة المسافة القياس ومشرح لماذا تختار أخرى سوف تفشل
- ربط معايير L1 و L2 إلى تنظيم LASSO و Ridge ومناطق القيود الهندسية الخاصة بهم
  لتحديد L1 و L2                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      
- إظهار كيف أن مجموعة البيانات نفسها تنتج مختلف الجيران القريبين تحت قياسات مختلفة
  ظهور نفس المجموعة البيانية في مختلف الدرجات تظهر في مختلف القريبة

> **【中文解读】**
> 距离函数定义了相似的含义──L1对应 LASSO(特征选择),L2对应 Ridge(防止过拟合),余弦距离适合词嵌入,编辑距离适合字符串──梯度剪剪 L2 范数限制梯度大小──

## المشكلة المشكلة المشكلة

> **【中文解读】**"هل هذه القطاعات الثنائية متشابهة؟" الإجابة تعتمد تماما على ما تختار من وظيفة المسافة. نفس البيانات في L2 أسفل هي القريبة، في بعد السلسلة يمكن أن تكون بعيدة جدا.

## المفهوم الأساسي

> **【拓展：范数在 AI 中的四大应用】**(1) **L2 正则化**:`loss + lambda * ||w||_2^2`, منع الوزن المفرط , تخفيف الوزن المفرط**梯度裁剪**:`||grad|| > max_norm`时缩放梯度,Transformer 训练的标配;(3) **余弦相似度**:RAG 检索和推系统的标准度,只看方向不看大小;(4) **LayerNorm**: للطبقة الواحدة من المخرجات لـ L2 归结, عملية تدريب ثابتة.

لا توجد أفضل مسافة عالمية. يعمل L2 للبيانات المكانية. تهيمن على شبيهة الكوسينات على NLP. يتعامل جاكارد مع المجموعات. تحرير المسافة يتعامل مع السلاسل. يحسب مهالانوبيس التواصلات. يتنقل واسستيرين كتلة الاحتمال. كل منهما يرمز افتراضًا مختلفًا حول ما يعنيه "مثل".
> 没有万能的最佳距离――L2 适合空间数据,余弦相似度主导 NLP, جاكارد 处理集合,编辑距离处理字符串,马氏距离考虑相关性,瓦斯斯坦 移动概率质量──每个编码关于"相似"含义的不同假设──

هذه الدروس تبني كل وظيفة مسافة رئيسية من الصفر، وتظهر لك عندما كل واحد هو الأداة الصحيحة، وتظهر كيف نفس البيانات تنتج مختلفة تماماً أقرب جيران اعتماداً على المقياس الذي تستخدم.
> هذا البرنامج يبدأ من الصفر في بناء كل وظيفة مسافة رئيسية، ويعرض متى تستخدم أي، ويعرض نفس البيانات تحت مقياس مختلفة تخلق مختلفة تماما القريبة.

### المعايير: قياس حجم المتجهات

يُقيس النمط "حجم" المتجه. يمكن كتابة كل وظيفة مسافة بين متجهين كنمط لفرقتهم: d(a، b) = a - b)
> 范数 قياس "大小"                                                                                                                                                                                                                                                           

### L1 نورم (منحةتان المسافة) ٠ L1 范数(منحةهاتن)

المعيار L1 يجمع القيم المطلقة لجميع المكونات.
> L1 范数将所有分量的绝对值相加.

```
||x||_1 = |x_1| + |x_2| + ... + |x_n|
```

يُسمى مسافة مانهاتن لأنه يقيس مدى المشي على شبكة مدينة حيث يمكنك فقط التحرك على طول المحاور. لا يوجد شوارع.
> يطلق عليه "من هاطن" بعد ذلك لأنه يقيس المسافة التي تتحرك على طول المحور على شبكة المدينة، لا يمكن أن تمر على خطوط الزاوية.

عندما تستخدم L1: البيانات النادرة عالية الأبعاد، والثبات إلى مستويات خارجية، مشاكل اختيار الميزات (تعزز تنظيم L1 النادرة).
> كيف تستخدم L1: مشكلة اختيار الصفات

اتصال إلى L1 تنظيم: إضافة إلى معدل الخسارة الخاص بك (Lasso) 1 يدفع الأوزان الصغيرة إلى الصفر بالضبط، وتقوم باختيار الميزات تلقائيًا. تعتبر عقوبة L1 منطقة قيودية شكل الماس، وتقع الزوايا على محورات تكون بعض الأوزان صفرًا.
> مع L1 正则化 (Lasso) 联系: 在损失函数中加小权重推至零,执行自动特征选择──L1 惩罚创建形束区域,角点在轴上──

### L2 نورم (مسافة يوكليدي)  L2 范数(欧氏距离)

القاعدة L2 هي المسافة المستقيمة الجذر التربيعي من مجموع المكونات التربيعية
> L2 范数是直线距离──分量平方和的平方根──

```
||x||_2 = sqrt(x_1^2 + x_2^2 + ... + x_n^2)
```

هذه المسافة التي تعلمتها في صف الهندسة فيثاغوراس في n أبعاد
> هذا هو الوصول إلى الجامعة على المسافة

اتصال إلى L2 تنظيم: إضافة Unww ديبا_2 إلى وظيفة الخسارة يعاقب الوزن الكبير. مثل L1 ، فإنه لا يدفع الوزن إلى الصفر. تعتبر عقوبة L2 منطقة قيود دائرية ، لذلك لا توجد زوايا على المحاور.
> مع L2 正则化 (→ 0)                                                                                                                                                                                                                                                          

```
MAE (L1 loss):  |y - y_hat|         Linear penalty. Robust to outliers. / 线性惩罚，对异常值鲁棒。
MSE (L2 loss):  (y - y_hat)^2       Quadratic penalty. Sensitive to outliers. / 二次惩罚，对异常值敏感。
```

### القواعد العامة العائلة

L1 و L2 هي حالات خاصة من معيار Lp:
> L1 و L2 هي مثالات خاصة لـ Lp 范数:

```
||x||_p = (|x_1|^p + |x_2|^p + ... + |x_n|^p)^(1/p)

p=1:    Diamond shape / 菱形
p=2:    Circle/sphere / 圆/球
p=inf:  Square/hypercube / 正方形/超立方体
```

### شبيهة الكويسين و المسافة الكويسينية

تقيس تشابه الكوزين زاوية بين متجهين، دون تجاهل حجمهم.
> 余弦相似度 قياس الزاوية بين اثنين من القطاعات، ignor­ 略大小──

```
cos_sim(a, b) = (a . b) / (||a||_2 * ||b||_2)
```

يمتد من -1 (الجهات المعاكسة) إلى +1 (المثل). مسافة الكوسين = 1 - كوسين_شبه.
> 范围 from -1(相反方向) to +1( نفس الاتجاه)。余弦距离 = 1 - 余弦相似度。

لماذا يهيمن الكوزين على النمط النووي والتركييبات: في النص، لا ينبغي أن يؤثر طول الوثيقة على التشابه. يجب أن يكون وثيقة عن القطط التي هي أطول مرتين ما زالت "مماثلة".
> لماذا يُمثل السيناريو في النمط النووي ووضعها في: في المقال، لم يكن طول المقالة على أثر على التشابهات.

### "مجالانوبيس" "مجال ماشي"

المسافة الأوكليدية تعامل مع جميع الأبعاد على قدم المساواة. مسافة ماهالانوبي تعتبر هيكل التباين للبيانات.
> 欧氏距离对所有维度一视同仁――马氏距离考虑数据的协同差结构――

```
d_M(x, y) = sqrt((x - y)^T * S^(-1) * (x - y))
```

بصورة بديهية: مسافة ماهالانوبي تُحلل أولاً وتُعادِل البيانات (التبييض) ، ثم تقوم بحساب مسافة L2 في هذا الفضاء المتحول.
> 直觉上: ماينس المسافة أولاً إلى المرتبطة و المرتبطة بتحقيق البيانات ، ثم في الحساب L2 بعد التغيير في الفضاء.

### جاكارد تشابه (للتجميعات)

تدابير تشابه جاكارد تتداخل بين مجموعتين.
> جاكارد تقيس مثلية اثنين من مجموعات التركيب.

```
J(A, B) = |A intersect B| / |A union B|
```

متى تستخدم Jaccard: مقارنة مجموعات من العلامات، وتشابه الوثائق، وكشف شبه المضاعفة، وتقييم نماذج التقسيم (IoU = Jaccard).
> 何時使用 Jaccard:比较标签集、文档相似度、近似重复检测、评估分割模型(IoU = Jaccard) ・・・

### إصلاح المسافة (مسافة ليفينشتاين)  编辑距离(ليفينشتاين 距离)

مسافة التحرير تعتبر الحد الأدنى من عمليات حرف واحد اللازمة لتحويل سلسلة واحدة إلى أخرى.
> 编辑距离计算将一个字符串转换为另一个所需的最小单字符操作数――用动态规划计算――

```
"kitten" -> "sitting"
kitten -> sitten  (substitute k -> s)
sitten -> sittin  (substitute e -> i)
sittin -> sitting (insert g)

Edit distance = 3
```

### ك.ل.م. التباين (ليس مسافة، ولكن يستخدم مثل واحدة)

يُقيّم اختلاف KL كيف يختلف توزيع الاحتمالات من التوزيع الآخر.
> ك.ل 散度 يقيس توزيع احتمالية مع اختلافات أخرى.

عندما ترى الاختلافات KL: VAEs، نزيف المعرفة، RLHF، طرق تراجع السياسة.
> في المشهد التالي انظر KL 散度:VAE、知识蒸、RLHF、策略梯度方法──

### مسافة (واسترين)

قياس مسافة واسيرستن الحد الأدنى من "العمل" اللازم لتحويل توزيع الاحتمال إلى آخر. إنها مقياس حقيقي (متقيماً ، يرضى عدم المساواة الثلاثية). يوفر تراجع حتى عندما لا تتداخل التوزيعات (انعكاس KL يذهب إلى اللانهاية). هذا الخصائص جعلها مركزية ل WGANs.
> واصيرين  المسافة القياسية سوف تحويل توزيع احتمالية إلى آخر الحد الأدنى من المطلوب "功"── انها مقياس حقيقي 对称、满足三角不等式)── في الوقت الذي لا يكتسب فيه توزيع  KL 散度变为无穷大)── هذا النوع يجعل من ذلك صميم WGAN──

### لماذا المهام المختلفة تحتاج إلى مسافة مختلفة لماذا المهام المختلفة تحتاج إلى مسافة مختلفة

| Task / 任务 | Best distance / 最佳距离 | Why / 原因 |
|------|--------------|-----|
| Text similarity / 文本相似度 | Cosine / 余弦 | Magnitude is noise, direction is meaning / 大小是噪声，方向是含义 |
| Image pixel comparison / 图像像素比较 | L2 | Spatial relationships matter / 空间关系重要 |
| Sparse high-dim features / 稀疏高维特征 | L1 | Robust, does not amplify rare large differences / 鲁棒 |
| Set overlap / 集合重叠 | Jaccard | Data is naturally set-valued / 数据天然是集合 |
| String matching / 字符串匹配 | Edit distance / 编辑距离 | Operations map to human editing / 操作映射人类编辑 |
| Outlier detection / 异常检测 | Mahalanobis / 马氏距离 | Accounts for feature correlations / 考虑特征相关性 |
| GAN training / GAN 训练 | Wasserstein | Provides gradients without overlap / 不重叠时仍提供梯度 |
| Embeddings (vector DB) / 嵌入（向量数据库） | Cosine or dot product / 余弦或点积 | Embeddings encode meaning in direction / 嵌入在方向中编码含义 |

### العلاقة مع التنظيم والعلاقة مع التنظيم

```
L1 regularization (Lasso):   loss + lambda * ||w||_1
  -> Sparse weights. Some weights become exactly zero. / 稀疏权重，某些权重变为零。
  -> Automatic feature selection. / 自动特征选择。

L2 regularization (Ridge):   loss + lambda * ||w||_2^2
  -> Small weights. All weights shrink toward zero. / 小权重，所有权重向零收缩。
  -> No feature selection. / 无特征选择。

Elastic Net:                  loss + lambda_1 * ||w||_1 + lambda_2 * ||w||_2^2
  -> Combines sparsity of L1 with stability of L2. / 结合 L1 的稀疏性和 L2 的稳定性。
```

لماذا L1 ينتج القصور ولكن L2 لا: تصور منطقة القيود في مساحة الوزن 2D. L1 هو الماس، L2 هو دائرة. على الأرجح أن نواح وظيفة الخسارة تلمس الماس في الزاوية، حيث وزن واحد هو صفر. أنها تلمس الدائرة في نقطة سلسة، حيث كل من الوزن غير صفر.
> لماذا L1  تظهر نادرة بينما L2 لا يحدث: فكر في 2D  منطقة الحزم في الفضاء الوزني. L1 هو  شكل، L2 هو دوري.

### أقرب جيران بحث أحدث جيران بحث

تحديدات القريبة القريبة (ANN) تتداول كمية صغيرة من الدقة لتحقيق مكاسب كبيرة في السرعة:
> قريبا مثل القريب الأخير (ANN) الخوارزمية باستخدام القليل من الدقة لتبديل التسارع الكبير:

```
Algorithm         Approach                      Used by
HNSW              Hierarchical navigable         FAISS, Qdrant, Weaviate
                  small-world graph
IVF               Inverted file index with       FAISS (billion-scale)
                  cluster-based search
Product quant.    Compress vectors, search       FAISS (memory-constrained)
                  in compressed space
```

HNSW هو الخوارزمية المهيمنة في قواعد البيانات المتجهة الحديثة.
> HNSW هي الوسيلة الرئيسية في قاعدة بيانات الحجم الحديثة.

## بناء ذلك تحرك لتحقيق
```figure
norm-unit-balls
```

## بناءها

### الخطوة 1: جميع وظائف القاعدة والمسافة . الخطوة 1: جميع وظائف النقاط والمسافة

انظر`code/distances.py`كل وظيفة بنيت من الصفر باستخدام فقط الرياضيات الأساسية Python.
> 完整实现见 `code/distances.py`.

### الخطوة الثانية: نفس البيانات، مسافة مختلفة، جيران مختلفين. الخطوة الثانية: نفس البيانات، مسافة مختلفة، جيران مختلفين.

التجربة في`distances.py`يخلق مجموعة بيانات، يختار نقطة استفسار، ويعرض كيف يتغير أقرب جيران اعتمادا على مقياس المسافة.
> 演示 إنشاء مجموعة بيانات، اختيار نقاط استفسار، عرض كيف يتغير القريبة مع الوصول إلى المدى.

### الخطوة الثالثة: إدخال بحث التشابه

يتضمن الرمز بحثًا مزيفًا يضم تشابهًا يجد أكثر "وثائق" تشابهًا لمطالبة باستخدام تشابه الكوزين مقابل مسافة L2.
> 代码包含模拟嵌入相似度搜索, باستخدام余弦相似度和L2 距离寻找最相似的"文档"──

## استخدمها في إطار التنفيذ

الاستخدام العملي الأكثر شيوعا: العثور على عناصر مماثلة في قاعدة بيانات متجهة.
> أشهر استخدامات عملية: البحث عن عناصر مماثلة في قاعدة بيانات الكمية

```python
import numpy as np

def cosine_similarity_matrix(X):
    norms = np.linalg.norm(X, axis=1, keepdims=True)
    norms = np.where(norms == 0, 1, norms)
    X_normalized = X / norms
    return X_normalized @ X_normalized.T

embeddings = np.random.randn(1000, 768)

sim_matrix = cosine_similarity_matrix(embeddings)

query_idx = 0
similarities = sim_matrix[query_idx]
top_k = np.argsort(similarities)[::-1][1:6]
print(f"Top 5 most similar to item 0: {top_k}")
print(f"Similarities: {similarities[top_k]}")
```

عندما تتصلين`model.encode(text)`ثم البحث في قاعدة بيانات المتجهات، وهذا ما يحدث تحت القبو.
> عندما ت调用`model.encode(text)`ثم عندما تبحث عن قاعدة بيانات الكمية، وهذا ما يحدث في الأسفل.

## تمارين التدريب

1. احسب مسافات L1، L2، و L- لا نهاية لها بين (1, 2, 3) و (4, 0, 6). تحقق من أن L-inf <= L2 <= L1 دائماً. أثبت لماذا هذا التسلسل مضمون.
   計算 (1, 2, 3) 和 (4, 0, 6)  之間 L1、L2 和 L-inf 距离──验证 L-inf <= L2 <= L1 始终成立──

2. قم بإنشاء متجهين حيث تكون شباهة الكوسين عالية (> 0.9) ولكن مسافة L2 كبيرة (> 10).
   创建两个余弦相似度高(> 0.9) ولكن L2 距离大(> 10) 的向量──几何解释──

3. قم بتنفيذ وظيفة تعيد أقرب جيران تحت مسافة L1، L2، كوسين، ومهالانوبيس. ابحث عن مجموعة بيانات حيث جميع الأربعة لا يوافقون.
   实现函数在 L1、L2、余弦和马氏距离下回归近邻──找到四种尺度全部不一致的数据集──

4. احسب مسافة Wasserstein بين [0.5، 0.5، 0، 0] و [0, 0، 0.5، 0.5] باستخدام طريقة CDF.
   用 CDF 方法计算 [0.5, 0.5, 0, 0] و [0, 0, 0.5, 0.5] 的 Wasserstein 距离──

5. قم بتنفيذ MinHash للحصول على تشابه تقريبي مع Jaccard. مقارنة مع Jaccard الدقيق.
   实现 MinHash 近似 جاكارد 相似度──与精确 جاكارد 比较──

## شروط الرئيسية

| Term / 术语 | What people say | What it actually means / 实际含义 |
|------|----------------|----------------------|
| Norm / 范数 | "Size of a vector" | A function that maps a vector to a non-negative scalar / 将向量映射到非负标量的函数 |
| L1 norm / L1 范数 | "Manhattan distance" | Sum of absolute component values. Produces sparsity. / 分量绝对值之和。产生稀疏性。 |
| L2 norm / L2 范数 | "Euclidean distance" | Square root of sum of squared components. / 分量平方和的平方根。 |
| Cosine similarity / 余弦相似度 | "Angle between vectors" | Dot product normalized by both magnitudes. Ranges -1 to +1. / 双方大小归一化的点积。范围 -1 到 +1。 |
| Mahalanobis distance / 马氏距离 | "Correlation-aware distance" | L2 distance in whitened space using covariance matrix. / 用协方差矩阵白化后的 L2 距离。 |
| Jaccard similarity / Jaccard 相似度 | "Set overlap" | Intersection size divided by union size. / 交集大小除以并集大小。 |
| Edit distance / 编辑距离 | "Levenshtein distance" | Minimum insertions, deletions, substitutions to transform strings. / 转换字符串的最少插入、删除、替换次数。 |
| KL divergence / KL 散度 | "Distance between distributions" | Not a true distance (not symmetric). / 不是真正的距离（不对称）。 |
| Wasserstein distance / Wasserstein 距离 | "Earth mover's distance" | Minimum work to transport mass between distributions. A true metric. / 在分布间传输质量的最小功。真正的度量。 |
| HNSW | "The vector DB algorithm" | Multi-layer graph for fast approximate nearest neighbor search. / 用于快速近似最近邻搜索的多层图。 |
| L1 regularization / L1 正则化 | "Lasso" | Drives weights to zero (sparsity). / 将权重驱动到零（稀疏性）。 |
| L2 regularization / L2 正则化 | "Ridge" or "weight decay" | Shrinks weights toward zero without sparsity. / 将权重向零收缩但不产生稀疏性。 |
| Elastic Net / 弹性网络 | "L1 + L2" | Combines L1 and L2 regularization. / 结合 L1 和 L2 正则化。 |

## المزيد من القراءة

- [FAISS: A Library for Efficient Similarity Search](https://github.com/facebookresearch/faiss)- مكتبة "ميتا" للبحث على نطاق مليار من النتائج
  الميتا من عشرة مليارات من الدرجة ANN 搜索库
- [Wasserstein GAN (Arjovsky et al., 2017)](https://arxiv.org/abs/1701.07875)- مسافة الأرض المتحرك في GANs
  واسيرستن  المسافة في GAN
- [Efficient Estimation of Word Representations (Mikolov et al., 2013)](https://arxiv.org/abs/1301.3781)- Word2Vec، حيث أصبح cosine الافتراضي
  Word2Vec،余弦相似度 أصبح مررًا
- [sklearn.neighbors documentation](https://scikit-learn.org/stable/modules/neighbors.html)- دليل عملي لمقاييس المسافات
   منطقة التدريب
