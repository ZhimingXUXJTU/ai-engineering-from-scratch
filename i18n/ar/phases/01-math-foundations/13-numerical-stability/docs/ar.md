# استقرار الرقمية

> نقطة العبور هي مجرد تجريئة لا تسرب، ستقوم بتعضيك أثناء التدريب، ولن تلاحظها
> النقطة المنزلية هي مجرد مجرد إختراق. ستقوم بتعقبك أثناء التدريب، وأنت لن ترى ذلك قادما.

**Type:** Build | **类型:** 动手
**Language:**" بايثون "**语言:**بايثون
**Prerequisites:** Phase 1, Lessons 01-04 | **前置知识:** Phase 1, Lessons 01-04
**Time:** ~120 minutes | **时间:** ~120 分钟

## أهداف التعلم

- تنفيذ softmax مستقرة عدديًا و log-sum-exp باستخدام خدعة الحد الأقصى
  باستخدام تقنيات خفض القيمة القصوى لتحقيق قيمة ثابتة من السلم و التسجيل المجموع
- تحديد التدفقات الزائدة، التدفقات السيئة، والإلغاء الكارثي في حسابات النقاط المتحركة
  识别浮点计算中的溢溢和灾难性抵消
- التحقق من التدرج التحليلي مقابل التدرج الرقمي باستخدام الاختلافات المحدودة المركزية
  استخدام مركز محدود التفاوت
- شرح لماذا يفضل bfloat16 على float16 للتدريب وكيف يمنع تحديد حجم الخسائر انخفاض تدفق التدفق
   شرح لماذا bfloat16 أكثر تكييفا للتدريب، وكيفية منع انخفاض التدفق

> **【中文解读】**
> 浮点数是漏水的抽象──训练 3 小时后损失 变 NaN 是最常见的崩──本章实现数值稳定的软max(减最大值技巧),解释为什么bfloat16比float16更适合训练──混合精度训练中使用损失扩展 防止小梯度下溢──

## المشكلة المشكلة المشكلة

> **【中文解读】**ثلاثة أمراض نموذجية للثابتة العددية: 1) تدريب 3 ساعات بعد الخسارة 变化 NaN某步计算溢出; 2) دقة مقارنة مقال مقال 2% float16 累积舍入差吃掉准确率; 3) ص交叉自写的交叉大逻辑 时返回 infsoftmax 溢出──这些都是浮点数的"漏水抽象", كل من هذه الأنواع لديها تقنيات تصحيح معيارية──

تضيف بيانًا مطبوعًا، والخطوط جيدة عند الخطوة 9000، عند الخطوة 9001 هي`inf`. من خلال خطوة 9 002 كل تراجع هو`nan`و التدريب ميت
> لقد أضفتُ طباعةً، وبدأتُ التسعّة آلاف خطوة طبيعية، وبدأتُ التسعّة آلاف خطوة طبيعية.`inf`حتى التاسعة والعشرين من الخطوات كل الدرجات هي`nan`، التدريب قد مات

أو: نموذجك يتجه إلى الانتهاء ولكن دقة هو 2% أسوأ من المطالبة الورقية. تفحص كل شيء. التطابق الهندسة المعمارية. المعلمات المرتفعة مطابقة. المعلومات تتطابق. المشكلة هي أن الورق استخدم float32 وأنت استخدم float16 دون التوسع الصحيح. 32 قطعة من التراكم التراكم التراكمية أكلت دقةك بهدوء.
> أو: تمت تدريب النموذج ولكن الدقة مقارنة بالدراسة تبلغ 2%. لقد تحققت من كل شيء.

أو: تقوم بتنفيذ خسارة الانتروبيا المتقاطعة من الصفر. يعمل على علامات صغيرة. عندما تتجاوز علامات 100، فإنه يعود`inf`لقد تجاوزت القوة الناعمة لأن`exp(100)`كل إطار ML يتعامل مع هذه مع خدعة خطين. لم تكن تعرف خدعة وجود.
> أو: أنت من رأس تحقيق交叉损失──小登录 时正常──当登录 超过 100 时返回 `inf`✿softmax 溢出, لأن ✿`exp(100)`تجاوزت نطاق تعبيرات "فلوات32"

الاستقرار الرقمي ليس من المشكلة النظرية. إنه الفرق بين عملية تدريب ناجحة وتفشل بصمت. كل خطأ ML خطير سوف تحل في نهاية المطاف إلى نقطة عائمة.
> الثبات العددي ليس مشكلة نظرية، إنه الفرق بين نجاح التدريب وفشل الصمت. كل خطأ ML خطير تم تجريبه في النهاية ينتهي إلى عدد النقاط.

## المفهوم الأساسي

> **【拓展：Softmax 的数值稳定技巧是面试必考题】**المرحل المرحل:`softmax(x) = exp(x) / sum(exp(x))`عندما يكون هناك قيمة كبيرة في x exp 溢出 解法: خفض القيمة القصوى `softmax(x) = exp(x - max(x)) / sum(exp(x - max(x)))`النتائج الرياضية لا تتغير ولكن القيمة العددية ثابتة.`F.cross_entropy`内部 استخدام سجل-softmax 而非分开计算, هذا هو السبب

### IEEE 754: كيف تخزين الحواسيب الأرقام الحقيقية

تخزين أجهزة الكمبيوتر الأرقام الحقيقية كمقيمات نقطة عائمة تتبع معيار IEEE 754. يحتوي العائمة على ثلاثة أجزاء: بيت إشارة، ومعبر، ومانتيسا (مهمة).
> 计算机 مطابق IEEE 754 标准将实数存储为浮点值──浮点数有三部分:符号位、指数和尾数──

```
Float32 layout (32 bits total):
[1 sign] [8 exponent] [23 mantissa]

Value = (-1)^sign * 2^(exponent - 127) * 1.mantissa
```

يحدد المنتيسا الدقة (كم عدد أرقام مهمة). يحدد المُعبر النطاق (كم عدد كبير أو صغير يمكن أن يكون).
> 尾数决定精度 ((多少有效数字) ، مؤشر حدة الحد

```
Format     Bits   Exponent  Mantissa  Decimal digits  Range (approx)
float64    64     11        52        ~15-16          +/- 1.8e308
float32    32     8         23        ~7-8            +/- 3.4e38
float16    16     5         10        ~3-4            +/- 65,504
bfloat16   16     8         7         ~2-3            +/- 3.4e38
```

فلوات32 يعطيك حوالي 7 أرقام عشرية من الدقة. فلوات16 يعطيك حوالي 3 أرقام. bfloat16 هو إجابة جوجل لمشكلة النطاق في فلوات16 -- نفس المعبرة 8-بيت مثل فلوات32 ولكن فقط 7 بيت mantissa.
> float32  أعطيك حوالي 7 位十进制精度──float16 约 3 位──bfloat16 هو إجابة جوجل على سؤال حول فلوات16  حدة  مقارنة مع فلوات32 مؤشر 8 位 ولكن فقط 7 位尾数── تدريب شبكة العصبية

### لماذا 0.1 + 0.2 != 0.3 . لماذا 0.1 + 0.2 != 0.3

لا يمكن تمثيل الرقم 0.1 بالضبط في نقطة عائمة ثنائية. في القاعدة 2 ، هو جزء متكرر. Float32 تقصر هذا إلى 23 بت من mantissa.
> 0.1 في ثاني نظام浮点中不能精确表示──它是循环小数──float32 将其截断为23位尾数──

```
In Python:
>>> 0.1 + 0.2
0.30000000000000004

>>> 0.1 + 0.2 == 0.3
False
```

هذا مهم بالنسبة لـ ML لأن: (1) مقارنات الخسارة مثل `if loss < threshold`يمكن أن تعطى إجابات خاطئة. (2) تجميع العديد من القيم الصغيرة تتحرك عن المبلغ الحقيقي. (3) الفحوصات والاختبارات التكرارية تفشل إذا قارنت العوامل العائمة مع`==`الحل: لا تقارن السلاحين مع`==`استخدم`abs(a - b) < epsilon`أو`math.isclose()`. . .
> هذا بالنسبة لـ ML  مهم: 1) 损失比较可能出错.`==`مقارنة: لا تستخدم أبدا`==`مقارنة بـ

### الإلغاء الكارثي

عندما تقوم بإسقاط رقمين متطوفين تقريباً متساوين، يتم إلغاء الأرقام المهمة ويتم تركك مع ضجيج التجويب الذي يتم تعزيزه إلى أرقام أوائل.
> عندما تخفض الصوت عن عدد نقطة التفوق المماثلة، فإن الرقم الفعال يعكس، ويتم رفع الصوت إلى الرقم المتحكم.

```
a = 1.0000001    (stored as 1.00000011920929 in float32)
b = 1.0000000    (stored as 1.00000000000000 in float32)

True difference:  0.0000001
Computed:         0.00000011920929

Relative error: 19.2%
```

الحل: إعادة ترتيب الصيغ لتجنب خصم أعداد كبيرة، تقريبا متساوية. للفوارانس، استخدم خوارزمية ويلفورد أو مركز البيانات أولا.
> 修复: إعادة ترتيب الصيغة لتجنب خفض عدد من الصيغ المقربة الكبيرة.

### التدفق والتدفق

التدفق الزائد يحدث عندما تكون النتيجة كبيرة جداً للاستعراض. التدفق السفلي يحدث عندما تكون صغيرة جداً.
> المنتج كبير جدا لا يمكن أن يعبر عن المنتج الصغير جدا

```
Float32 boundaries:
  Maximum:  3.4028235e+38
  Overflow:  anything > 3.4e38 becomes inf
  Underflow: anything < 1.4e-45 becomes 0.0

exp(88.7)  = 3.40e+38   (barely fits in float32)
exp(89.0)  = inf         (overflow)
```

في اللغة الإنجليزية`exp()`يظهر في softmax، sigmoid، والحسابات الاحتمالية. `log()`يظهر في الإنتروبي المتقاطع، احتمالات التسجيل، واختلاف KL.
> في المرحلة الأولى`exp()`ظهرت الآن في الحسابات المرجعية`log()`ظهرت في الصفحة الثانية

### خدعة التسجيل المشترك

الحوسبة`log(sum(exp(x_i)))`الخدعة: خصم القيمة القصوى قبل أن تعبر.
> 直接计算 `log(sum(exp(x_i)))`عدد القيمة المحددة: تقنية: تقليل القيمة القصوى قبل تحديد المعدل

```
log(sum(exp(x_i))) = max(x) + log(sum(exp(x_i - max(x))))
```

لماذا هذا يعمل: بعد الحد `max(x)`، أكبر مؤشر هو`exp(0) = 1`لا يمكن تجاوزها. على الأقل تعبير واحد في الجملة هو 1، لذلك الجملة هي على الأقل 1، و`log(1) = 0`لا تدفق إلى`-inf`هو ممكن.
> لماذا فعال: خفض`max(x)`بعد، أكبر مؤشر هو`exp(0) = 1` مستحيل أن يُنتشر  على الأقل 1، لذا و على الأقل 1`log(1) = 0`ليس من الممكن أن يصل`-inf`.

هذه الحيلة تظهر في كل مكان في ML: التطبيع اللطيف الأقصى، فقدان الانتروبيا المتقاطعة، جمع احتمالات التسجيل، مزيج من غوسيان، استنتاج التغيرات.
> هذه المهارة لا توجد في ML:softmax 归结化、交叉损失、对数概率求和、高斯混合、变分推断──

### لماذا تحتاج Softmax إلى حيلة Max-سحب لماذا Softmax تحتاج إلى تقنية خفض القيمة القصوى

بدون الحيلة، تسبب التنفيذات من [100، 101, 102] التجاوز. مع الحيلة، قم بإسقاط ماكس ((x) = 102:
> 没有技巧时,logits [100, 101, 102] 导致溢出──有技巧时,减去最大(x) = 102:

```
exp(100 - 102) = exp(-2) = 0.135
exp(101 - 102) = exp(-1) = 0.368
exp(102 - 102) = exp(0)  = 1.000
sum = 1.503

softmax = [0.090, 0.245, 0.665]
```

الاحتمالات هي نفسها الحساب آمن هذا ليس تحسيناً إنه شرط للصواب
> الاحتمالات هي نفسها تماما.

### النفط والحالات: الكشف والوقاية

`nan`و`inf`ينتشر فيروسياً من خلال الحسابات`nan`في تحديث التراجع يجعله الوزن`nan`، الذي يجعل كل الناتج اللاحق `nan`التدريب ميت في غضون خطوة واحدة
> `nan`和 `inf`通过计算病毒式传播――梯度更新中的一个 `nan`لتحويل الوزن`nan`, جعل كل النفذات المتغيرة`nan`تدريب خطوة على الخطوة

كيف`nan`يظهر: `0.0 / 0.0`،`inf - inf`،`inf * 0`،`sqrt()`من السلبي`log()`الوقاية: مدخلات الضغط إلى`exp()`إضافة إيبسيلون إلى المعالم، استخدام التطبيقات المستقرة، قطع التراجع.
> `nan`كيفية ظهورها:`0.0/0.0`.`inf-inf`.`inf*0`、 負数`sqrt()`、 負数`log()` الوقاية: الحد`exp()`输入、给分母加epsilon、使用稳定实现、梯度剪

### التحقق من التدريج الرقمي

يمكن أن يكون لدى التدرج التحليلي (من التنشر الخلفي) أخطاء. يحققها التحقق الرقمي من التدرج عن طريق حساب التدرج مع اختلافات محدودة.
> 解析梯度 (من المنعكس الاتجاه) قد يكون هناك خطأ.

```
df/dx ~= (f(x + h) - f(x - h)) / (2h)
```

قواعد الإبهام: relative_error < 1e-7: مثالي؛ < 1e-5: مقبول؛ > 1e-3: شيء خاطئ؛ > 1: خطأ تماما.
> 经验法则:相对错误 < 1e-7:完美; < 1e-5:可接受;> 1e-3:有问题;> 1: كامل خطأ。

### تدريب الدقة المختلطة تدريب الدقة المختلطة

أجهزة التشغيل المعاصرة لديها أجزاء من أجزاء التنسور التي تحسب مضاعفات المصفوفة float16 2-8x أسرع من float32.
> معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة معالجة.

```
1. Maintain float32 master copy of weights
2. Forward pass in float16 (fast)
3. Compute loss in float32 (prevents overflow)
4. Backward pass in float16 (fast)
5. Scale gradients to float32
6. Update float32 master weights
```

التحديد لتحقيق تدفقات التدفقات القائمة على التدفقات القائمة على التدفقات القائمة على التدفقات القائمة على التدفقات القائمة على التدفقات القائمة على التدفقات القائمة على التدفقات القائمة على التدفقات القائمة على التدفقات القائمة على التدفقات القائمة على التدفقات القائمة على التدفقات القائمة على التدفقات القائمة على التدفقات القائمة على التدفقات القائمة على التدفقات القائمة على التدفقات القائمة على التدفقات القائمة على التدفقات القائمة على التدفقات القائمة على التدفقات القائمة على التدفقات القائمة على التدفقات القائمة على التدفقات القائمة على التدفقات القائمة على التدفقات.
> إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التداول: إعادة التدول: إعادة التدول: إعادة التدول: إعادة التدول: إدولة: إعادة التدولة: إعادة التدولة: إعادة التدولة: إعادة التدولة: إعادة التدولة: إعادة التدولة: إعادة التدولة: إعادة التدولة: إعادة التدولة: إدولة: إعادة إدولة إعادة إعادة إدولة إدولة إدولة إدولة إدولة إدولة إدولة إدولة إدولة إدولة إدولة إدولة إدولة إدولة إدولة إدولة إدولة إدولة إدولة إدولة إدولة إدولة إدولة إدولة إدولة إدولة إ

### لماذا bfloat16 فوز للتدريب

فلوات16 لديه دقة أكبر (10 بيت من مانتيسا) ولكن نطاق محدود (أقصى حد ~ 65.504). bfloat16 لديه دقة أقل ولكن نفس نطاق فلوات32 (أقصى حد ~ 3.4e38).
> تحليق16 精度更高(10 位尾数) ولكن المدى محدود(最大 ~65,504) ・bfloat16 精度较低但范围与 float32 相同(最大 ~3.4e38)。 تدريب وقت المدى أكثر أهمية。

### التقطيع التدريجي

يحدث التدفقات المتفجرة عندما ينمو التدفقات بشكل متسارع. هناك نوعان من التقطيع: التقطيع حسب القيمة (تقطيع كل عنصر) والتقطيع حسب المعيار (تقييم متجه كامل بحيث لا يتجاوز معايره عتبة).`torch.nn.utils.clip_grad_norm_()`-لا، لا
> 梯度爆炸发生在梯度指数增长时──两种剪裁:按值剪裁 限制每个元素) 和按范数剪裁 缩放整个向量使范数不超过值)──按范数剪裁保留梯度方向──

القيم النموذجية: `max_norm=1.0`لتحولات، `max_norm=0.5`لـ RL`max_norm=5.0`للشبكات الأسهل
> 典型值: محول`max_norm=1.0`, ر ر استخدام `max_norm=0.5`, بسهولة استخدام الشبكة`max_norm=5.0`.

### الأخطاء العددية الشائعة لـ ML

**Bug: Loss is NaN after a few epochs.**السبب: التسجيلات كبيرة جداً، ومدفوعة المعدلات المضافة.
> **Bug: 几个 epoch 后 loss 变 NaN。**原因:logits 太大,softmax 溢出──修复: استخدام稳定softmax,降低学习率,添加梯度剪──

**Bug: Validation accuracy is lower by 1-3%.**السبب: دقة مختلطة دون قياس ضياع مناسب.
> **Bug: 验证精度低 1-3%。**原因:混合精度没有正确的损失缩放──修复:启动动态损失缩放,或转换到bfloat16──

**Bug: `exp()` returns `inf` in loss computation.**إصلاح: استخدام `torch.nn.functional.log_softmax()`الذي يطبق التسجيلات المجموعة-exp داخليا.
> **Bug: 损失计算中 `exp()` 返回 `inf`。**修复: استخدام `torch.nn.functional.log_softmax()`.

## بناء ذلك تحرك لتحقيق

### الخطوة 1: إظهار حدود دقة نقطة عائمة
**Bug: Validation accuracy is lower than expected by 1-3%.**
السبب: دقة مختلطة دون قياس ضياع مناسب. تدفق التدفق التدريجي يُبعد صامتًا تحديثات صغيرة.
إصلاح: تمكين تحليل الخسائر الديناميكية، أو الانتقال إلى bfloat16.

**Bug: Gradient norms are 0.0 for some layers.**
السبب: الخلايا العصبية الميتة لـ ReLU (كل المدخلات سلبية) ، أو التدفق القادم.
الإصلاح: استخدم LeakyReLU أو GELU، استخدم تراكم التراجع، تحقق من تشغيل الوزن.

**Bug: Model works on one GPU but gives different results on another.**
السبب: ترتيب تراكم نقطة عائمة غير محدد. تخفيضات GPU متوازية جمع في ترتيبات مختلفة على أجهزة مختلفة، وتضافة نقطة عائمة ليست ارتباطية.
الإصلاح: تقبل الاختلافات الصغيرة (1e-6) ، أو تعيين `torch.use_deterministic_algorithms(True)`و تقبل عقوبة السرعة

**Bug: `exp()` returns `inf` in loss computation.**
السبب: تم نقل اللوجات الخامة إلى`exp()`بدون خدعة الحد الأقصى
إصلاح: استخدام `torch.nn.functional.log_softmax()`الذي يطبق التسجيلات المجموعة-exp داخليا.

**Bug: Training diverges after switching from float32 to float16.**
السبب: لا يمكن أن يمثل float16 مقياسات تراجعية أقل من 6e-8 أو تنشيطات أعلى من 65,504.
الإصلاح: استخدم الدقة المختلطة مع قياس الخسائر (AMP) ، أو استخدم bfloat16 بدلاً من ذلك.

```figure
logsumexp-stability
```

## بناءها

### الخطوة الأولى: إظهار حدود دقة نقطة العبور

```python
print("=== Floating Point Precision ===")
print(f"0.1 + 0.2 = {0.1 + 0.2}")
print(f"0.1 + 0.2 == 0.3? {0.1 + 0.2 == 0.3}")
print(f"Difference: {(0.1 + 0.2) - 0.3:.2e}")
```

### الخطوة الثانية: تنفيذ البراغي مقابل المثابرة المرنة

```python
import math

def softmax_naive(logits):
    exps = [math.exp(z) for z in logits]
    total = sum(exps)
    return [e / total for e in exps]

def softmax_stable(logits):
    max_logit = max(logits)
    exps = [math.exp(z - max_logit) for z in logits]
    total = sum(exps)
    return [e / total for e in exps]

safe_logits = [2.0, 1.0, 0.1]
print(f"Naive:  {softmax_naive(safe_logits)}")
print(f"Stable: {softmax_stable(safe_logits)}")

dangerous_logits = [100.0, 101.0, 102.0]
print(f"Stable: {softmax_stable(dangerous_logits)}")
# softmax_naive(dangerous_logits) would return [nan, nan, nan]
```

### الخطوة الثالثة: تنفيذ سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل سجل س

```python
def logsumexp_stable(values):
    c = max(values)
    return c + math.log(sum(math.exp(v - c) for v in values))
```

### الخطوة الرابعة: تنفيذ الانتروبية المتقاطعة المستقرة

```python
def cross_entropy_stable(true_class, logits):
    max_logit = max(logits)
    shifted = [z - max_logit for z in logits]
    log_sum_exp = math.log(sum(math.exp(s) for s in shifted))
    log_prob = shifted[true_class] - log_sum_exp
    return -log_prob
```

### الخطوة 5: التحقق من التدريجية الخطوة 5: التحقق من التدريجية

```python
def numerical_gradient(f, x, h=1e-5):
    grad = []
    for i in range(len(x)):
        x_plus = x[:]
        x_minus = x[:]
        x_plus[i] += h
        x_minus[i] -= h
        grad.append((f(x_plus) - f(x_minus)) / (2 * h))
    return grad

def check_gradient(analytical, numerical, tolerance=1e-5):
    for i, (a, n) in enumerate(zip(analytical, numerical)):
        denom = max(abs(a), abs(n), 1e-8)
        rel_error = abs(a - n) / denom
        status = "OK" if rel_error < tolerance else "FAIL"
        print(f"  param {i}: analytical={a:.8f} numerical={n:.8f} "
              f"rel_error={rel_error:.2e} [{status}]")
```

## استخدمها في إطار التنفيذ

انظر`code/numerical.py`للتنفيذات الكاملة مع إثبات جميع الحالات المحتملة.
> 完整实现见 `code/numerical.py`.

```python
# 梯度裁剪
def clip_by_norm(gradients, max_norm):
    total_norm = math.sqrt(sum(g**2 for g in gradients))
    if total_norm > max_norm:
        scale = max_norm / total_norm
        return [g * scale for g in gradients]
    return gradients

# NaN/Inf 检测
def check_tensor(name, values):
    has_nan = any(math.isnan(v) for v in values)
    has_inf = any(math.isinf(v) for v in values)
    if has_nan or has_inf:
        print(f"WARNING {name}: nan={has_nan} inf={has_inf}")
        return False
    return True
```

## أرسلها .

هذا الدرس ينتج عن:
> 本课程产出:

- `code/numerical.py`مع softmax مستقرة، و log-sum-exp، والانتروبيا المتقاطعة، والتحقق من التراجع، ومحاكاة الدقة المختلطة
  包含 ثابتة softmax ‬الملحوظات الجملة-exp ‬الوسائل ‬المتسعة ‬التحقق من الدرجة ومزيج تحديد المزيج
- `outputs/prompt-numerical-debugger.md`للتشخيص من NaN/Inf والقضايا العددية في التدريب
  تستخدم في التدريب التشخيص مشكلة NaN/Inf و

## تمارين التدريب

1. **Catastrophic cancellation.**احسب التباين [1000000.0، 1000001.0، 1000002.0] باستخدام الصيغة البديلة `E[x^2] - E[x]^2`ثم احسبها باستخدام خوارزمية ويلفورد على الانترنت. مقارنة الأخطاء ضد التباين الحقيقي (0.6667).
   **灾难性抵消。**استخدام الصيغة البسيطة و Welford 算法 حساب [1000000.0, 1000001.0, 1000002.0]

2. **Precision hunt.**العثور على أصغر قيمة إيجابية في float32 `x`مثل هذا`1.0 + x == 1.0`تأكد من ان هذا يطابق`numpy.finfo(numpy.float32).eps`. . .
   **精度搜索。**找到使 `1.0 + x == 1.0`أقل قيمة صحيحة

3. **Log-sum-exp edge cases.**اختبر`logsumexp_stable`وظيفة مع: (أ) جميع القيم متساوية، (ب) قيمة واحدة أكبر بكثير من البقية، (ج) جميع القيم سلبية جدا (-1000).
   **Log-sum-exp 边界情况。**测试稳定 log-sum-exp 在极端输入下表现──

4. **Gradient checking a neural network layer.**تنفيذ طبقة خطية واحدة `y = Wx + b`وتحقق من صحة المصفوفة الثلاثة × 2
   **梯度检查神经网络层。**تحقيق الصوابيات المتعلقة بالصوابيات

5. **Loss scaling experiment.**محاكاة التدريب مع العجلة16: قياس ما جزء من التدرج يصبح صفر. ثم تطبيق استحداث الخسارة وتقياس مرة أخرى.
   **损失缩放实验。**模拟 float16 训练,测量梯度变为零的比例,然后应用损失缩放再测量──

## شروط الرئيسية

| Term / 术语 | What people say | What it actually means / 实际含义 |
|------|----------------|----------------------|
| IEEE 754 | "The float standard" | International standard defining binary floating point formats. / 定义二进制浮点格式的国际标准。 |
| Machine epsilon / 机器精度 | "The precision limit" | The smallest value e such that 1.0 + e != 1.0. For float32, ~1.19e-7. / 使 1.0 + e != 1.0 的最小值。float32 约 1.19e-7。 |
| Catastrophic cancellation / 灾难性抵消 | "Precision loss from subtraction" | Significant digits cancel when subtracting nearly equal numbers. / 相减近似相等数时有效数字抵消。 |
| Overflow / 溢出 | "Number too big" | A result exceeds the maximum representable value and becomes inf. / 结果超过最大可表示值变为 inf。 |
| Underflow / 下溢 | "Number too small" | A result is closer to zero than the smallest representable positive number. / 结果比最小可表示正数更接近零。 |
| Log-sum-exp trick / Log-sum-exp 技巧 | "Subtract the max first" | Computing log(sum(exp(x))) by factoring out exp(max(x)). / 通过提取 exp(max(x)) 计算 log(sum(exp(x)))。 |
| Stable softmax / 稳定 softmax | "Softmax that does not explode" | Subtracting max(logits) before exponentiating. / 指数化前减去最大 logit。 |
| Gradient checking / 梯度检查 | "Verify your backprop" | Comparing analytical vs numerical gradients to catch bugs. / 比较解析和数值梯度以捕获 bug。 |
| Mixed precision / 混合精度 | "Float16 forward, float32 backward" | Using lower-precision for speed, higher-precision for accuracy. / 低精度加速，高精度保准确。 |
| Loss scaling / 损失缩放 | "Prevent gradient underflow" | Multiplying loss by a large constant to keep gradients in float16 range. / 将损失乘以大常数使梯度保持在 float16 范围内。 |
| bfloat16 | "Brain floating point" | Google's 16-bit format with 8 exponent bits. Preferred for training. / Google 的 16 位格式，8 位指数。训练首选。 |
| Gradient clipping / 梯度裁剪 | "Cap the gradient norm" | Scaling the gradient vector so its norm does not exceed a threshold. / 缩放梯度向量使范数不超过阈值。 |
| NaN | "Not a Number" | Special float value from undefined operations. Propagates through all arithmetic. / 未定义操作的特殊浮点值。通过所有算术传播。 |
| Inf | "Infinity" | Special float value from overflow or division by zero. / 溢出或除零产生的特殊浮点值。 |
| Numerical gradient / 数值梯度 | "Brute force derivative" | Approximating a derivative by evaluating f(x+h) and f(x-h). / 通过求 f(x+h) 和 f(x-h) 近似导数。 |

## المزيد من القراءة

- [What Every Computer Scientist Should Know About Floating-Point Arithmetic (Goldberg 1991)](https://docs.oracle.com/cd/E19957-01/806-3568/ncg_goldberg.html)-- المرجع النهائي
  浮点算术 صلاحيات المرجعية
- [Mixed Precision Training (Micikevicius et al., 2018)](https://arxiv.org/abs/1710.03740)-- ورقة NVIDIA حول استحداث خسائر
  NVIDIA 损失缩放论文
- [AMP: Automatic Mixed Precision (PyTorch docs)](https://pytorch.org/docs/stable/amp.html)-- دليل عملي
  بيتورش 混合精度实践指南
- [bfloat16 format (Google Cloud TPU docs)](https://cloud.google.com/tpu/docs/bfloat16)-- لماذا اختارت جوجل هذا النموذج
  Google 选择 bfloat16 لأسباب
- [Kahan Summation (Wikipedia)](https://en.wikipedia.org/wiki/Kahan_summation_algorithm)-- خوارزمية لتقليل خطأ التجاعيد
  减少舍进差的卡هان 求和算法
