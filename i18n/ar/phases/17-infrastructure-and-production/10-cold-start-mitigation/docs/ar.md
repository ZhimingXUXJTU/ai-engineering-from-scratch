# التخفيف من بدء البرد لدرجة الماجستير العليا بدون خادم

> تصورة نموذج 20 جيجابايت تستغرق 5-10 دقيقة (7 بي) إلى 20+ دقيقة (70 بي) للذهاب من البرد إلى الخدمة. في عالم حقيقي بدون خادم، هذا ليس إثارة، إنه انقطاع. تعمل التخفيفات في خمس طبقات: صور العقد المسبقة (Bottlerocket على AWS ، قوس حجم مزدوج) ، وتدفق النموذج (NVIDIA Run:ai Model Streamer ، الأصلي في vLLM) ، صور سريعة في ذاكرة GPU (مواقع التفتيش المودالية ، تغيير تشغيل أسرع بنحو 10 مرات) ، ومجمعات دافئة (`min_workers=1`), تحميل مستويات (أنابيب NVMe→DRAM→HBM من ServerlessLLM ، خفض التأخير 10-200x) ، والهجرة الحية التي تحرك رموز المدخل (KB) بدلا من كاش KV (GB). ينشر Modal 2-4s البرد بدايات كطابق؛ الباسيطان 5-10s افتراضي ، فرعية ثانية مع التدفئة المسبقة. هذه الدروس تعلمك قياس، ميزانية، وتجميع الطبقات الخمسة.

> **【中文解读】**هذا المقطع يعرض استراتيجيات التأجيل في الرد على التأجيل الأول


**Type:** Learn | **类型:** 学习
**Languages:** Python (stdlib, toy cold-start path simulator) | **语言:** Python
**Prerequisites:** Phase 17 · 02 (Inference Platform Economics), Phase 17 · 03 (GPU Autoscaling) | **前置知识:** Phase 17 · 02 (Inference Platform Economics), Phase 17 · 03 (GPU Autoscaling)

>  **【前置】**学本节前请先掌握:Phase 17·02(平台经济学)、Phase 17·03(GPU 扩缩) ・・・LLM بدون خادم 冷启动 = 5-20 分钟(不是热身,是停服) ・・・
>  **【类比】**冷启动缓解 = "汽车预热"──朴素加载 = 钥匙一从零启动(20 分钟);五层加速:(1) 预热节点镜像;(2) 模型流式加载;(3) GPU 内存快照(Modal 10 倍提速);(4) 暖池 min_workers=1;(5) 分层加载(ServerlessLLM NVMe→DRAM→HBM,10-200 倍延迟降低)──Modal实测 2-4 秒冷启动,Baseten 5-10 秒预热版亚秒)。
**Time:** ~60 minutes | **时间:** ~60 minutes

## أهداف التعلم

- قم بإدراج الطبقات الخمس للتخفيف من بدء البرد وسمي أداة أو نمط في كل طبقة.
  中文翻译:列举冷启动缓解的五层策略,并说出每层一个工具或模式──
- احسب الوقت الإجمالي للبدء البارد كجمع (إمدادات العقدة) + (وزن التنزيل) + (وزن الحمل إلى HBM) + (بدء المحرك) لنموذج 70B.
  中文翻译:计算 70B 模型总冷启动时间 = 节点供给 + 权重下载 + 权重加载到HBM + 引擎初始化──
- شرح لماذا تحويل الهجرة الحية توكنات المدخلات (KB) وليس KV cache (GB) وما هي العقوبة (إعادة الحساب).
  中文翻译:解释为什么实时迁移传输输入代币(KB) بدلا من KV 缓存(GB),以及代价是什么(重新计算)。
- أسمائ التداول في حوض الاستحمام الحر (دفع مقابل جهاز التشغيل المضغوط أو تقبل ذيل البدء البارد) والعتبة SLA التي `min_workers > 0`يصبح إلزاميًا.
  中文翻译:说出热池权衡 ((为空 GPU 付费或接受冷启动尾部),以及 `min_workers > 0`变为强制性的 SLA 值。

## المشكلة المشكلة المشكلة

> **【中文解读】**مشكلة بدء البرد في محطة الجامعة بدون خادم:70B 模型 from zero to service needs 3-8 分钟(节点供应 45-60s + 容器拉取 120-300s + 权重载 45-120s + 引擎初始化 10-30s) ، SLA 远超2s 的解决方案是保留热池(min_workers=1), ولكن هذا يعني 24/7 支付空 GPU 费用5 产品各保留 1 热副本,每月 3600 GPU-小时 无论是否有用户调──

> **【拓展：Serverless LLM 平台对比】**2026 سنة برنامج LLM بدون خادم منصة لبدء البرد: مودال 凭借 GPU 快照技术实现 2-4s بكرء البدء ؛ الصناعة أسرع ؛ البيستين 默认 5-10s ،预加热后可低于 1s ؛ AWS Lambda + 容器镜像通常 10-30s ؛ GCP Cloud Run + GPU 较新,冷启动约 15-30s;; بالنسبة إلى TTFT P99 < 60s 70B + 模型 ، الحرارة هي إلزامية لا توجد أي عملية تحسين البرد في 60s داخل الانتهاء من جميع العمليات.

إنّ محاميكِ بدون خادم يصلون إلى الصفر خلال الليل، في الثامنة صباحاً، تُصعد حركة المرور، ويتوقع الطلب الأول:

> 您的无服务器 LLM 端点在夜间缩容到零──早晨 8 点流量激增──第一个请求等:

1. كاربينتر يقدم عقدة GPU: 45-60s.
   中文翻译:كاربنتر 供给GPU 节点:45-60 秒──
2. الحاوية تسحب صورة 30 جيجا غابايت مع الوزن: 120-300s.
   中文翻译:容器拉取 30GB 的含权重镜像:120-300 秒──
3. يحمل المحرك الوزن إلى HBM: 45-120s اعتمادا على حجم النموذج وسرعة التخزين.
   ترجمة: المحرك سوف يقل وزنه إلى HBM:45-120 ثانية، يعتمد على حجم النموذج وسرعة التخزين
4. vLLM أو TRT-LLM يبدأ الرسوم البيانية CUDA، مجموعة cache KV، tokenizer: 10-30s.
   中文翻译:vLLM أو TRT-LLM 初始化 CUDA graph、KV 缓存池、分词器:10-30 秒──

إجمالي: 220-510s (حوالي 3-8 دقائق) قبل عودة رمز واحد. SLA الخاص بك هو 2s. أنت شحن حمام دافئ (`min_workers=1`و يبدو أن المشكلة تختفي  ولكن الآن تدفع مقابل GPU واحدة عديمة الفائدة 24x7. إذا كان لديك الخدمة 5 منتجات كل منها مع نسخة واحدة دافئة، وهذا هو 5 × 24 × 30 = 3600 ساعة GPU / شهر سواء كان مستخدم واحد أو لا.

> 总计:220-510 秒(约 3-8 分钟)才能返回一个代币――SLA الخاص بك هو 2 秒――你部署热池(`min_workers=1`) يبدو أن المشكلة قد اختفت ولكن الآن أنت 24/7 لخدمة GPU 付费

التخفيف من بدء البرد هو كيفية الحفاظ على اقتصاد الخادم بدون تقريب تأخير دائما على.

> التخفيف البارد هو الحفاظ على عدم وجود خدمة اقتصادية في الوقت نفسه تقترب من تأخير الخدمة الدائمة.

## المفهوم الأساسي

### الطبقة 1  صور العقدة المسبقة (Bottlerocket)

> **【中文解读】**الأول:  التشغيل المقبل  النقطة  الصورة  النظام التشغيلي والبيانات  الصورة  الصورة  الصورة  الصورة  الصورة  الصورة  الصورة  الصورة  الصورة  الصورة  الصورة  الصورة  الصورة  الصورة  الصورة  الصورة  الصورة  الصورة  الصورة  الصورة  الصورة  الصورة  الصورة  الصورة  الصورة  الصورة  الصورة  الصورة  الصورة  الصورة  الصورة  الصورة  الصورة  الصورة  الصورة  الصورة  الصورة  الصورة  الصورة  الصورة  الصورة  الصورة  الصورة  الصورة  الصورة  الصورة  الصورة  الصورة  الصورة  الصورة `EC2NodeClass`中引用快照 ID──新节点启动权重已在本地 NVMe 上消除镜像拉取步骤,为大型模型节省 2-4 分钟──GCP 和 Azure 有类似自定义 VM 镜像模式──

على AWS، تقوم بنية Bottlerocket ذات حجم مزدوج بتفصل نظام التشغيل عن البيانات. صور حجم البيانات مع صورة الحاوية الخاصة بك مسبقاً. راجع معرف الصورة الفورية في جهازك `EC2NodeClass`. تعبئة العقد الجديدة مع الأوزان بالفعل على NVMe المحلية  الخطوة 2 و جزء من 3 تختفي. يعمل مع Karpenter بشكل أصلي. توفير نموذجي: 2-4 دقيقة لكل بدء بارد للنماذج الكبيرة.

> على AWS، سيتم فصل النظام التشغيلي عن البيانات في إطار إطار إطار بوتلروكيت.`EC2NodeClass`中引用快照 ID──新节点启动时权重已在本地 NVMe 上步骤 2 和部分步骤 3 消除──原生与卡宾特 配合──典型节省:大型模型每次冷启动 2-4 分钟──

ما يعادله على GCP: صور VM مخصصة مع طبقات حاوية مخبأة مسبقا. على Azure: صور سريعة من القرص المدارة بنفس النمط.

> GCP 等价方案:预容器层的自定义 VM 镜像──Azure:托管磁盘快照加相同模式──

### الطبقة 2  نموذج التدفق (Run:ai Model Streamer)

> **【中文解读】**ثانوية نموذج تدفق تحميل.NVIDIA Run:ai Model Streamer لا تحتاج إلى كل الملفات تدفق تحميل.

بدلاً من تحميل الملف الكامل قبل الإجابة على الطلب الأول ، قم بتدفق الوزن إلى طبقة ذاكرة GPU طبقة بعد طبقة وبدء المعالجة بمجرد أن يكون أول كتلة محول مقيمة. يتم شحن NVIDIA Run:ai Model Streamer الأصلي في vLLM 2026. يعمل مع S3 ، GCS ، و NVMe المحلي. يقلل من وقت الحمل الوزن بنصف تقريبًا للنماذج الكبيرة عن طريق التداخل I / O مع إعداد الحساب.

> ليس على الإجابة على الطلب الأول قبل تحميل الملف الكامل ، ولكن سوف يتم تحميل الوزن بشكل متسلسل إلى خزنة GPU ، وبدء معالجة بعد الانتهاء من تحميل الكتل التحولية الأولى على الفور.

### الطبقة 3  صور لقطات ذاكرة GPU (Modal)

> **【中文解读】**الموديل في أول تحميل على حالة GPU (حيث يتم تحميل الرسم البياني لـ (كيف) ، ثم يتم إعادة تشغيلها مباشرة إلى HBM比 إعادة تشغيلها بسرعة 10x)). هذا هو التقنية القريبة من "2 ثانية إطلاق الحرارة لـ" GPU‬.

> **【拓展：冷启动优化策略叠加】**五层冷启动缓解可叠加使用:(1) 预播种镜像(消除镜像拉取) + (2) 模型流式加载(减半权重加载时间) + (3) GPU 快照(消除重重加载) + (4) 热池(避免冷启动) + (5) 分层加载(NVMe→DRAM→HBM) ∼全叠加可将70B 模型从 328s冷启降到约15s22x 改善。选择哪几层取决于SLA 严格程度和预算。

يقوم مودال بتفتيش حالة GPU (الوزن، الرسوم البيانية CUDA، منطقة cache KV) بعد الحمل الأول. تقوم عمليات إعادة تشغيل لاحقة بتحطيم المعلومات مباشرة إلى HBM  10x أسرع من إعادة تشغيل. هذا هو أقرب شيء لـ "تشغيل GPU دافئة في ثواني". التجارة: الصور الفورية هي لكل topology GPU، لذلك إذا قام Karpenter بتحريركك إلى SKU مختلف، تقوم بإعادة التفتيش.

> في الموديل في أول تحميل بعد وضع GPU  الوزن ✓ CUDA الرسم البياني ✓ KV 缓存区域) القيام بموجب نقطة الاختبار‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬

### الطبقة 4  حمامات دافئة (مئات العمال = 1)

> **【拓展：Serverless LLM 平台的冷启动对比】**2026 سنة لا يوجد محكم في مجال التعليمات العليا على الطاولة الباردة:Modal 以 GPU 快照技术实现 2-4s(قطاع الأعمال الأكثر سرعة) ؛Baseten 默认 5-10s,预加热后 <1s;AWS Lambda + 容器镜像通常 10-30s(不含模型加载);原始 70B 模型冷启动 3-8 分钟。Modal 的快照技术是关键差异它将 GPU 状态(权重 + CUDA graph + KV Cache 区域)序列化,重启直反序列化到 HBM,重启快 10x。代价是快照与 GPU 拓绑定,迁移到不同 SKU 需要重制快照──

أبسط التخفيف: إبقاء نسخة واحدة جاهزة دائما. التكلفة هي معدل ساعة واحد من GPU 24x7.$0.85-$1.50/ساعة لتجنب بدء بارد في 30s) ومهينة إلى الكبيرة (دفع 4 دولار / ساعة لتجنب بدء بارد في 5 دقائق). عتبة SLA حيث تصبح المسبحات الدافئة إلزامية: عادة TTFT P99 < 60s على نموذج 70B +.

> الحل الأسهل: الحفاظ على نسخة واحدة دائماً جاهزة. تكلفة GPU واحدة.$0.85-$1.50/小时 لتجنب 30 ثانية من البرد) ، والنموذج الكبير هو نسبياً صديقاً(付 $4/小时 لتجنب 5 دقائق من البرد)。 الحرارة تتغير إلى SLA القسرية 值: عادةً 70B + 模型 على TTFT P99 < 60 ثانية。

### الطبقة 5  التحميل المتعدد المستويات (LLM بدون خادم)

تعامل ServerlessLLM مع التخزين على أنه سلسة التسلسل: NVMe (سرعة ولكن كبيرة) ، DRAM (متوسطة ولكن مستوى) ، HBM (صغيرة ولكن فورية). يتم تحميل الوزن مسبقا إلى DRAM؛ تحميل على الطلب إلى HBM. تقارير ورقة تقرير 10-200x خفض التأخير على الأحمال الباردة مقابل البراغية القرص إلى HBM. تبني الإنتاج مبكر ولكن التكاملات مع vLLM موجودة.

> سوف تخزين لفترة من المدة إلى المدة: (NVMe(快但大)、DRAM(中等但分层)、HBM(小但即时)。权重预载到DRAM;按需加载到HBM。论文报告冷启动延迟降低 10-200 倍──生产采用早,但已经存在与vLLM的集成──

### الطبقة 6  الهجرة الحية (نمط مكافأة)

عندما تصبح العقدة غير متوفرة (إزالة النقاط ، تنفذ العقدة) ، فإن النمط التقليدي هو بدء نسخة أخرى باردة وتصفيف صف طلب. الهجرة الحية تنتقل رموز المدخل (كيلوبايت) إلى وجهة يتم تحميل النموذج وتقوم بإعادة حساب cache KV على وجهة النظر. تعد الحسابات الأخرى أرخص من نقل GB من cache KV عبر الشبكة. تطبق على التنفيذات المفصلة.

> عندما يُمكن استخدام النقطة عندما يُمكن استخدامها عندما يُمكن استخدام النقطة عندما يُمكن استخدام النقطة عندما يُمكن استخدام النقطة عندما يُمكن استخدام النقطة عندما يُمكن استخدام النقطة عندما يُمكن استخدام النقطة عندما يُمكن استخدام النقطة عندما يُمكن استخدام النقطة عندما يُمكن استخدام النقطة عندما يُمكن استخدام النقطة عندما يُمكن استخدام النقطة عندما يُمكن استخدام النقطة عندما يُمكن استخدام النقطة عندما يُمكن استخدام النقطة عندما يُمكن استخدام النقطة عندما يُمكن استخدام النقطة عندما يُمكن استخدام النقطة عندما يُمكن استخدام النقطة عندما يُمكن استخدام النقطة عندما يُمكن استخدام النقطة عندما يُمكن استخدام النقطة عندما يُمكن استخدام النقطة عندما يُمكن استخدام النقطة عندما يُمكن استخدام النقطة عندما يُمكن استخدام النقطة عندما يُمكن استخدامها عندما يُمكن استخدام النقطة عندما يُمكن استخدامها عندما يُمكن استخدام النقطة عندما يُمكن استخدامها عندما يُمكن استخدام النقطة عندما يُمكن استخدامها عندما يُمكن استخدام النقطة عندما يُمكن استخدامها.

### الرياضيات الحرارية

> **【中文解读】**热池数学: بالنسبة لـ P99 TTFT SLA 为 2s 的服务,问题不是"热池 نعم/لا" بل"多少热副本、哪些路径需要"──高价值交互路径(实时聊天、语音代理)→ min_workers=1-2;后台批处理路径(夜间分类)→规模到零可接受;高级层级 → 按租户专用热副本。简单算术:5 个产品每 1 热副本 = 5 × 24 × 30 = 3600 GPU-hours/月,无论是否有用户调用──

بالنسبة للخدمة مع P99 TTFT SLA من 2s، السؤال ليس "حوض دافئ نعم / لا" ولكن "كم نسخ دافئة، والمسارات التي تحصل عليها".

> بالنسبة لـ P99 TTFT SLA 为 2 秒的服务, المسألة ليست "المرحلة الحرارية نعم/لا" بل "كم عدد المرحلة الحرارية، أي طرق تحتاج إليها"

- مسارات تفاعلية ذات قيمة عالية (الرداد المباشر، وكيل الصوت): `min_workers=1-2`. . .
  中文翻译:高价值交互路径 ((实时聊天、语音代理):`min_workers=1-2`.
- مسارات البطاقات الخلفية (تصنيف ليلي): المقبولة من مقياس إلى الصفر، يمكن تحمل البدء البارد لمدة 5-10 دقائق.
  中文翻译:后台批处理路径(夜间分类): قبول缩容到零,5-10 分钟冷启动可容忍。
- مستوى المكافأة: `min_workers`لكل مستأجر لديه قدرة مخصصة.
  中文翻译:高级层级:按租户 `min_workers`专用容量──

### القياس قبل التحسين

> **【中文解读】**70B 模型冷启动解剖(示意数据):节点供应50s + 镜像拉取180s + 权重到HBM 75s + 引擎初始化20s + 首次前向3s = 总计 328s。全缓解后:预播种消除镜像拉取、模型流式加载减半权重加载、GPU 快照消除重复初始化 = 约15s 总冷启动(22x 降低)。

تشريح البدء البارد لنموذج 70B على عقدة جديدة (موضحية):

| Phase | Time | Mitigation |
|-------|------|-----------|
| Node provision | 50s | Bottlerocket + pre-seeded image, warm pool |
| Image pull | 180s | Pre-seeded data volume (eliminate) |
| Weights to HBM | 75s | Model streamer (halve); GPU snapshot (eliminate) |
| Engine init | 20s | Persistent CUDA graph cache |
| First forward | 3s | Min inherent latency |
| **Total cold** | **328s** | |
| **Total with mitigations** | **~15s** | 22x reduction |

### أرقام يجب أن تتذكر

- بدء البرد المتدري: 2-4 ثانية (مع صور GPU).
  中文翻译:مودال冷启动:2-4 秒(استعمل GPU 快照)
- الباسيتين افتراضي البرد: 5 إلى 10 ثانية؛ ثانوية دون التدفئة المسبقة.
  中文翻译:باسيطان 默认冷启动:5-10 秒;预加热后亚秒级。
- البداية الباردة 70B الخام: 3-8 دقائق.
  中文翻译:原始 70B 冷启动:3-8 分钟。
- أداء: أيزم: 2x زيادة في الوزن
  中文翻译:Run:ai نموذج الموسيقى:约 2 倍权重加载加速。
- تحميل مستويات غير خادم: 10-200x تخفيض تأخر (أرقام ورقة).
  中文翻译:ServerlessLLM 分层加载:10-200 倍延迟降低(论文数据) 』

## استخدمها في إطار التنفيذ
```figure
cold-start-pipeline
```

## استخدمها

`code/main.py`يُظهر مسار بدء البرد مع كل تخفيف وبدون ذلك. يُذكر إجمالي وقت بدء البرد، وتكلفة المسبح الحر، ومعدل طلبات التكامل الذي يزيد عن المسبح الحر على نفسه.

> `code/main.py`建模有/无每种缓解的冷启动路径──报告总冷启动时间、热池成本和热池自付自足的亏平衡请求率──

## أرسلها .

هذا الدرس يُنتج`outputs/skill-cold-start-planner.md`. بالنظر إلى SLA، حجم النموذج، وشكل حركة المرور، يختار ما التخفيفات التي يجب أن تتراكم.

> 本课产出 `outputs/skill-cold-start-planner.md` إعطاء SLA  نموذج حجم وتحركات الشكل، اختيار ما فوقها استراتيجيات التخفيف

## تمارين التدريب

1. أركض`code/main.py`- حساب معدل طلبات التكامل فوقها نسخة دافئة أرخص من دفع ضريبة البدء الباردة عن طريق انخفاض طلبات إضافية في SLO.
   中文翻译:运行 `code/main.py` حسابات الحرارة النسخة عن طريق SLO أسعار التكلفة المطلوبة للانفصال عن دفع ضريبة البدء الباردة أرخص  معدل التكلفة المطلوبة 
2. تقوم بتنفيذ نموذج 13B مع P99 TTFT SLA من 3s. اختر الحد الأدنى من مكافحة (أقل طبقات) التي تحقق ذلك.
   中文翻译:你部署一个13B 模型,P99 TTFT SLA 为 3 秒──选择实现其最小缓解(最少层级)──
3. يزيل التزاوج المسبق لقطة الزجاجات التقطير الصورة ولكن الأوزان لا تزال تحميل من اللقطة إلى HBM. احسب ساعة الجدار لنموذج 70B إذا كانت NVMe المدعومة لقطة الفور تقرأ عند 7 جيجابايت / ثانية.
   اللغة الصينية ترجمة:Bottlerocket 预播种消除镜像拉取, ولكن الوزن لا يزال بحاجة إلى من شاشة照加载 إلى HBM。 حسابات سريعة照支持 NVMe 以 7 GB/s 读取时 70B 模型的实际耗时。
4. يقدم مزودك بدون خادم لقطات اللقطات GPU (مودال) ويمنع فريقك لأن "التقطات اللقطة تسرب PII".
   中文翻译:你的无服务器提供 GPU 快照(Modal), ولكن رفض الفريق بسبب "快照泄露 PII"──辩论双方实际风险是什么,缓解方案是什么(临时快照、加密、命名空间隔离)?
5. صمم سياسة حرارة المسبح المرتبة: كم عدد النسخ الدافئة للمستخدمين المدفوعين، والمستخدمين التجريبية، والحملات المكونة من العمل؟
   中文翻译:设计分层热池策略:付费用户、试用用户和批处理工作负载各多少热副本?展示计算──

## شروط الرئيسية

| Term | What people say | What it actually means |
|------|----------------|------------------------|
| Cold start | "the big pause" | Time from request to first token on a fresh replica |
| Warm pool | "always-on minimum" | `min_workers >= 1` to keep at least one replica ready |
| Pre-seeded image | "baked AMI" | Node image with container weights pre-resident |
| Bottlerocket | "AWS node OS" | AWS container-optimized OS with dual-volume snapshot support |
| Model streamer | "streaming load" | Overlap weights I/O with compute setup |
| GPU snapshot | "checkpoint to HBM" | Serialize post-load GPU state; deserialize on restart |
| Tiered loading | "NVMe + DRAM + HBM" | Hierarchy of storage tiers; load on demand |
| Live migration | "move tokens" | Transfer input (KB), recompute KV on destination |
| `min_workers` | "warm replicas" | Serverless minimum keep-alive count |
| Scale-to-zero | "full serverless" | No cost when idle; accept full cold-start tax |

## المزيد من القراءة

- [Modal — Cold start performance](https://modal.com/docs/guide/cold-start) مقاييس ومعمارة نقاط التفتيش التي نشرتها مودال.
- [AWS Bottlerocket](https://github.com/bottlerocket-os/bottlerocket) نمط تصوير مفاجئ حجم البيانات المسبقة.
- [NVIDIA Run:ai Model Streamer](https://github.com/run-ai/runai-model-streamer) تعبئة الوزن المتداخل مع إعداد الحساب.
- [Baseten — Cold-start mitigation](https://www.baseten.co/blog/cold-start-mitigation/) كتاب لعبة التدفئة المسبقة
- [ServerlessLLM paper (USENIX OSDI'24)](https://www.usenix.org/conference/osdi24/presentation/fu) تصميم الشحن المرتبط.
- [NVIDIA — Disaggregated LLM Inference on Kubernetes](https://developer.nvidia.com/blog/deploying-disaggregated-llm-inference-workloads-on-kubernetes/) الهجرة الحية لتنفيذات منفصلة.
