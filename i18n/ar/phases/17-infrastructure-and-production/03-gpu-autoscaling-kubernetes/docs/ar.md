# GPU التوسيع الذاتي على Kubernetes  كاربينتر، KAI المخطط، المخطط العصابات ‬

> ثلاث طبقات، وليس واحدة. كربنتر إمدادات العقد الديناميكية (أقل من دقيقة واحدة، 40٪ أسرع من Cluster Autoscaler). يقوم KAI Scheduler بتنظيم المجموعات، وعلم التوبولوجيا، والصفوف الهرمية  يمنع فخ التخصيص الجزئي 7 من 8 حيث تنتظر سبعة عقدات وتحرق على GPU واحدة مفقودة. مقياسات ذاتية على مستوى التطبيق (NVIDIA Dynamo Planner ، llm-d Workload Variant Autoscaler) على إشارات محددة للإستنتاج  عمق الصف ، استخدام cache KV  ليس دورة عمل CPU / DCGM. فخ الكلاسيكي من " HPA " هو ذلك`DCGM_FI_DEV_GPU_UTIL`هو قياس دورة العمل: يمكن أن يكون 100% 10 طلبات أو 100. vLLM يخصص مسبقا ذاكرة cache KV ، لذلك لا يسبب الذاكرة أبداً التنظيم. هذه الدروس تعلمك لتكوين الطبقات الثلاث وتجنب Karpenter الافتراضي `WhenEmptyOrUnderutilized`سياسة التي تنهي تشغيل وظائف الجيبو في منتصف الإستثناء

> **【中文解读】**هذا القسم يعرض على GPU التوسع الذاتي Kubernetes 上 LLM 推理服务的 GPU 资源自动扩展策略──
**Type:** Learn
**Languages:** Python (stdlib, toy queue-depth autoscaler simulator)
**Prerequisites:** Phase 17 · 02 (Inference Platform Economics), Phase 17 · 04 (Serving Engine Internals)
**Time:** ~75 minutes


**Type:** Learn | **类型:** 学习
**Languages:** Python (stdlib, toy queue-depth autoscaler simulator) | **语言:** Python（标准库，队列深度自动扩缩模拟器）
**Prerequisites:** Phase 17 · 02 (Inference Platform Economics), Phase 17 · 04 (vLLM Serving Internals) | **前置知识:** Phase 17 · 02（推理平台经济学）, Phase 17 · 04（vLLM 服务内部）

>  **【前置】**学本节前请先掌握:Phase 17·02(平台经济学)、Phase 17·04(vLLM)、Kubernetes 基础──三层扩缩:Karpenter(节点层) + KAI Scheduler(Pod 层帮规划) + 应用层(队列深度/KV利用率)。
>  **【类比】**GPU 扩缩 = "餐厅运力调度"──Karpenter = 开新店(分钟级);KAI = 桌位组合(帮安排 防 7/8 部分分配,7 桌等 1 桌);应用层 = 服务员按等位队列长度调座。HPA 陷:DCGM 利用率是占空比,100% 可能是 10 个或 100 个请求必须使用 Goodput(Phase 17·08) 替代──

## أهداف التعلم

- رسم الرسم البياني لثلاث طبقات التوسع الذاتي (توفير العقد، وتخطيط العصابات، ومستوى التطبيق) وسمية الأداة المستخدمة في كل طبقة.
  中文翻译: رسم ثلاث طبقات تخصيصات التوسع الذاتي
- اشرح لماذا`DCGM_FI_DEV_GPU_UTIL`هو إشارة HPA الخطأ لـ vLLM وتسمية اثنين من البدائل (عمق الصف ، استخدام cache KV).
  中文翻译: شرح لماذا`DCGM_FI_DEV_GPU_UTIL`هو vLLM  خطأ HPA 信号,并说出两个 بديل
- وصف جدول المجموعة والوضع الفشل في التخصيص الجزئي الذي يمنعه KAI Scheduler (7 من أصل 8 GPUs idle).
  中文翻译:描述帮 调度和 KAI Scheduler 防止的部分分配故障模式(8 个GPU 中 7 个空) 
- أسم سياسة تكامل كاربينتر (`WhenEmptyOrUnderutilized`) التي تنهي تشغيل وظائف الجيبو وتحدد البديل الآمن لعام 2026.
  中文翻译:说出终止正在运行GPU 任务的卡宾特 合并策略(`WhenEmptyOrUnderutilized`),并说明2026 سنة الإستبدال الأمني.

## المشكلة المشكلة المشكلة

> **【中文解读】**يحتوي GPU على ثلاثة مستويات من أشكال الاختلاف: 1) HPA استخدام إشارة خاطئة (GPU 占用率而非队列深度) ، مما يؤدي إلى هذا التوسع غير الموسع ((2) Cluster Autoscaler 节点供应太慢,长提示请求超时 ((3) 多 GPU 分布式推理时部分分配 ((7-من-8 فخ) ،7 GPU 空转等第 8 个.

> **【拓展：GPU 集群管理】**2026 سنة كوبرنيتس أصبحت منصة إعداد قياسية لخدمات التقدير LLM. NVIDIA DGX Cloud. Google GKE. AWS EKS. توفر GPU.

فريقك يقوم بتقديم خدمة لـ"محققين في القانون" على "كوبرنيتس"`DCGM_FI_DEV_GPU_UTIL`إشارة. أدوات الخدمة عند استخدام 100٪ خلال ساعات العمل. HPA لا تزيد أبدا  انها تعتقد بالفعل أنك مليئة. تضيف نسخة يدويا؛ TTFT ينخفض. HPA لا يزال لا يصل إلى نطاق. الإشارة تكذب عليك.

> فريقك في كوبرنيتس قام بتنفيذ خدمة الجامعة`DCGM_FI_DEV_GPU_UTIL`作为信号设置了HPA──服务在业务时段保持在100%利用率──HPA 从不扩容它认为你已经满满了──你手动添加一副本;TTFT 下降──HPA 仍然不扩容──信号在欺骗你──

بشكل منفصل، تستخدم Cluster Autoscaler للعقدات. يصل طلب 1M-token في الساعة 2 صباحاً؛ يقضي الكluster 3 دقائق في تزويد العقدة، وتتوقف أوقات الطلب.

> من ناحية أخرى، تستخدم Cluster Autoscaler 管理节点──凌晨2点来一个M符号的提示;集群花 3分钟供给节点,请求超时──

مرة أخرى بشكل منفصل، تقوم بتنفيذ نموذج 70B يتطلب 8 GPU عبر 2 عقدات. المجموعة لديها 7 GPU مجانية و 1 متوزعة على 3 عقدات. Cluster Autoscaler يوفر عقدة لـ 1 GPU المفقودة. سبعة عقدات تنتظر 4 دقائق حرق المال بينما Kubernetes الحصول على آخر GPU.

> مرة أخرى، قمت بتنفيذ نموذج 70B يتطلب 2 نقاط 8 GPUs.

ثلاث طبقات، ثلاثة أوضاع فشل مختلفة. التوسع الذاتي المعرف على GPU في عام 2026 ليس "تشغيل HPA". إنه يضم إمدادات العقد، وتخطيط العصابات، وتوسع إشارات التطبيق.

> ثلاث طبقات، ثلاث أنواع مختلفة من الاختلالات. في عام 2026، فإن GPU تسيير التشغيل الذاتي ليس "فتح HPA".

## المفهوم الأساسي

### الطبقة 1  توفير العقدة (Karpenter)

> **【中文解读】**الطابق الأول هو تزويد القطاعات. القرص الرئيسي: القرص الرئيسي: القرص الرئيسي: القرص الرئيسي: القرص الرئيسي: القرص الرئيسي: القرص الرئيسي: القرص الرئيسي: القرص الرئيسي: القرص الرئيسي: القرص الرئيسي: القرص الرئيسي: القرص الرئيسي: القرص الرئيسي: القرص الرئيسي: القرص الرئيسي: القرص الرئيسي: القرص الرئيسي: القرص الرئيسي: القرص الرئيسي: القرص الرئيسي: القرص الرئيسي: القرص الرئيسي: القرص الرئيسي: القرص الرئيسي: القرص الرئيسي: القرص الرئيسي: القرص الرئيسي: القرص الرئيسي: القرص الرئيسي: القرص الرئيسي: القرص الرئيسي: القرص الرئيسي: القرصصص الرئيسي: القررررر: القررررررررررر: القررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررررر`WhenEmptyOrUnderutilized`合并策略它会终止运行推理的GPU节点转移到更便宜的实例类型,导致请求失败和模型重新加载(5-20分钟中断)。GPU池应使用 `WhenEmpty`+ `consolidateAfter: 1h`استراتيجية الأمن

كاربينتر يراقب الحوافز المنتظرة والعقدات الإمدادية في غضون 45-60 ثانية (تستغرق Cluster Autoscaler عادةً 90-120 ثانية بالنسبة لعقدات GPU).`NodePool`القيود  إذا كان الحجر يحتاج إلى 8 H100s والعنقود لا يمتلك عقدة مطابقة، كاربينتر توفر واحد مباشرة بدلا من تحسين مجموعة موجودة.

> كاربينتر  مراقبة انتظار ضبط Pod، في حوالي 45-60 ثانية لتزويد القاعدة `NodePool`约束动态选择实例类型 إذا كان Pod 需要 8 H100 且集群没有匹配节点,Karpenter 直接供应一个,而不是扩展现有组──

**The consolidation trap**: كاربينتر الاختلالات `consolidationPolicy: WhenEmptyOrUnderutilized`هو خطير لمجمعات GPU. سوف ينهي عقدة GPU قيد التشغيل لتحويل القنابل إلى مثال أرخص من الحجم الصحيح. لتحديد تحميلات العمل التي تعني إخلاء طلبات تشغيل وإعادة تحميل نموذج 70B على العقدة الجديدة. الخسارة هي دقائق من القدرة بالإضافة إلى فشل طلب.

> **合并陷阱**: كاربينتر 默认`consolidationPolicy: WhenEmptyOrUnderutilized`بالنسبة لـ GPU 池很危险──它会终止正在运行的 GPU 节点,将Pod 迁移到更便宜的合适实例──对于推理工作负载,这意味着驱逐运行中的请求,并重新加载70B 模型在新节点──损失是数分钟的容量加上请求失败──

إعداد آمن لمجمعات GPU:

```yaml
disruption:
  consolidationPolicy: WhenEmpty
  consolidateAfter: 1h
```

يسمح لكربنتر بتجميع العقد الفارغة حقا بعد ساعة ولكن لا تطرد أبدا وظيفة جارية.

> 让卡宾تر在一小时后合并真正空的节点,但永远不驱逐运行中的任务──

### الطبقة 2  تخطيط العصابات (KAI Scheduler)

> **【中文解读】**الطبقة الثانية هي تنظيم المخططات. المخطط الكاي  حل القيود الافتراضية كوب-مخططات  غير قابلة للتعامل مع ثلاثة مشاكل: 1) المخططات الجانبية  كاملة بدون تنظيم، 8-GPU  التفكير أو كل الإطلاق أو كل الانتظار؛ 2) التو التعرف  حسب NVLink / InfiniBand /机架拓放置 Pod؛ 3) التنافس على مجموعة من الفريقات  العديد من الفريقات نفس GPU 池时按优先级和配额管理.

> **【拓展：GPU 调度器生态】**2026 عام خيارات GPU 调度器 تتضمن KAI Scheduler(أصل Karp، دعم عصابة + أوضاع + صف) 、يونيكورن(Apache 项目, دعم صف و استغلال) 、 وكذلك المخطط المضبط kube-scheduler + 设备插件。KAI Scheduler هو الحل الوحيد المضمون إلى نظام التخطيط العصابات، وقد تم تصميمها بواسطة Ray 和 vLLM إنتاج-حجمة 集集集──

يقوم KAI Scheduler (المشروع "Karp" الذي تم تسميته بعد ذلك) بمعالجة ما لا يفعله المخطط الكوبي المعياري الافتراضي:

**Gang scheduling** التخطيط كل أو لا شيء. خلية استنتاج مُوزعة تتطلب 8 GPUs إما أن تبدأ كل 8 معاً أو لا تفعل أي شيء. بدون هذا، تحصل على فخ جزئي التخصيص: 7 من 8 خليط تبدأ، تنتظر إلى أجل غير مسمى، تحرق المال.

**Topology awareness** معرفة أي GPUات تشارك NVLink ، والتي تجلس على نفس الرف ، والتي لديها InfiniBand بينها. ضع القنابل وفقا لذلك. يجب أن يبقى عبء عمل متوازي التنسور DeepSeek-V3 67B على نطاق NVLink واحد. يحترم KAI Scheduler ذلك.

**Hierarchical queues** تتنافس فرق متعددة عن نفس مجموعة GPU مع الأولوية والحصة. يتم تخطي مصاصة الإنتاج لفريق A من قبل وظيفة تدريب الفريق B فقط إذا سمحت قواعد الأولوية.

يتم نشر KAI إلى جانب المخطط kube كمناسب ثانوي؛ تقوم بتعليف أحمال العمل لاستخدامها. يدمج Ray و vLLM stack production.

> كاي كعدد ثانوي للمعدل ومخطط للمجدد تم تعيينها ؛ تم تمكنت من استخدامها على الحمل المالي.

### الطبقة 3  إشارات مستوى التطبيق

> **【中文解读】**الطبقة الثالثة هي التطبيقات التجارية.`DCGM_FI_DEV_GPU_UTIL`يعد معدل استغلال GPU  دورة الوظيفة) مؤشر100% قد يعني 10 个请求 أو 100 个请求، لأن GPU 都在忙碌──vLLM 预分配 KV Cache 内存، حتى لو كان هناك طلب واحد، استخدام الذاكرة يقترب من 90٪، مما يؤدي إلى HPA القائم على الذاكرة 永远不会缩容──2026 سنة صحيحة التوسع الإشارة يجب أن تكون عمق صف صف、KV Cache استغلال率、每副本 P99 TTFT 和 Goodput──

> **【拓展：推理感知自动扩缩】**NVIDIA Dynamo Planner 和 llm-d Workload Variant Autoscaler هو جهاز توسيع متخصص في تصميم التفكير في مجال الماجستير في عام 2026. فإنها تستهلك مباشرة مؤشرات داخلية لمحرك التفكير.

**The HPA trap**: `DCGM_FI_DEV_GPU_UTIL`هو مقياس دورة العمل  يقيس ما إذا كانت جهاز المعالجة المعالجة المعالجة يعمل في كل فترة أخذ العينات. يمكن أن يعني الاستخدام بنسبة 100٪ 10 طلبات متزايدة أو 100؛ كان جهاز المعالجة المعالجة المعالجة المعالجة مشغولا في كلتا الحالتين. يتم قياس الحجم على دورة العمل بشكل أعمى.

أسوأ من ذلك، فإن محركات vLLM ومحركات مماثلة تقوم بتخصيص ذاكرة cache KV مسبقاً (حتى `--gpu-memory-utilization`يظل استخدام الذاكرة قريبًا من 90٪ حتى عند طلب واحد.

**2026 replacement signals**:

- عمق الصف (عدد الطلبات التي تنتظر الوفاء مسبقًا).
  中文翻译: قطار طولة ((انتظر عدد الطلبات المملوء)
- استخدام cache KV (ما هو جزء من الكتل المخصصة لترتيبات نشطة).
  中文翻译:KV 缓存利用率(توزيع إلى النسبة الكلي من سلسلة نشطة)。
- كل نسخة P99 TTFT (إشارة SLA الخاص بك).
  中文翻译:每副本 P99 TTFT(你的 SLA 信号)
- قوة الإنتاج (طلبات تلبية جميع المواقع المتداولة في الثانية).
  中文翻译:Goodput ((每秒满足所有SLO 的请求数) ⋅

تستخدم NVIDIA Dynamo Planner و llm-d Workload Variant Autoscaler هذه الإشارات ونسخ النسخ. أنها تحل محل HPA بالكامل لخدمة LLM.

> NVIDIA Dynamo Planner 和 llm-d Variant Autoscaler 消费这些信号并扩缩副本──它们完全取代ت HPA في خدمات LLM──

### متى تستخدم ما

| Scale decision / 扩缩决策 | Tool / 工具 |
|----------------|------|
| Add/remove nodes / 添加/移除节点 | Karpenter |
| Schedule multi-GPU jobs / 调度多 GPU 任务 | KAI Scheduler |
| Add/remove replicas / 添加/移除副本 | Dynamo Planner / llm-d WVA (or custom HPA on queue depth) |
| Choose GPU type / 选择 GPU 类型 | Karpenter NodePool |
| Preempt low-priority / 抢占低优先级 | KAI Scheduler queues |

> **【拓展：GPU 集群成本优化策略】**وتشمل استراتيجيات رئيسية لـ GPU 集群成本 في عام 2026: 1) مثالات GPU Spot InstanceAWS/GCP/Azure Spot  يمكن توفير 60-70٪ ، ولكن تحتاج إلى معالجة وقفة(كاربنتر + 热池缓解);(2) التوسع الذاتي كاربنتر في فترة غير مرتفعة                                                                                                                                                                                                                     

### إزالة الملفات المُزقة تعقد كل شيء

> **【中文解读】**تمت زيادة تعقيدات التوسع في المرحلة 17·17): تم زيادة تعقيدات التوسع في المرحلة 17·17.

> **【拓展：Kubernetes GPU 生态】**2026 سنة تشمل:NVIDIA GPU Operator ((تثبيت محركات التشغيل الذاتي / CUDA / حاوية أدوات) ✓NVIDIA Device Plugin ((GPU  الموارد العثور على وتوزيع) ✓MIG(GPU متعدد الحالات ، سوف تقطع إلى العديد من الحالات) ✓ وقت ✓GPU 共享) ✓结合 Karpenter + KAI Scheduler + Dynamo Planner ، يمكن تحقيق من الحد إلى Pod 调度 إلى副本扩展完整 GPU ✓

إذا قمت بتشغيل تعريفات المقبلات/تفكيك المفصلة (المرحلة 17 · 17) ، لديك فئتين من فئتين القنبلة مع محفزات مختلفة للتوسع: مقياس القنبلة المقبلات على عمق الصفوف، مقياس القنبلة المعدة على ضغط الاحتفاظ KV.`Services`مع HPA لكل دور. لا تحاول وضع HPA واحد أمام كليهما.

> إذا كنت تعمل على الانفصال المفصلة قبل الشحن / فك الحفرة (مرحلة 17 · 17) ، لديك اثنين من نوعين من الحفرة التي لديها مختلف التوسع المحركات: قبل الشحن الحفرة حسب عمق التوسع، فك الحفرة الحفرة حسب KV ضغط الاحتفاظ وتوسعها.`Services`كل دور لديه HPA الخاص به لا تحاول وضع HPA واحد في المواجهة بينهما

### البدء البارد مهم هنا أيضاً

تخفيف بدء البرد (مرحلة 17 · 10) هو حيث يصبح وقت إمدادات العقد مرئيًا للمستخدم. استحرار كاربينتر 45-60 ثانية بالإضافة إلى تحميل نموذج 20 جيجابايت بالإضافة إلى إنيت المحرك يعني طلب من الصفر يستغرق 2-5 دقائق.`min_workers=1`) للطرق الحرجة لـ SLO، أو استخدام التفتيش على شكل Modal في طبقة التطبيق.

> التشغيل البارد • المرحلة 17 · 10) هو نقطة إمداد الوقت يصبح المستخدم قابل للتعرف على مكانها.`min_workers=1`), أو في التطبيقات استخدام الوضع الوسيط 风格的检查点──

### أرقام يجب أن تتذكر

- إمدادات عقدة كاربينتر: ~ 45-60s مقابل Cluster Autoscaler ~ 90-120s (عقدات GPU).
  中文翻译:خزينة 节点供给:约45-60 秒 مقابل كلاستر اوتوسكالر 约90-120 秒(GPU 节点) ⋅
- المخطط KAI يمنع فخ النفايات القصرية
  中文翻译:KAI المخطط 防止部分分配浪费7 من 8 陷。
- `DCGM_FI_DEV_GPU_UTIL`كإشارة HPA: مكسورة؛ استخدم عمق الصف أو استخدام KV.
  中文翻译:`DCGM_FI_DEV_GPU_UTIL`作为 HPA 信号:有缺陷; استخدام قوة التسلسل أو استخدام KV 率。
- كاربنتر `WhenEmptyOrUnderutilized`: ينهي تشغيل وظائف الجيبو`WhenEmpty + consolidateAfter: 1h`للاستنتاج
  中文翻译:كاربنتر `WhenEmptyOrUnderutilized`:终止运行中的 GPU 任务──推理使用 `WhenEmpty + consolidateAfter: 1h`.

## استخدمها في إطار التنفيذ

> **【拓展：GPU 自动扩缩成本模型】**محور تكلفة تحسين GPU التوسع الذاتي هو تقليل الوقت المكلف لتحويل المعلومات.$3/hr）为例，8-GPU 集群 24/7 运行每月成本约 $17,280── من خلال كاربينتر 按需供应 + `WhenEmpty`合并策略 + 推理感知 HPA،可在非高峰时段自动缩放容量到2GPU,将月成本降低至约8,640$ (省 50%) .
```figure
autoscaling
```

## استخدمها

`code/main.py`يحاكي محاكاة ثلاث طبقات على عبء عمل GPU منفجر. يقارن HPA الباهض (دورة العمل) ، HPA عمق الصف ، و KAI-غند المخطط للمحاسبة. يبلغ عن طلبات غير متوفرة ، دقائق GPU عديمة الفائدة ، والنتيجة المركبة.

> `code/main.py`في突发 GPU 工作负载上模拟三层自动扩缩机──比较简单 HPA(占用率) 队列深度 HPA 和 KAI 调度扩缩──报告未满足的请求数、空 GPU 分钟数和综合评分分──

## أرسلها .

هذا الدرس يُنتج`outputs/skill-gpu-autoscaler-plan.md`نظراً لتوبولوجيا المجموعة وشكل الحملة والحجم المحدد، فإنه يصمم خطة قياس السيارات ثلاث طبقات.

> 本课产出 `outputs/skill-gpu-autoscaler-plan.md` وضع مجموعة التوسع والشكل والحمل المحدد، تصميم خطة التوسع الذاتي ثلاثية المستويات

## تمارين التدريب

1. أركض`code/main.py`تحت عبء عمل متفرق، كم من الطلبات التي يلقي بها HPA في دورة العمل الباهية التي تلتقط HPA في عمق الصف؟ من أين يأتي الفرق؟
   中文翻译:运行 `code/main.py`◊ في ظل الحمل المفاجئ للعمل، فقدت معدلات الاستهلاك البسيطة لـ HPA كم عدد الطلبات التي تم القبض عليها من خلال صف عمق HPA؟
2. تصميم كارتبينتر NodePool لمجموعة تخدم Llama 3.3 70B FP8 على H100 SXM5. تحديد `capacity-type`،`disruption.consolidationPolicy`،`consolidateAfter`و تلوث يبقي عبء العمل غير من GPU بعيدا عن هذه العقد
   中文翻译:为在H100 SXM5 上服务 لاما 3.3 70B FP8 的集群设计 كاربنتر NodePool──指定 `capacity-type`.`disruption.consolidationPolicy`.`consolidateAfter`和将非 GPU 工作负载隔离的污点──
3. فريقك يبلغ أن التنفيذات عالقة في انتظار لأن "GPU متاحة ولكن القنبلة لن يجدد. " التشخيص  هل هذا كاربينتر، كوب-جدول، أو KAI جدول؟ أي المقاييس تؤكد؟
   中文翻译:你的团队报告部署卡在等待状态因为"GPU可用但Pod 无法调度"──诊断是卡宾特、立方时间表还是KAI 时间表?
4. اختر إشارة إلى غلافات التملأ المفصلة ذاتية القياس والإشارة المختلفة لغلافات فك الشفرة، تبرير كليهما
   中文翻译:选择一个信号来扩缩分离式预填充 Pod,另一个信号用于解码 Pod──为两者提供理由──
5. حساب تكلفة `WhenEmptyOrUnderutilized`فخّ التوحيد على خدمة إنتاج 24 × 7 التي تتوسط 60 حدثًا في اليوم يُسقط الطلبات عند P99 TTFT > 10s.
   中文翻译:计算 `WhenEmptyOrUnderutilized`合并陷在24x7生产服务上的成本,该服务平均每天 60 مرة طلبات ضائع事件,P99 TTFT > 10 秒──

## شروط الرئيسية

| Term / 术语 | What people say / 通俗说法 | What it actually means / 实际含义 |
|------|----------------|------------------------|
| Karpenter | "the node provisioner" / "节点供给器" | Kubernetes node autoscaler; sub-minute provisioning / Kubernetes 节点自动扩缩器；亚分钟级供给 |
| Cluster Autoscaler | "the old scaler" / "旧扩缩器" | Kubernetes node autoscaler predecessor; slower, group-based / K8s 节点扩缩前身；更慢，基于组 |
| KAI Scheduler | "the GPU scheduler" / "GPU 调度器" | Secondary scheduler for gang + topology + queues / 用于 gang + 拓扑 + 队列的二级调度器 |
| Gang scheduling | "all or nothing" / "全有全无" | Schedule N pods atomically or defer all of them / 原子调度 N 个 Pod 或全部推迟 |
| Topology awareness | "rack-aware" / "机架感知" | Place pods based on NVLink/IB/rack placement / 基于 NVLink/IB/机架放置 Pod |
| `DCGM_FI_DEV_GPU_UTIL` | "GPU utilization" / "GPU 利用率" | Duty-cycle metric; NOT a scaling signal for LLMs / 占用率指标；不是 LLM 的扩缩信号 |
| Queue depth | "waiting requests" / "等待请求" | Correct HPA signal for prefill-bound scaling / 预填充扩缩的正确 HPA 信号 |
| KV cache utilization | "memory pressure" / "内存压力" | Correct HPA signal for decode-bound scaling / 解码扩缩的正确 HPA 信号 |
| Consolidation | "Karpenter consolidation" / "Karpenter 合并" | Node termination to cheaper instance type / 终止节点迁移到更便宜实例 |
| `WhenEmpty + 1h` | "safe consolidation" / "安全合并" | Policy that doesn't evict running GPU jobs / 不驱逐运行中 GPU 任务的政策 |

## المزيد من القراءة

- [KAI Scheduler GitHub](https://github.com/kai-scheduler/KAI-Scheduler) وثائق التصميم ومثلة على التكوين.
- [Karpenter Disruption Controls](https://karpenter.sh/docs/concepts/disruption/) سياسة تعزيز النطقية والإغراض غير الآمنة مع GPU.
- [NVIDIA — Disaggregated LLM Inference on Kubernetes](https://developer.nvidia.com/blog/deploying-disaggregated-llm-inference-workloads-on-kubernetes/) دينامو خطط التوسع الإشارات.
- [Ray docs — KAI Scheduler for RayClusters](https://docs.ray.io/en/latest/cluster/kubernetes/k8s-ecosystem/kai-scheduler.html)نمط تكامل الأشعة
- [AWS EKS Compute and Autoscaling Best Practices](https://docs.aws.amazon.com/eks/latest/best-practices/aiml-compute.html) إدارة الإرشادات الخاصة بـ "كوبرنيتس".
- [llm-d GitHub](https://github.com/llm-d/llm-d) تصميم متغيرات الحمل العامل
