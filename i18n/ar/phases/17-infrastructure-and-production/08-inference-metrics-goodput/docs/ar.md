# مقاييس الإستدلال  TTFT، TPOT، ITL، Goodput، P99 推理指标 Goodput

> أربعة مقاييس تحدد ما إذا كان نشر استنتاج يعمل. TTFT هو المكملة المسبقة بالإضافة إلى الصف بالإضافة إلى الشبكة. TPOT (مُساوية ITL) هو تكلفة فك رموز الحدود في الذاكرة لكل رمز. التأخير من نهاية إلى نهاية هو TTFT زائد TPOT مرات طول الخروج. إنّ التوصيل هو الرموز لكل ثانية المجمعة في جميع أنحاء الأسطول. لكن ما يهم للمنتج هو الجيدبوت  الجزء من الطلبات التي استوفت كل SLO في وقت واحد. إنّ التوصيل العالي عند التوصيل الخير منخفض يعني أنك تقوم بمعالجة رموز لا تصل أبداً إلى المستخدمين في الوقت المحدد. أرقام مرجعية للاما-3.1-8B-إدريس على TRT-LLM في عام 2026: متوسط TTFT 162 ms، متوسط TPOT 7.33 ms، متوسط E2E 1.093 ms. دائماً أبلغ عن P50، P90, P99  أبداً فقط يعني. وراقب فخ القياس: GenAI-Perf يستبعد TTFT من حساب ITL، LLMPerf يضم ذلك؛ اثنين من الأدوات تختلف على TPOT لنفس الجولة.

> **【中文解读】**هذا المقطع يعرض مؤشر التقييم و Goodput  قياس LLM  تقييم نوعية الخدمات
**Type:** Learn
**Languages:** Python (stdlib, toy percentile calculator and goodput reporter)
**Prerequisites:** Phase 17 · 04 (Serving Engine Internals)
**Time:** ~60 minutes


**Type:** Learn | **类型:** 学习
**Languages:** Python (stdlib, toy percentile calculator and goodput reporter) | **语言:** Python（标准库，百分位计算器和 Goodput 报告器）
**Prerequisites:** Phase 17 · 04 (vLLM Serving Internals) | **前置知识:** Phase 17 · 04（vLLM 服务内部）

>  **【前置】**学本节前请先掌握:Phase 17·04(vLLM)、统计基础(百分位)。推理指标四件套:TTFT(首代币 时间) + TPOT(每代币 时间) + 吞吐量 + Goodput。
>  **【类比】**推理指标 = "餐厅 KPI"。TTFT = 顾客坐下第一道菜上桌(prefill+queue+network);TPOT = 后续每道菜间隔(解码成本);吞吐量 = 餐厅每小时出餐总数;Goodput = 满足所有SLO 要求比例(关键!)。陷:高吞吐低 Goodput =做了很多菜但客人没有按值吃到──必须报报P50/P90/P99 不能只报报均──GenAI-Perf 和 LLMPerf 对TPOT 口径不同相同运行结果会冲突──
**Time:** ~60 minutes | **时间:** ~60 分钟

## أهداف التعلم

- تحديد TTFT، TPOT، ITL، E2E، والعبور، والخيار جيد بدقة وتسمية المكونات التي تقوم كل منها بعملياتها.
  中文翻译:精确定义 TTFT、TPOT、ITL、E2E、吞吐量和 Goodput,并说出每个指标测量的组件──
- شرح لماذا متوسط هو الإحصاءات الخاطئة لخدمة ماجستير في العلوم العليا وكيفية قراءة P50/P90/P99.
  ترجمة باللغة الصينية: شرح لماذا متوسط القيمة هو إحصاءات الخدمة في ماجستير في إدارة الأعمال، وكيفية قراءة P50/P90/P99。
- قم ببناء مقيد متعدد SLO (مثل TTFT<500 ms و TPOT<15 ms و E2E<2 s) وحسب النتائج المثبتة ضده.
  中文翻译:构造 SLO 多约束(如 TTFT<500ms 且TPOT<15ms 且E2E<2s)并据此计算 Goodput。
- أسمائ أدوات مقياسية لا توافقان على TPOT لنفس السباق وشرح السبب.
  قال أن اثنين في نفس النشاطات على TPOT  تظهر نتائج مختلفة من أدوات اختبار الأساسية并解释原因.

## المشكلة المشكلة المشكلة

> **【中文解读】**推理服务有多个延迟轴,每个轴以不同方式失败──预先是计算有限的,随提示长度增长; 解码是内存有限的,随批量大小增长; 排队延迟是运维问题; 网络是物理距离问题──需要不同的指标来衡量每个维度,需要百分数,还需要综合指标来说"用户是否获得了预期的体验"这就是 Goodput──

> **【拓展：LLM 推理指标体系】**2026 سنة نظام مؤشر كامل لترشيح الجامعة 推理包括:(1) TTFT(首代币 延迟) 用户感知到的首次响应时间;(2) TPOT/ITL(每代币 延迟/间代币 延迟) 流式输出平滑度;(3) E2E(端到端延迟) 从请求到完成的总时间;(4) Throughput(吞吐量) 集群效率指标;(((((有效吞吐) 同时满足所有 SLA 请求比例;;ML已Perf Inference v6.0  Goodput 作为官方提交指标.

"إنّ إمكانية إرسالنا هي 15 ألف رمز في الثانية". ماذا إذاً؟ إذاً، إذاً إذا تجاوزت 40٪ من الطلبات 2 ثوانٍ من النهاية إلى النهاية، فإنّ المستخدمين قد هجروا الجلسة. الإرسال وحده لا يخبرك ما إذا كان المنتج يعمل.

> "إن حجم الإنتاج لدينا هو 15000 رمز في الثانية ".. ماذا بعد ذلك؟ إذا تجاوز 40% من الطلبات من نهاية إلى نهاية 2 ثواني، فإن المستخدم سوف يتخلى عن المحادثة "..

الإستدلال لديه محورات متعددة من التخفيف وكل واحد يفشل بشكل مختلف. المكملة المسبقة مرتبطة بالحسابات وتقاسم مع طول سريع. إنّ التشخيص مقيدٌ بالذاكرة و يُقيم مع حجم اللحظة تأخير الصف مشكلة عملية الشبكة مشكلة المسافة المادية تحتاج إلى مقاييس مختلفة لكل منها، وتحتاج إلى رصيد، وتحتاج إلى مركب واحد يقول "هل حصل المستخدم على ما كان يتوقع"

> 推理有多个延迟轴,每个轴以不同方式失败──预填是计算有限的,随提示长度增长──解码是内存有限的,随批次大小增长──排列延迟是运维问题──网络是物理距离问题──你需要不同的指标,需要百分数,还需要综合指标说"هل المستخدم حصل على التجربة المطلوبة"这就是 Goodput──

## المفهوم الأساسي

### TTFT  وقت لأول رمز

> **【中文解读】**TTFT = وقت الصف + شبكة_طلب + prefill_time。 الاحتمالات المسبقة في الوقت الطويل تشتمل على عرض 32K في Llama 3.3 70B FP8 H100 上需要大约 800ms من الاحتمالات المسبقة.

`TTFT = queue_time + network_request + prefill_time`

يهيمن التملأ قبل الإشارات عندما تكون طويلة. على Llama-3.3-70B FP8 على H100 ، يستغرق طلب 32k ~ 800 ms من التملأ قبل الإشارة. وقت الصف هو سلوك المخطط تحت الحمل. طلب الشبكة هو وقت الأسلاك بما في ذلك TLS. TTFT هو التأخير الذي يراه المستخدم قبل أن يتم تشغيل أي شيء مرة أخرى.

> 预充在长提示时占主导──Llama-3.3-70B FP8 在 H100 上,32K 提示需要大约800ms的纯预充──排队时间是调度器的行为──网络请求是包括TLS的线缆时间──TTFT是用户在任何内容流式返回前感知到的延迟──

### التخميس بين الشعار

> **【中文解读】**TPOT(وقت لكل رمز الخروج) = ITL(التأخير بين الشعارات) = تأخير تشفير لكل شعارات‬公式:TPOT = (decode_forward_time + scheduler_overhead) / tokens_produced‬

العديد من الأسماء لمقدار واحد`TPOT`(الوقت لكل رمز الخروج)`ITL`(التأخير بين الشعار) ،`decode latency per token` كل نفس. هو الوقت بين رموز التدفق المتتالية بعد الأولى.

> عدد من الاسماء`TPOT`(كل إشارة إصدار 时间)`ITL`(توقيت الـ 延迟)`每 token 解码延迟`都是同一个──它是第一个标志 之后连续流式标志 间时间──

`TPOT = (decode_forward_time + scheduler_overhead) / tokens_produced`

على نفس كومة Llama-3.3-70B H100 مع التملأ المقطع ، TPOT يعني ~ 7 ms. بدون التملأ المقطع ، خلال التملأ المقبل الطويل على تسلسل مجاور ، يمكن أن ترتفع TPOT إلى 50 ms. مشاهدة P99, ليس متوسط.

> في نفس Llama-3.3-70B H100                                                                                                                                                                                                                                                          

### تأخير E2E

`E2E = TTFT + TPOT * output_tokens + network_response`

بالنسبة للمخرجات الطويلة (> 500 رمز) ، E2E يهيمن على TPOT. بالنسبة للمخرجات القصيرة مع الطلبات الطويلة، E2E يهيمن على TTFT. تقرير E2E المكون من طول المخرج.

> 对于长输出(>500 توكن),E2E بواسطة TPOT 主导──对于长提示的短输出,E2E بواسطة TTFT 主导──报告按输出长度分条件的E2E──

### التشغيل

`throughput = total_output_tokens / elapsed_time`

المقياسات المجمعة تخبرك بفعالية الأسطول لا تخبرك بصحة الطلب الفردي

> 聚合指标―― أخبرك بكفاءة الكتلة―― لا أخبرك بحالة صحية لطلب واحد――

### المقدمة التي تهتم بها فعلا

> **【中文解读】**إن الإنتاج الجيد هو المؤشر الشامل الوحيد المهم حقا ً.SLO هو多约束的一个请求只有同时满足 TTFT <= a、TPOT <= b、E2E <= c 才算"好"。高吞吐量在60% Goodput 时是失败;低吞吐量在 99% Goodput 时才是目标──2026年 MLPerf Inference v6.0 和 AI 平台供应商内部 SLA 追踪都以 Goodput 为核心指标──

`goodput = fraction of requests meeting (TTFT <= a) AND (TPOT <= b) AND (E2E <= c)`

إن SLO هو قيود متعددة. الطلب "جيد" فقط إذا تم الاحتفاظ بكل قيود. الجيد هو النسبة. التنفيذ العالي عند 60% الجيد هو الفشل. التنفيذ الأقل عند 99% الجيد هو الهدف.

> SLO هو منظمات كثيرة. فقط عندما يتم تلبية جميع القيود، الطلب هو "جيد" من. Goodput هو هذا النسبة. 60% Goodput هو منسوبات عالية هو الفشل.

في عام 2026، Goodput هو المقياس المستخدم في تقديمات MLPerf Inference v6.0 وفي متابعة SLA الداخلية في مزودي منصات الذكاء الاصطناعي.

> 2026 年,Goodput 是 MLPerf Inference v6.0 提交和 AI 平台提供商内部 SLA 追踪使用的指标──

### لماذا العدالة هي الإحصاءات الخاطئة

> **【中文解读】**التوزيع المتأخر LLM هو يميناً.‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬

توزيعات تأخر LLM هي متوجية إلى اليمين. يمكن لشحنة تشكيل مع جار طويل التميز شحن 500 رمز مع TPOT ~ 7 ms و 20 رمز مع TPOT ~ 60 ms. متوسط TPOT هو 9 ms. P99 TPOT هو 65 ms. المستخدمين ضرب P99 بانتظام  لهذا السبب يغادرون.

> التوزيع المتأخر LLM هو على اليمين. واحد يحتوي على طويلاً على الحملة الجارية. يمكن إصدار 500 تبت من حوالي 7ms. وتوب من 20 تبت من حوالي 60ms.

دائماً أبلغ عن الثلاثة (P50، P90, P99). لخبرة المستخدم، P99 هو الذي تحسن.

> 始终报告三元组(P50、P90、P99) ・・・ بالنسبة للتجربة المستخدمة، فإن P99 هي ما تحتاج إلى تحسينها‬

### أرقام المرجعية  Llama-3.1-8B-Instruct on TRT-LLM، 2026

- متوسط TTFT: 162 ms
  中文翻译: متوسط القيمة TTFT:162ms
- متوسط TPOT: 7.33 ms
  中文翻译: متوسط قيمة TPOT:7.33ms
- متوسط E2E: 1,093 ms
  中文翻译: متوسط قيمة E2E:1,093ms
- P99 TPOT: يختلف من 10-25 ms اعتمادا على تكوين المكملات المسبقة.
  中文翻译:P99 TPOT:10-25ms,取决于分块预填充配置──

هذه هي نقاط مرجعية NVIDIA المنشورة. تتغير مع حجم النموذج (70B سيعرض 3-5x) ، والأجهزة (H100 مقابل B200 ~ 3x) ، والحميل.

> هذه هي بيانات مرجعية نشرتها NVIDIA.

### فخ القياس

> **【中文解读】**اثنين من أدوات اختبار الأساسيات الأكثر استخداما في عام 2026 في TPOT تنتج نتائج مختلفة: NVIDIA GenAI-Perf سوف تثبت من ITL  حسابات خارجة ((من التوكن 2 开始) ،LLMPerf 包含 TTFT(من التوكن 1 开始)  نفس الطلب ((TTFT 500ms、100 输出 代币、700ms فك التشفير) ،GenAI-Perf 报告 ITL=7.07ms ،LLMPerf 报告 ITL=12.00ms。永远说明使用哪个工具,永远发布定义──

> **【拓展：LLM 基准测试工具生态】**2026 سنة LLM 推理基准测试工具包括:(1) NVIDIA GenAI-PerfTriton 客户端,全面指标覆盖,ITL 不含TTFT;(2) LLMPerf(Anyscale)Rust-backed 分词,流式感知,含TTFT 的ITL;(3) LLM-Locust(TrueFoundry)Locust 扩展,修复了GIL 问题;4) guidellm大规模合成基准测试;5)((((6 v2026.1.0流式感知,Kubernetes-native──选择工具时要了解其ITL 定义差异──

اثنان من أدوات مقياسية 2026 الأكثر استخداماً يختلفان حول TPOT لنفس السباق:

- **NVIDIA GenAI-Perf**: يستبعد TTFT من حساب ITL. يبدأ ITL من رمز 2.
  中文翻译:**NVIDIA GenAI-Perf**: من ITL 计算中排除 TTFT──ITL من # 2 个标志 开始──
- **LLMPerf**: يتضمن TTFT. يبدأ ITL من رمز 1.
  中文翻译:**LLMPerf**: يتضمن TTFT──ITL من # 1 个标志 开始──

بالنسبة لطلب مع TTFT 500 ms و 100 رموز خروج في 700 ms إجمالي التشفير ، تقارير GenAI-Perf `ITL = 700/99 = 7.07 ms`، تقارير " LLMPerf "`ITL = 1200/100 = 12.00 ms`اختيار الأداة يغير الرقم

> 对于一个TTFT 500ms、100 输出代币、700ms 总解码的请求,GenAI-Perf 报告 `ITL = 700/99 = 7.07ms`,LLMPerf  تقرير `ITL = 1200/100 = 12.00ms` أدوات اختيار تغيير الرقم

دائماً أعلن أداة، دائماً نشر التعريف

> 始终说明使用哪个工具──始终发布定义──

### بناء SLO

> **【拓展：LLM SLO 设定参考】**2026 سنة推的消费级 70B 对话模型 SLO:TTFT P99 <= 800ms、TPOT P99 <= 25ms、E2E P99 <= 3s(<300 رمز 输出)、Goodput >= 99%。企业级 SLO 收紧 TTFT(200-400ms) ولكن放宽 E2E──测量方法:使用真流量或LLMPerf 合成流量(`--mean-input-tokens 800 --stddev-input-tokens 300 --mean-output-tokens 150`), هدف 2x 峰值并发,运行 30-50 次代取百分位数──

وضع نظام تحديد المستهلكين المعقول لنموذج الدردشة 70B في عام 2026:

- TTFT P99 <= 800 ms
  中文翻译:TTFT P99 <= 800ms(首代币 延迟上限)
- TPOT P99 <= 25 ms
  中文翻译:TPOT P99 <= 25ms(每代币 延迟上限)
- E2E P99 <= 3 ثانية لخروجيات <300 رمز.
  中文翻译:E2E P99 <= 3s(<300 رمز 输出) 』
- هدف الإنتاج الجيد >= 99٪
  中文翻译:Goodput 目标 >= 99%。

تُشدد أجهزة التشغيل التشغيلي التشغيلي TTFT (200-400 ms) وتُسخّر E2E. المهم هو كتابتها، وقياسها الثلاثة، وتتبع الناتج الجيد كمركز واحد.

> 企业级 SLO 收紧 TTFT(200-400ms)并放宽 E2E──关键是写下来、测量全部三、并将Goodput 作为单一综合指标追踪──

### كيفية قياس

- تشغيل حركة المرور الحقيقية أو الواقعية الاصطناعية (LLMPerf مع `--mean-input-tokens 800 --stddev-input-tokens 300 --mean-output-tokens 150`)
  中文翻译:运行真实流量或逼真合成流量(LLMPerf 使用 `--mean-input-tokens 800 --stddev-input-tokens 300 --mean-output-tokens 150`(‬)
- الهدف 2x ذروة التزامن في السباق المرجعي.
  中文翻译:基准测试运行目标为 2倍峰值并发──
- إستخدم 30 إلى 50 تكرار، خذ مئات مئوية من العينة المشتركة.
  中文翻译:运行 30-50 次代,取合并样本的百分位数──
- نشر مع اسم الأداة، نسخة الأداة، النموذج، الأجهزة، التزامن، التوزيع السريع.
  中文翻译:发布时标注工具名、版本、模型、硬件、并发数、提示分布──

## استخدمها في إطار التنفيذ
```figure
throughput-latency
```

## استخدمها

`code/main.py`هو آلة حسابية جيدة التخفيض. توليد توزيع التخفيف الاصطناعي، وتطبيق SLO، وحساب التخفيض. كما يظهر الفرق GenAI-Perf مقابل LLMPerf TPOT على نفس المسار.

> `code/main.py`هو واحد مثل Goodput  محاسبات ‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬

## أرسلها .

> **【拓展：SLO 设定与 Goodput 门控】**2026 سنة تُقدّم 70B على مقالٍ نموذج SLO:TTFT P99 <= 800ms、TPOT P99 <= 25ms、E2E P99 <= 3s(<300 رمز 输出)、Goodput 目标 >= 99%。 SLO 收紧 TTFT(200-400ms) ولكن إضفاء الضغط على E2E。

هذا الدرس يُنتج`outputs/skill-slo-goodput-gate.md`. بالنظر إلى عبء العمل و SLO ، فإنه ينتج وصفة مقياسية جاهزة للمعلومات والمواد المتحركة والتي تقوم بإنشاء البوابات على النمو الجيد بدلاً من النمو.

> 本课产出 `outputs/skill-slo-goodput-gate.md` إعطاء تحديد عبء العمل و SLO، فإنه ينتج برنامج اختبار أساسية للطاقة الذرية / الجهاز التنفيذي، باستخدام Goodput وليس الانتشار كجهاز تحكم.

## تمارين التدريب

1. أركض`code/main.py`كيف يتغير التوصيل الجيد عندما تضغط على P99 TPOT من 30 ms إلى 15 ms؟
   中文翻译:运行 `code/main.py` توليد مع 1 ٪ 尾部尖刺的分布── عندما P99 TPOT من 30ms 收紧到 15ms 时 Goodput 如何变化?
2. أحد البائعين يقتبس "15,000 توك/س في Llama 3.3 70B H100". أسمائ ثلاثة أسئلة يجب طرحها قبل الثقة بها.
   中文翻译:供应商报价"لاما 3.3 70B H100 上 15,000 توك/س"
3. لماذا يحمي الملفات المسبقة المكونة من قطع من المواد البشرية P99 TPOT ولكن لا يعني TPOT؟
   中文翻译: لماذا分块预填充保护 P99 TPOT ولكن لا تحمي متوسط قيمة TPOT؟
4. بناء SLO المستهلك لمساعد الصوت (الرمز الأول يسمع وليس يقرأ). أي مقياس هو الأكثر مرئية للمستخدم؟
   中文翻译:为语音助手构建消费级 SLO(首代标志是听到而非读到) ―― أي مؤشر يظهر بشكل أفضل للمستخدم؟
5. اقرأ وثائق LLMPerf README و وثائق GenAI-Perf. حدد ثلاثة مقاييس أخرى حيث تختلف الأدوات.
   中文翻译:阅读 LLMPerf README 和 GenAI-Perf 文档──找出工具在另外三个标志上的分歧──

## شروط الرئيسية

| Term / 术语 | What people say / 通俗说法 | What it actually means / 实际含义 |
|------|----------------|------------------------|
| TTFT | "time to first token" / "首 token 时间" | Queue + network + prefill; dominated by prefill at long prompts / 队列+网络+预填充；长提示时由预填充主导 |
| TPOT | "time per output token" / "每输出 token 时间" | Memory-bound decode cost per token after first / 首个 token 后每 token 的内存受限解码成本 |
| ITL | "inter-token latency" / "inter-token 延迟" | Same as TPOT in most tools (not all — see GenAI-Perf) / 大多数工具中同 TPOT（非所有——见 GenAI-Perf） |
| E2E | "end to end" / "端到端" | TTFT + TPOT * output_len; response-side network on top / TTFT + TPOT * 输出长度；加上响应端网络 |
| Throughput | "tok/s" / "token 每秒" | Fleet efficiency; useless without latency percentiles / 集群效率；无延迟百分位数则无意义 |
| Goodput | "SLO-met rate" / "SLO 达标率" | Fraction of requests meeting every SLO constraint simultaneously / 同时满足所有 SLO 约束的请求比例 |
| P99 | "tail" / "尾部" | 1-in-100 worst-case latency; the user experience metric / 百分之一最差延迟；用户体验指标 |
| SLO multi-constraint | "the joint" / "联合约束" | AND of all three latency bounds; a request fails if any one is violated / 三个延迟界限的 AND；任一违反即失败 |
| GenAI-Perf vs LLMPerf | "the tool trap" / "工具陷阱" | Tools disagree on whether ITL includes TTFT / 工具在 ITL 是否包含 TTFT 上不一致 |

## المزيد من القراءة

- [NVIDIA NIM — LLM Benchmarking Metrics](https://docs.nvidia.com/nim/benchmarking/llm/latest/metrics.html) التعريف القنوني لـ TTFT، ITL، TPOT.
- [Anyscale — LLM Serving Benchmarking Metrics](https://docs.anyscale.com/llm/serving/benchmarking/metrics) تعريفات بديلة وصفة قياس.
- [BentoML — LLM Inference Metrics](https://bentoml.com/llm/inference-optimization/llm-inference-metrics) قياس مُطبق على عمليات النشر الحقيقية.
- [LLMPerf](https://github.com/ray-project/llmperf) مقياس مفتوح المصدر القائم على الأشعة.
- [GenAI-Perf](https://github.com/triton-inference-server/perf_analyzer/blob/main/genai-perf/README.md) أداة مقياسية لـ NVIDIA.
- [MLPerf Inference](https://mlcommons.org/benchmarks/inference-datacenter/) مقياس مقارنة قائم على الخيارات الجيدة المقبول في القطاع.
