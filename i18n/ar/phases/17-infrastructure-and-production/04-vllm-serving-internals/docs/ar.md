# vLLM خدمة داخلية: الصفحةالتدقيق، التعبئة المستمرة، المزدوجة المزدوجة.
# خدمة محركات داخلية  الصفحةالتأمل، التعبئة المستمرة، المزدوج المزدوج

> إنّ معدل إمداد المحركات العاملة الحديثة يعتمد على ثلاث أخطاء مشتركة، وليس على خدعة واحدة. الاهتمام المرفوع دائماً يعمل يتم حقن الطلبات الجديدة في اللحظة النشطة بين إعادة تشكيل الكود. قطع مُزقتة من المُسجلات المُقدمة، تُطالب بالطول، لذا لا تُجوع رموز التشفير أبداً. تشغيل كل ثلاثة و Llama 3.3 70B FP8 على واحد H100 SXM5 يدفع 2,200-2,400 tok / s عند 128 متزامن  حوالي 25% فوق vLLM الخاص الافتراضي و 3-4x حلقة PyTorch ساذجة. يقرأ هذا الدروس الجدول و نواة الاهتمام من vLLM  المحرك المرجعي لجميع التقنيات الثلاث  على مستوى يمكنك رسم الرسم، وينتهي مع لعبة مستمرة المجموعة في `code/main.py`أن المخططات تملأ وتفكّر بالطريقة التي تقوم بها (VLLM)

> **【中文解读】**vLLM في 2026 المملكة المملكة على أساس ثلاثة تعقيديات تحسين:PagedAttention(分页注意力)始终开启;连续批处理在解码代间注入新请求;分块预填片长提示以防止解码 टोकن 饥饿。三者全开时,Llama 3.3 70B FP8 在单卡H100 上以 128 并发达2,200-2,400 tok/s比朴素 PyTorch 循环快 3-4 倍。

> **【拓展：vLLM → LLM 推理服务标准】**vLLM هو 2026 أكثر مشروعة مفتوحة المصدر LLM 推理服务引擎。PagedAttention 借借借操作系统的虚拟内存分页思想管理 KV Cache، سوف تحكم معدلات الفساحات في 4% 以下──连续批处理允许在解码步骤间动态加入新请求,大幅提升 GPU利用率──这是生产环境部署 LLM 的必学技术──

**Type:** Learn | **类型:** 学习
**Languages:** Python (stdlib, toy continuous batching scheduler) | **语言:** Python（标准库，连续批处理调度器模拟）
**Prerequisites:** Phase 17 · 01 (Model Serving), Phase 11 (LLM Engineering) | **前置知识:** Phase 17 · 01（模型服务）, Phase 11（LLM 工程）
**Time:** ~75 minutes | **时间:** ~75 分钟

>  **【前置】**学本节前 رجاءً قبل التعلم: المرحلة 11·12(التفكير في تحسين الأساس) 、KV cache 概念、连续批处理──vLLM 是 2026 开源推理引擎的事实标准──
>  **【类比】**vLLM 三件套 = "高效餐厅厨房"。PagedAttention = 分块管理 KV cache(像操作系统虚拟内存分页,碎片率 < 4%);Continuous Batching = 动态拼单(新请求随时插入运行批量);Chunked Prefill = 切长快速(长输入切片避免阻塞解码)。Llama 3.3 70B FP8 في H100 上 128 并发达 2200-2400 tok/s,比朴素实现快 3-4 ⋅times

## أهداف التعلم

- شرح PagedAttention كمنصب متخزن KV: الكتل، جداول الكتل، ولماذا تبقى التجزئة أقل من 4% عند تحميل الإنتاج.
  中文翻译:将 PagedAttention 解释为 KV 缓存分配器:块、块表,以及为什么在生产负载下碎片率保持在4%以下──
- الرسم البياني لتنظيمات مستمرة على مستوى التكرار: كيف تترك التسلسلات المنتهية اللسلة والتي تتضمن جديدة دون استنزاف.
  中文翻译:在代级别绘制连续批处理: كيف يغادر المسلسل المنتهي ، كيف يتم إضافة المسلسل الجديد بدون حاجة إلى خال خالي.
- وصف المكملات المسبقة المقطوعة في جملة واحدة واسم مقياس التأخير الذي يحمي (لمحة: إنه ذيل TTFT ، وليس متوسط التنقل).
  中文翻译:用一句话描述分块预填充,并说出它保护哪个延迟指标(提示:是 TTFT 尾部,而不是平均吞吐量)
- اسم 2026 vLLM v0.18.0 gotcha الذي يضرب فرق تمكين كل تحسين في وقت واحد.
  中文翻译:说出 2026 年 vLLM v0.18.0 中同时启用所有优化团队会遇到问题──

## المشكلة المشكلة المشكلة

> **【中文解读】**朴素 PyTorch 服务循环一次处理一请求──静态批处理将所有请求填充到最长序列,浪费 GPU资源并让快请求等待慢请求──vLLM 通过三个核心优化解决这个问题:PagedAttention(KV Cache 碎片率从 60-80% 降至 4% 以下) 连续批处理(在解码代间动态加入新请求) 分块预填充(将长提示切片以防止解码饥饿) 

حلقة خدمة PyTorch البديلة تعمل طلب واحد في كل مرة: إضفاء الرمز، إضفاء التمثال، فك الشفرة حتى EOS، العودة. في مستخدم واحد هذا يعمل. في مئة، هو صف من الناس الصبورين. الإصلاح الواضح  الإعداد ثابت  يضع كل طلب في أطول طلب في النافذة، ويقوم بتشفير كل إصدار إلى أطول إصدار متوقع، ويقفز المجموعة بأكملها في أبطأ تسلسل. تدفع مقابل التشغيل الذي لا تستخدمه، والطلبات السريعة تنتظر الطلبات البطيئة.

> 朴素 PyTorch 服务循环一次处理一个请求:分词、预填充、解码直到 EOS、返回。 عندما يقوم مستخدم واحد بإجراء هذا، فإنه ينتظر صبورًا.

حلّت (VLLM) ثلاث مشاكل في وقت واحد. PagedAttention يمنع تكسير الاحتفاظ الكهربائي من تناول 60-80% من ذاكرة GPU بالطريقة التي تقوم بها التخصيص المتماشى الكلاسيكي. يسمح التجمع المستمر لطلبات بالانضمام والخروج من المجموعة بين كل تكرار لتفكيك الرمز، لذلك تكون المجموعة مليئة بالعمل الحقيقي دائمًا. يكسّر التمليح المسبق المقطوع عرض رمز 32 كيلو في شرائح رمز 512 تتداخل مع فك الشفرة، لذا لا يتجمد عرض طويل كل رمز فك الشفرة على جهاز التلفزيون.

> vLLM أحياناً حل ثلاثة مشاكل.PagedAttention وقف KV 缓存碎片像经典连续分配那样吞 60-80% من GPU 内存.‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬

إنّه من المفترض أن تكون هناك ثلاثة أشكال في الإنتاج 2026، عليك أن تفهم ما تفعله كلّ واحد، لأنّ أوضاع الفشل كلها على المخطط، وليس على النموذج.

> إنّه من المفترض أن تكون هناك ثلاثة أشكال للإنتاج في عام 2026، تحتاج إلى معرفة كلّ شيء، لأنّ أسلوب الإصابة يقع على المُعدّل، وليس على النموذج.

## المفهوم الأساسي

### PagedAttention كنظام ذاكرة افتراضية

> **【中文解读】**PagedAttention 借借鉴操作系统虚拟内存分页思想管理 KV Cache。 تقسيم تسلسل تقسيم لكل سلسلة预分配最大长度(مثلاً 8192 رموز) ، ولكن متوسط الطلب يستخدم فقط 1500 رمزاً، يضيع 82% من HBM。 PagedAttention سوف تقسيم KV Cache إلى كتلة ثابتة كبيرة الحجم(默认 16 رمزاً) ، لكل سلسلة من هذه الكتب تمتلك لوحة تخطيط منطقي موقع إلى ID كتلة جسدية، حسب الحاجة تقسيم، معدل القمح أقل من 4%。 هذا هو الموزع الوحيد في vLLM، من خلال`--gpu-memory-utilization`(默认 0.9) التحكم KV مخزن متاح HBM مثال

> **【拓展：KV Cache 内存管理演进】**تمر KV Cache 内存管理 ثلاث أجيال من التطور: 1) 连续预分配简单但浪费60-80٪ 内存; 2) PagedAttention(vLLM 2023) 分页管理,碎片率 <4%,成为行业标准; 3) RadixAttention(SGLang 2024)  في المشهد المشترك السابقة لتحسين المزيد من التنفيذ، من خلال شجرة الراجعة 索引 تحقيق عبر الطلب KV 复用.

كاش كيف هو`num_layers × 2 × num_heads × head_dim × seq_len × bytes_per_element`في كل تسلسل. بالنسبة للاما 3.3 70B عند 8192 رمزا، وهذا حوالي 1.25 جيجابايت لكل تسلسل في BF16. إذا قمت بحجز مسبق 8192 فتحة لكل طلب ولكن متوسط الطلب لا يستخدم سوى 1500 رمزا، كنت تضيع حوالي 82% من HBM احتجت.

> كل سلسلة من الكويتات`num_layers × 2 × num_heads × head_dim × seq_len × bytes_per_element` إلاما 3.3 70 ب في 8192 رمز  BF16 下 كل سلسلة حوالي 1.25 جيجابايت  إذا كنت لكل طلب تمتلك 8192 فتحة ، ولكن متوسط الطلب يستخدم فقط 1500 رمز ، فأنت تضيع حوالي 82% ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ 

تستعير PagedAttention الفكرة من ذاكرة نظام التشغيل الافتراضي. لا يتم تعيين cache KV على التسلسل. يتم تخصيصه في كتلة ذات حجم ثابت (توهات 16 توهات افتراضية). لكل تسلسل جدول كتلة يقوم بتخريط مواقع الوهات المنطقية الخاصة به إلى أجهزة تعريف كتلة جسدية. عندما ينمو تسلسل ما بعد كتلة المخصصة، يتم إضافة كتلة أخرى. عندما تنتهي، تعود كتلةها إلى حوض البحيرة.

> PagedAttention 借借借操作系统虚拟内存的思想──KV 缓存不是每个序列连续的──它以固定大小的块(默认16代币) 分布──每个序列有一个块表,将逻辑代币 位置映射到物理块 ID──当序列增长超过已分配块时,添加一个新块──完成后,块回归池中──

انخفضت التجزئة من 60-80% (معتاد) إلى أقل من 4% (PagedAttention). لا يمكنك تمكين PagedAttention مع العلم  هو السفن التخصيصية الوحيدة vLLM. الزر هو `--gpu-memory-utilization`(الوضع الافتراضي 0.9) ، الذي يخبر vLLM كمية HBM أن تستخدمه للكتلات KV بعد وزن الحمل والتنشيط.

> 碎片率 from 60-80% (经典) 降至4% 以下 (PagedAttention)  你不需要用标志启动 (PagedAttention)  它是vLLM 唯一分配器──旋是`--gpu-memory-utilization`(默认 0.9), أخبر vLLM فى تحميل الوزن والتحريك بعد KV 块预留多少 HBM

### الإعداد المستمر على مستوى التكرار

> **【中文解读】**连续批处理在每个解码步骤之间做出接受/释放决策──每个代:(1) 移除已完成(EOS أو max_tokens) 序列;(2) 检查等队列,如果有空 KV 块则接受新序列;(3) لتنفيذ جميع序列 في قائمة RUNNING مرة واحدة إلى الأمام التوزيع──批次大小不定,不同输入位置序列共享一次融合前向计算──2026 vLLM V1 调度器的核心不变量是:调度器解码代运一次,而不是每一个请求运一次──

"المركبات الديناميكية" القديمة تنتظر نافذة (قول 10 ms) لملء مجموعة ، ثم تشغيل prefill + decode + decode + decode حتى تنتهي كل تسلسل. ترك التسلسلات السريعة مبكرا وجلس ساكنا بينما انتهت GPU من البطيئات.

> 旧的"动态批处理" تنتظر نافذة ((مثلاً 10ms) لتملأ المجموعات ، ثم تشغيل prefill + decode + decode + decode حتى يتم إتمام كل سلسلة.

يعمل الإعداد المستمر بين كل خطوة لتفكيك.`RUNNING`في كل إعادة التكرار:

> 连续批处理在每个解码步骤之间操作──将运行中的序列集合称为 `RUNNING`列表──每次代:

1. أي تسلسل في`RUNNING`الذي ضرب EOS أو max_tokens يتم إزالتها.
   中文翻译:`RUNNING`يتم إزالة أي تسلسل من EOS أو max_tokens
2. يبحث المخطط في صف الانتظار. إذا كان هناك كتلة KV مجانية، فإنه يسمح بتسلسلات جديدة (مليئة أو استئناف).
   中文翻译:调度器查看等队列──如果有空 KV 块,它接纳新序列(预填充或恢复)──
3. المخطط الأمامي يذهب على أي شيء الآن في`RUNNING`، إصدار رمز جديد لكل تسلسل
   中文翻译:前向传播对 `RUNNING`كل المحتوى يعمل، كل سلسلة إصدار رمز جديد

لا يتم إعادة حجم الحزمة إلى رقم ثابت. تتشارك التسلسلات في مواقع مختلفة في خروجهما واحدة متضخمة إلى الأمام. في 2026 vLLM هذا يسمى `V1 scheduler`. عدم تغير المفتاح: يقوم المخطط بتشغيل مرة واحدة لكل تكرار للكشف وليس مرة واحدة لكل طلب.

> 批次大小从不填充到固定数字──输出不同位置的序列共享一次融合前向传播──2026年 vLLM 中这称为 `V1 scheduler`◊关键不变量:调度器每个解码代运行一次,而不是每个请求运行一次。

### الاحتمالات المسبقة المقطوعة تحمي ذيل TTFT

> **【中文解读】**تم حل مشكلة "结" لمجموعة أخرى من التفريغات. تطلب تذكير 32K في نموذج 70B حوالي 800ms من التفريغات النقية. في حين أن جميع التفريغات الأخرى من التفريغات تتوقع. تم إعادة التفريغ إلى حجم مسدود. تم إعادة التفريغ إلى حجم مسدود.

> **【拓展：vLLM 生产部署最佳实践】**تشمل التكوينات الرئيسية لـ 2026 سنة في إدارة الإنتاج:`--gpu-memory-utilization 0.9`预留 90% HBM 给 KV Cache ؛(2) `--max-model-len`根据实际需求设置而非默认最大值;(3) 分块预填充默认开启但不兼容某些推测解码模式;(4) `--enable-prefix-caching`في المشهد RAG/Agent 场景下可大幅减少重复预填;5) Prometheus 指标端点用于监控队列深度和 KV利用率──

يحتوي إعداد الـ 32k-token على Llama 3.3 70B على ~ 800 ms من إعداد الـ prefill النقي على H100 واحد. أثناء إعداد الـ prefill ، قم بتشخيص الرموز لكل تسلسل آخر في المجموعة. في حلقة خدمة ، يصبح تأخير الـ first-token latency (TTFT) من إشارة طويلة إطار التأخير بين الرموز (ITL) لعدد من المستخدمين الآخرين.

> 预充是计算密集型的──Llama 3.3 70B 上一个32K 代币的提示在单卡H100 上需要约800ms 的纯预充. 预充运行时,预充中所有其他序列的解码代币都在等待. 在服务循环中, 长提示的首个代币延迟(TTFT) 变成了几十个其他用户的代币间延迟(ITL) 毛刺.

تقسم الملفات المسبقة المقطوعة المقطوعة المقطوعة إلى قطع ذات الحجم الثابت (أعلامة افتراضية 512) وتجدد كل قطعة كوحدة. بين القطعات يمكن للمخطط أن يقدم تسلسلات فك التشفير بنموذج واحد. يمكنك تبادل ضرب تأخير الملفات المسبقة المطلقة الصغيرة (بضع سم في كل قطعة) مقابل اضطراب وقت فك التشفير أقل بكثير. تنخفض P99 ITL تحت الحمل المختلط من ~ 50 سم إلى ~ 15 سم في المعايير المنشورة.

> سيتم تحديد المكونات المحددة إلى 512 رمزًا ، كل قطعة كقطعة تحديد. بين الكتل ، يمكن للمحاكم أن يقدم سلسلة الحل التشريفي إلى رمز واحد.

### التفاعل بين الثلاثة التخلفات

جميع الميزات الثلاث تتضمن بعضها البعض. PagedAttention يمنح الجدول الجدولي مصدر KV ذو حبوب دقيقة للتجارة مع. الحزمة المستمرة تحتاج إلى هذا المصدر ذو حبوب دقيقة لذلك فإن قبول تسلسل جديد لا يفرض إعادة التنسيق العالمية. الاحتمالات المزروعة هي قرار يتخذه الجدول الجدولي على نفس`RUNNING`قائمة  إنها سياسة واحدة أخرى لمخطط، وليس نظام منفصل.

> ثلاثة خصائص متعلقة بعضها البعض. الاهتمام المرتبط للمعدل يوفر كمية صغيرة من الموارد الكهربائية لتتدوين.`RUNNING`القرارات التي تم اتخاذها على القائمة هي استراتيجية أخرى للتنظيم، وليس نظام مستقل.

لا تحتاج إلى معرفة كل علم، عليك أن تعرف ما يُحسن من المخطط: الخدمة الخيرية تحت ميزانية الكتلة الكهربائية، تخضع لقطع المكملات المسبقة.

> أنت لا تحتاج إلى معرفة كل علامة. أنت تحتاج إلى معرفة ما هو المعدل.

### 2026 v0.18.0 حصلت على

> **【中文解读】**vLLM v0.18.0 中 لا يمكن تشغيله في نفس الوقت `--enable-chunked-prefill`و النموذج المخطط 推测解码`--speculative-model`)― الاستثناء الوحيد هو N-gram GPU في V1 调度器 推测解码。 لا تقرأ البيانات الإصدارية حول فتح جميع علامات التحسينات التي يواجهها فريق في التشغيل خطأ في التشغيل، وليس إعادة التشغيل.

في vLLM v0.18.0 لا يمكنك الجمع `--enable-chunked-prefill`مع تشفير المضاربة على النموذج المخطط (`--speculative-model`) الاستثناء الموثق هو تشفير GPU التكهنوي N-gram في المخطط V1. الفريق الذي يُحَرِّك كلّ العلم دون قراءة ملاحظات الإصدار يحصل على خطأ في الوقت الإجراء عند بدء، وليس رجعةً ناعمةً. إذا كان مكاسبك المضاربة تستحق تمكين إعادة التعبئة المقطوعة للخيار  الجواب الصحيح في عام 2026 هو غالباً EAGLE-3 دون إعادة التعبئة المقطوعة، وليس نموذج مشروع زائد إعادة التعبئة المقطوعة التي لا تجمع.

> في vLLM v0.18.0، لا يمكنك تشغيلها في نفس الوقت`--enable-chunked-prefill`و النموذج المخطط 推测解码`--speculative-model`)。 استثناء في سجلات المستندات هو N-gram GPU في V1 调度器 推测解码。 عدم قراءة بيان الإصدار حول فتح جميع العلامات على الفريق في البدء في التشغيل واجهت خطأ في التشغيل وليس إعادة التأهيل.

### أرقام يجب أن تتذكر

- إلاما 3.3 70B FP8، H100 SXM5، 128 متزامن، كل ثلاثة على: 2,200-2,400 توك / ثانية.
  中文翻译:Llama 3.3 70B FP8,H100 SXM5,128 并发,三个优化全开:2,200-2,400 توك/س.
- نفس النموذج، vLLM الافتراضي (لا يوجد إعادة إكمال قطعة): ~1,800 توك/س.
  中文翻译:同模型,默认 vLLM(无分块预填充): حوالي 1800 توك/س
- نفس النموذج، بطيء PyTorch الحلقة الأمامية: ~ 600 توك / ثانية.
  中文翻译:同模型,朴素 PyTorch 前向循环: حوالي 600 توك/س.
- النفايات المزقتة في KV تحت PagedAttention عند عبء الإنتاج: <4%.
  中文翻译:PagedAttention 在生产负载下 KV 碎片浪费:<4%──
- P99 ITL تحت الحمل المختلط: ~ 15 ms مع إعادة التعبئة المزروعة، ~ 50 ms بدون.
  中文翻译:混合负载下 P99 ITL:有分块预填充约15ms,无约50ms。

### كيف يبدو المخطط

```
while True:
    finished = [s for s in RUNNING if s.is_done()]
    for s in finished: release_blocks(s); RUNNING.remove(s)

    while WAITING and have_free_blocks_for(WAITING[0]):
        s = WAITING.pop(0)
        allocate_initial_blocks(s)
        RUNNING.append(s)

    # schedule prefill chunks + decode in one batch
    batch = []
    for s in RUNNING:
        if s.in_prefill:
            batch.append(next_prefill_chunk(s))   # e.g. 512 tokens
        else:
            batch.append(decode_one_token(s))     # 1 token

    run_forward(batch)                            # one fused GPU call
```

`code/main.py`هذا بالضبط هذا الحلقة في stdlib Python مع عدد رمز مزيف وتخفيف المضي قدما مزيفا تشغيله يظهر كيفية إزالة القطع المسبقة يحتفظ بتشريح تسلسل حي خلال إزالة طويلة.

> `code/main.py`هو هذا الدورة المميزة للمعايير Python لتنفيذها، باستخدام رمزات مزيفة 计数 و مزيفة قبل إلى تأخير.

## استخدمها في إطار التنفيذ
```figure
tensor-parallel
```

## استخدمها

`code/main.py`يحاكي نظام المواعيد على شكل vLLM مع ميزات قابلة للتبديل. قم بتشغيله لرؤية:

> `code/main.py`模拟一个带有可切换功能的 VLLM 风格调度器──运行

- `NAIVE`وضع: طلب واحد في كل مرة، لا يوجد شحنة.
  中文翻译:`NAIVE`模式: مرة واحدة طلب، بدون معالجة
- `STATIC`وضع: المكعب والانتظار، الاختيار الكلاسيكي.
  中文翻译:`STATIC`模式: تملأ وتنتظر، تصنيف الكلاسيكي
- `CONTINUOUS`وضع: القبول والإفراج على مستوى التكرار.
  中文翻译:`CONTINUOUS`模式: 代级的接纳和释放──
- `CONTINUOUS + CHUNKED`وضع: إملأ قطع من قبل متداخلة مع فك الرمز.
  中文翻译:`CONTINUOUS + CHUNKED`模式:预填充切片与解码交错──

وتظهر الخروج إجمالي الانتقال (الرموز لكل ثانية افتراضية) ، المتوسط TTFT، و P99 ITL. `CONTINUOUS + CHUNKED`يجب أن يهيمن الصف على حركة المرور المختلطة.

> 输出显示总吞吐量(每虚拟秒代币 数) 、TTFT 平均值和 P99 ITL。`CONTINUOUS + CHUNKED`行在混合流量下应占主导地位

## أرسلها .

> **【拓展：LLM 推理引擎对比】**2026 عام الشهيرة المفتوحة LLM 推理引擎包括:vLLM(通用生产默认,PagedAttention+连续批处理) √SGLang(前共享优化,RadixAttention) √TensorRT-LLM(NVIDIA 专属,Blackwell 上吞吐最高) √ llama.cpp(CPU/边缘,GGUF 格式) √选择取决于硬件(CPU/GPU/Hopper/Blackwell) √工作负载(通用聊天/Agent/RAG) 和合规要求自(托管/云托管) √vLLM √60% 根据生产部署份额的产量部署((2026占据 AI 基础设施调查) √

هذا الدرس يُنتج`outputs/skill-vllm-scheduler-reader.md`. بالنظر إلى إعداد الإعداد (حجم الحزمة، استخدام ذاكرة KV، حجم التملأ المقطوع، إعداد المضاربة) ، فإنه ينتج تشخيص المخطط الذي يسمي أي من الثلاثة أخطاء افتراضية هي عرق الزجاجة وما الذي يجب ضبطه.

> 本课产出 `outputs/skill-vllm-scheduler-reader.md` تخصيص الخدمات المحددة (بثّة أحجام، كيوات، نسبة استخدام الاحتياطي، وتخصيص المجموعات المحددة) ، وتولد تشخيص المعدّل، وتشير إلى أيّ من المفهومات الثلاث هي المعدّل، وكيفية تخصيصها.

## تمارين التدريب

1. أركض`code/main.py`. مقارنة`STATIC`إلى`CONTINUOUS`على عبء عمل مع طلبات مختلطة قصيرة وطويلة. من أين يأتي الفجوة في التعبئة من  كفاءة الإعداد المسبق، كفاءة فك الشفرة، أو تأخير الذيل؟
   中文翻译:运行 `code/main.py`◊ في المزدوج الطلبات المختلفة`STATIC`和 `CONTINUOUS`◊ الفرق في التعبئة من أين  إثبات الوفاء المسبق  إثبات التشغيل المسبق أو تأخير التشغيل؟
2. تعديل جدول اللعب لإضافة `--max-num-batched-tokens`. ما هي القيمة الصحيحة لـ H100 يعمل على Llama 3.3 70B FP8؟ (تلميح: إنها وظيفة من حجم كتلة KV وعدد كتلة حرة، وليس HBM خام.)
   中文翻译:修改模拟调度器添加 `--max-num-batched-tokens` H100 运行 Llama 3.3 70B FP8 的正确值是多少?
3. إعادة قراءة ملاحظات إصدار vLLM v0.18.0. أي مزيج من العلامات يستبعد بعضها البعض؟ قم بإدراجها.
   中文翻译:重新读 vLLM v0.18.0 发布说明──哪些标志组合互斥?列出它们──
4. حساب نفايات تقطيع مخزن KV لمتابعة 1000 طلب مع متوسط 1500 رمزا خروجا، std 600 رمزا، تحت (أ) التخصيص المتماشى لكل طلب عند 8192 أقصى، (ب) PagedAttention مع 16 حجر رمزا.
   中文翻译:计算 1,000 个请求的 KV 缓存碎片浪费(平均值 1,500 输出代币,标准差 600),在 (أ) 最大 8192 的连续每请求分配和 (ب) 16 代币块的 PagedAttention 下。
5. شرح في فقرة واحدة لماذا يساعد التعبئة المسبقة في قطع من المواد في P99 ITL ولكن لا تسفر عن المعدات بشكل منفصل. من أين تأتي الفوز في الممارسة العملية؟
   中文翻译:用一段话解释为什么分块预填充帮助P99 ITL但不单独提升吞吐量──实际中吞吐量提升从何而来?

## شروط الرئيسية

| Term / 术语 | What people say / 通俗说法 | What it actually means / 实际含义 |
|------|----------------|------------------------|
| PagedAttention | "the KV trick" / "KV 技巧" | Fixed-size block allocator for KV cache; fragmentation <4% / KV 缓存的固定大小块分配器；碎片率 <4% |
| Block table | "the page table" / "页表" | Per-sequence map from logical token position to physical KV block / 每序列的逻辑 token 位置到物理 KV 块的映射 |
| Continuous batching | "dynamic batching, but right" / "正确的动态批处理" | Admit/release decisions made every decode iteration / 每个解码迭代做出接纳/释放决策 |
| Chunked prefill | "prefill splitting" / "预填充切片" | Break long prefill into 512-token slices interleaved with decode / 将长预填充切为 512 token 片段与解码交错 |
| TTFT | "first token time" / "首 token 时间" | Prefill + queue + network; dominated by prefill at long prompts / 预填充+队列+网络；长提示时由预填充主导 |
| ITL | "inter-token latency" / "token 间延迟" | Time between consecutive decode tokens; dominated by batch size / 连续解码 token 之间的时间；由批次大小主导 |
| Goodput | "throughput that meets SLO" / "满足 SLO 的吞吐量" | Tokens/sec where every request still hit TTFT and ITL targets / 每秒 token 数，每个请求仍满足 TTFT 和 ITL 目标 |
| V1 scheduler | "the new scheduler" / "新调度器" | vLLM's 2026 scheduler; N-gram spec decode is the chunked-prefill-compatible path / vLLM 2026 调度器；N-gram 推测解码与分块预填充兼容 |
| `--gpu-memory-utilization` | "the memory knob" / "内存旋钮" | Fraction of HBM reserved for KV blocks after weights and activations / 加载权重和激活后为 KV 块预留的 HBM 比例 |

## المزيد من القراءة

- [vLLM documentation — Speculative Decoding](https://docs.vllm.ai/en/latest/features/spec_decode/) مصدر رسمي حول توافقية التمليئة المقطوعة والتشفير المضارب.
- [vLLM Release Notes (NVIDIA)](https://docs.nvidia.com/deeplearning/frameworks/vllm-release-notes/index.html) 2026 إطلاق التسلسل والسلوكية الخاصة بالنسخة.
- [vLLM Blog — PagedAttention](https://blog.vllm.ai/2023/06/20/vllm.html) الكتابة الأصلية التي لا تزال تحدد كيفية التفكير حول المخصص.
- [PagedAttention paper (arXiv:2309.06180)](https://arxiv.org/abs/2309.06180) تحليل التجزئة وتصميم المخطط.
- [Aleksa Gordic — Inside vLLM](https://www.aleksagordic.com/blog/vllm) المخطط المفصل V1 المشي مع الرسوم البيانية للنيران.
