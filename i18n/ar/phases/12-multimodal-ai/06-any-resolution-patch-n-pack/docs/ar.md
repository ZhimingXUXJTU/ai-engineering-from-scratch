# أي-مقرر الرؤية: معطقة-ن-باك و NaFlex  أي تصميم

> الصور الحقيقية ليست 224x224 مربع. الإيصالات 9:16، الرسم البياني 16:9، الفحص الطبي قد يكون 4096x4096, صورة الشاشة المحمولة 9:19.5. الإجابة VLM قبل 2024  تحويل حجم كل شيء إلى مربع ثابت  ألقى الإشارة التي تجعل OCR، فهم الوثائق، ومشاهد عالية الدقة العمل التحليل. أظهرت NaViT (جوجل 2023) أنه يمكنك إصلاح معجون ذات القرار المتغير إلى مجموعة واحدة من المحولات مع تقشير خط الكتل. أسقطت M-RoPE (2024) من Qwen2-VL جداول المواقع المطلقة بالكامل. أينيرس من LLaVA-NeXT تمزق صور عالية القرار إلى قاعدة + صور فرعية. إصدار NaFlex من SigLIP 2 (2025) هو الآن مرموز افتراضي لشركات VLM المفتوحة التي تريد نقطة تفتيش واحدة لتخدم كل نسبة الجانب. هذا الدروس يطبق معطفاً من نهايتها إلى النهاية

> **【中文解读】**الصورة الحقيقية ليست 224x224 مربع  الاستحواذ هو 9:16, الصورة هي 16:9, الصورة الطبية يمكن 4096x4096♦2024 قبل عام VLM 统一将图像缩缩放成固定正方形,这会丢失 OCR、文档理解和高分辨率场景解析的关键信息──本课讲解如何让变压器以原始分辨率处理任意宽高比图像──

> **【拓展：金融文档场景的分辨率挑战】**في المشهد المالي، فإن حجم المستندات التي يتم إرسالها، والتي تم إرسالها، والتي تم إرسالها، والتي تم إرسالها، والتي تم إرسالها، والتي تم إرسالها، والتي تم إرسالها، والتي تم إرسالها، والتي تم إرسالها، والتي تم إرسالها، والتي تم إرسالها، والتي تم إرسالها، والتي تم إرسالها، والتي تم إرسالها، والتي تم إرسالها، والتي تم إرسالها، والتي تم إرسالها، والتي تم إرسالها، والتي تم إرسالها، والتي تم إرسالها، والتي تم إرسالها، والتي تم إرسالها، والتي تمثل في إرسالها، والتي تمثل في إرسالها، والتي تمثل في إرسالها، والتي تمثل في إرسالها، والتي تمثل في إرسالها، والتي تمثل في إرسالها، والتي تمثل في إرسالها، والتي تمثل في إرسال، والتي تمثل في إرسال، والتي تمثل في إرسال، والتي تمثل في إرسال، والتي تمثل في إرسال، والتي تمثل في إرسال، والتي تمثل في إرسال، والتي تمثل في إرسال، والتي تمثل في إرسال، والتي تمثل في إرسال، والتي تمثل في إرسال، والتي تمثل في إرسال، والتي تمثل في إرسال، والتي تمثل في إرسال، والتي تمثل في إرسال، والترات إلى إرسال، والترات إلى إرسال، والترات إلى إرسال، والترات إلى إرسال، والترات إلى إرسال، والترات إلى إرسال، والترات إلى إرسال، والترات إلى إرسال، والترات إلى إرسال، والترات إلى إرسال، والترات إلى إرسال، والترات إلى إرسال، والترات إلى إرسال.

**Type:** Build  | **类型:** 构建
**Languages:** Python (stdlib, patch packer + block-diagonal mask)  | **语言:** Python（标准库，补丁打包器 + 块对角掩码）
**Prerequisites:** Phase 12 · 01 (ViT patches), Phase 12 · 05 (LLaVA)  | **前置知识:** Phase 12 · 01（ViT补丁）、Phase 12 · 05（LLaVA）
**Time:** ~120 minutes  | **时间:** ~120 分钟

>  **【前置】**学本节前请先掌握:Phase 12·01(ViT 把图切成补丁);Phase 12·05(LLaVA 投影器);Phase 7·04(RoPE 位置编码,本节用2D-RoPE)
>  **【类比】**NaViT's patch-n'-pack = "تحركات طعام"──传统 ViT = وضع كل شيء على صندوق واحد
> ️ **【易错点】**实现 NaViT 时忘记块对角掩码 → 三张图的补丁 会互相做注意,模型训练完全失败──修复:必须构建 (N,N) 的注意矩阵,只允许注意,块外为 -inf──

## أهداف التعلم

- قم بتجميع اللصوص من مجموعة من الصور ذات القرار المتغير إلى تسلسل واحد وبناء قناع الاهتمام المقطعية.
  > إعادة تعديل الصورة ذات الصلة المختلفة إلى سلسلة واحدة، وتكوين كتلة على الزاوية
- اختر بين AnyRes tiling (LLaVA-NeXT) ، NaFlex (SigLIP 2) ، و M-RoPE (Qwen2-VL) لمهمة معينة.
  > 根据任务选择 AnyRes 切片(LLaVA-NeXT)、NaFlex(SigLIP 2) أو M-RoPE(Qwen2-VL)。
- حساب ميزانيات الرمزية لـ OCR والرسوم البيانية والتصوير بدون إعادة الحجم.
  >  حسابات غير محدودة في حالة OCR 图表 و صور الـ
- أسمائ أنواع الفشل الثلاثة لتحديد حجم التربيع: النص المحقق، المحتوى المقطع، والرموز المهدرة على التدليك.
  > 列举正方形缩放的三种失败模式:文字压缩,内容裁剪,padding 浪费――

## المشكلة

المتحولات تتوقع تسلسل. مجموعة هي كومة من تسلسلات نفس الطول. إذا كانت صورك 224 × 224, تحصل على 196 علامة اللصوص في كل مرة, لا حاجة إلى ملء, العمل قد تم. تدرب على 224, استنتاج على 224, لا تفكر أبدا عن القرار مرة أخرى.

> المحول 期望固定长度序列──一批就是一堆等长序列──如果 الصورة كلها 224x224, كل مرة تنتج 196 رمز إضافة, لا حاجة لملء, مشكلة حل── تدريب وتقرير都用 224,永远不用考虑分辨率──

العالم لا يتعاون. الوثائق هي صورة (8.5x11 بوصة، 2:3-شبه). صور الشاشة الرسمية هي المناظر الطبيعية (16:9). الإيصالات طويلة ونحيفة (1:3). سفن التصوير الطبي في 2048x2048 أو أكبر. صور الشاشة من أجهزة الهاتف المحمول هي 1170x2532 (0.46:1).

> 现实世界不配合──文档是向的(8.5x11 英寸,约 2:3)──图表截图是横向的(16:9)──收据又高又窄(1:3)──医疗影像动 2048x2048或更大──移动设备截图是1170x2532(0.46:1)──

ثلاثة خيارات قبل عام 2024 و لماذا تفشل كل واحد منها:

> ثلاثة خيارات قبل عام 2024 وأسباب فشلها:

1. إعادة الحجم إلى مربع ثابت (224 × 224 أو 336 × 336). يزعج الكتيب والوجوه. يدمّر التقييم المنخفض علامات الرسم البياني ومحتوى OCR. الممارسة القياسية حتى LLaVA-1.5.
   > 缩放为固定正方形(224x224 或 336x336) ―― 缩放会扭曲文字和人脸──下采样会破坏图表标签和OCR 内容──LLaVA-1.5 之前的标准做法──
2. تُرمي معظم الصورة، واختيار موقع المحاصيل هو مشكلة رؤيتها الخاصة.
   > 剪裁为固定宽高比比──会丢弃大部分 الصورة، ثم اختيار وضع قطع نفسها هو مشكلة في الرؤية──
3. المكعب إلى أطول جانب. يصلح التشوهات ولكن يضيع 50٪ + من الرموز على التشغيل لصور الصورة. تكلفة الاهتمام التربيعي على جميع تلك الرموز الرموز.
   > 填充到最长边──修复扭曲但对屏图像浪费50%+的代币 在填充上──所有填充代币的注意成本是二次的──

> **【中文解读】**المحول  توقع تسلسل طول ثابت  الصورة في العالم الحقيقي  الصورة                                                                                                                                                                                                                                                       

الجواب 2024-2025: دع المحول يأكل اللقطات عند قرار الصورة الأصلي، واكتشف كيفية حزم مجموعة متفرقة في تسلسل واحد دون إهدار الحساب.

> إجابة 2024-2025: دع Transformer مباشرة مع "كمل" تصميمات القرار الأصلي، ثم فكّر في كيفية وضع مختلف التكوينات المُحَمَّلَة في سلسلة دون أن تضيع الحسابات.

## المفهوم الأساسي

### "نيفيت" و "بيتش-ن-باك"

كان NaViT (Dehghani et al., 2023) ورقة التي أظهرت هذا العمل على نطاق واسع.

> نيفيت ((Dehghani 等人,2023) يثبت هذا التفكير على نطاق واسع يمكن القيام به.

1. لكل صورة في اللحظة، احسب شبكة اللحظة الأصلية في حجم اللحظة المختار (قول 14).
   > لقطعة من الصور، لتحديد الإصلاحات الكبيرة (((مثلا 14) حسابات الأصلية الإصلاحات الشبكة
2. سطحوا مساحات كل صورة إلى تسلسل طولها المتغير
   > تعديل كل صورة لتسوية سلسلة طولها المتغيرة
3. قم بتجميع جميع اللقطات في تسلسل طويل للشحنة.
   > سوف تتم إصلاح جميع الصور إلى سلسلة طويلة كقطعة
4. قم ببناء قناع الاهتمام المكون من شكل كتلة بحيث تظهر معطيات الصورة A فقط داخل الصورة A
   > بناء كتلة على الزاوية الاهتمام، بحيث تصحيحات الصورة A فقط في الصورة A بناء الاهتمام داخلها‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬
5. يحمل معلومات الموقف لكل إصبع (2D RoPE أو إضافة الموقف الفقري).
   > لكل إضافة تحمل معلومات الموقع ((2D RoPE أو 分数位置嵌入)

تصبح مجموعة من ثلاث صور في 336x336 (576 رمزا) ، 224x224 (256 رمزا) ، و 448x336 (768 رمزا) تسلسلًا واحدًا 1600 رمزاً مع قناع كتلة خط خط 1600x1600. لا يوجد ملابس. لا توجد حسابات مضيعة. يتعامل المحول بنسب الجانب التعسفي.

> ثلاث张不同分辨率图像(336x336=576 رمز、224x224=256 رمز、448x336=768 رمز) من المجموعات تحولت إلى سلسلة من 1600 رمز، مع 1600x1600 بلاك على الزاوية掩码──零填充,零浪费──Transformer الطبيعي المعالجة أي宽高比──

كما قدم NaViT تساقط المفاتيح الجزئية أثناء التدريب  تساقط 50% من المفاتيح عشوائيا عبر المفرد  والتي تساعد على تنظيم وتسريع التدريب. الورثة SigLIP 2 هذا.

> أدرجت NaViT أيضاً تدريبات وقت عدد الإصلاحات المفروضة في الحزمة كما تترك 50% إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصلاحات إصات إصات إصات إصات إصات إصات إصات إصات إصات إصات إص

> **【中文解读】**مركز NaViT: 三张不同分辨率的图像(576 + 256 + 768 = 1600 个代币) تم وضعها في سلسلة واحدة ، باستخدام كتلة على الزوايا لتخفيف منع الانتباه عبر الصورة.

### أيريس (LLaVA-Next) ‬ أيريس 切片策略

أيريس من LLaVA-NeXT هو البديل العملي. بالنظر إلى صورة عالية القرار ومؤشر ثابت (CLIP أو SigLIP في 336), الطلاء الصورة:

1. اختر ترتيب الشبكة من مجموعة محددة مسبقاً  (1x1) ، (1x2), (2x1), (1x3), (3x1), (2x2), إلخ  التي تناسب تناسب الجانب الصورة بشكل أفضل. ➡️ من مجموعة تعريفات سابقة
2. طلاء الصورة الكاملة في الشبكة؛ كل طلاء يصبح 336x336 محصول.
3. أيضاً قم بتصنيف صورة صغيرة: الصورة بأكملها تم تحويلها إلى 336x336 كرمز سياق عالمي.
4. رمز كل طوب عبر رمز التشفير المجمد 336 . تحكم رموز الطوب + رموز الصورة الصغيرة.

لـ 672x672 صورة في شبكة 2x2 بالإضافة إلى صورة صغيرة: 4 * 576 + 576 = 2880 رمزاً بصرياً. مكلفة ولكنها فعالة  يرى ماجستير في التدريب التفاصيل المحلية والسياق العالمي.

AnyRes هو الطريق المفضل عندما يتم تجميد مرموزك ويؤيد فقط قرار واحد. فإنه يفجر عدد الرموز للصور الكبيرة (صورة 1344x1344 في شبكة 4x4 هي 9216 + 576 ≈ 9800 رموز، والتي تملأ معظم سياق 8k LLM).

> **【中文解读】**أيريس  مناسبة للبرمجة يتم تحديدها وتدعم فقط حالة حلول واحدة.  التكلفة هي رمز الصورة الكبيرة.

### M-RoPE (Qwen2-VL) ٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠٠

قوان 2-VL قدمت إضافة الموقف المتحرك متعدد الطرق. بدلاً من المواقع الفقرية من NaViT أو البلاط والصورة الصغيرة من AnyRes ، يحمل كل ملصق وضعًا ثلاثي الأبعاد (الزمني ، والارتفاع ، والعرض). تتعامل دورانات البحث / المفتاح مع H ، W ، والطول الزمني التعسفي.

M-RoPE يرسل القرار الديناميكي الأصلي دون إعادة التدريب. عند استنتاج إطعام أي صورة HxW ، ينتج إمبيدر اللمسات H/14 x W/14 رموز ، وتحصل كل رموز على موقعها (t = 0 ، r = صف ، c = col) ، يقوم RoPE بتدوير الاهتمام بأوتارية مناسبة ، ويقوم Qwen2.5-VL و Qwen3-VL بمواصلة هذا. V2PE من InternVL3 هو نفس الفكرة مع تشفير متغير لكل طريقة.

على عكس AnyRes ، M-RoPE هو O(H x W / P^2) رموز عند قرار محلي  لا تخزين الطلاء فوق. على عكس NaViT ، لا يزال يتوقع صورة واحدة لكل خط. لا يزال الحزم عبر القرارات تحتاج إلى معطلة-n'-pack على الأعلى.

> **【中文解读】**M-RoPE لكل إضافة تمنح ثلاث ابعاد الموقع ((وقت、 ارتفاع、 عرض)) ، باستخدام حركة الموقف编码 معالجة أي H、W 和 طول الوقت‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬

### نافلكس (SigLIP 2) نافلكس 灵活分辨率

NaFlex هو وضع التردد الأصلي لمفتاح SigLIP 2. يعمل نموذج واحد على طولات تسلسل متعددة (256, 729, 1024 رمزا) عند الاستنتاج. في الداخل يستخدم معطفاً على نمط NaViT أثناء التدريب والمواقع الفصلية المطلقة لكل معطفاً. نقطة البيع: نقطة تفتيش واحدة ، اختر ميزانية رمزاً الخاصة بك عند الاستنتاج بناءً على المهمة.

لمهام تعريفية (التصنيف، الاستعراض) ، 256 رمزا. ل OCR أو فهم الرسم البياني، 1024 رمزا. لا إعادة التدريب.

> **【中文解读】**نقطة بيع أساسية NaFlex: نقطة تفتيش، تقديم تقرير عند تحديد المهام الزمنية 预算──语义任务使用 256 رمز، OCR أو رسم الخط لفهم باستخدام 1024 رمز، لا حاجة إلى تدريبات ثقيلة── هذا مناسب للغاية لبناء خدمات متعددة الأشكال حساسة للتكلفة 按需分配令子 预算──

### قناع التعبئة

القناع المُشكل الحجر هو المكان الذي تعثّر فيه معظم التطبيقات.`N_total`تغطية الصور `i=0..B-1`مع الطول `n_i`، القناع`M`من الشكل`(N_total, N_total)`هو 1 إذا كان كلا المؤشرات تقع في نفس الكتل الصورة، وإلا 0. يمكنك بناءها من قائمة الطول التراكمية:

```
offsets = [0, n_0, n_0+n_1, ..., N_total]                         # 累积偏移量
M[i, j] = 1 iff there exists b where offsets[b] <= i < offsets[b+1] # 同一图像块内为1
                        and offsets[b] <= j < offsets[b+1]
```

هذا خط واحد في PyTorch مع`torch.block_diag`أو جمع صريح. مسار طول المتغير في FlashAttention (`cu_seqlens`) يفرغ القناع بالكامل ويتم خلال تسلسلات باستخدام الجهاز التنسوري التراكمي مباشرة  ~ 10x أسرع من قناع كثيف للكفائز النموذجية.

> **【中文解读】**块对角掩码 确保每张图像的补丁只关注自己,不会"看到"同批次中其他图像的补丁.`cu_seqlens`) مباشرة باستخدام التركيز على التجميع الطويل والكميئي، قفز فوق المظروفات المكثفة، السرعة حوالي 10 مرات.

### تميزات الوسوم الوسوم الوسوم الوسوم الوسوم الوسوم الوسوم الوسوم الوسوم الوسوم الوسوم الوسوم الوسوم الوسوم الوسوم الوسوم الوسوم الوسوم الوسوم الوسوم الوسوم الوسوم الوسوم الوسوم الوسوم الوسوم الوسوم الوسوم الوسوم الوسوم الوسوم الوسوم الوسوم الوسوم الوسوم الوسوم الوسوم الوسوم

اختر استراتيجيتك حسب المهمة:

- أوراق / مستندات: 1024-4096 رموز. SigLIP 2 NaFlex في 1024, أو AnyRes 3x3 + صورة صغيرة.
- الرسوم البيانية و الواجهة الفعلية: 729-1024 رموز في 384-448 الأصلي. Qwen2.5VL تحليلا ديناميكيا مع أقصى حد للبيكسل.
- صور طبيعية: 256-576 رمزاً لا بأس. ماجستير الشراكة التدريجي يرى ما يكفي. ادفع مقابل رمزاً حيث كثافة المحتوى مرتفعة.
- الفيديو: 64-128 رمزا لكل إطار بعد جمع المساحة، 2-8 فبص الدروس 12.17 تغطي هذا.

قاعدة الإنتاج 2026: اختر قبعة أكس-بيكسل لكل مهمة، قم بتشفير النسبة الأصلية إلى تلك القفصة، وعبّر اللحظة، وقلق من التدليك.`min_pixels`و`max_pixels`لهذا القنبلة بالضبط

> **【拓展：Token 预算与成本优化】**في بيئة الإنتاج، فإن التوجهات  الميزانية تؤثر مباشرة على API 成本和延迟── لتوزيع 1024+ رمز لمهام OCR أمر ضروري، ولكن الطبيعي الصورة مع 256 رمز يكفي.

> 🤔 **【困惑】**تعلم完本节还会问:1) 真要部署 OCR 系统,min_pixels 和 max_pixels 该设多少? 文档类 min=28*28*4、max=28*28*2560(Qwen2-VL 默认);过高会爆显存,过低会丢失小字──2) لماذا لا تستخدم مباشرة 2k×2k? رمز ثاني انفجار,长文档一张图就吃掉 LLM بأكمله 上下文──3) NaFlex vs AnyRes 哪个更通用? NaFlex(SigLIP 2) أكثر حداثة, نقطة تفتيش 适应所有分辨率;AnyRes 是过渡方案────

## استخدمها في ممارسة التنقل
```figure
mm-patch-n-pack
```

## استخدمها

`code/main.py`يطبق معطفاً "بالتزاوج" لفرقة مختلفة من الصور ذات إحداثيات البيكسل كاملة.

> `code/main.py`باستخدام عدد كامل من الصور تم تحقيق مجموعة مختلفة من الصور.

- يأخذ قائمة من حجم الصورة (H, W).
  > 接收 (H, W) 图像尺寸列表
- يحسب طول تسلسل اللصوص لكل صورة عند حجم اللصوص 14.
  > 计算每张图像在补丁大小 14 下的序列长度──
- يُغلفهم في تسلسل واحد بطول إجمالي`sum(n_i)`. . .
  > 打包为总长度 `sum(n_i)`من سلسلة واحدة
- يُبني قناع الإنتباه المُشكل (كثيف، لضوحة).
  > 构建块对角注意力掩码(密集格式,便于理解)
- يُقارن التكلفة المُغلفة مقابل الحجم التربيعي وتربية AnyRes.
  > مقابل تكلفة التعبئة مقابل التكفيضات والقطع المعدنية
- يطبخ جدول ميزانية رمزية لفرقة مختلطة (الوصول، الرسم البياني، لقطة الشاشة، الصورة).
  > 打印混合批次(收据、图表、截图、照片) الوهم 预算表

إشغله، الأرقام التي تنسحب هي السبب في كل 2026 مفتوحة VLM يستخدم معطفاً

> 运行它──印出的数字就是 كل 2026 开源 VLM 都使用补丁包的原因──

## أرسلها على الإنترنت

هذا الدرس يُنتج`outputs/skill-resolution-budget-planner.md`. بالنظر إلى حجم العمل المختلط في النسب الجوهرية (OCR والرسوم البيانية والصور والإطارات الفيديو) وميزانية الرمز الكلي ، فإنه يختار الاستراتيجية الصحيحة (NaFlex أو AnyRes أو M-RoPE أو مربع ثابت) ويصدر تكوينًا لكل طلب. استخدم هذه المهارة عند قياس VLM لمنتج  فإنه يمنع انفجار الرمز الصامت 10x الذي يقتل ميزانيات التخفيف.

> 本课产出 `outputs/skill-resolution-budget-planner.md` إعطاء خليط واسع مقارنة مع حجم العمل تحميل (OCR 图表 图片 照片 视频 ) و مجموع الوهم 预算 ، فإنه يختار استراتيجية صحيحة ((NaFlex AnyRes  M-RoPE أو مربع ثابت) ويولد حسب الطلب التكوين  استخدام هذه المهارة عند اختيار حجم VLM للمنتجات  يمكن تجنب  تأخير الميزانية  10 مرات الوهم  انفجار 

> **【中文解读】**هذا النوع من المنتجات أداة تخطيط الميزانية بالتحديد.

## تمارين التدريب

1. الإيصالات هي 600 × 1500 (1:2.5). في حجم اللصق 14، كم رموز ذات القرار الأصلي؟ كم بعد التربيع إلى 336؟ والتي تفقد دقة OCR أكثر في الممارسة العملية؟
   | 一张收据 600x1500（1:2.5）。补丁大小14下，原始分辨率多少 token？缩放到336正方形后多少？实际中哪种 OCR 精度损失更大？

2. قم ببناء قناع الحجر المقطعية لفرقة من أربعة صور بطول 256، 576، 729، 1024. تحقق من المصفوفة الاهتمام هو 2585x2585 و لديها بالضبط `256^2 + 576^2 + 729^2 + 1024^2`إدخالات غير صفر.
   | 为四张长度分别为 256、576、729、1024 的图像构建块对角掩码。验证注意力矩阵为 2585x2585 且非零元素数精确为 `256^2 + 576^2 + 729^2 + 1024^2`。

3. لصور 1792x896 في اللمسة 14، قم بالمقارنة: (أ) مربع حجم إلى 336 ثم ترميز، (ب) AnyRes 2x1 + صورة صغيرة، (ج) M-RoPE في الأصلية. أي استخدام أقل رموز؟ الذي يحافظ على أكثر التفاصيل؟
   | 对于 1792x896 的图像（补丁14），对比：(a) 缩放到336正方形，(b) AnyRes 2x1+缩略图，(c) M-RoPE 原始分辨率。哪种 token 最少？哪种保留最多细节？

4. تنفيذ تساقط المزج الجزئي: مع إعطاء تسلسل معبأ ، إسقاط 50٪ من الرموز بشكل متساوي عشوائي ، وتحديث قناع الشكل الكلي وفقا لذلك. قياس تغير ضيق القناع.
   | 实现分数补丁丢弃：给定打包序列，随机均匀丢弃50%的token，更新块对角掩码，测量掩码稀疏度变化。

5. اقرأ القسم 3.2 من ورقة Qwen2-VL (arXiv:2409.12191).`min_pixels`و`max_pixels`السيطرة و لماذا كلتا الحدود مهمة
   | 阅读 Qwen2-VL 论文第 3.2 节（arXiv:2409.12191）。用两句话描述 `min_pixels` 和 `max_pixels` 控制什么，为什么两个边界都很重要。

## شروط رئيسية

| Term | What people say | What it actually means | 中文含义 | 中文释义 |
|------|-----------------|------------------------|----------|---------|
| Patch-n'-pack | "NaViT-style packing" | Concatenate variable-length patch sequences from different images into one batch dimension | 将不同图像的可变长度补丁序列拼接到一个批次维度 | |
| Block-diagonal mask | "Packing mask" | Attention mask that confines each image's patches to attend only to themselves, not neighbors in the pack | 块对角掩码：限制每张图像的补丁只关注自身 | |
| AnyRes | "LLaVA-NeXT tiling" | Split a high-res image into a grid of fixed-size tiles plus a global thumbnail; encode every tile with a fixed encoder | 将高分辨率图像切分为固定大小网格+全局缩略图 | |
| NaFlex | "SigLIP 2 native-flex" | Single SigLIP 2 checkpoint that serves 256/729/1024-token budgets at inference without retraining | 单一 SigLIP 2 checkpoint 推理时支持多种 token 预算 | |
| M-RoPE | "Multimodal RoPE" | 3D rotary position encoding (time, row, column) that handles arbitrary H, W, T without position tables | 三维旋转位置编码（时间、行、列），处理任意宽高和时间长度 | |
| cu_seqlens | "FlashAttention packing" | Cumulative-length tensor the FlashAttention varlen path uses instead of a dense block-diagonal mask | FlashAttention 可变长度路径使用的累积长度张量 | |
| min_pixels / max_pixels | "Resolution bounds" | Qwen2.5-VL per-request knobs capping token count on very small or very large inputs | Qwen2.5-VL 按请求控制最小/最大像素数的参数 | |
| Visual token budget | "How many tokens per image" | Rough count of patch tokens emitted per image; sets the LLM's prompt budget and attention cost | 每张图像产生的补丁 token 数，决定 LLM 的提示预算和注意力成本 | |

## المزيد من القراءة

- [Dehghani et al. — Patch n' Pack: NaViT (arXiv:2307.06304)](https://arxiv.org/abs/2307.06304)‬ ‫تدريبات القرارات المتعددة
- [Wang et al. — Qwen2-VL (arXiv:2409.12191)](https://arxiv.org/abs/2409.12191)‬ ‫كوان2VL ‬ ‫متعدد الطرق ‬ ‫تدوير الموقع
- [Laurençon et al. — What matters when building vision-language models? (Idefics2, arXiv:2405.02246)](https://arxiv.org/abs/2405.02246)‬ ‫العناصر الرئيسية لبناء VLM
- [Tschannen et al. — SigLIP 2 (arXiv:2502.14786)](https://arxiv.org/abs/2502.14786)‬ ‫ " سيجليب 2 " نافلكس
- [Qwen Team — Qwen2.5-VL Technical Report (arXiv:2502.13923)](https://arxiv.org/abs/2502.13923)‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬
