# نقاط التفتيش والرجوع

> كل انتقال من حالة الرسم البياني يستمر عندما يقع عامل في حادث، ينتهي عقده الإيجار ويأخذ عامل آخر في آخر نقطة تفتيش. Cloudflare Durable Objects تحافظ على حالة على مدار ساعات أو أسابيع. يحدد خطة إعادة التنفيذ لكل عمل (المدرسة 15) التحقق بعد العمل يُغلق الخلق قانون الاتحاد الأوروبي للاستخبارات الذكية المادة 14 تجعل الإشراف البشري الفعال إلزامياً للأنظمة ذات المخاطر العالية  هذا يعني في الواقع أن نقاط التفتيش يجب أن تكون قابلة للتحقيق، ويجب أن يتم التدريب على عمليات الإعادة إلى العمل، ويجب أن تتجاوز مسار التحقيق عملية الانتشار. وضع الفشل الحاد: بدون أزرار الاختفاء والتحقق من الشروط المسبقة، يمكن لإعادة المحاولة بعد الفشل المؤقت إضافة إثنين إلى إجراء تمت الموافقة عليه بالفعل. التحقق بعد العمل هو ما يلتقطه.

> **【中文解读】**كل حالة رسم تحويل استمرارية. العامل  عندما تنتهي فترة تأجيرها، العامل الآخر  في نقطة التفتيش الأخيرة للاستعادة. Cloudflare Durable Objects 跨数小时或数周持有状态.‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬

> **【拓展：幂等+前置条件+验证+回滚四件套】**仅等不够:考虑"当余额 > $1000 时从 A 转 $100 إلى B" من الموافقة على الحركة. تمرّد بعد الانهيار، فقط  وغيرها من الموافقة على الاختبار، ولكن إذا كان A 余额 في الانهيار والإعادة التأهيل بين طريق آخر من العمل ينخفض إلى 500 دولار،  عدم وجودها في الانطلاق من خلال النفقات.

**Type:** Learn | **类型:** 学习
**Languages:** Python (stdlib, checkpoint and rollback state machine) | **语言:** Python（标准库，检查点和回滚状态机）
**Prerequisites:** Phase 15 · 12 (Durable execution), Phase 15 · 15 (Propose-then-commit) | **前置知识:** Phase 15 · 12（持久执行），Phase 15 · 15（propose-then-commit）
**Time:** ~60 minutes | **时间:** ~60 分钟

>  **【前置】**学本节前请先掌握:Phase 15·12(التنفيذ المستدام) Phase 15·15(مقترح ثم-التزام) 、数据库事务(ACID) 本节把"持久执行"+"提议-提交"组合成完整安全网──
>  **【类比】**检查点回滚 = "مؤلفات اللعبة و读档"──检查点 = خودڪار存档(每通过一关存一次);回滚 = 读档(这关打错了回到上一关)──后果性动作四件套 = 等键(防止重启两次执行) + 前置条件(重启后世界状态仍符合预期) + 动作后验证(确认真实副作用发生) + 失败回滚(恢复到动作前)──
> ️ **【易错点】**只有等键没有前置条件检查 → 重启时余额已被其他流程改了仍执行 → 透支──修复: يجب أن يتم مراجعة "الوضع العالمي" في الوقت الذي تم فيه التأمين على الوضع العالمي"

## المشكلة المشكلة المشكلة

> **【中文解读】**يسمح الاختبار والتحويل الجهاز الوكيل في عملية التنفيذ للحفاظ على حالة السرعة، والخروج من الخطأ في الوقت الذي يستعيد فيه إلى حالة جيدة سابقة. هذا يشبه أمر قاعدة البيانات والتحكم في نسخة غيت.

> **【拓展：checkpoints rollback】**检查点-回滚是可靠的代理 系统的基础设施――实现选择:(1) 文件系统级使用 Git 或快照保存文件状态;(2) 数据库级使用事务保证数据一致性;(3) 应用级代理自我管理检查点(如LangGraph) ――关键权衡是检查点粒度太细会增加开销,太粗会丢失更多工作──

إن التنفيذ المستمر (الدرس 12) يجعل العميل المتعثر قابلاً لإعادة التنفيذ. المقترح ثم التزام (الدرس 15) يجعل الإجراء المعتمد قابلاً للتدقيق.

> 持久执行(第 12 课)使崩 وكيل قابلة للاستعادة;; اقتراح ثم التزام;;第 15 课)使批准动作可审计;;

هذا الدرس ينضم إليهم: ماذا يحدث عندما يتم تنفيذ إجراء مقبول جزئيًا ، يتعطل ، ويتم استئنافها؟ متى يتم إعادة التدريب ، و ضد أي حالة؟

> هذا الدراسة ترتبط بهم: ماذا يحدث عندما يتم إصدار عملية التأمين؟

الأنظمة الحقيقية تُحكم هذا بشكل مختلف:

> نظام اتصال حقيقي مختلف:

> **【中文解读】**هذا القسم يعرض مفهوم وكيل الذكاء الاصطناعي وطرق تحقيقه. وكيل هو نظام مستقل محرك LLM، قادر على مراقبة البيئة، والتفكير في القرارات، وتنفيذ العمليات والدورات التالية حتى إنجاز الهدف.

- **LangGraph**نقاط التفتيش كل انتقال إلى PostgreSQL. عند حادث العاملين، يتم إطلاق العقد الإيجاري ويعود عامل آخر إلى آخر نقطة التفتيش. تتوقف عمليات العمل على `interrupt()`، والتي نفسها تستمر
  中文翻译:**LangGraph**سوف تحويل كل صورة إلى نقطة التفتيش إلى PostgreSQL.`interrupt()`فوق توقف، استمرارها نفسها
- **Cloudflare Durable Objects**تحافظ على حالة المفتاح على مدار ساعات أو أسابيع. تحفّز الحساب مع التخزين للعمل المعتمد.
  中文翻译:**Cloudflare Durable Objects**跨数小时或数周持有每键状态──将计算与已批准的存储同址──
- **Microsoft Agent Framework**يُعرض`Checkpoint`الأسباب البدائية في API سير العمل؛ إعادة التشغيل بالإضافة إلى عدم القدرة على التشغيل تغطي محاولات إعادة التشغيل.
  中文翻译:**Microsoft Agent Framework**في عمل التيارات API`Checkpoint`原语;重放加等覆盖重试。

في كل حالة، الجمع الذي يعمل فعلا هو: مفتاح عدم القدرة + التحقق من الشروط السابقة + التحقق من بعد الإجراء + إعادة التحقق من الفشل.

> في كل حالة، الجمعيات الفعالة الفعلية هي: 等键 + 前置条件检查 + 动作后验证 + 验证失败时回滚──

## المفهوم الأساسي

### كل انتقال يستمر كل تحول يستمر

التحول إلى حالة الرسم البياني هو أي خطوة تحرك تدفق العمل من حالة واحدة إلى أخرى. لا تزال تنفيذات البغاءات قائمة إلا في نقاط تعيين محددة. لا تزال تنفيذات الإنتاج قائمة في كل انتقال. التكلفة (بعض الكتابة الإضافية) صغيرة نسبياً إلى مكاسب الاعتماد (تراجع الأرض في أي مكان ، وتحقيق استرداد الإيجار).

> 图状态转换是将工作流从一个命名状态转移到另一个命名状态的任何步骤──简单实现只在特定提交点持久化;生产实现持久化每转换──成本几次额外写) 相对可靠性收益(重放落在任何地方、租约恢复精确) 小──

### استعادة الإيجار

عندما يقع عامل، لا يفقد سير العمل؛ ينفذ العقد (الادعاء القصير على أن هذا العامل يقوم بتنفيذ هذا الجري) ببساطة. يقوم عامل آخر بتحديد آخر نقطة تفتيش وتستأنف. آلية الإيجار هي ما يسمح لنظم الإنتاج بالبقاء على قيد الحياة في الانتشارات المتحركة دون فقدان العمل أثناء الطيران.

> العامل 崩时工作流不丢失;租约(هذا العامل 正在执行此运行的短暂声明) مجرد过期──另一名工 拾起最新检查点恢复──租约机制让生产系统在不丢失的情况下进行工作中工作──活滚部署──

### الإستقلال بالإضافة إلى الشروط المسبقة

لا يكفي التحرر وحده. فكر في: يتم الموافقة على سير العمل ل "تحويل$100 from A to B when balance > $1000. " يتم الالتزام بتدفق العمل ، يتعطل في منتصف التنفيذ ، ويتم استئناف. إذا تم التحقق من مفتاح الاختفاء ، ويتم استئناف التنفيذ ، فإن التحويل ينفذ مرة واحدة (صحيحة). ولكن ضع في اعتبارك أن بين التعطل والاستئناف ، ينخفض توازن A إلى 500 دولار عبر تدفق عمل مختلف. لا يزال التحقق من الاختفاء يمر ؛ الشروط المسبقة لا. دون التحقق من الشروط المسبقة ، نُرسل مسدًا.

>  等不够──考虑: العمل تمت الموافقة عليه "$1000 时从 A 转 $100 إلى ب"。 عمليات التسليم والتفريغ في تنفيذ التسجيلات والتعويضات.

كل عمل يتطلب كلتا:

> كل حركة مثيرة تحتاج إلى اثنين:

- **Idempotency key**: يمنع إنجاز المزدوج.
  中文翻译:**幂等键**: منع إنجاز مرتين
- **Precondition check**: يؤكد أن الدولة لا تزال متوافقة مع ما تمت الموافقة عليه.
  中文翻译:**前置条件检查**: حالة تأكيد ما زالت مع موافقة

### التحقق بعد العمل التحقق بعد الحركة

"الأداة التي عادت 200" ليست التحقق. التحقق الحقيقي يقرأ مرة أخرى الحالة المستهدفة ويؤكد تأثير الجانب حدث فعلا.

> "工具返回 200" ليست تجربة.

- تحديث قاعدة البيانات: `UPDATE ... RETURNING *`ثم تأكد من حالة تطابق الصف المرجع المقصودة.
  中文翻译:数据库更新:`UPDATE ... RETURNING *`ثمّ أعلن العودة إلى حالة التكيف المحدد
- إرسال البريد الإلكتروني: تحقق من المجلد المرسل لتحديد الهوية للرسالة بعد الإرسال.
  中文翻译:邮件发送:提交后检查 ارسال-folder 中的消息 ID──
- كتابة الملف: قراءة الملف مرة أخرى وتشويقها.
  中文翻译:文件写:回读文件并哈希。
- دعوة إطار العمل: متابعة`GET`على الموارد المستهدفة.
  中文翻译:API 调用:对目标资源的后续 `GET`.

إذا فشلت التحقق، فإن تدفق العمل في حالة سيئة معروفة.

> 验证失败时工作流处于已知坏状态──回滚启动──

### خطط التداول

كل إجراء متواصل في تقديم المقترحات ثم التزام (الدرس 15) يحمل خطة إعادة التنفيذ.

> المقترح ثم الالتزام (第 15 课) 中每后果性动作带回滚计划──类型:

- **In-band rollback**: عكس الآثار الجانبية مباشرة (`DELETE`بعد`INSERT`،`Send-correction-email`بعد إرسالها).
  中文翻译:**带内回滚**: مضاربات مباشرة`INSERT`后 `DELETE`、 ارسال بعد ارسال更正邮件)
- **Compensating transaction**: عمل جديد يُحبط الأصلي (أنماط SAGA القياسية).
  中文翻译:**补偿事务**:抵消原始动作的新动作(标准 SAGA 模式)
- **Out-of-band rollback**: تحذير الإنسان، توقف سير العمل، ترك الحالة السيئة للتحقيق.
  中文翻译:**带外回滚**: انتباهي البشر 暂停工作流 留下坏状态 للتحقيق

يجب أن يذكر في الاقتراح عدم إعادة التنفيذ ("لا يمكننا إلغاء ذلك") ، حيث تتطلب الإجراءات التي لا تتضمن إعادة التنفيذ أقوى HITL في وقت التزاماتها (المرحلة 15 من التحدي والرد).

> لا-عودة 回滚(" نحن لا نستطيع إلغاء هذا") يجب أن يكون في تـتـيـتـيـتـيـة تمـت تـتـيـتـيـتـيـة.

### قانون الاتحاد الأوروبي للاستخبارات الذكية المادة 14 القراءة التشغيلية

تتطلب المادة 14 "إشراف بشري فعال" على الأنظمة ذات المخاطر العالية.

> 第 14 条要求高风险系统的有效人类监督"──运营术语中,实现者读为:

- نقاط التفتيش قابلة للتحقيق من قبل المراجعة.
  中文翻译:检查点可被审计者查询──
- يتم تدريب الردود (تم اختبارها من نهاية إلى آخر مرة واحدة على الأقل).
  中文翻译:回滚演练 ((على الأقل من آخر إلى آخر تجربة مرة واحدة))
- تتبقى مسار المراجعة على الانتشار (الخلفية لم تكن مؤقتة).
  中文翻译:审计追踪跨部署存活(检查点后端非临时)
- يتم إشعار التحققات الفاشلة، وليس تسجيلها بصمت.
  中文翻译:失败验证被警报而非静默记录。

سير العمل الذي يتوقف في منتصف الالتزام، ويستأنف، ويؤدي إلى إكمال التأثير الجانبي دون مسار التحقق + الردع لا ينجو من اختبار المادة 14.

> 提交中崩、恢复、无验证+回滚路程完成副作用 工作流不通过第 14 条测试──

### وضع الفشل الحاد: إنجاز المضاعف

أكثر حوادث الإنتاج شيوعا في هذا المجال: الإجراء المعتمد، الإجراءات المبدئية، العودة 200, تدفق العمل يتعطل قبل استمرار حالة، استئناف وإعادة تنفيذ.

> أكثر حوادث الإنتاج شيوعًا في هذا المجال:动作批准、提交开始、返回 200、工作流在持久化状态前崩、恢复并重新执行──

1. تمت الموافقة على العمل، مفتاح الإستثمار
   中文翻译:动作批准,等键 k。
2. الالتزام يبدأ، ينفذ، يعيد 200.
   中文翻译:提交开始、执行、返回 200。
3. تدفق العمل يتعطل قبل أن تستمر حالة "التزام".
   中文翻译:工作流在持久化"已提交"状态前崩。
4. سير العمل يستمر؛ يرى "موافق ولكن ليس ملتزم"؛ يعيد تنفيذها.
   中文翻译:工作流恢复; انظر"批准但未提交";重新执行。
5. تأثير الجانب يطلق مرتين
   中文翻译: مضاربة التأثيرات

التخفيف: استمر في إعداد "في الطيران" قبل تنفيذها، تنفيذها بمفتاح إعفاء، ثم علامة "ملتزم" فقط بعد نجاح التحقق من بعد الإجراء. إذا فشلت إطلاق النار الإجراءية وتكشف الحالة، تعرف على التحقق وإعادة إطلاق النار (إذا لزم الأمر). إذا نجحت إطلاق الحالة وتفشل الإجراء، يمكنك التحقق من وإطلاق النار مرة واحدة بالضبط عبر مسار الاسترداد.

> 缓解:执行前持久化"in-flight"意图,用等键执行,仅在动作后验证成功后标记"已提交"──如动作触发而状态写失败,你知道要验证(如必要) 重触发──如状态写成功而动作失败,你验证并通过恢复路径精确触发一次──

## استخدمها في إطار التنفيذ
```figure
checkpoint-replay
```

## استخدمها

`code/main.py`يقوم السائق بتنظيم عملية التدريب المحددة مع إمكانية الإصلاح، والشروط المسبقة، والتحقق، والإعادة التدريب. يحاكي السائق أربعة سيناريوهات: الجري النظيف، والحاولات المجددة بعد الحادث (التقاط إمكانية الإصلاح) ، وفشل الإصلاح (إجراءات إزالة التدريب دون إطلاق النار) ، والتحقق من الفشل (إطلاق إعادة التدريب).

> `code/main.py`实现带等、前置条件、验证和回滚的检查点工作流──驱动器模拟四场景:干净运行、崩后重试(等捕获)、前置条件失败(工作流停止不触发)、验证失败(回滚触发)。

## أرسلها .

`outputs/skill-rollback-rehearsal.md`يقوم بتصميم اختبار التجربة التدريبية للجريان العمل المقترح والتحقق من الخلفية في نقطة التفتيش لتحقيق استمرارية مسار التدقيق.

> `outputs/skill-rollback-rehearsal.md`لتقديم اقتراحات العمل للتصميم والتحقيق

## تمارين التدريب

1. أركض`code/main.py`تأكد من أنّه في حالة الحادث أثناء الالتزام، يتمّ إطلاق النار مرة واحدة بالضبط في كلّ محاولة إعادة.
   中文翻译:运行 `code/main.py` التحقق من أربع المشاهد.

2. تعديل نمط "علامة كما تم أولاً، ثم القيام به" بحيث يكتب الحالة حرائق بعد الإجراء. إعادة عرض سيناريو الحطام. قياس عدد الإجراءات المكررة النار.
   中文翻译:修改"先标记完成再做"模式使状态写在动作后触发──重跑崩场景──测量多少重动作触发──

3. صياغة خطة إعادة التأثير لفعلية إنتاجية محددة (مثل "رسل إلى قناة Slack"). تصنيف كمنطقة داخل المجموعة، أو تعويض، أو خارج المجموعة. توجيه الاختيار.
   中文翻译:为特定生产动作 ((مثلاً "发到Slack 频道") 设计回滚计划──分类为带内、补偿或带外──论证选择──

4. خذ تدفق عمل واحد تعرفه. حدد كل انتقال حالة. حدد كل واحد مع متطلبات استدامة (استمر / لا تستمر). عد تلك التي لا تستمر حاليا.
   中文翻译:取一个你了解的工作流――识别每个状态转换――标记各个持久性要求(持久化/不持久化) ――计数你当前不持久化的。

5. اختبار التردد المتكرر: تصميم اختبار من نهاية إلى نهاية يقوم بتشغيل تدفق عمل حقيقي، ويصطدم به، و يؤكد إطلاق مسار التردد. ما الذي يؤكد عليه الاختبار؟
   中文翻译:演练回滚测试:设计端到端测试运行真实工作流、崩、确认回滚路径触发──测试断言什么?

## شروط الرئيسية

| Term | What people say | What it actually means |
|---|---|---|
| 术语 | 通俗说法 | 实际含义 |
| Checkpoint | "Save point" | Every graph-state transition persists to a durable store |
| 检查点 | "保存点" | 每个图状态转换持久化到持久存储 |
| Lease | "Worker claim" | Short-lived claim that a worker is executing a run; expires on crash |
| 租约 | "Worker 声明" | worker 正在执行运行的短暂声明；崩溃时过期 |
| Precondition | "State gate" | Assertion that the state is still consistent with the approved action |
| 前置条件 | "状态门" | 状态仍与批准动作一致的断言 |
| Post-action verify | "Re-read check" | Confirm the side effect actually happened in the target system |
| 动作后验证 | "回读检查" | 确认副作用在目标系统中实际发生 |
| In-band rollback | "Direct undo" | Reverse the side effect with the inverse operation |
| 带内回滚 | "直接撤销" | 用逆操作反转副作用 |
| Compensating transaction | "SAGA undo" | A new action that neutralizes the original |
| 补偿事务 | "SAGA 撤销" | 抵消原始动作的新动作 |
| Mark-as-done-first | "Status write order" | Persist the committed status before returning from commit |
| 先标记完成 | "状态写顺序" | 从提交返回前持久化已提交状态 |
| Article 14 | "EU AI Act human oversight" | Operational: queryable checkpoints, rehearsed rollbacks, auditable trail |
| 第 14 条 | "EU AI 法案人类监督" | 运营：可查询检查点、演练回滚、可审计追踪 |

## المزيد من القراءة

- [Microsoft Agent Framework — Checkpointing and HITL](https://learn.microsoft.com/en-us/agent-framework/workflows/human-in-the-loop) البدائية في نقاط التفتيش وتعافي الإيجار
  中文翻译:检查点原语和租约恢复。
- [Cloudflare Agents — Human in the loop](https://developers.cloudflare.com/agents/concepts/human-in-the-loop/) الأشياء الدائمة كقاع الدولة.
  中文翻译:الأشياء الدائمة 作为状态基板。
- [EU AI Act — Article 14: Human oversight](https://artificialintelligenceact.eu/article/14/) نقطة أساسية للتنظيم
  中文翻译:监管基线。
- [Anthropic — Measuring agent autonomy in practice](https://www.anthropic.com/research/measuring-agent-autonomy)إطار موثوقية لأجليات العمل على الأفق الطويل.
  中文翻译:长程工作流的可靠性框架──
- [Anthropic — Claude Code Agent SDK: agent loop](https://code.claude.com/docs/en/agent-sdk/agent-loop) شكل سير العمل لـ Claude Code Routines.
  中文翻译:كلود كود روتينات العمل
