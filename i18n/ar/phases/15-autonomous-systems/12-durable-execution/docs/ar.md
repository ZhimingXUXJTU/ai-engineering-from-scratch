# عملاء خلفية طويلة الأمد: إعدام دائم

> لا يوجد عوامل إنتاجية طويلة الأفق`while True`كل مكالمة LLM تصبح نشاطًا مع نقطة التفتيش، والإعادة المحاولة، والإعادة التعبير. تم دمج OpenAI Agents SDK من Temporal في GA مارس 2026. تقوم روتينات كلود كود (أنثروبيك) بتشغيل طلبات كود كود المجدولة دون عملية محلية مستمرة. تقوم الجلسات بإيقاف المدخلات البشرية، وتتبع النشرات، وتستمر من أحدث نقطة التفتيش المفتاحية التي يضعها`thread_id`وراء التجربة الجديدة يقع نمط قديم  تشكيل تدفق العمل  مع مدخل جديد: LLM يدعو أنشطة غير تحديدية يجب أن تكرر بشكل تحديدي عند التعافي.

> **【中文解读】**النمو المتوسط العميل غير موجود`while True`في كل برنامج LLM 调用成为带检查点、重试和重放活动── Temporal of OpenAI Agents SDK 集成于 2026 年 3 月 GA。Claude Code Routines(Anthropic) في حالة عدم استمرار عملية عمل 调用──会话在人类输入时暂停、跨部署存活、从最新检查点恢复──新工效学背后是旧模式工作流编排和一个新输入:LLM 调用作为必须在恢复时确定性重放的非确定性活动──

> **【拓展：LLM 调用 = 活动的精确契合】**المعلمين في مجال التدريبات التدريبية المختلفة: غير المحددة: الحرارة > 0) √ الثمن √ المال والإبطاء) √ قد يفشل √ السرعة المحدودة √ فترة فائقة √ لديها عوارض ضارة √ استخدام أدوات التدريبة √`thread_id`+ 后端存储 + 最近检查点恢复──

**Type:** Learn | **类型:** 学习
**Languages:** Python (stdlib, minimal durable-execution state machine) | **语言:** Python（标准库，最小持久执行状态机）
**Prerequisites:** Phase 15 · 10 (Permission modes), Phase 15 · 01 (Long-horizon agents) | **前置知识:** Phase 15 · 10（权限模式），Phase 15 · 01（长程 Agent）
**Time:** ~60 minutes | **时间:** ~60 分钟

>  **【前置】**学本节前请先掌握:Phase 15·10(权限模式)、Phase 15·01(长程代理)、分布式系统基础(检查点、重试、等)。
>  **【类比】**التنفيذ المستدام = "نقطة تخزين العميل"。 العميل العادي = 玩游戏没存档(崩=重头);Durable = كل LLM 调用后自动存档(崩=读最近的存档)。
> ️ **【易错点】**副作用工具(写数据库、调外部 API) 不存缺陷键 → 恢复时重复执行可能导致业务错误(用户被扣两次款) ――修复: كل جانب إضافي调用必须带等键(如`idempotency-key: uuid`), بعد ذلك按键去重──

## المشكلة المشكلة المشكلة

> **【中文解读】**持久执行确保 وكيل 任务在故障后能恢复──传统 Agent 在内存运行,进程崩意味着从头开始──持久执行将状态保存到外部存储(数据库、文件系统),任何时都可以从最近检查点恢复──Temporal 和 LangGraph 是实现持久执行的两个主流框架──

> **【拓展：durable execution】**持久执行对长时间运行的代理 至关重要――如果一个需要运行 2 小时的代理 在第90 分钟崩,没有持久执行就意味着重新开始―― 暂时 通过事件后源实现持久工作流,长图 通过检查点实现持久状态图――2026年最佳实践是每一个重要步骤后自动保存检查点――

فكر في وكيل يعمل لمدة أربع ساعات، يدعو ثلاثة أدوات، يطلب من المستخدم مرتين، ويقوم بأربعين مكالمة في الدراسة العليا. في منتصف الطريق، يستعمل المضيف على إعادة تشغيل.

> 考虑一个运行四小时的代理――它调用三个工具――提示用户两次――进行40次的LLM调用――中途,运行它的主机重启――

ماذا يحدث؟

> ماذا حدث؟

- في سذاجة`while True`حلقة: كل شيء قد فقد. يبدأ التشغيل من جديد. تقوم الدعوات الثلاثة للأدوات (مع آثار جانبية حقيقية) بإجراءها مرة أخرى. يتم طلب المستخدم مرة أخرى للأشياء التي وافقت عليها بالفعل. يتم إعادة فواتير أربعين مكالمة LLM.
  中文翻译: 在朴素 `while True`循环中:一切丢失──运行从头开始重新.
- مع تنفيذ دائم: يستأنف التشغيل من أحدث نقطة تفتيش. لا يتم إعادة تنفيذ الأنشطة التي تم إكمالها بالفعل؛ يتم إعادة تشغيل نتائجها من السجل الدائم. لا يقر المستخدم مرة أخرى الأشياء التي وافق عليها بالفعل. لا يتم إعادة فواتير المكالمات LLM التي تم إجراؤها بالفعل.
  中文翻译:有持久执行时:运行从最近检查点恢复──已完成活动不重新执行; 其结果从持久日志重放──用户不重新批准已批准的事──已做 LLM 调用不重新计费──

هذه نفس النمط الذي كانت محركات تدفق العمل تشغيله منذ عقد (تيمبورال، كادنس، تشيرامي أوبر) ، والجديد هو أن دعوات الـ LLM أصبحت الآن نوعا من النشاط غير القياسي، مكلفة، مع آثار جانبية، وتتناسب مع هذا النمط بشكل نظيف.

> هذا هو نفس النموذج الذي تمت عرضهه في عام 2010م.

> **【中文解读】**持久化执行解决长程 代理的可靠性问题:四小时运行中主机重启时,朴素循环丢失一切(工具重新执行、用户重新审批、LLM 重新计费),而持久化执行从最近检查点恢复,已完成的活动从持久日志重放而不是重新执行──临时的 OpenAI Agents SDK 集成于2026年3月 GA──核心洞察:LLM 调用是一种活动不确定性、昂贵、有副作用,模式完美适应工作流引擎──

موضوع الدروس: تدهور موثوقية الأفق الطويل (ميتر يلاحظ "تدهور 35 دقيقة"  تنخفض معدل النجاح بشكل رباعي تقريبًا مع الأفق). تمكّن التنفيذ المستدام من القيام بأداء أطول من مدعومة ملف موثوقية، وهو وسيلة جديدة لفشل آمن إذا كان التصميم صحيحًا وغير آمن إذا كان التصميم خاطئًا.

> موضوع هذا الدورة: التراجع في الوقائع الموثوقة: تمرر على المياه المتحركة. تمر على المياه المتحركة. تمر على المياه المتحركة. تمر على المياه المتحركة. تمر على المياه المتحركة. تمر على المياه المتحركة. تمر على المياه المتحركة. تمر على المياه المتحركة. تمر على المياه المتحركة. تمر على المياه المتحركة. تمر على المياه المتحركة. تمر على المياه المتحركة. تمر على المياه المتحركة. تمر على المياه المتحركة. تمر على المياه المتحركة. تمر على المياه المتحركة. تمر على المياه المتحركة. تمر على المياهم المتحركة. تمر على المياهم. تمر على المياهم.

## المفهوم الأساسي

### الأنشطة، وتدفقات العمل، والإعادة التشغيل

- **Workflow**: رمز التنسيق التحديدي. يحدد تسلسل الأنشطة، والفروع، والانتظار. يجب أن يكون تحديديا حتى يتم إعادة تشغيله من سجل الأحداث دون اختلاف مفاجئ.
  中文翻译:**工作流**: تحديدات التخطيط الكود. تعريف سلسلة الأنشطة.
- **Activity**: وحدة عمل غير تحديدية، والتي قد تفشل. دعوة LLM، دعوة أداة، كتابة الملفات، طلب HTTP. يتم تسجيل كل نشاط مع مدخلاته (بمجرد اكتمالها) نتائجها.
  中文翻译:**活动**: عدم اليقين 可能失败的工作单元──LLM 调用、工具调用、文件写、HTTP 请求── كل نشاط يسجل دخولها و(完成时)输出──
- **Event log**: مخزن الدعم الدائم. يتم تسجيل كل نشاط يبدأ، يكتمل، يفشل، يحاول مرة أخرى، وكل قرار سير العمل.
  中文翻译:**事件日志**تم تسجيل كل نشاط بدأ، اكتمل، فشل، إعادة المحاولة، وقرارات كل عمل.
- **Replay**: عند الاسترداد، يتم إعادة تشغيل رمز سير العمل من البداية؛ كل نشاط قد تم إكماله بالفعل يعيد نتيجه المسجل دون إعادة تنفيذ. يتم إعادة تشغيل الأنشطة التي لم تتم.
  中文翻译:**重放**: عند الاسترداد، يعمل التدفقات من الرأس إلى الوراء؛ كل نشاط قد تم إنجازه يعود إلى نتائجه السجلية دون إعادة تنفيذها.

هذا هو نفس الشكل الذي يعيد تقديم React ضد DOM افتراضي، أو Git إعادة بناء شجرة عمل من المشاركات.

> هذا مع رد فعل على DOM افتراضي أو Git من شكل شكل شجر إعادة بناء العمل المقدم نفسه.

### لماذا تتناسب مكالمات الماجستير مع النمط لماذا الماجستير

دعوات ماجستير في العلوم:

> الـ LLM 调用是:

- غير محددة (الدرجة الحرارة > 0، حتى درجة الحرارة 0 تتحرك عبر إصدارات النموذج).
  中文翻译:非确定性(الدرجة الحرارة > 0؛ حتى لو كانت درجة الحرارة 0 跨模型版本漂移) 』
- مكلفة (المال والخمول).
  中文翻译:昂贵(金钱和延迟)
- فشل محتمل (حدود الفائدة، توقعات).
  中文翻译:可能失败(速率限制、超时)
- الآثار الجانبية (إذا استدعوا الأدوات).
  中文翻译:有副作用 ((如果调用工具) 』

هذا هو بالضبط ملف النشاط. إغلاق كل مكالمة LLM كنشاط يمنحك تجربة جديدة مع تعديل تعديلي، التحقق عبر إعادة التشغيل، ومتابعة قابل للعب لتحذير.

> هذا هو الملف الحراري. سوف يستخدم كل ماجستير في التدريبات التدريبية للتدريبات التدريبية.

### نقاط التفتيش المحددة بواسطة`thread_id`‬ ‬ ‬`thread_id`نقطة فحص

لانغغغراف، Microsoft Agent Framework، Cloudflare Durable Objects، وكلود كود روتينات جميعها تجمع على نفس شكل API: `thread_id`(أو ما يعادله) يحدد الجلسة؛ كل انتقال حالة يستمر إلى الخلفية (PostgreSQL افتراضي، SQLite ل dev، Redis ل التخزين الآلي) ؛ استئناف يقرأ أحدث نقطة التفتيش.

> إطار عامل مايكروسوفت إطار كودفلاير الأجسام الدائمة و كلود كود روتينات تلقى نفس API  شكل:`thread_id`(أو ما هو عليه) 识别会话;每个状态转换持久化到后端(默认 PostgreSQL、dev 用SQLite、缓存用Redis);恢复读最新检查点。

اختيار الطرف الخلفي مهم:

> 后端选择重要:

- **PostgreSQL**: مستمرة، قابلة للتسجيل، تتجاوز عمليات التنفيذ.
  中文翻译:**PostgreSQL**:持久、可查询、跨部署存活──长图 默认──
- **SQLite**: محلي-dev فقط؛ يفقد البيانات عبر مضيفين.
  中文翻译:**SQLite**: فقط تطوير محلي؛ عبر المجال الخسارة البيانات
- **Redis**: سريع ولكن مؤقت ما لم يتم تشكيل AOF/مقطع الفورية.
  中文翻译:**Redis**: لكن مؤقت، إلا إذا تم تعيين AOF/快照。
- **Cloudflare Durable Objects**: تم توزيعها بصورة شفافة؛ يتم توزيعها بواسطة مفتاح فريد؛ يحتفظ بها لساعات إلى أسابيع.
  中文翻译:**Cloudflare Durable Objects**: شفافة ومتوزعة، بفترة مفردة،

### الإنسان المدخل كدولة من الدرجة الأولى

يطلب التزام بعد ذلك (الدرس 15) حالة "انتظار البشر" دائمة. يتوقف سير العمل، ويضع الصف الخارجي الطلب المتعلّق، ويستأنف الموافقة من ذلك النقطة بالضبط. بدون استمرار هذا هو أفضل جهد؛ مع ذلك، يصل موافقة بين عشية وضحاها وتبدأ سير العمل في الصباح.

> المقترح ثم الالتزام (第 15 课) تحتاج إلى استمرار "انتظار البشر" حالة.

### التدهور الذي يستغرق 35 دقيقة

لاحظ METR أن كل فئة من العاملات التي تم قياسها تظهر تدهور موثوقية أكثر من ~ 35 دقيقة من العملية المستمرة.

> الميتر  مشاهدة كل قياس  الفئة الوكالة  في حوالي 35 دقيقة من التشغيل المتواصل بعد كل ما أظهر انخفاض في الوقوف بثقة.

مضاعفة مدة المهمة يضاعف حوالي أربعة أضعاف معدل الفشل. لا يصلح التنفيذ المستدام هذا الأمر؛ فإنه يسمح لك بالعمل أطول من مدعومة ملف موثوقية. النمط الآمن هو الجمع بين الاستدامة مع نقاط التفتيش التي تتطلب HITL الطازجة عند إعادة الدخول، ومع مفاتيح قتل الميزانية (درس 13) التي تصل إلى الحد الإجمالي الحساب بغض النظر عن الوقت في الساعة الجدارية.

> 任务时长翻倍大致使失败率四倍──持久执行不修复此; it makes you run better than reliability archives supported's 更多时间──安全模式是将持久性与重新进入时需要新鲜HITL的检查点结合,与不论墙钟封顶总计算的预算终止开关(第13课)结合──

### عندما يكون الإنفاذ الدائم هو الإجابة الخاطئة

- يطول أقل من بضع دقائق دون دخول بشري.
  ترجمة: قصير في دقائق قليلة بدون إدخال البشر
- استرداد المعلومات القراءة فقط
  中文翻译:严格只读信息检索。
- المهام التي تتطلب فيها الصدق نهاية إلى نهاية داخل نافذة سياق واحدة (بعض مهام التفكير؛ بعض الجيل من إطلاق النار).
  ترجمة: صحيحة مطلوبة في نافذة فوق فوق فوق

## استخدمها في إطار التنفيذ
```figure
memory-consolidation
```

## استخدمها

`code/main.py`يطبق محرك تنفيذ طويل الأمد في stdlib Python. يدعم:

> `code/main.py`استخدام قاعدة البيانات Python 实现最小持久执行引擎──它支持:

- `@activity`المعدل الذي يسجل المدخلات والمخرجات إلى سجل الأحداث JSON.
  中文翻译:`@activity`装饰器将输入输出记录到 JSON 事件日志。
- وظيفة سير العمل التي تسلسل الأنشطة.
  中文翻译:将活动排序的工作流函数。
- أ`run_or_replay(workflow, event_log)`وظيفة تعيد إعادة تشغيل الأنشطة المكتملة دون إعادة تنفيذها.
  中文翻译:`run_or_replay(workflow, event_log)`函数重放已完成活动而不重新执行──

يحاكي السائق تدفق عمل ثلاث نشاطات، ويصطدم في منتصف الطريق، ويظهر (أ) محاولة ساذجة لإعادة تنفيذ كل شيء مقابل (ب) إعادة تشغيل تشغيل فقط النشاط المفقود.

> 驱动器模拟三活动工作流,中途崩,展示 (أ) 朴素重试重新执行一切 مقابل (ب) 重放只运行缺失活动──

## أرسلها .

`outputs/skill-durable-execution-review.md`يراجع نشر عميل طويل الأمد المقترح من أجل شكل تنفيذ دائم صحيح: الأنشطة، والتحديد، والخلفية في نقاط التفتيش، وحالة المدخلات البشرية، وسياسة HITL على المودة.

> `outputs/skill-durable-execution-review.md`审查提议长时运行 وكيل 部署 正确持久执行形状: نشاطات 确定性 检查点后端 人类输入状态和恢复时 HITL 策略。

## تمارين التدريب

1. أركض`code/main.py`لاحظ الفرق في عدد النشاطات والتنفيذ بين محاولة إعادة البناء وإعادة التشغيل. قم بتغيير نقطة الانهيار وظهر تغييرات عدد إعادة التشغيل وفقا لذلك.
   中文翻译:运行 `code/main.py` مشاهدة التغييرات في التغييرات في التغييرات في التغيرات في التغيرات في التغيرات في التغيرات في التغيرات في التغيرات في التغيرات في التغيرات في التغيرات في التغيرات في التغيرات في التغيرات في التغيرات في التغيرات في التغيرات في التغيرات في التغيرات في التغيرات في التغيرات في التغيرات في التغيرات في التغيرات في التغيرات في التغيرات في التغيرات في التغيرات في التغيرات في التغيرات في التغيرات في التغيرات في التغيرات في التغيرات

2. حول محرك الألعاب للاستخدام`thread_id`محاكاة جلستين متزايدة تتشارك المحرك وتأكيد أن سجلات الأحداث لا تتصادم.
   中文翻译:将玩具引擎转为显式使用 `thread_id`◊模拟共享引擎的两个并发会话并确认其事件日志不冲突──

3. خذ نشاطًا واحدًا في محرك الألعاب. أدخل عدم التحديد (خاتم زمني في حائط الساعة داخل قرارات سير العمل). أظهر الاختلاف في التكرار. شرح كيفية التعامل مع المحركات الحقيقية مع هذا (سجل الآثار الجانبية ، `Workflow.now()`(إيه بي)
   中文翻译:在玩具引擎中取一个活动──引入不确定性──工作流决策内壁钟时间)──演示重放上的分歧──解释真实引擎如何处理──副作用注册──`Workflow.now()`(إيه بي)

4. اقرأ مقالة "المرحلة التشغيلية خلف عوامل عميقة الإنتاج" من "لانغ تشين"، قم بإدراج كل حالة تستمر فيها الوقت التشغيلي وسمي أي وضع فشل تغطي كل منهما.
   中文翻译:阅读 لانغ تشين's "المرحلة الخلفية من وراء العملاء العميقة في الإنتاج"文章──列出运行时持久化的每个状态并命名各覆盖的失败模式──

5. تصميم سياسة نقطة التفتيش لمهمة التشفير المستقلة لمدة 6 ساعات أين تقوم بتفتيش نقطة التفتيش؟ كيف تبدو استئناف على الانهيار؟ ما الذي يتطلب HITL جديد؟
   中文翻译:为6小时自主编码任务设计检查点策略──你在哪个检查点?崩恢复什么样?什么需要新鲜HITL?

## شروط الرئيسية

| Term | What people say | What it actually means |
|---|---|---|
| 术语 | 通俗说法 | 实际含义 |
| Workflow | "Agent's script" | Deterministic orchestration code; replayable from event log |
| 工作流 | "Agent 的脚本" | 确定性编排代码；可从事件日志重放 |
| Activity | "A step" | Non-deterministic unit (LLM call, tool call); logged before and after |
| 活动 | "一步" | 非确定性单元（LLM 调用、工具调用）；前后记录 |
| Event log | "The backing store" | Durable record of every state transition |
| 事件日志 | "后端存储" | 每个状态转换的持久记录 |
| Replay | "Resume" | Re-run workflow; completed activities return logged results without re-execution |
| 重放 | "恢复" | 重跑工作流；已完成活动返回记录结果而不重新执行 |
| Checkpoint | "Save point" | Persisted state keyed by thread_id; latest-wins on resume |
| 检查点 | "保存点" | 以 thread_id 为键的持久状态；恢复时最新优先 |
| thread_id | "Session key" | Identifier that scopes durable state |
| thread_id | "会话键" | 范围化持久状态的标识符 |
| 35-minute degradation | "Reliability decay" | METR: success rate drops ~quadratically with horizon |
| 35 分钟衰减 | "可靠性衰减" | METR：成功率与时间线大致平方反比下降 |
| Non-determinism | "Drift on replay" | Wall clock, random, LLM output; must be registered as side effect |
| 非确定性 | "重放漂移" | 墙钟、随机、LLM 输出；必须注册为副作用 |

## المزيد من القراءة

- [Anthropic — Claude Code Agent SDK: agent loop](https://code.claude.com/docs/en/agent-sdk/agent-loop)الميزانية، والدورات، والإستئناف من المفاهيم.
  中文翻译:预算、轮次和恢复语义。
- [Microsoft — Agent Framework: human-in-the-loop and checkpointing](https://learn.microsoft.com/en-us/agent-framework/workflows/human-in-the-loop) شكل طلبات المعلومات
  中文翻译:طلباتالمعلوماتحدث 形态。
- [LangChain — The Runtime Behind Production Deep Agents](https://www.langchain.com/conceptual-guides/runtime-behind-production-deep-agents) متطلبات محددة في الوقت الذي يتم فيه تشغيله.
  中文翻译:具体运行时要求。
- [OpenAI Agents SDK + Temporal integration (Trigger.dev announcement)](https://trigger.dev) شكل النشاط لدورات الدراسة في القانون
  中文翻译:LLM 调用活动形态。
- [Anthropic — Measuring agent autonomy in practice](https://www.anthropic.com/research/measuring-agent-autonomy) مرجع التدهور لمدة 35 دقيقة
  中文翻译:35 分钟衰减参考。
