# المحول الكامل  مرموز + مرموز
# 完整 محول  编码器 + 解码器

> الانتباه هو النجم. كل شيء آخر  بقايا، التطبيع، التغذية، الانتباه المتقاطع  هو الرفف الذي يسمح لك بتجميعها بعمق.

> الاهتمام هو المشكلة الرئيسية. كل شيء آخر: الاختلافات المتصلة، التوحيد، التواصل، والشبكة، والانقلاب.

> **【中文解读】**ضع الانتباه الذاتي ‧الرأس المتعدد ‧FFN、البقايا ‧ الطبقة الطبيعية 组装成完整的变压器──这是注意是你需要的论文的实现──

**Type:** Build | **类型:** 动手
**Language:**" بايثون "**语言:**بايثون
**Prerequisites:** Phase 7 · 02 (Self-Attention), Phase 7 · 03 (Multi-Head Attention), Phase 7 · 04 (Positional Encoding) | **前置知识:** 阶段 7 · 02（自注意力），阶段 7 · 03（多头注意力），阶段 7 · 04（位置编码）
**Time:** ~75 minutes | **时间:** ~75 分钟

## المشكلة المشكلة المشكلة

طبقة انتباه واحدة هي مستخرج ميزة ، وليس نموذج. واحد من المتمولات في كل طبقة ليس قدرة كافية للغة. تحتاج إلى عمق  وقطع عمق دون الأنابيب الصحيحة.

>  واحد الاهتمام المستوى هو الخصائص المزودة، وليس النموذج‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬

أُحتوي ورقة فاسواني 2017 على ست قرارات تصميم حولت طبقة من الاهتمام إلى كتلة قابلة للتجميع. يتلقى كل محول منذ  إكودر فقط (BERT) ، و decoder فقط (GPT) ، و encoder-decoder (T5)  نفس العظم. في عام 2026 تمت إصلاح الكتل (RMSNorm ، SwiGLU ، pre-norm ، RoPE) ولكن العظم هو نفسه.

> في عام 2017 قام Vaswani 论文 باحتواء ستة قرارات تصميم ، لتحول طبقة من الاهتمام إلى كتلة قابلة للتراكم. بعد ذلك كل Transformer纯编码器(BERT) 纯解码器(GPT) 编码器-解码器(T5) 都继承了相同的骨架.

هذه الدروس هي العظم. الدروس التالية تخصيصه  06 للكودرات، 07 للكودرات، 08 للكودرات-الديكودرات.

> هذا الدرس هو البناء. الدرس التالي هو تخصصه.

> **【中文解读】**单个注意层只是一个特征提取器,不是完整模型――论文2017将六个设计决策包包装成可堆叠的块:嵌入+位置编码、自注意力、FFN、残差连接、层归化、交叉注意力――所有后续变体 变体BERT、GPT、T5都继承了相同的骨架――

## المفهوم الأساسي

![Encoder and decoder block internals, wired](../assets/full-transformer.svg)

### القطع الستة القطع الستة

1. **Embedding + positional signal.**الوهم → المتجهات. وضعية حقن عن طريق RoPE (حديث) أو السينوسيدل (كلاسيكي).
   **嵌入 + 位置信号。**رمز → 向量──通过 RoPE(现代) أو正弦编码(经典)注入位置──

2. **Self-attention.**كل موقف يُعتني بالآخر مغرم في مُشفرات
   **自注意力。**كل موقع يبحث عن جميع المواقع الأخرى.

3. **Feed-forward network (FFN).**الموضع من حيث الطبقة الثنائية من المعدات: `W_2 · activation(W_1 · x)`نسبة التوسع 4× حسب الاختيار
   **前馈网络 (FFN)。**位置级两层 MLP:`W_2 · activation(W_1 · x)`◊默认扩展比 4×──

4. **Residual connection.** `x + sublayer(x)`بدون هذا، تتلاشى التدرج بعد حوالي 6 طبقات
   **残差连接。** `x + sublayer(x)`لا يوجد هذا، التعدد يختفي بعد ستة مستويات

5. **Layer normalization.** `LayerNorm`أو`RMSNorm`يُثبت التيار المتبقية
   **层归一化。** `LayerNorm`أو`RMSNorm`(现代) ――稳定残差流──

6. **Cross-attention (decoder only).**تظهر الأسئلة من المفكّر والفاتيح والقيم من خروج المفكّر.
   **交叉注意力（仅解码器）。**استفسار من المعدل، المفاتيح والقيم من المعدل المخرج.

### حجر تشفير (استخدم من قبل BERT، T5 تشفير) 编码器块
شاهد تدفق المتجه عبر كتلة واحدة: الاهتمام يختلط عبر المواقع، والباقي يحمله إلى الأمام، FFN يغيره، والطبيعة تبقي التيار مستقرا.

```figure
transformer-block
```

### بلوك المشفير (المستخدم من قبل BERT، T5 encoder)

```
x → LN → MHA(self) → + → LN → FFN → + → out
                     ^              ^
                     |              |
                     └── residual ──┘
```

إنّ المُشفّر مُتجهّيّ، لا تخفيض، كلّ المواقع تُرى كلّ المواقع.

> المعد هو متجهين. لا يوجد مخبأ.

### حظر فك الكمبيوتر (المستخدم من قبل GPT، T5 فك الكمبيوتر)

```
x → LN → MHA(masked self) → + → LN → MHA(cross to encoder) → + → LN → FFN → + → out
```

يحتوي المفكّر على ثلاث طبقات فرعية لكل كتلة. الوسط  الاهتمام المتقاطع  هو المكان الوحيد الذي تتدفق فيه المعلومات من المفكّر إلى المفكّر. في بنية المفكّر فقط النقي (GPT) ، يتم حذف الاهتمام المتقاطع وتتمكن من تزويد الاهتمام الذاتي بمظهر + FFN.

> في كل قطعة من الكودر هناك ثلاثة طبقات. في الوسط هناك التركيز على التقاطع.

### قبل القاعدة مقابل بعد القاعدة

الورق الأصلي:`x + sublayer(LN(x))`و`LN(x + sublayer(x))`. بعد الطبيعية فقدت النحو في عام 2019  من الصعب التدريب بعمق دون تدفئة دقيقة.`LN`* قبل * طبقة فرعية) هو 2026 الافتراضي: Llama، Qwen، GPT-3+، Mistral جميع استخدامها.

> المقالة:`x + sublayer(LN(x))`و`LN(x + sublayer(x))`◊ بعد التأهيل في عام 2019 أو نحو ذلك فقدان  بدون تدريبات دقيقة`LN`في الاطار* قبل*) هو 2026 سنة من الاعتبار: لاما、كوين、 جي بي تي-3+、ميسرال 都 استخدامها.

### 2026 الحجر الحديثة 2026 الحجر الحديثة

| Component / 组件 | 2017 | 2026 |
|-----------|------|------|
| Normalization / 归一化 | LayerNorm | RMSNorm |
| FFN activation / FFN 激活函数 | ReLU | SwiGLU |
| FFN expansion / FFN 扩展比 | 4× | 2.6×（SwiGLU 使用三个矩阵，总参数匹配） |
| Position / 位置编码 | Sinusoidal absolute / 绝对正弦 | RoPE |
| Attention / 注意力 | Full MHA | GQA (or MLA) |
| Bias terms / 偏置项 | Yes / 有 | No / 无 |

يقلل RMSNorm من متوسط مركزية LayerNorm (حساب واحد أقل) ، والتي توفر الحساب وتستقر تجريبيا على الأقل.`Swish(W1 x) ⊙ W3 x`) يتجاوز بشكل متسق ReLU/GELU FFN بنحو 0.5 نقطة ppl في ورق Llama و PaLM و Qwen.

> تمت إزالة RMSNorm من متوسط القيمة المركزية لـ LayerNorm ((قل مرة خفض الف) ، وفرت كمية الحساب، على الأقل على تجربة نفس الاستقرار.`Swish(W1 x) ⊙ W3 x`) في Llama、PaLM 和 Qwen 论文中一致地比 ReLU/GELU FFN 好約 0.5 个困惑度点──

> **【中文解读】**2026 سنة الحديثة تحويل الكمبيوترات 块 مقابل النسخة الأصلية 2017 سنة:LayerNorm→RMSNorm,ReLU→SwiGLU,后归一化→前归一化,绝对位置编码→RoPE,全多头注意力→GQA。 كل تحسين تدريجي, ولكن الجمع معا تحسن بشكل ملحوظ الاستقرار التدريبية والجودة النموذجية。

> **【拓展：为什么 Decoder-only 成为主流】**على الرغم من أن بنية المعدل-فاشل المعدل لها ميزة طبيعية في المهام مثل الترجمة ، إلا أن نموذج GPT ✓ لا يزال يتمتع بالفوز في التوسع والعمومية. يمكن استخدامه في معالجة المهام الفهمية والإنتاجية ، وتدريب أهداف التوحد.

### عدد المعايير العدد

لمكتبة واحدة مع`d_model = d`وتوسيع FFN `r`:

> لـ واحد`d_model = d`وFFN 扩展比为 `r`:

- (م.ه.إم):`4 · d²`(مقاطع Q، K، V، O)
  (م.ه.إم):`4 · d²`(Q、K、V、O 投影)
- FFN (SwiGLU): `3 · d · (r · d)`- -`3rd²`
  (FFN(SwiGLU):`3 · d · (r · d)`- -`3rd²`
- المعايير: لا تُعتبر مهمّة
  归一化:可忽略

> **【拓展：参数计数与模型规模的实际意义】**العناصر من المحول يركز بشكل رئيسي على تركيز الاهتمام على الصواريخ ((4d^2) و FFN(حوالي 8d^2 ل SwiGLU) وسطها.

## بناء ذلك تحرك لتحقيق

### الخطوة الأولى: حجر البناء الخطوة الأولى: بناء الطابور

باستخدام الصفحة الصغيرة`Matrix`الصف من الدروس 03 (منسخة إلى هذا الملف من أجل الاستقلال):

> استخدام第 03 课中的微型 `Matrix`类(已复制到此文件以保持独立):

- `layer_norm(x, eps=1e-5)` انقطاع المتوسط، تقسيم ب std.
  `layer_norm(x, eps=1e-5)`                                                                                                                                                                                                                                                              
- `rms_norm(x, eps=1e-6)` تقسيم بواسطة RMS لا يوجد معدل منقطعة
  `rms_norm(x, eps=1e-6)` إضافة إلى RMS.
- `gelu(x)`و`silu(x) * W3 x`(سويجلي)
  `gelu(x)`和 `silu(x) * W3 x`(سويجلي)
- `ffn_swiglu(x, W1, W2, W3)`. . .
- `encoder_block(x, params)`و`decoder_block(x, enc_out, params)`. . .

### الخطوة الثانية: توصيل مُشفّر طبقةين ومُشفّر طبقةين. الخطوة الثانية: توصيل مُشفّر طبقةين ومُفَكّر طبقةين

قم بتجميعها، قم بإدخال خروج المُشفّر إلى كلّ إشعار مُشفّر، أضف إشارةً أخيرةً قبل إطلاق الخروج.

> 堆叠它们──将编码器输出传入每个解码器交叉注意力──在输出投影前添加最终 LN──

```python
def encode(tokens, params):
    x = embed(tokens, params.emb) + sinusoidal(len(tokens), params.d)
    for block in params.encoder_blocks:
        x = encoder_block(x, block)
    return x

def decode(target_tokens, encoder_out, params):
    x = embed(target_tokens, params.emb) + sinusoidal(len(target_tokens), params.d)
    for block in params.decoder_blocks:
        x = decoder_block(x, encoder_out, block)
    return x
```

### الخطوة الثالثة: اجري على مثال لعبة. الخطوة الثالثة: على مثال لعبة

إرسال مصدر 6 رموز و هدف 5 رموز عبر. التحقق من شكل الخروج هو `(5, vocab)`لا تدريب، هذا الدروس يتعلق بالهندسة المعمارية، وليس الخسارة.

> 输入 6 个代币的源和 5 个代币的目标──验证输出形状是 `(5, vocab)` لا تدرّب  لا تدرّب على التركيز على البنية، لا تدرّب على التلفزيون

### الخطوة الرابعة: قم بتبديل RMSNorm + SwiGLU  الخطوة الرابعة: قم بتبديل RMSNorm + SwiGLU

استبدل LayerNorm و ReLU-FFN بـ RMSNorm و SwiGLU. تأكد أن الأشكال لا تزال مطابقة. هذا هو تحديث 2026 مع استبدال وظيفة واحدة.

> استخدام RMSNorm 和 SwiGLU بدل LayerNorm 和 ReLU-FFN── تصحيح الشكل لا يزال متطابق── هو من خلال وظيفة واحدة استبدال تحقق 2026 سنة الحديثة──

## استخدمها في إطار التنفيذ

تنفيذات مرجعية PyTorch/TF: `nn.TransformerEncoderLayer`،`nn.TransformerDecoderLayer`لكن معظم رمز الإنتاج 2026 يُسجل كتله الخاص لأن:

> PyTorch/TF 参考实现:`nn.TransformerEncoderLayer`.`nn.TransformerDecoderLayer`ولكن معظم كود الإنتاج في عام 2026 سيتم بناؤه على حدة، لأن:

- الاهتمام الفلكي يُدعى داخل الاهتمام، وليس عبر `nn.MultiheadAttention`. . .
  الانتباه فلاش في الانتباه الداخلي، لا تمر `nn.MultiheadAttention`.
- المجلس الوطني للشؤون القانونية/المجلس الوطني للشؤون الوطنية (GQA/MLA) ليس في إشارة المجلس الوطني للشؤون الوطنية.
  GQA / MLA 不在标准库参考中──
- (روبي) ، (رمس نورم) ، (سويغلو) ليست طرق (بيتورش) القابلة للتعيين.
  روبي رمس نورم سويغلو ليست القيمة الاختيارية لبي تورش

**Encoder vs decoder vs encoder-decoder — when to pick:**

> **编码器 vs 解码器 vs 编码器-解码器——何时选择：**

| Need / 需求 | Pick / 选择 | Example / 示例 |
|------|------|---------|
| Classification, embeddings, QA over text / 分类、嵌入、文本 QA | Encoder-only / 纯编码器 | BERT, DeBERTa, ModernBERT |
| Text generation, chat, code, reasoning / 文本生成、聊天、代码、推理 | Decoder-only / 纯解码器 | GPT, Llama, Claude, Qwen |
| Structured input → structured output (translation, summarization) / 结构化转换 | Encoder-decoder / 编码器-解码器 | T5, BART, Whisper |

> **【中文解读】**三种架构的选择:Encoder-only(BERT)适合分类和嵌入;Decoder-only(GPT/Llama)适合生成和通用任务;Encoder-Decoder(T5/BART)适合有明确"源序列"的结构化转换任务──2026年的主流选择是Decoder-only,因为它的扩展性最好,训练最简洁──

> **【拓展：SwiGLU 为何优于 ReLU】**يظهر تجربة نموذج SwiGLU (SwiGLU) من خلال آلية التحكم بالبوابات أن قدرة FFN على التعبير أقوى.

## أرسلها .

انظر`outputs/skill-transformer-block-reviewer.md`. تقوم المهارة بمراجعة تنفيذ كتلة محول جديدة ضد الاختلافات الافتراضية لعام 2026 وتعريض الأجزاء المفقودة (معدل توسع قبل المعيار، RoPE، RMSNorm، GQA، FFN).

> 参见 `outputs/skill-transformer-block-reviewer.md` هذه المهارة وفقاً لهيئة التأمين لعام 2026 مراجعة جديدة لتحقيق كتلة تحويلات، ومعينة عن غياب جزءها.

## تمارين التدريب

1. **Easy / 简单。**احتساب المعايير في كودر_بلوكك في `d_model=512, n_heads=8, ffn_expansion=4, swiglu=True`. التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التحقق من التد`sum(p.numel() for p in block.parameters())`. . .
   计算 `d_model=512, n_heads=8, ffn_expansion=4, swiglu=True`时 encoder_block 的参数──通过实现块并使用 `sum(p.numel() for p in block.parameters())`验证‬

2. **Medium / 中等。**التحول من بعد القاعدة إلى قبل القاعدة. قم بتشغيل كل منهما وقياس معايير التفعيل بعد 12 طبقة متداخلة على إدخال عشوائي. يجب أن تنفجر تنشيطات بعد القاعدة؛ يجب أن تبقى التفعيلات قبل القاعدة محدودة.
   من التأثيرات اللاحقة إلى التأثيرات الاولى. تم قياس 12 مستوى من التركيبات على النوعية التشغيلية على الدخول المحتمل. يجب أن تنفجر النوعات اللاحقة من التأثيرات اللاحقة. يجب أن يبقى التأثيرات اللاحقة من الحدود.

3. **Hard / 困难。**تنفيذ تشفير- تشفير 4 طبقات على مهمة نسخة لعبة (نسخ `x`التدريب 100 خطوة. تقرير الخسارة. تبادل في RMSNorm + SwiGLU + RoPE  هل تنخفض الخسارة؟
   في اللعب نسخ مهمة`x`) على تحقيق 4 مستويات المعدات-فصل المعدات. تدريب 100 خطوة.

## شروط الرئيسية

| Term | What people say / 人们怎么说 | What it actually means / 实际含义 |
|------|------------------------------|----------------------------------|
| Block / 块 | "One transformer layer" / "一个 Transformer 层" | Stack of norm + attention + norm + FFN, wrapped in residual connections. 归一化 + 注意力 + 归一化 + FFN 的堆叠，包裹在残差连接中。 |
| Residual / 残差连接 | "Skip connection" / "跳跃连接" | `x + f(x)` output; enables gradient flow through deep stacks. `x + f(x)` 输出；使梯度流能穿过深层堆叠。 |
| Pre-norm / 前归一化 | "Normalize before, not after" / "先归一化，不是后归一化" | Modern: `x + sublayer(LN(x))`. Trains deeper without warmup gymnastics. 现代：`x + sublayer(LN(x))`。无需预热技巧即可训练更深的网络。 |
| RMSNorm | "LayerNorm without the mean" / "没有均值的 LayerNorm" | Divide by RMS; one less op, same empirical stability. 除以 RMS；少一次操作，经验上同样稳定。 |
| SwiGLU | "The FFN everyone switched to" / "大家都换成的 FFN" | `Swish(W1 x) ⊙ W3 x → W2`. Beats ReLU/GELU on LM ppl. 在 LM 困惑度上击败 ReLU/GELU。 |
| Cross-attention / 交叉注意力 | "How the decoder sees the encoder" / "解码器如何看到编码器" | MHA with Q from decoder, K/V from encoder outputs. MHA 的 Q 来自解码器，K/V 来自编码器输出。 |
| FFN expansion / FFN 扩展比 | "How wide the middle MLP is" / "中间 MLP 有多宽" | Ratio of hidden-size to d_model, usually 4 or 2.6 (SwiGLU). 隐藏大小与 d_model 的比率，通常为 4 或 2.6（SwiGLU）。 |
| Bias-free / 无偏置 | "Drop the +b terms" / "去掉 +b 项" | Modern stacks omit biases in linear layers; slight ppl improvement, smaller model. 现代堆栈在线性层中省略偏置；轻微的困惑度改善，更小的模型。 |

## المزيد من القراءة

- [Vaswani et al. (2017). Attention Is All You Need](https://arxiv.org/abs/1706.03762) تحديدات الكتل الأصلية
  Vaswani 等人(2017)  原始块规范。

- [Xiong et al. (2020). On Layer Normalization in the Transformer Architecture](https://arxiv.org/abs/2002.04745)لماذا قبل الطبيعية يضرب بعد الطبيعية بشكل عميق
  شيونغ وغيره (2020)  لماذا قبل التوحيد في المستوى العميق

- [Zhang, Sennrich (2019). Root Mean Square Layer Normalization](https://arxiv.org/abs/1910.07467) RMSNorm

- [Shazeer (2020). GLU Variants Improve Transformer](https://arxiv.org/abs/2002.05202)ورقة SwiGLU
  شازير (2020)  SwiGLU 论文。

- [HuggingFace `modeling_llama.py`](https://github.com/huggingface/transformers/blob/main/src/transformers/models/llama/modeling_llama.py) الكانونيكل 2026 كتل فقط
  العناق في الوجه`modeling_llama.py` 2026 سنة قواعد الكمبيوترات
