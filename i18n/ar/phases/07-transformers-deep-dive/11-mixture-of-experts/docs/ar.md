# مزيج من الخبراء (مؤ) 混合专家模型 (مؤ)

> محول 70B كثيف ينشط كل معايير لكل رمز. 671B MoE ينشط 37B فقط لكل رمز ويضربه على كل مقياس.

> **【中文解读】**MoE فقط تنشيط جزء من الشبكة المتخصصة معالجة كل رمز، زيادة كبيرة في عدد المواد دون زيادة حجم الحسابات.

**Type:** Hands-on | **类型:** 动手
**Language:**" بايثون "**语言:**بايثون
**Prerequisites:** Phase 7 · 05 (Full Transformer), Phase 7 · 07 (GPT) | **前置知识:** Phase 7 · 05 (Full Transformer), Phase 7 · 07 (GPT)
**Time:** ~45 minutes | **时间:** ~45 分钟

## المشكلة المشكلة المشكلة

فلوفات محولات كثيفة عند الاستنتاج يساوي عدد المعايير (مرة 2 للخطوة الأمامية). قم بتقييم نموذج كثيف وتدفع كل رمز كامل الفاتورة. بحلول عام 2024 كان الحدود تضرب جدار الحساب: لتكون أكثر ذكاءً ، كنت بحاجة إلى المزيد من فلوفات لكل رمز.

> 密 推理时的FLOPs 等于其参数(前向传播乘以2) 扩大密模型意味着每个代币都应付全部代价──到2024年,前沿模型遇到了计算墙:要变得更聪明,需要指数级增长的每代币FLOPs──

خليط الخبراء يقطع هذا الرابط.`E`خبراء مستقلين + جهاز توجيه يختار`k`خبراء لكل رمز. مجموع المعايير = `E × FFN_size`. المعلمات النشطة لكل رمز = `k × FFN_size`. تشكيل عام 2026: `E=256`،`k=8`. مقياسات تخزين مع`E`، حساب مقياس مع `k`. . .

> المختلفة المختلفة المختلفة المختلفة المختلفة المختلفة المختلفة المختلفة المختلفة المختلفة المختلفة المختلفة المختلفة المختلفة المختلفة المختلفة المختلفة المختلفة المختلفة المختلفة المختلفة المختلفة المختلفة المختلفة المختلفة المختلفة المختلفة المختلفة المختلفة المختلفة المختلفة المختلفة المختلفة المختلفة المختلفة المختلفة المختلفة المختلفة المختلفة المختلفة المختلفة المختلفة المختلفة المختلفة المختلفة المختلفة المختلفة المختلفة المختلفة المختلفة المختلفة المختلفة المختلفة المختلفة`E`个独立专家 + 一个路由器,每个代币 选择 `k`个专家──总参数 = `E × FFN_size` عدد فعال لكل رمز = `k × FFN_size`                                                                                                                                                                                                                                                              `E=256`.`k=8`الاحتفاظ`E`扩展,计算随 `k`扩展‬

الحدود 2026 هي تقريبا تماما MoE: DeepSeek-V3 (671B إجمالي / 37B نشط) ، Mixtral 8×22B ، Qwen2.5-MoE ، Llama 4 ، Kimi K2 ، gpt-oss. على قائمة تحليل الاصطناعي المستقلة ، أفضل 10 نماذج مفتوحة المصدر هي جميعًا MoE.

> 2026 سنة المقدمة تقريبا تماما هو MoE:DeepSeek-V3(671B 总参数 / 37B 活跃) 、 مخلوط 8×22B、Qwen2.5-MoE、Llama 4、Kimi K2、gpt-oss── في قائمة التقييم المستقلة للتحليل الاصطناعي،排名前 10 من الموديلات المفتوحة هي MoE──

> **【中文解读】**MoE 打破了"参数 = 计算量"等式── كل FFN 层替换为 E 个独立专家 + 路由器,每 token 只有激活 k 个专家──总参数随着 E 增长,但每个 token 的计算量只随着 k 增长──典型配置 E=256, k=8,存储随着 E 缩缩,计算随着 k 缩缩──这是2020年代最重要的扩展思路──

> **【拓展：DeepSeek-V3 的 MoE 创新】**تمتلك DeepSeek-V3  671B  إجمالي العناصر ولكن كل رمز فقط تنشط 37B  من خلال 256 خبيراً في طريق + 1 خبيراً في المشاركة لتحقيقها. كما أنّها قدمت استراتيجية توازن الحمل غير ذات صلة بالخسارة المُساعدة، وتجنب انهيار مسارات MoE التقليدية. في قائمة التحليل الاصطناعي، تمتلك DeepSeek-V3 أقل من واحد عشرة من تكاليف التفكير GPT-4 لتحقيق أداء قابل للمقارنة.

## المفهوم الأساسي

![MoE layer: router selects k of E experts per token](../assets/moe.svg)

### تبادل FFN

كتلة محول كثيفة:

> 密 صبغات:

```
h = x + attn(norm(x))
h = h + FFN(norm(h))
```

حظر المياه

```
h = x + attn(norm(x))
scores = router(norm(h))              # (N_tokens, E)
top_k = argmax_k(scores)              # pick k of E per token
h = h + sum_{e in top_k}(
        gate(scores[e]) * Expert_e(norm(h))
    )
```

كل خبير هو FFN مستقل (عادة SwiGLU). الجهاز التوجيهي هو طبقة خطية واحدة. كل رمز يختار نفسه `k`ويحصل على مزيج مغلق من نتائجهم.

> كل متخصص هو FFN مستقلة ((عادة SwiGLU)  الممرات هي طبقة واحدة  كل رمز  اختيار الخاص بك `k`المهتمين، الحصول على إصداراتها المختلطة.

### مشكلة توازن الحمل

إذا قام الجهاز بتسجيل 90% من الرموز عبر الخبير 3 ، فإن الخبراء الآخرين يضيعون

> إذا كان المجهاز سيقوم بتوزيع 90% من الرمز على الخبراء الثلاثة، فإن الخبراء الآخرين سوف "جوعون"

1. **Auxiliary load-balancing loss**إضافة عقوبة متناسبة مع التباين في استخدام الخبراء يعمل، ولكن يضيف مُعدل فائض و إشارة تراجعة ثانية.
   中文翻译:**辅助负载均衡损失**(تحول المتحول  مختلط)  إضافة مع الخبراء استخدام فوارس النسبة العقاب‬‬ فعالة، ولكن زيادة العوامل والإشارات الثانية درجة‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬
2. **Expert capacity + token dropping**كل معالج عمل على الأكثر`C × N/E`الوهم، الوهم المفرط يخطو الطبقة، يؤذي الجودة.
   中文翻译:**专家容量 + token 丢弃**(مبدء مبكر) ✿ كل متخصص`C × N/E`个标志;溢出的标志 跳过该层――损害质量――
3. **Auxiliary-loss-free balancing**(DeepSeek-V3). إضافة تحيز تعلم لكل خبير الذي يغير اختيار الموجّه أعلى-ك. يتم تحديث التحيز خارج خسارة التدريب. لا جريمة على الهدف الرئيسي. فتح كبير 2024.
   中文翻译:**辅助损失无关均衡**(DeepSeek-V3)── إضافة إلى تعلم إلى حد ذاته، إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة إلى إضافة

نهج DeepSeek-V3: بعد كل خطوة تدريبية، لكل خبير، تحقق ما إذا كان استخدامها فوق الهدف أو أقل منه.`±γ`. استخدامات اختيار`scores + bias`الاحتمالات الخبيرة المستخدمة في إغلاق البوابات هي الخام`scores`غير متغيرة، تفصل التوجيه عن التعبير

> طريقة DeepSeek-V3: بعد كل خطوة تدريبية، فحص كل متخصص ما إذا كان استخدامها مرتفعًا أو أقل من الهدف.`±γ` اختيار استخدام `scores + bias` احتمالية استخدام المختصين في التحكم في المجال غير متغيرة`scores`将路由与表达解──

### خبراء مشتركون

تقسم DeepSeek-V2/V3 الخبراء أيضًا إلى * مشاركة * و * توجيه. تمر كل رمز عبر جميع الخبراء المشتركين. يتم اختيار الخبراء المشتركين من خلال top-k. يتم التقاط الخبراء المشتركين من المعرفة العامة. يتخصص الخبراء المشتركون. يتم تشغيل V3 بمشاركة الخبير 1 بالإضافة إلى 8 من 256 توجيه.

> DeepSeek-V2/V3 أيضاً سيتم تقسيم المختبرين إلى * المشاركة* و* الطريق* من فئتين. كل رمز تمر عبر جميع المختبرين المشتركين.

### خبراء في الحيوانات

كلاسيكية (GShard، Switch): كل خبير واسع مثل FFN كامل. `E`صغيرة (864) ، `k`هو صغير (12).

> 经典 MoE(GShard、Switch): كل متخصص مع FFN كاملة 一样宽──`E`较小(8-64) ،`k`较小(1-2)。

الحديثات الحميدة الحمقى (DeepSeek-V3، Qwen-MoE): كل خبير أصغر (1/8 حجم FFN). `E`هو كبير (256+) ، `k`أكبر (8+) نفس مجموع المعايير، ولكن الجمعيات تتحرك بسرعة أكبر. `C(256, 8) = 400 trillion`ويمكن أن يكون هناك "خبراء" لكل رمز، حيث ترتفع الجودة، والخمول يبقى ثابتًا.

> 现代细粒度 MoE(DeepSeek-V3、Qwen-MoE): كل متخصص أصغر(1/8 FFN`E`较大(256+) ،`k`أيضاً أكبر ((8+)。`C(256, 8) = 400 万亿`种可能的"专家"组合──质量提升,延迟不变──

> **【拓展：MoE 的路由崩塌问题】**التحديات الأساسية في تدريبات الـ MoE هي انهيار الطريق (إنهيار الـ راوتر) الجهاز يمكن أن يُوزع معظم الـ توكنات على عدد قليل من الخبراء، مما يؤدي إلى عدم وصول الخبراء الآخرين إلى التدريب.

### ملف التكلفة

لكل رمز، لكل طبقة:

> كل رمز، كل طبقة:

| Config | Active params / token | Total params |
|--------|-----------------------|--------------|
| 配置 | 每个 token 活跃参数 | 总参数量 |
| Mixtral 8×22B | ~39B | 141B |
| Llama 3 70B (dense) | 70B | 70B |
| DeepSeek-V3 | 37B | 671B |
| Kimi K2 (MoE) | ~32B | 1T |

ديب سيك-ف3 يضرب Llama 3 70B (كثيفة) على كل مقياس تقريباً أثناء القيام بذلك**fewer active FLOPs per token**. المزيد من المعايير = المزيد من المعرفة. أكثر فعالية FLOPs = أكثر حسابات لكل رمز.

> في كل اختبار، فاز DeepSeek-V3 على إطلاق النار على Llama 3 70B**每个 token 的活跃 FLOPs 更少**△更多参数 = 更多知识──更多活跃 FLOPs = كل رمز 更多计算──MOE 将两者解──

### الصيد: الذاكرة

جميع الخبراء يعيشون على GPU بغض النظر عن أي واحد يطلق. تحتاج نموذج 671B إلى ~ 1.3 TB من VRAM لوزن fp16.

> جميع الخبراء بغض النظر عن إمكانية تنشيطهم يقيمون على GPU. نموذج 671B يحتاج إلى حوالي 1.3TB من fp16 权重显存.

> **【中文解读】**الوزن الأساسي لـ MoE: باستخدام الحسابات المتغيرة من الذاكرة. يصل العنصر النشط للـ DeepSeek-V3 باستخدام 37B إلى أداء نموذج أكثر من 70B ، ولكن يحتاج إلى 1.3TB من الاحتفاظ الأساسي من جميع الخبراء. وهذا دفع إلى تطوير تقنية الموازاة الخبراء.

> **【拓展：细粒度专家 vs 粗粒度专家】**传统 MoE(Switch Transformer) استخدام عدد قليل من المهنيين الكبار(E=8-64)。现代细粒度 MoE(DeepSeek-V3) استخدام عدد كبير من المهنيين الصغار(E=256+), كل المهني فقط 1/8 من FFN 宽度──组合数 C(256,8) 约为40000000000种,远超粗粒度的组合空间──质量提升显著,延迟基本不变──

## بناء ذلك تحرك لتحقيق
```figure
expert-routing
```

## بناءها

انظر`code/main.py`طبقة موكسيومة صغيرة من الصفراء النقية مع:

> 参见 `code/main.py` مستوى معين من المعايير المطبقة، يتضمن:

- `n_experts=8`خبراء في المجموعة المتحركة (حجمهم خطي واحد، للإشارة)
  中文翻译:`n_experts=8`个类 SwiGLU 专家( كل واحد 线性层, للمثليات)
- التوجيه العلوي k=2
  中文翻译:أعلى-ك=2 路由
- أوزان المفاتيح المعتادة لـ softmax
  中文翻译:softmax 归一化门控权重
- التوازن الخالي من الخسائر المساعدة عبر التحيز لكل خبير
  中文翻译: من خلال تحديد المهارات لتحقيق التوازن

### الخطوة الأولى: جهاز التوجيه

```python
def route(hidden, W_router, top_k, bias):
    scores = [sum(h * w for h, w in zip(hidden, W_router[e])) for e in range(len(W_router))]
    biased = [s + b for s, b in zip(scores, bias)]
    top_idx = sorted(range(len(biased)), key=lambda i: -biased[i])[:top_k]
    # softmax over ORIGINAL scores of the chosen experts
    chosen = [scores[i] for i in top_idx]
    m = max(chosen)
    exps = [math.exp(c - m) for c in chosen]
    s = sum(exps)
    gates = [e / s for e in exps]
    return top_idx, gates
```

التشوه يؤثر على الاختيار، وليس وزن البوابة. هذا هو خدعة DeepSeek-V3  التشوه تصحيح عدم توازن الحمل دون توجيه توقعات النموذج.

> 偏置影响选择,不影响门控制权重──这是 DeepSeek-V3 技巧偏置纠正负载不平衡,但不干预模型的预测──

### الخطوة الثانية: تشغيل 100 رمز عبر جهاز التوجيه

تتبع ماهية خبراء النار كم مرة. بدون التحيز، الاستخدام منحرف. مع حلقة تحديث التحيز (`-γ`لخبراء يستخدمونها بشكل مفرط`+γ`(بالإضافة إلى استخدامات غير مستخدمة) ، يتقارب الاستخدام إلى توزيع موحد على مدى عدة تكرارات.

> تتبع أي متخصصين تم تنشيطهم عدة مرات. لا يوجد إعادة تعيين، لا يتساوى استخدامهم.`-γ`, استخدام غير كاف`+γ`), استخدام الكمية في عدة أجيال بعد الاستلام إلى متوسط التوزيع.

### الخطوة 3: مقارنة عدد المعلمات

طبع "موازي الكثافة" من إعداد MoE. DeepSeek-V3-شكل: 256 توجيه + 1 مشاركة، 8 نشطة، d_model=7168. إجمالي عدد المعايير هو عيون. العدد النشط هو سبعة من كثافة Llama 3 70B.

> 印印 MOE 配置的"密等价"──DeepSeek-V3 形状:256 个路由 + 1 个共享,8 个活跃,d_model=7168──总参数数令人惊叹──活跃参数数只有密 拉玛 3 70B 的七分之一──

## استخدمها في إطار التنفيذ

تحميل المظهر

> "تقبيل الوجه"

```python
from transformers import AutoModelForCausalLM, AutoTokenizer
model = AutoModelForCausalLM.from_pretrained("mistralai/Mixtral-8x22B-v0.1")
```

2026 استنتاج الإنتاج: vLLM تدعم توجيه MoE بشكل طبيعي. SGLang لديه أسرع مسار متوازي خبير. كلا يتعاملان تلقائيًا مع اختيار top-k وموازي الخبراء.

> 2026 سنة توليد التقييم: vLLM 原生支持 MoE 路由──SGLang 拥有最快的专家并行路径──都自动处理顶级k 选择和专家并行──

**When to pick MoE:**
- تريد جودة الحدود بتكلفة أقل من التخمين لكل رمز
  ترجمة: تُريد أن تكون أقل من كل رمز
- لديك البنية التحتية الموازية لـ VRAM / الخبراء
  ترجمة: أنت لديك ما يكفي من البنية التحتية
- عبء عملك ثقيل من الشيكات (المحادثة، الرموز) وليس ثقيل من السياق (وثائق طويلة).
  中文翻译:你的工作负载是标志性密集型(聊天、代码) وليس على下文密集型(长文档) 👇

**When NOT to pick MoE:**
- نشر الحافة  تدفع تكاليف التخزين الكامل لأي FLOP نشط.
  中文翻译:边缘部署你要为任何活跃FLOP 支付全部储存──
- خدمة مستخدم واحد مهمة للثبات  توجيه الخبراء يضيف التكلفة العامة.
  ترجمة: تأخير حساسة للعملاء الأفراد
- النماذج الصغيرة (<7B)  ميزة الجودة في MoE تظهر فقط فوق عتبة الحساب (~ 6B أجزاء نشطة).
  中文翻译:小模型(<7B) MoE 质量优势只在计算值以上(约6B 活跃参数) 才会出现──

## أرسلها .

انظر`outputs/skill-moe-configurator.md`. تختار المهارة إيه، ك، ومشاركة الخبراء لتخطيط لوزارة التنمية الجديدة مع إعطاء ميزانية المعلمات، ورمز التدريب، والهدف التنفيذ.

> 参见 `outputs/skill-moe-configurator.md` هذه المهارة  حسب الميزانية  رمز التدريب  عدد و هدف التنفيذ،  اختيار الوزارة الجديدة  اختيار الوزارة و  مشاركة المهارات

## تمارين التدريب

1. **Easy.**أركض`code/main.py`شاهد كيف تحديث التحيز الخالي من الخسائر المساعدة يُساوى استخدام الخبراء أكثر من 50 تكرار
   中文翻译:运行 `code/main.py`◊ مشاهدة المعونة فقدان عدم الالتزام كيفية استخدام المخصص في 50 مرات
2. **Medium.**استبدل جهاز التوجيه المعلم بالجهاز التوجيه المستند إلى الهاش (الحدد، لا تعلم). مقارنة الجودة والتوازن. لماذا هو جهاز التوجيه المعلم أفضل؟
   中文翻译: باستخدام حاشة القاعدة على الحاجات الناشئة لبدل حاجات الناشئة للعلم
3. **Hard.**تنفيذ "التوجيه المماثل للتزويج" (ممارسة DeepSeek-V3.2): سجل ما يطلقه الخبراء أثناء الاستنتاج، وإجبار نفس التوجيه أثناء حساب التراجع. قياس التأثير على إعداد سياسة لعبة-تراجع.
                                                                                                                                                                                                                                                                 

## شروط الرئيسية

| Term | What people say | What it actually means |
|------|-----------------|-----------------------|
| 术语 | 通俗说法 | 实际含义 |
| Expert | "One FFN among many" | An independent feed-forward network; parameters dedicated to a sparse slice of the FFN computation. |
| 专家 | "众多 FFN 之一" | 独立的前馈网络；专用于 FFN 计算的稀疏切片的参数。 |
| Router | "The gate" | A tiny linear layer that scores each token against each expert; top-k selection. |
| 路由器 | "门控" | 一个小线性层，对每个 token 与每个专家打分；top-k 选择。 |
| Top-k routing | "k active experts per token" | Each token's FFN computation goes through exactly k experts, weighted by gate. |
| Top-k 路由 | "每个 token 激活 k 个专家" | 每个 token 的 FFN 计算经过恰好 k 个专家，按门控加权。 |
| Auxiliary loss | "Load-balance penalty" | Extra loss term that penalizes skewed expert usage. |
| 辅助损失 | "负载均衡惩罚" | 惩罚专家使用不均衡的额外损失项。 |
| Auxiliary-loss-free | "DeepSeek-V3's trick" | Balance via per-expert bias on the router's selection only; no extra gradient. |
| 辅助损失无关 | "DeepSeek-V3 的技巧" | 仅通过路由器选择上的逐专家偏置实现均衡；无额外梯度。 |
| Shared expert | "Always on" | Extra expert through which every token passes; captures common knowledge. |
| 共享专家 | "始终开启" | 每个 token 都通过的额外专家；捕获通用知识。 |
| Expert parallelism | "Shard by expert" | Distribute different experts to different GPUs; route tokens across the network. |
| 专家并行 | "按专家分片" | 将不同专家分配到不同 GPU；通过网络路由 token。 |
| Sparsity | "Active params < total params" | The ratio `k × expert_size / (E × expert_size)`; 37/671 ≈ 5.5% for DeepSeek-V3. |
| 稀疏性 | "活跃参数 < 总参数" | 比率 `k × expert_size / (E × expert_size)`；DeepSeek-V3 为 37/671 ≈ 5.5%。 |

## المزيد من القراءة

- [Shazeer et al. (2017). Outrageously Large Neural Networks: The Sparsely-Gated Mixture-of-Experts Layer](https://arxiv.org/abs/1701.06538)الفكرة
  中文翻译:مؤسيسة الوطنية
- [Fedus, Zoph, Shazeer (2022). Switch Transformer: Scaling to Trillion Parameter Models with Simple and Efficient Sparsity](https://arxiv.org/abs/2101.03961)-التبديل، كلاسيكي
  中文翻译:تحول المتحول،经典的MOE 论文。
- [Jiang et al. (2024). Mixtral of Experts](https://arxiv.org/abs/2401.04088) مخلوط 8 × 7B.
  中文翻译:مختلطة 8×7B 论文。
- [DeepSeek-AI (2024). DeepSeek-V3 Technical Report](https://arxiv.org/abs/2412.19437) MLA + MoE + MTP بدون خسائر مساعدة.
  中文翻译:DeepSeek-V3 技术报告,MLA + 辅助损失无关 MoE + MTP。
- [Wang et al. (2024). Auxiliary-Loss-Free Load Balancing Strategy for Mixture-of-Experts](https://arxiv.org/abs/2408.15664)ورقة التوازن القائمة على التحيز
  中文翻译: على أساس تحديد التوازن
- [Dai et al. (2024). DeepSeekMoE: Towards Ultimate Expert Specialization in Mixture-of-Experts Language Models](https://arxiv.org/abs/2401.06066) المختبر المشترك المشترك المشترك المشترك المشترك المشترك المشترك المشترك
  中文翻译:DeepSeekMoE 论文,细粒度 + 共享专家拆分──
- [Kim et al. (2022). DeepSpeed-MoE: Advancing Mixture-of-Experts Inference and Training](https://arxiv.org/abs/2201.05596) ورقة خبراء مشتركة أصلية.
  中文翻译:DeepSpeed-MoE 原始共享专家论文──
