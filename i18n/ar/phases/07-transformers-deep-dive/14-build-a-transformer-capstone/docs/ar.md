# بناء محول من الصفر  الحجر الرئيسي  من الصفر بناء محول  毕业项目

> ثلاث عشرة درساً، نموذج واحد، لا اختصار

> **【中文解读】**整合所有知识,从零实现完整的 GPT架构──这是本阶段的核心实践理解这个,你就能读懂任何变压器的代码──

**Type:** Hands-on | **类型:** 动手
**Language:**" بايثون "**语言:**بايثون
**Prerequisites:** Phase 7 · 01 through 13. Don't skip. | **前置知识:** Phase 7 · 01 through 13. Don't skip.
**Time:** ~120 minutes | **时间:** ~120 分钟

## المشكلة المشكلة المشكلة

لقد قرأت كل ورقة، لقد وضعت الانتباه، شقق متعددة الرؤوس، تشفيرات المواقع، حواجز تشفير و تشفير، فقدان BERT و GPT، MoE، KV cache. الآن اجعلهم يعملون معا على مهمة حقيقية.

> لقد قرأت كل مقالة. لقد تحقق الاهتمام. لقد تمت تفكيكها. لقد تمت تعديلها. لقد تمت تعديلها. لقد تمت تعديلها.

الحجر الرئيسي: تدريب محول صغير فقط للكشف من نهاية إلى نهاية على مهام نمذجة لغة على مستوى الشخصيات. يقرأ شكسبير. يولد شكسبير جديد. هو صغير بما يكفي لتدريب على جهاز كمبيوتر محمول في أقل من 10 دقائق. هو صحيح بما فيه الكفاية أن تبادل مجموعة بيانات أكبر وتدريب أطول يحصل لك LM الحقيقي.

> 毕业项目: 在字符级语言建模任务上端到端训练一个小型解码器专用变压器──它读取莎士比亚,生成新的莎士比亚──它足够小,可以在笔记本上完成训练在10分钟内──它足够正确,转换成更大的数据集和更长的训练时间就能得到真正的语言模型──

هذا هو "nanoGPT" من الدورة. انها ليست أصلية  Karpathy 2023 دراسة nanoGPT هو التنفيذ المرجعية كل طالب يكتب مرة واحدة على الأقل. نحن رفع الشكل وإعادة تشكيل حول ما قمنا بتغطيته.

> هذا هو "البرنامج النانو جي بي تي" ــ إنه ليس من أصل كارباتي 2023 تعليمي نانو جي بي تي هو كل طالب يكتب على الأقل مرة واحدة من المرجح تحقيقها ــ لقد استعيرنا بنيته، ووفقا لما تعلمناه تم تغييرها ــ

> **【中文解读】**هذا المشروع التخرج سوف يبدأ في 13 节 من جميع المعرفة التكامل: رسم الخطية درجة اللغة بناء التوجهات التوضيحية التوضيحات وضع وضع وضع المواقع الرقمية المعتادة والعدد من الأسباب الاهتمام السريع FFN والعجزات الاتصال، تدريب واحد يمكن أن يتم الانتهاء من شيكسبير في 10 دقائق على دفتر النتائج.

> **【拓展：从 nanoGPT 到生产级 LLM】**كارباتي نانو جي بي تي هو أفضل نقطة بداية للتعلم من تحويل. التفاوت الرئيسي في درجة الإنتاج LLM من نانو جي بي تي إلى حجم البيانات.

## المفهوم الأساسي

![Transformer-from-scratch block diagram](../assets/capstone.svg)

الهندسة المعمارية، تلاحظ:

> 架构,带注释:

```
input tokens (B, N)
   │
   ▼
token embedding + positional embedding  ◀── Lesson 04 (RoPE option)
   │
   ▼
┌──── block × L ────────────────────┐
│  RMSNorm                          │  ◀── Lesson 05
│  MultiHeadAttention (causal)      │  ◀── Lesson 03 + 07 (causal mask)
│  residual                         │
│  RMSNorm                          │
│  SwiGLU FFN                       │  ◀── Lesson 05
│  residual                         │
└────────────────────────────────── ┘
   │
   ▼
final RMSNorm
   │
   ▼
lm_head (tied to token embedding)
   │
   ▼
logits (B, N, V)
   │
   ▼
shift-by-one cross-entropy            ◀── Lesson 07
```

### ما نُرسل

> نحن نقدم المحتوى:

- `GPTConfig` مكان واحد لتكوين جميع المعايير المضادة.
  中文翻译:`GPTConfig` مكان لتصميم جميع المعايير العالية
- `MultiHeadAttention` سببية، دفعة، مع طريق اختياري في نمط فلاش (PyTorch's `scaled_dot_product_attention`)
  中文翻译:`MultiHeadAttention` 因果的、批量,可选闪光 风格路径(PyTorch 的 `scaled_dot_product_attention`(‬)
- `SwiGLUFFN` FFN الحديث.
  中文翻译:`SwiGLUFFN` 现代 FFN。
- `Block` الاهتمام المسبق للطبيعة، والغلفة المتبقية + FFN.
  中文翻译:`Block` قبل التأثير، تركيزات التركيز + FFN
- `GPT` التوابل، الكتل المكتبة، رأس LM، توليد (().
  中文翻译:`GPT` 嵌入、堆叠块、LM 头、生成()
- حلقة تدريب مع آدم و، كوسين LR، تراجع تراجع.
  中文翻译:带 AdamW、余弦学习率、梯度裁剪的训练循环──
- إشارة على مستوى "كار" على نص "شكسبير".
  中文翻译:شيكسبير 文本上的字符级分词器──

> **【中文解读】** كامل GPT 实现包含:配置类型、多头因果注意力(可选闪点注意)、SwiGLU FFN、前归归一化残差块、完整 GPT 模型类型(嵌入 + 堆叠块 + LM 头 + 生成函数)、AdamW + 余弦学习率训练循环──为了简洁, استخدم learnstyle位置嵌入(而不是 RoPE) ولم ينجز KV 缓存, ولكن التمرين يتطلب منك إضافة هذه──

### ما لا نُرسل

> نحن لا نقدم محتوى:

- يستخدم الممارسة المميزة المميزة (ROPE) في الدروس 04. هنا نستخدم التوابل المميزة المكتسبة من أجل البساطة.
  中文翻译:RoPE  在第 04 课有概念实现──这里为简洁起见使用学习式位置嵌入──练习要求你替换为RoPE──
- كش كيف خلال الجيل  كل خطوة جيل يعيد حساب الاهتمام على المسبق الكامل. أبطأ ولكن أبسط. التمارين تطلب منك إضافة كش كيف.
  中文翻译:生成时的KV 缓存  كل خطوة في الابتداع على كامل الاول 重新计算注意力──更慢但更简单──练习要求你添加KV 缓存──
- الاهتمام الفلاش  PyTorch 2.0+ إرسالات تلقائية إذا كانت المدخلات تتطابق ؛ نستخدم `F.scaled_dot_product_attention`. . .
  中文翻译:فلاش انتباه  PyTorch 2.0+ 在输入匹配时自动分派;我们使用 `F.scaled_dot_product_attention`.
- الـ (مـو)  واحد FFN لكل كتلة. لقد رأيت الـ (مـو) في الدروس 11.
  中文翻译:مؤ  كل بلاك واحد FFN──你在第11 课见过مؤ

### المقاييس المستهدفة

على جهاز كمبيوتر محمول Mac M2، 4 طبقات، 4 رأس، d_model=128 GPT تدرب على 2000 خطوة على `tinyshakespeare.txt`:

> على Mac M2 笔记本上,4 层、4 头、d_model=128 من GPT `tinyshakespeare.txt`上训练 2000 步:

- فقدان التدريب يتقارب من ~ 4.2 (الصدفة) إلى ~ 1.5 في حوالي 6 دقائق.
  ترجمة: تدريب خسارة من حوالي 4.2 ((随机) في حوالي 6 دقائق في وصول إلى حوالي 1.5
- النتائج المختارة تبدو شكسبير: الكلمات القديمة، وقطع الخطوط، الأسماء الخاصة مثل "روميو:" تظهر.
  中文翻译:采样输出看起来像莎士比亚:古词、换行、"ROMEO:"等专名词出现──
- فقدان الفائدة (حفاظ على 10% الأخير من النص) تتبع خسارة التدريب عن كثب، لا يوجد أي مبالغ في هذا الحجم / الميزانية.
  الصينية: تجربة خسارة (بقي على الـ 10% الأخيرة)

> **【拓展：从字符级到子词级 Tokenizer】**هذا المشاريع تستخدم رمزية درجة الوهمية (BPE) بسهولة ولكن منخفضة النفع.

## بناء ذلك تحرك لتحقيق
```figure
n5-block-stack
```

## بناءها

هذه الدروس تستخدم PyTorch.`torch`(إنشاء المركز المركزي جيد) انظر`code/main.py`السيناريو يدير:

> 本课使用 PyTorch──安装 `torch`(CPU 版本即可)`code/main.py`❖ 該脚本處理:

- التنزيل`tinyshakespeare.txt`إذا لم تظهر (أو قرأت نسخة محلية).
  中文翻译: لو缺失则下载 `tinyshakespeare.txt`(أو قرأت مقالنا)
- إشارة البايت على مستوى البايت
  中文翻译:字节级字符分词器──
- التقسيم بين القطار والقطار عند 90/10.
  中文翻译:90/10 的训练/验证分割──
- حلقة تدريب مع bf16 التلقائي على الأجهزة المدعومة.
  中文翻译:支持硬件上的 bf16 自动混合精度训练循环──
- تمت اختبار العينات بعد التدريب
  中文翻译: تمرين بعد الانتهاء من采样。

### الخطوة الأولى: البيانات

```python
text = open("tinyshakespeare.txt").read()
chars = sorted(set(text))
stoi = {c: i for i, c in enumerate(chars)}
itos = {i: c for c, i in stoi.items()}
encode = lambda s: [stoi[c] for c in s]
decode = lambda xs: "".join(itos[x] for x in xs)
```

65 حرفاً فريدة، مخزون لغات صغير، يتناسب بحجم الكلمات البيتين، لا توجد حروف إضافية، لا توكنيزر درامي.

> 65 个唯一字符──微型词表──适配 4 字节词大小──没有BPE,没有分词器的麻烦──

### الخطوة الثانية: النموذج

انظر`code/main.py`. الكتل هي كتاب دراسي من الدروس 05  قبل القاعدة، RMSNorm، SwiGLU، MHA السببية.

> 参见 `code/main.py`◊ هذا المكون هو الدروس في الدورة 05 ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬ ‬

### الخطوة الثالثة: حلقة التدريب

الحصول على مجموعة عشوائية من طول 256 نافذة رمزية للأمام، التحولات المتقاطعة واحد، الخلفية، خطوة آدم و.سجل، تكرر.

> 获取随机批量长度为 256 标志窗口──前向传播──偏移一位的交叉──反向传播──AdamW 步进──记录──重复──

```python
for step in range(max_steps):
    x, y = get_batch("train")
    logits = model(x)
    loss = F.cross_entropy(logits.view(-1, vocab_size), y.view(-1))
    loss.backward()
    torch.nn.utils.clip_grad_norm_(model.parameters(), 1.0)
    opt.step()
    opt.zero_grad()
```

### الخطوة الرابعة: العينة

عند إعطاء طلب، مراراً وتكراراً، عينة من علامات "أعلى" ، وإضافة، واستمر. توقف بعد 500 رمزاً.

> 给定一个提示,反复前向传播,从顶部p 采样,追加,继续──500 个标志 后停止──

### الخطوة 5: قراءة الخروج

بعد 2000 خطوة:

> 2000 步后:

```
ROMEO:
Away and mild will not thy friend, that thou shalt wit:
The chief that well shame and hath been his friends,
...
```

ليس شكسبير، ولكن شكسبير، فوز واضح لـ 800 ألف برميل و 6 دقائق على جهاز كمبيوتر محمول

> ليس شكسبير ولكن مثل شكسبير حوالي 800 ألف شخص على دفتر اليد 6 دقائق من الفوز الواضح

## استخدمها في إطار التنفيذ

هذه الحجر الرئيسي هو معمارة مرجعية ثلاثة امتدادات لنشرها إلى شيء حقيقي:

> هذا المشروع هو بنية مرجعية. يمكن أن يُوسع إلى أن يصبح نظامًا مفيدًا حقًا:

1. **Swap the tokenizer.**استخدم بيبي (مثل `tiktoken.get_encoding("cl100k_base")`يرتفع حجم الكلمات من 65 إلى ~ 50,000. تحتاج القدرة النموذجية إلى التوسع للتعويض.
   中文翻译:**替换分词器。**استخدام BPE`tiktoken.get_encoding("cl100k_base")`)── الكلمات العلامة الكبيرة من 65  قفز إلى حوالي 50,000──
2. **Train on a bigger corpus.**استخدام`OpenWebText`أو`fineweb-edu`10B رموز على A100 واحد يستغرق حوالي 24 ساعة ل 125M-param GPT.
   中文翻译:**在更大的语料上训练。**استخدام `OpenWebText`أو`fineweb-edu`(HuggingFace) ―― 在单张 A100 上用10B代币 训练 125M 参数 GPT 约需24小时──
3. **Add RoPE + KV cache + Flash Attention.**تمارين أدناه ستقوم بتجريبهما
   中文翻译:**添加 RoPE + KV 缓存 + Flash Attention。**التدريبات التالية ستساعدك على إنجاز كل خطوة

ينتهي هذا الأمر باعتباره GPT بمعيار 125M الذي يولد اللغة الإنجليزية السريعة. ليس نموذجًا حدوديًا. ولكن نفس مسار الشفرة  أكبر فقط  هو ما يستخدمه كارباتي، إيليوثرااي، ومعهد ألين لتدريب نقاط التفتيش للبحوث في عام 2026.

> في النهاية حصلت على واحد يمكن أن تنتج 125M 参数 GPT في اللغة الإنجليزية. ليس على النحو الأمامي. ولكن نفس الشكل من الطرق  مجرد أكبر  هو كارباتي  الاي الاي و الين معهد في 2026 تعليمة البحث والتحقق المستخدمة.

> **【拓展：Karpathy 的 nanoGPT 与教育意义】**أندريه كارباتي نانو جي بي تي ((2023) هو واحد من أكثر التدريس تأثيرا في تاريخ التعليم الذكاء الاصطناعي.

## أرسلها .

انظر`outputs/skill-transformer-review.md`. المهارة تدرس تنفيذ محول من الصفر لتحقيق الدقة في جميع الدروس السابقة الثلاثة عشر.

> 参见 `outputs/skill-transformer-review.md` هذه المهارة  مراجعة تحويلات من الصفر إلى الصفر  التحقق من صحة جميع المواد 13

## تمارين التدريب

1. **Easy.**أركض`code/main.py`تأكد من أن خسارة التحقق من التحقق من النموذج الذي تدرب عليه في الخطوة الأخيرة أقل من 2.0. تغيير `max_steps`من 2000 إلى 5000  هل يواصل فقدان القيمة التحسن؟
   中文翻译:运行 `code/main.py` التحقق من أن الخسارة النهائية لموديل التدريب الخاص بك أقل من 2.0 `max_steps`هل ستواصل الخسائر في التحقق من 2000 إلى 5000؟
2. **Medium.**استبدل التوابل الموضعية المتعلمة بالروبي.`MultiHeadAttention`-توقفوا و تحققوا من انخفاض ضياع القيمة
   中文翻译:用 روپي 替换学习式位置嵌入──在 `MultiHeadAttention`معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل معدل
3. **Medium.**تنفيذ كاش كيه في حلقة العينات. توليد 500 رمز مع ودون كاش. يجب أن تتحسن الساعة الحائطية 520 × على جهاز كمبيوتر محمول.
   ترجمة: في حلقة الاختيار لتحقيق KV 缓存──有缓存和无缓存各生成 500 代币──笔记本应该有5-20 倍的实际时间改善──
4. **Hard.**إضافة رأس ثان إلى النموذج الذي يتوقع التوقعات التالية زائد واحد (MTP  التوقعات متعددة التوكن من DeepSeek-V3). تدريب المشترك. هل يساعد؟
   中文翻译:添加第二个头预测下下一个代币(MTP来自DeepSeek-V3的多代币 预测) ――联合训练──有帮助吗?
5. **Hard.**استبدل FFN الفردي لكل كتلة مع 4 خبراء MoE. راوتر + طريق 2 أعلى. انظر كيف تتغير فقدان val عند المماثلة المعلمات النشطة.
   中文翻译:将每块的单个FFN 替换为4 专家 MoE──路由器 + top-2 路由──观察在匹配活跃参数下验证损失的变化──

## شروط الرئيسية

| Term | What people say | What it actually means |
|------|-----------------|-----------------------|
| 术语 | 通俗说法 | 实际含义 |
| nanoGPT | "Karpathy's tutorial repo" | Minimal decoder-only transformer training code, ~300 LOC; the canonical reference. |
| nanoGPT | "Karpathy 的教程仓库" | 最小解码器专用 Transformer 训练代码，约 300 行；经典参考。 |
| tinyshakespeare | "The standard toy corpus" | ~1.1 MB of text; every character-LM tutorial since 2015 uses it. |
| tinyshakespeare | "标准玩具语料库" | 约 1.1 MB 文本；自 2015 年以来每个字符级语言模型教程都用它。 |
| Tied embeddings | "Share input/output matrix" | LM head weight = transpose of token embedding matrix; saves parameters, improves quality. |
| 绑定嵌入 | "共享输入/输出矩阵" | LM 头权重 = token 嵌入矩阵的转置；节省参数，提高质量。 |
| bf16 autocast | "Training precision trick" | Run forward/back in bf16, keep optimizer state in fp32; standard since 2021. |
| bf16 自动混合精度 | "训练精度技巧" | 前向/反向用 bf16，优化器状态用 fp32；2021 年以来的标准。 |
| Gradient clipping | "Stops spikes" | Cap global grad norm at 1.0; prevents training blowups. |
| 梯度裁剪 | "阻止尖峰" | 将全局梯度范数限制在 1.0；防止训练爆炸。 |
| Cosine LR schedule | "The 2020+ default" | LR ramps up linearly (warmup) then decays cosine-shaped to 10% of peak. |
| 余弦学习率调度 | "2020+ 默认" | 学习率线性升温（warmup）然后余弦衰减到峰值的 10%。 |
| MFU | "Model FLOP Utilization" | Achieved FLOPs / theoretical peak; 40% dense, 30% MoE is strong in 2026. |
| MFU | "模型 FLOP 利用率" | 实际 FLOPs / 理论峰值；2026 年稠密 40%、MoE 30% 是好的。 |
| Val loss | "Held-out loss" | Cross-entropy on data the model never saw; overfit detector. |
| 验证损失 | "留出损失" | 模型从未见过的数据上的交叉熵；过拟合检测器。 |

## المزيد من القراءة

- [The Annotated Transformer (Harvard NLP)](https://nlp.seas.harvard.edu/annotated-transformer/) التنفيذ الكلاسيكي الملاحظ.
  中文翻译:哈佛 NLP 的注解版 تحويل،经典参考实现──
