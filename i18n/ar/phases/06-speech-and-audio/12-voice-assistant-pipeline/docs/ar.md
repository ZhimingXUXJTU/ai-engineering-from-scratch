# بناء خط أنابيب مساعد الصوت  المرحلة 6 كابستون  بناء语音助手流水线  阶段 6 毕业项目

> كل شيء من الدروس 01-11 ، تم خياطة كل شيء معاً. بناء مساعد صوتي يستمع ، ويعبر ، ويتحدث مرة أخرى. في عام 2026 هذه مشكلة هندسية حل ، وليس مشكلة بحثية  ولكن تفاصيل التكامل تقرر ما إذا كانت تسير.

> **【中文解读】**وضع كل المواد في الدرس، وبناء مساعد صوت قادر على سماع، والفكر، والتكلم. في عام 2026، هذه مشكلة هندسية حلّتها، وليس مشكلة دراسة.

**Type:** Build | **类型:** 构建
**Languages:** Python | **语言:** Python
**Prerequisites:** Phase 6 · 04, 05, 06, 07, 11; Phase 11 · 09 (Function Calling); Phase 14 · 01 (Agent Loop) | **前置知识:** 阶段 6 · 04、05、06、07、11；阶段 11 · 09（函数调用）；阶段 14 · 01（智能体循环）
**Time:** ~120 minutes | **预计用时:** ~120 分钟

## المشكلة المشكلة المشكلة

بناء مساعد من نهاية إلى نهاية:

> 构建一个端到端助手:

> **【中文解读】**السؤال الذي يطرحه هذا القسم هو: كيف يمكن فهم هذه التقنية بشكل صحيح وتطبيقها في التجهيز العملي. فهم السياق يساعد على فهم النوع المختص من التكنولوجيا. في النظام الواقعي الذكاء الاصطناعي، فإن التكنولوجيا الخطأ غالباً ما تكون أكثر تكلفة من التنفيذ التفصيلي.

1. يلتقط المدخلات الميكروفونية (16 كيلوهرتز)
   捕获麦克风输入(16 كيلوهرتز 单声道)
2. يكتشف بداية/نهاية خطاب المستخدم.
   检测用户语音的开始/结束──
3. ينسخ التدفق
   流式转录──
4. يمر النسخة إلى ماجستير في العلوم التي يمكن أن تدعو الأدوات (تايمر، الطقس، التقويم).
   سوف يحول المقالة إلى الـ LLM (التي يمكن تدوينها)
5. تُشغل رسالة ماجستير في العلوم إلى TTS
   أن يكون الجامعة 文本流式传输给TTS.
6. يعيد الصوت إلى المستخدم
   إلى المستخدم播放音频。
7. يتوقف إذا قام المستخدم بتقاطع الرد في منتصفها.
   إذا كان المستخدم في رد فعل توقف

هدف التأخير: أول بايت صوتي TTS في غضون 800 ميس من الانتهاء من المستخدم من التعبير على جهاز كمبيوتر محمول. هدف الجودة: لا كلمات مفقودة، لا ترجمة الهلوسة على الصمت، لا تسرب من نسخ الصوت، لا نجاح في الحقن السريع.

> 延迟目标:在笔记本CPU 上用户说完话后 800 ms 内发出第一 TTS 音频字节──质量目标:不漏词、静音不产生幻觉字幕、无声音克隆泄漏、提示注入不成功──

## المفهوم الأساسي

> **【中文解读】**هذا المقطع يعرض المفاهيم والنظريات الأساسية. فهم هذه المفاهيم هو شرط لتحقيق التنفيذ التالي، وكذلك النقاط المعرفة في المقابلة والممارسة التجريبية.


![Voice assistant pipeline: mic → VAD → STT → LLM+tools → TTS → speaker](../assets/voice-assistant.svg)

### المكونات السبعة

1. **Audio capture.**ميكروفونات → 16 كيلوهرتز مونو → 20 ميس قطعة. عادة `sounddevice`في Python أو AudioUnit/ALSA/WASAPI الأصلي في الإنتاج.
   **音频捕获。**麦克风 → 16 كيلوهرتز 单声道 → 20 ms 块──Python 中通常用 `sounddevice`, توليد البيئة مع الأصل AudioUnit/ALSA/WASAPI
2. **VAD (Lesson 11).**سيلرو فاد @ عتبة 0.5، تقرير الدقيقة 250 ms، صمت تعليق 500 ms. إشارات "بدء" و "نهاية".
   **VAD（第 11 课）。**Silero VAD @ 值 0.5, أدنى语音 250 ms,静音持续 500 ms──信号"开始"和"结束"──
3. **Streaming STT (Lesson 4-5).**التدفقات الهزيمة، Parakeet-TDT، أو Deepgram Nova-3 (API). النسخ الجزئية + النهائية.
   **流式 STT（第 4-5 课）。**التهريب الهزئ ‧باراكيت-تي دي تي أو ديبجرام نوفا-3 ‧ إبي) ‧ جزء + 最终转录──
4. **LLM with tool calling.**GPT-4o / Claude 3.5 / Gemini 2.5 Flash. مخطط JSON للأدوات. رموز التدفق.
   **带工具调用的 LLM。**GPT-4o / Claude 3.5 / Gemini 2.5 Flash──工具的 JSON schema──流式代币──
5. **Streaming TTS (Lesson 7).**كوكورو-82M (أسرع فتح) أو كارتيسيا سونيك (التجارية). بدء TTS بعد 20 رمزا LLM.
   **流式 TTS（第 7 课）。**كوكورو-82M ((أسرع من المفتوحات) أو كارتيسيا سونيك ((التجارة))
6. **Playback.**المتكلم خارج، رمزية للشبكات ذات النطاق النطاق المنخفض.
   **回放。**扬声器输出;低带宽网络用 opus 编码。
7. **Interruption handler.**إذا أطلقت (فاد) أثناء تشغيل (تيتس) ، توقف تشغيل (تيتس) ، إلغ (اللم) ، إعادة تشغيل (ستيتس)
   **打断处理器。**إذا كان TTS 播放期间 VAD 触发, توقف播放、取消 LLM、重启 STT──

### الوضعين الثلاثة التي ستضربها

> ### ستواجهين ثلاثة أنماط من الفشل

1. **First-word clip.**يبدأ VAD ضربة متأخرة جداً، "هي" المستخدم مفقود، حد البدء عند 0.3، وليس 0.5.
   **首词截断。**VAD 启动晚一拍──用户的""丢失──起始值用0.3而不是0.5──
2. **Mid-response interrupt confusion.**يواصل LLM توليد بعد انقطاع المستخدم؛ يتحدث المساعد على المستخدم.
   **回应中打断混乱。**المستخدم ‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬‬
3. **Silence hallucination.**"تخرج النصائح "شكراً على مشاهدتك على الإطار الصامت للتدفئة
   **静音幻觉。**فـســـــســــــــــــــــــــــــــــــســــــــــــــــــســــــــــــــــــــــــســــــــــــــــــــــــــــســــــــــــــــــــــــــــــــــــــســــــــــــــــــــــــــــــــــــــــــــســـــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــ

### 2026 مستويات الإشارة الإنتاجية

| Stack | Latency | License | Notes |
|-------|---------|---------|-------|
| LiveKit + Deepgram + GPT-4o + Cartesia | 350-500 ms | commercial API | Industry default 2026 |
| Pipecat + Whisper-streaming + GPT-4o + Kokoro | 500-800 ms | mostly open | DIY-friendly |
| Moshi (full-duplex) | 200-300 ms | CC-BY 4.0 | Single-model; different architecture, lesson 15 |
| Vapi / Retell (managed) | 300-500 ms | commercial | Fastest to launch; limited customization |
| Whisper.cpp + llama.cpp + Kokoro-ONNX | offline | open | Privacy / edge |

| 技术栈 | 延迟 | 许可 | 备注 |
|--------|------|------|------|
| LiveKit + Deepgram + GPT-4o + Cartesia | 350-500 ms | 商业 API | 2026 行业默认 |
| Pipecat + Whisper-streaming + GPT-4o + Kokoro | 500-800 ms | 多数开源 | DIY 友好 |
| Moshi（全双工） | 200-300 ms | CC-BY 4.0 | 单模型；不同架构，第 15 课 |
| Vapi / Retell（托管） | 300-500 ms | 商业 | 最快上线；定制有限 |
| Whisper.cpp + llama.cpp + Kokoro-ONNX | 离线 | 开源 | 隐私/边缘 |

> **【中文解读】**هذا المقطع من خلال الكود من الصفر لتحقيق الخوارزمية النووية. هذا النوع من "من الصفر" يمكن أن يساعد على فهم المبدأ الخلفي للإطار، عندما يواجهون مشكلة لن يتم تعقلها في الصندوق الأسود.

> **【拓展：语音 AI 的产品化】**تقنية الصوت تواجه تحديات فريدة في مجال تصنيع المنتجات: مختلفة النطق، ضوضاء الخلفية، الالتقاط بعيداً، الكثير من الناس يتحدثون، وما إلى ذلك.

> **【拓展：多语言语音技术】**الاختلافات الكبيرة في الاختلافات بين الصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت والصوت.

> **【拓展：语音隐私与安全】**语音数据包含大量个人隐私信息 (صوت纹、对话内容) ◦深度伪造 (عمق مزيف) 语音技术 يمكن استخدامها للاختراق (احتيال) 音频水印 (آويو المياه) 及声纹反欺诈 (الاحتيال) ◦ مكافحة الاحتيال (الاحتيال) ◦





## بناء ذلك تحرك لتحقيق
```figure
v4-voice-latency
```

## بناءها

### الخطوة الأولى: التقاط الميكروفون مع التقطيع (مخططات مزيفة)

```python
import sounddevice as sd

def mic_stream(chunk_ms=20, sr=16000):
    q = queue.Queue()
    def cb(indata, frames, time, status):
        q.put(indata.copy().flatten())
    with sd.InputStream(channels=1, samplerate=sr, blocksize=int(sr * chunk_ms/1000), callback=cb):
        while True:
            yield q.get()
```

### الخطوة الثانية: التقاط المديرات المضمنة بمفتاح VAD

```python
def capture_turn(stream, vad, pre_roll_ms=300, silence_ms=500):
    buf, pre, triggered = [], collections.deque(maxlen=pre_roll_ms // 20), False
    silent = 0
    for chunk in stream:
        pre.append(chunk)
        if vad(chunk):
            if not triggered:
                buf = list(pre)
                triggered = True
            buf.append(chunk)
            silent = 0
        elif triggered:
            silent += 20
            buf.append(chunk)
            if silent >= silence_ms:
                return b"".join(buf)
```

### الخطوة الثالثة: التدفق على STT → LLM → TTS

```python
async def turn(audio_bytes):
    transcript = await stt.transcribe(audio_bytes)
    async for token in llm.stream(transcript):
        async for audio in tts.stream(token):
            await speaker.play(audio)
```

### الخطوة الرابعة: أداة الدعوة داخل حلقة LLM

```python
tools = [
    {"name": "get_weather", "parameters": {"location": "string"}},
    {"name": "set_timer", "parameters": {"seconds": "int"}},
]

async for chunk in llm.stream(user_text, tools=tools):
    if chunk.type == "tool_call":
        result = dispatch(chunk.name, chunk.args)
        continue_streaming(result)
    if chunk.type == "text":
        await tts.stream(chunk.text)
```

### الخطوة 5: التعامل مع المقاطعة

> **【中文解读】**هذا المقال يوضح كيفية استخدام إطار متقدم مثل PyTorch、HuggingFace وغيرها) سريعة تطبيق هذه التقنية.


```python
tts_task = asyncio.create_task(tts_loop())
while True:
    chunk = await mic.get()
    if vad(chunk):
        tts_task.cancel()
        await speaker.stop()
        await new_turn()
        break
```




> **【拓展：语音与情感计算】**语音 ليس فقط نقل المعلومات الكلمات، ولكن أيضا تحمل إشارات عاطفية غنية (语调、语速、音高变化)  情感语音识别 (语调、语速、音高变化)  语音情绪识别 (语音识别, SER) في مجالات الاختبار الصحي والصحي والصحي والصحة النفسية والتعليم الذكي وغيرها.

## استخدمها في إطار التنفيذ

انظر`code/main.py`لتنمية قابلة للتشغيل التي تصل جميع المكونات السبعة مع نماذج القطع، حتى تتمكن من رؤية شكل خط الأنابيب حتى دون أجهزة.

> 参见 `code/main.py`获取可运行的模拟,将七组件用模块连接,无需硬件即可看到流水线形状──实际实现时,将模块替换为:

- `silero-vad`(`pip install silero-vad`) / VAD 模块
- `deepgram-sdk`أو`openai-whisper`/ 流式 STT
- `openai`(`gpt-4o`) أو `anthropic`/ LLM + 工具调用
- `kokoro`أو`cartesia`/ 流式 TTS
- `sounddevice`لـ I/O / 音频输入输出



## الفخاخ

> 常见陷

- **Logging PII forever.**الصوت المُتَحرك بالكامل هو المعلومات الشخصية في معظم الولايات القضائية.
  **永久记录 PII。**完整轮次音频在多数司法管辖区属于PII──30 天保留,静态加密──
- **No barge-in.**المستخدمون سيقاطعون، يجب أن يتوقف مساعدك عن الحديث
  **没有抢话。**المستخدم سوف يتوقف. المساعد الخاص بك يجب أن يتوقف عن الحديث.
- **TTS that blocks.**تث سمنشري يمنع حلقة الحدث. استخدم async أو خيط منفصل.
  **阻塞式 TTS。**مع التقدم التابع لـ TTS  حلقة الحوادث المثيرة للقلق
- **No tool-call error handling.**أدوات الفشل يجب أن يحصل ماجستير في العلوم على الخطأ + محاولة مرة أخرى ثم تخفيض gracefully.
  **没有工具调用错误处理。**الـ (آلات) ستفشل. يجب أن تتلقى خطأ + تجربة مرة أخرى ثم تحسّن.
- **Overzealous hallucination filters.**"فلتراً أكثر" ويقول المساعد "لا أستطيع المساعدة" "فلتراً أقل" ويقول أي شيء
  **过度激进的幻觉过滤。**过度过助手会重复"我帮不了"──过不足则什么都说──在留出集上校准──
- **No wake-word option.**الاستماع دائماً هو مسؤولية خصوصية. أضف بوابة استيقظ (Porcupine أو openWakeWord).
  **没有唤醒词选项。**持续监听是隐私负担──添加唤醒词门控(Porcupine أو openWakeWord)──

> **【中文解读】**هذا المادة يركز على كيفية نشر النموذج كمنتج متاح. من النموذج الأصلي إلى النظام في مرحلة الإنتاج، تحتاج إلى النظر في العديد من الخصائص في تحسين الأداء والتعامل الخاطئ والتحكم.


## أرسلها .

إبقوا`outputs/skill-voice-assistant-architect.md`. بالنظر إلى القيود المفروضة على الميزانية + الحجم + اللغة + الامتثال، قم بإعداد تحديد كامل للمجموعة.

> 保存为 `outputs/skill-voice-assistant-architect.md` تم تحديد الميزانية + 规模 + 语言 + 合规约束,产出完整技术规格‬

## تمارين التدريب

1. **Easy.**أركض`code/main.py`إنه يحاكي دور كامل واحد من نهاية إلى نهاية مع وحدات الصفوف والطبعات في كل مرحلة
   **简单。**运行 `code/main.py` 模块模拟一个完整轮次端到端并印出各阶段延迟
2. **Medium.**استبدل النصيب من STT بنموذج Whisper الحقيقي على نسخة مسجلة مسبقاً`.wav`. قياس WER و التأخير من نهاية إلى نهاية
   **中等。**في الموقع`.wav`上用真实 Whisper 模型替换STT 模块──测量 WER 和端到端延迟──
3. **Hard.**إضافة دعوة أداة: تنفيذ `get_weather`(أي إطار إطار إطار عمل) و `set_timer`. توجيه الدرجة العليا من خلال الأدوات والتحقق من أنه عندما يقول المستخدم "وضع 5 دقيقة التوقيت" تعمل الوظيفة الصحيحة والرد المتكلم يؤكد ذلك.
   **困难。**添加工具调用: تنفيذ `get_weather`(أي API) و `set_timer` من خلال الوسائل من خلال الـ LLM، التحقق عندما يقول المستخدم "وضع جهاز تحديد الوقت لمدة 5 دقائق" يتم استخدام وظيفة صحيحة

> **【中文解读】**في لغة "ما يقوله الناس" مقابل "ما يعنيه في الواقع" تم التمييز بين لغة اليومية والتقنية المحددة.


## شروط الرئيسية

| Term | What people say | What it actually means |
|------|-----------------|-----------------------|
| Turn | A user + assistant round-trip | One VAD-bounded user speech + one LLM-TTS response. |
| Barge-in | Interruption | User speaks while assistant talks; assistant stops. |
| Wake word | "Hey assistant" | Short keyword detector; Porcupine, Snowboy, openWakeWord. |
| End-pointing | Turn ending | VAD + min-silence decision that user has finished. |
| Pre-roll | Pre-speech buffer | Keep 200-400 ms of audio before VAD fires to avoid first-word clip. |
| Tool call | Function invocation | LLM emits JSON; runtime dispatches; result feeds back in-loop. |

| 术语 | 人们常说的 | 实际含义 |
|------|-----------|---------|
| 轮次 | 用户+助手一个来回 | 一次 VAD 界定的用户语音 + 一次 LLM-TTS 回应。 |
| 抢话 | 打断 | 助手说话时用户开口；助手停止。 |
| 唤醒词 | "嘿助手" | 短关键词检测器；Porcupine、Snowboy、openWakeWord。 |
| 端点检测 | 轮次结束 | VAD + 最小静音决策用户已说完。 |
| 预滚 | 语音前缓冲 | 在 VAD 触发前保留 200-400 ms 音频以避免首词截断。 |
| 工具调用 | 函数调用 | LLM 输出 JSON；运行时分发；结果在循环中反馈。 |

> **【中文解读】**延伸阅读 يوفر موارد عالية الجودة للتعلم المتعمق.


## المزيد من القراءة

- [LiveKit — voice agent quickstart](https://docs.livekit.io/agents/) إشارة إلى مستوى الإنتاج
  LiveKit语音智能体快速入门生产级参考──
- [Pipecat — voice agent examples](https://github.com/pipecat-ai/pipecat)إطار عمل صديقي للشراء
  بيبكات 语音智能体示例 DIY 友好框架。
- [OpenAI Realtime API](https://platform.openai.com/docs/guides/realtime) المسار المُدار الصوتي الأصلي
  OpenAI API托管的语音原生路径──
- [Kyutai Moshi](https://github.com/kyutai-labs/moshi) إشارة كاملة (الدرس 15).
  كيوتاي موشي 全双工参考(第 15 课) 』
- [Porcupine wake-word](https://picovoice.ai/products/porcupine/)-إغلاق الكلمات المُستيقظة
  الخنزير 唤醒词唤醒词门控――
- [Anthropic — tool use guide](https://docs.anthropic.com/en/docs/build-with-claude/tool-use) دعوة وظيفة LLM.
  أدوات استخدام الأدوات الإنسانية

> **【中文解读】**延伸阅读 يوفر موارد عالية الجودة للتعلم المتعمق، بما في ذلك المقالات والتدريس والأدوات.

