# المجموعة المحادثة والمتحدثين اختيار 群聊 选择 发言人

> يضع التنسيق المشترك للمحادثة N العملاء في محادثة واحدة؛ وظيفة اختيار (LLM، دوري-روبين، أو حسب الطابع) يختار من يتحدث بعد ذلك. هذا هو النموذج الأساسي من المحادثة المتعددة العملاء الناشئة  العملاء لا يعرفون دورهم في الرسم البياني ثابت، فإنهم فقط يتفاعلون مع المجموعة المشتركة. أوتوجين غروب تشات و AG2 GroupChat هي التنفيذات المرجعية: تم الحفاظ على تعبيرات GroupChat في AutoGen v0.2 في فورك AG2; أعادت AutoGen v0.4 كتابتها كنموذج ممثل مدفوع بالحدث. وضعت مايكروسوفت AutoGen في وضع الصيانة في فبراير 2026 وجمعتها مع Kernel Semantic في Microsoft Agent Framework (RC فبراير 2026). البدائية GroupChat تبقى في كل من AG2 و Microsoft وكيل الإطار  تعلمها مرة واحدة، استخدامه في كل مكان.

> **【中文解读】**هذا المقطع يعرض مجموعة من المفاوضين اختيار  أكثر وكيل  بحث في تحديد من ي发言、何时发言的机制──

> **【拓展：group chat speaker selection→具体应用】**群聊发言人选择是多代理 讨论中的关键问题谁发言、什么时候发言、发言多久──三种主要策略:(1) 轮流制按固定顺序发言;(2) 相关性制最相关的代理发言;(3) 仲裁制 专门协调者决定谁发言──AutoGen 的 GroupChat 使用 LLM 作为仲裁者──


**Type:** Learn | **类型:** 学习
**Languages:** Python (stdlib) | **语言:** Python (标准库)
**Prerequisites:** Phase 16 · 04 (Primitive Model) | **前置知识:** Phase 16 · 04 (Primitive Model)
**Time:** ~60 minutes | **时间:** ~60 分钟

>  **【前置】**学本节前请先掌握:Phase 16·04(原语模型)、AutoGen 基础──群聊 = N 个 وكيل 共享一个对话池,发言人选择决定谁说话──
>  **【类比】**群聊发言人选择 = "رئيس المؤتمر"──轮流制 = 圆桌按序;相关性制 = 谁懂谁说;仲裁制 = رئيس المحدد──AutoGen GroupChat باستخدام LLM 当主持人高成本但灵活──2026

## مشكلة إدخال مشكلة

الرسوم البيانية ثابتة (LangGraph) رائعة عندما يعرف التدفق العمل. المحادثات الحقيقية ليست ثابتة: أحياناً يسأل المشفر المراجع، أحياناً الباحث، أحياناً الكاتب. إن وضع الرسوم البيانية الصلبة في كل عملية إرسال ممكن ينتج انفجارًا حافة. تريد * العملاء الذين يتفاعلون مع حوض مشاركة * ، مع بعض الوظائف التي تحدد من يتحدث بعد ذلك.

> 静态图(LangGraph) في العمل流已知时很好──حقيقة الحوار ليست静态: أحياناً يقوم المُعدّد بضغط المراجع، أحياناً يسأل الباحث، أحياناً يسأل المؤلف──硬编码 كل اتصال ممكن يحدث عند الانفجار──أنت تريد*الوكيل أن يتفاعل مع حفرة المشاركة، من خلال وظيفة ما يقرر من يطرح الخطاب التالي──

مشكلة انفجار الحافة حقيقية: نظام 5 عملاء مع كل التسليمات الممكنة لديه 25 حافة موجهة. أضف عميل السادس لديك 36. نهج الرسم البياني لا ينحدر إلى محادثات ناشئة. تحتاج إلى حوض.

> مشكلة الإنفجار حقيقية: مع كل المعلومات المحتملة للاتصال مع 5 عملاء في النظام ، هناك 25 عملاء في النظام ، هناك 26 عملاء في النظام ، هناك 36 عملاء في النظام ، لا يمكن أن يتم توسيع النظام إلى الحوار المباشر ، تحتاج إلى حزمة.

هذا بالضبط ما تفعله (أوتوجين غروب تشات)

> هذا ما فعله (أوتوجين غروب تشات)

## مفهوم النواة

### الشكل

```
              ┌─── shared pool ────┐
              │   m1  m2  m3  ...  │
              └─────────┬──────────┘
                        │ (everyone reads all)
      ┌───────┬─────────┼─────────┬───────┐
      ▼       ▼         ▼         ▼       ▼
    Agent A  Agent B  Agent C  Agent D  Selector
                                           │
                                           ▼
                                  "next speaker = C"
```

كل عميل يرى كل رسالة يتم استدعاء وظيفة اختيار في كل جولة لتحديد من يتحدث بعد ذلك

> كل وكيل يرى كل خبر. كل دور يستخدم وظيفة الاختيار لتحديد المتحدث التالي.

تجمع الشفافية الكاملة هو كل من قوة و ضعف GroupChat. القوة: أي وكيل يمكن أن تتفاعل مع أي شيء قال أي شخص. الضعف: بعد 20 جولة، كل السياق من وكلاء ضخم، مكلفة، ومختلطة. التخفيفات: مشروع نطاق مشاهدة لكل وكيل (الدرس 15) أو إنهاء مبكرا.

> الميزة: أي عميل يمكن أن يكون رد فعل على أي شيء يقول أي شخص. ضعيف: بعد 20 جولة، كل عميل على أعلى

### الوجوه الثلاثة المختارة

**Round-robin.**دورة ثابتة. تحديد. يُقيس خطياً في N لكنه يتجاهل السياق  يُحصل على المُبرمج على التحول حتى عندما يكون الموضوع مراجعة قانونية.

> **轮询。**固定循环──确定性──按N 线性扩展但忽略上下文 حتى لو كان الموضوع هو مراجعة قانونية، يمكن للمرء المعدين أيضا الحصول على الحق في الإفادة──

**LLM-selected.**مكالمة إلى ماجستير في مجال التعليمات العليا التي تقرأ مجموعة الأخيرة وتعطي أفضل المتحدث التالي. واعية السياق ولكن بطيئة: كل دور يضيف مكالمة ماجستير في مجال التعليمات العليا. افتراض AutoGen.

> **LLM 选择。**调用 LLM 读取最近池并返回最佳下文发言人──上下文感知但慢:

**Custom.**وظيفة Python مع أي منطق تريد. نموذجية: LLM-اختيار مع قواعد الرد (على سبيل المثال، "أعطى دائما المحقق التحول بعد المبرمج").

> **自定义。**فان وظيفة Python ، باستخدام أي منطق تريده.

### API للعملاء المقابلين

```
agent = ConversableAgent(
    name="coder",
    system_message="You write Python.",
    llm_config={...},
)
chat = GroupChat(agents=[coder, reviewer, tester], messages=[])
manager = GroupChatManager(groupchat=chat, llm_config={...})
```

`GroupChatManager`عندما يقوم وكيل بإكمال دورة، يدعو المدير الكائن الذي يعيد العامل التالي.

> `GroupChatManager`持有选择器──当代理 完成一轮时,管理者调用选择器, 回归下一个代理──循环继续直到终止条件──

وظيفة الاختيار هي قلب مجموعة الشات. قم بتغييرها، وتغيير أسلوب التوسيق. اختيار الجولة = تحديد. اختيار LLM = تكييف. اختيار مخصص = أي قواعد ترميزها. نفس الأصول، التوسيق مختلف.

> 选择器函数是 GroupChat的核心──换掉它,改变编排风格──轮询选择器 = 确定性──LLM 选择器 = 自适应──自定义选择器 = 你编码的任何规则──相同原语,不同编排──

### الإنهاء

ثلاثة أنماط شائعة:

> ثلاثة أشكال:

- **Max rounds.**القفص الصلب في التحولات الكاملة.
  中文翻译:**最大轮数。**总轮数的硬上限──
- **"TERMINATE" token.**يمكن للعملاء إرسال رسالة من الرقابة، ويقف المدير عندما يظهر واحد.
  中文翻译:**"TERMINATE" 标记。**يمكن للعميل إرسال رسالة من المراقبين، والمدير يتوقف عند ظهورها
- **Goal-reached check.**مُحقق خفيف يدير كل دور ويقفز المحادثة عندما تنتهي
  中文翻译:**目标达成检查。**الاختبار النياني كل جولة من العمل ووقف المحادثة عند الانتهاء.

### تمزق AutoGen -> AG2 و Microsoft Agent Framework
### العائلة: الشوك والاندماج

في أوائل عام 2025 ، بدأت مايكروسوفت إعادة كتابة كبيرة من AutoGen (v0.4) حول نموذج ممثل مدفوع بالحدث. قام المجتمع بتشكل سمانيات GroupChat من AutoGen v0.2 باسم AG2 ، الحفاظ على API التي دمجها المستخدمون المبكرون.

> في أوائل عام 2025، بدأت مايكروسوفت في إعادة كتابة أدوات أوتوجين (v0.4) حول الجهاز المحرك للحوادث.

كان الشوك ضرورياً لأن v0.4 قد كسر التوافق الخلفي بطرق أساسية`GroupChat`،`ConversableAgent`و`GroupChatManager`API مستقرة، في حين أن v0.4 يقدم بدائيات جديدة مدفوعة بالحوادث. يتم الحفاظ على كلا الخطين بنشاط اعتبارا من 2026.

> الفرق ضروري، لأن v0.4 في الجوانب الأساسية تدمير التوافقات اللاحقة.`GroupChat`.`ConversableAgent`和 `GroupChatManager`API 稳定,而 v0.4 引入新事件驱动原语──截至 2026年,两条线都积极维护──

في فبراير 2026، أعلنت مايكروسوفت أن AutoGen ستذهب إلى وضع الصيانة، مع دمج نموذج الممثل القائم على الأحداث في **Microsoft Agent Framework**(RC فبراير 2026 ، والتي اندمجت الآن مع Kernel Semantic). مفهوم GroupChat يبقى في كلا المسارات؛ تفاصيل التنفيذ تختلف. AG2 هو المفضل في النهاية لبرنامج v0.2 متوافق.

> 2026 سنة فبراير ، Microsoft أعلن AutoGen  دخول وضعية الحفاظ ، الحدث المحرك الجهاز 模型合并到 **Microsoft Agent Framework**(2026 年 2 月 RC,现已与语义核心合并) ――GroupChat 概念在两个轨道中存活;实现细节不同──AG2 是 v0.2 兼容代码的首选上游──

الدرس: API سطح يطول من الإطار. الرمز المكتوب ضد AutoGen v0.2's GroupChat API في 2024 لا يزال يعمل دون تغيير عبر AG2 في 2026. الإطار تتحرك؛ البدائيات (المجمله المشترك + المختار) لا. رهان على البدائيات.

> تعليميات:API 表面比框架持久──2024 سنة على نحو من تطوير السيارات v0.2 GroupChat API 编写的代码在 2026 سنة من خلال AG2 仍然不变运行──框架变化;原语(共享池 + 选择器) 不变──押注原语──

### عندما يتناسب GroupChat

- **Emergent conversations.**لا تريد أن تُسلك كل المتحدث المحتمل
  中文翻译:**涌现对话。**أنت لا تريد أن تتصل مسبقاً مع المتحدث التالي
- **Role-mixing tasks.**يطلب المبرمج الباحث، والباحث يطلب من الموثق، والمتوثق يطلب من المبرمج مرة أخرى. التدفق ليس DAG.
  中文翻译:**角色混合任务。**المُعدّل يسأل المُدّبّر، المُدّبّر يسأل المُدّبّر، المُدّبّر يسأل المُدّبّر، المُدّبّر يسأل المُدّبّر، المُدّبّر يسأل المُدّبّر، المُدّبّر يسأل المُدّبّر، المُدّبّر يسأل المُدّبّر، المُدّبّر يسأل المُدّبّر، المُدّبّر يسأل المُدّبّر، المُدّبّر يسأل المُدّبّر، المُدّبّر ليس يومًا.
- **Exploratory problem-solving.**فكر في "اجتماع العصابة العقلية" وليس "خط التجميع".
  中文翻译:**探索性问题解决。**فكر في "تفاهة العاصفة" بدلاً من "تفاهة الإعداد"

### عندما يفشل

- **Strict determinism.**اختيار الـ "مدرسة العلوم" يمكن أن يكون غير متسقّق، نفس الإجراءات، مختلفة، متحدثين مختلفين.
  中文翻译:**严格确定性。**الـ LLM 选择器可能不一致──相同提示,不同运行,不同下一个发言者──
- **Sycophancy cascades.**العملاء يتوقفون على من يتحدث بأكثر ثقة
  中文翻译:**谄媚级联。**الوكيل يلتزم بأكثر المتحدثين ثقة
- **Context bloat.**كل وكيل يقرأ كل رسالة، بعد 10 جولات يكون السياق ضخم. استخدم التنبؤات (الدرس 15) لتوسيع نطاق المشاهد.
  中文翻译:**上下文膨胀。**كل عميل 读取每条消息;10轮后上下文巨大──使用投影(درس 15)来限定视图──
- **Hot speakers.**أحد الوكلاء يهيمن على المحادثة لأن المختار يفضل تخصصاته.
  中文翻译:**热发言者。**وكيل يدير المحادثة، لأن الاختيار يتوجه إلى تخصصه.

### دردشة المجموعة مقابل المشرف

نفس الأسباب الأولية، مختلفة التخلفات:

> 相同原语,不同默认值:

- المراقب: أحد العملاء يخطط و الآخرين ينفذون. اختيار هو "اسأل المخطط ماذا يفعل".
  中文翻译:监督者:一个代理 规划,其他执行──选择器是"问规划者做什么──"
- دردشة المجموعة: جميع الوكلاء هم أقرانهم؛ الاختيار هو وظيفة على المجموعة المشتركة.
  中文翻译:群聊:所有代理 是对等的;选择器 是共享池上的函数──

كلاهما يستخدم الأسباب الأربعة من الدروس 04. تشات المجموعة تفرض افتراضياً إلى التوسيق المختارة في LLM والحالة المشتركة الكاملة.

> 两者都使用课堂 04 的四个原语──群聊默认使用LLM 选择的编排和全池共享状态──

الخيار بين المشرف والردشة الجماعية يتعلق في الغالب *من الذي يحتفظ بالخطة*. المشرف: وكيل واحد يمتلك الخطة والوفود. الدردشة الجماعية: الخطة ضمنية، والتي تظهر من المحادثة. الأول أكثر قابلية للسيطرة؛ الثاني أكثر مرونة.

> الاختيار بين المراقب والجماعة هو أساسا حول* من يملك خطة*── المراقب: وكيل 拥有计划并委派──

## بناء ذلك تحرك لتحقيق
```figure
swarm-speaker
```

## بناءها

`code/main.py`تنفيذ مجموعةChat من الصفر في stdlib. ثلاثة وكلاء (مبرمج، المراجع، المدير) ، والإختيارات الجولة والشركة القانونية المختارة، وإنهاء على `TERMINATE`إشارة

> `code/main.py`استخدام القاعدة المعيارية من رأس لتحقيق مجموعةChat.`TERMINATE`علامة على النهاية

يطبخ المشاهد النسخة المحادثة بالإضافة إلى تعقب قرار المختار لكلتا التغيرات.

> 演示 طباعة سجلات الحوار وكذلك متابعة محركات اتخاذ القرار من نوعين من المتغيرات

## استخدمها في إطار التنفيذ

`outputs/skill-groupchat-selector.md`يضبط اختيار مجموعةChat لمهمة معينة  دورية مقابل اختيار LLM مقابل المخصص ، وما هي مدخلات المختار (رسائل حديثة ، تخصصات الوكيل ، حسابات التحول) لاستخدامها.

> `outputs/skill-groupchat-selector.md`لتحديد المهام التنظيمية المجموعةChat  اختيارات  轮询 vs LLM 选择 vs 自定义,以及使用什么选择器输入(最近消息、Agent 专长、轮次计数)

## أرسلها .

قائمة التحقق:

> 检查清单:

- **Max rounds cap.**دائماً، 10-20 لمهام نموذجية
  中文翻译:**最大轮数上限。**总是使用──典型任务 10-20──
- **Speaker-balance metric.**تدوير المسار لكل عامل، تحذير عندما يتجاوز عدم التوازن عتبة.
  中文翻译:**发言者平衡指标。**تتبع كل عميل من دورة؛ عندما يتفوق التوازن على قيمة وقت الإشعار.
- **Termination token.** `TERMINATE`أو وكيل مُحقق مُخصص.
  中文翻译:**终止标记。** `TERMINATE`أو عميل مُحقق متخصص
- **Projection or scoped memory.**بعد ~ 10 رسائل، فكر في إعطاء كل وكيل فقط عرض محدد لمنع التورم السياق.
  中文翻译:**投影或范围内存。**بعد 10 أشهر، فكر في أن كل عميل يحتوي على نطاق واحد فقط لمنع التضخم في الملفات التالية.
- **Selector logging.**بالنسبة للخيارات المختارة في الجامعة، قم بتسجيل إدخال المختار واختياره. وإلا فإن إزالة التحذير مستحيل.
  中文翻译:**选择器日志。**对于LLM 选择变体,记录选择器的输入和选择──否则调试不可能──

## تمارين التدريب

1. أركض`code/main.py`مقارنة المحادثة تحت دور المكافحة مقابل المختارين في القانون
   中文翻译:运行 `code/main.py`◊ مقارنة الاستفسارات مقابل الـ LLM  اختيار تحت الحوار  في كل نموذج
2. إضافة قاعدة "أقصى حد يتحدث لكل عميل" في المختار كيف يؤثر ذلك على النص؟
   中文翻译: 在选择器中添加"每代理最大发言次数"规则──它如何影响记录?
3. تنفيذ إيقاف متفق عليه: توقف عندما يعود المراجع "موافق عليه". كم مرة يبدأ قبل السقف الجاري؟
   中文翻译:实现目标达终止: عندما يعود المراجع "موافق عليه" توقف.
4. اقرأ وثائق AutoGen المستقرة على GroupChat. حدد المختار الافتراضي المستخدم من `GroupChatManager`. . .
   中文翻译:阅读 AutoGen 稳定文档中的 GroupChat──识别 `GroupChatManager`استخدام الاختيار المتبقي
5. اقرأ إعادة الإعلان AG2 وقارن مجموعة v0.2 GroupChat الخاصة بها مع إصدار v0.4 القائم على الأحداث. ما هي الخصائص الملموسة (الإنتاجية، وتسامح الأخطاء، وتكوين) التي يضيفها v0.4؟
   中文翻译:阅读 AG2 仓库并比较其 v0.2 GroupChat 与 v0.4 事件驱动版本──v0.4 添加了什么具体属性(吞吐量、容错、可组合性)?

## شروط الرئيسية

| Term | What people say | What it actually means |
|------|----------------|------------------------|
| GroupChat / 群聊 | "Agents in one chat room" / "一个聊天室中的 Agent" | Shared message pool + selector function. AutoGen / AG2 primitive. / 共享消息池 + 选择器函数。AutoGen / AG2 原语。 |
| Speaker selection / 发言者选择 | "Who talks next" / "谁下一个说话" | The function that picks the next agent. Round-robin, LLM-selected, or custom. / 选择下一个 Agent 的函数。轮询、LLM 选择或自定义。 |
| GroupChatManager / 群聊管理者 | "The meeting host" / "会议主持人" | AutoGen component that owns the selector and loops over turns. / 拥有选择器并循环轮次的 AutoGen 组件。 |
| ConversableAgent / 可对话 Agent | "The base agent" / "基础 Agent" | AutoGen base class; an agent that can send and receive messages. / AutoGen 基类；可以发送和接收消息的 Agent。 |
| Termination token / 终止标记 | "The 'stop' word" / "停止词" | Sentinel string (usually `TERMINATE`) that ends the chat. / 结束聊天的哨兵字符串（通常是 `TERMINATE`）。 |
| Hot speaker / 热发言者 | "One agent dominates" / "一个 Agent 主导" | Failure mode where the selector keeps picking the same agent. / 选择器持续选择同一 Agent 的失败模式。 |
| Context bloat / 上下文膨胀 | "Pool grows unbounded" / "池无限增长" | Each agent reads every prior message; context grows with turns. / 每个 Agent 读取每条先前消息；上下文随轮次增长。 |
| Projection / 投影 | "Scoped view" / "范围视图" | Role-specific view into the shared pool to prevent context bloat. / 角色特定的共享池视图以防止上下文膨胀。 |

## المزيد من القراءة

- [AutoGen group chat docs](https://microsoft.github.io/autogen/stable/user-guide/core-user-guide/design-patterns/group-chat.html) تنفيذ المرجعية
  中文翻译:AutoGen 群聊文档  参考实现
- [AG2 repo](https://github.com/ag2ai/ag2) المجتمع AutoGen v0.2 استمرار
  中文翻译:AG2 仓库  社区 AutoGen v0.2 延续
- [Microsoft Agent Framework docs](https://microsoft.github.io/agent-framework/) الوصي المدمج، RC فبراير 2026
  中文翻译:Microsoft Agent Framework 文档  合并后的继任者,2026 年 2 月 RC
- [Microsoft Agent Framework docs](https://learn.microsoft.com/en-us/agent-framework/) الوصي المدمج، RC فبراير 2026
- [AutoGen v0.4 release notes](https://microsoft.github.io/autogen/stable/) إعادة كتابة تفاصيل نموذج الممثل الذي يقود به الأحداث
  中文翻译:AutoGen v0.4 发布说明  事件驱动 actor 模型重写详情
