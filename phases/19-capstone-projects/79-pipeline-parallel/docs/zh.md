# 流水线并行与气泡分析

> 张量并行把矩阵乘切到各 rank。流水线并行把模型切到各 rank，每个 rank 一个阶段。微批次在流水线中流动。开头和结尾的空闲时间就是气泡；压低它是这门手艺的全部。

> **【中文解读】** 本课讲与数据并行、张量并行正交的第三条并行轴——流水线并行：把模型按深度切成 N 个阶段，每个 rank 持有一段，微批次像流水线上的产品一样流过去、反传再流回来。核心量化工具是气泡分数 (N-1)/(M+N-1)。学完本课你应能模拟 GPipe 调度、推导气泡、对比 1F1B 调度，并用"每阶段计算量相等"的原则划分阶段。

> **【拓展：三条并行轴→三维混合并行】** 大模型训练的显存压力催生了三条正交的并行轴：数据并行（每卡完整模型、不同数据）、流水线并行（按层切模型）、张量并行（按矩阵乘切层）。Megatron-LM 把三者叠成 3D 并行（DP×PP×TP）跑千亿模型。本课专注流水线轴的两大调度——GPipe（先填后排）与 1F1B（一前一后交错，Megatron/PipeDream 的生产选择），以及气泡分数的推导。与 lesson 78 的 ZeRO 不同：ZeRO 切的是"状态"，流水线切的是"计算本身"。

> 🔗 **【前置】** 学本课前请先掌握：(1) Phase 19 · 76——send/recv 与集合通信原语，双 rank 真流水线 demo 建立在其上；(2) 前向/反传与激活值的生命周期概念——气泡分析的核心约束是"激活值必须存活到它的反传"；(3) lesson 78 的 ZeRO——两条分片轴正交且常组合（ZeRO-1 + pipeline）。后续衔接：lesson 80 把每阶段的参数分片写进检查点，lesson 81 把 DDP+ZeRO 组装成端到端 demo。

**类型：** 动手构建
**语言：** Python
**前置条件：** Phase 19 Track C 课程 42-49
**预计用时：** 约 90 分钟

## 学习目标

- 把一个顺序模型切成 N 个阶段，并在 N 个 rank 上模拟前向流水线。
- 用 GPipe 调度（先全部前向填充、再反传）把 M 个微批次排进流水线，并计算气泡分数。
- 把气泡与 Megatron-LM 和 PipeDream 使用的交错式 1F1B 调度做对比。
- 论证阶段划分：每阶段计算量相等比每阶段参数量相等更重要。

## 问题引入

> **【中文解读】** 本节回答"为什么还需要流水线"：单卡装不下 70B 参数，ZeRO-3 每层前向仍要 allgather 整层；流水线让每 rank 只持一个阶段、显存线性下降，代价是串行化——气泡。想让气泡小就得有大量微批次，这直接驱动了微批次大小的设计。

一个 fp16 的 70B 参数模型光参数就需要 140 GB。没有消费级 GPU 装得下。ZeRO-3 把参数分片到各 rank，但每次前向步每个 rank 仍要 allgather 整层，为每层支付 log(N) 跳。流水线并行走另一条路：把模型切成 N 个阶段，每个 rank 放一个。第 1 层的前向在 rank 0 上完成后把激活张量交给 rank 1；rank 1 跑第 2 层再交给 rank 2；依此类推。反传反向流动。因为每个 rank 只持一个阶段，显存线性下降；计算是串行的——这就是气泡问题。

气泡是流水线开头的空闲时间（等第一个微批次到达最后一个阶段）和结尾的空闲时间（等最后一个微批次排空回流）。M 个微批次、N 个阶段时，每阶段气泡分数是 (N-1)/(M+N-1)。M=8、N=4 时是 27%。M=64、N=4 时是 4.5%。每步微批次越多气泡越小，这意味着每个微批次的 batch 要小——这正是驱动微批次设计的约束。

## 核心概念

```mermaid
flowchart LR
  R0[rank 0: stage 0 / layer 0] --> R1[rank 1: stage 1 / layer 1]
  R1 --> R2[rank 2: stage 2 / layer 2]
  R2 --> R3[rank 3: stage 3 / loss]
  R3 -.backward.-> R2
  R2 -.backward.-> R1
  R1 -.backward.-> R0
```

### GPipe 调度

> **【中文解读】** GPipe 是"先填后排"：全部 M 个微批次先跑完前向，再倒序跑反传。代价是激活显存随 M 线性增长（每个微批次的激活必须活到它的反传）。时间账：前向 M+N-1 周期、反向再 M+N-1 周期，每阶段有用功 2M、气泡 2(N-1)，气泡分数 (N-1)/(M+N-1)。M 远大于 N 就能藏住气泡。

先让全部 M 个微批次填满前向流水线，再倒序排空反传。每个微批次的激活值必须保持到它自己的反传，所以显存随 M 线性增长。前向花 M+N-1 个周期，反向再花 M+N-1 个周期。每阶段有用功是 2M 个周期；每阶段气泡是 2(N-1) 个周期。当前向和反向各占一个时间单位时，气泡分数是 (N-1)/(M+N-1)。选 M 远大于 N 就能藏住气泡。

### 1F1B 调度

> **【中文解读】** 1F1B 是"一前一后"交错：微批次的前向一到达最后一个阶段就立刻开始它的反传。气泡仍是 N-1 个周期，但激活显存只受流水线深度约束、不再随微批次数增长——这就是生产流水线全用 1F1B 的原因。

交错执行：某个微批次的前向一到达最后一个阶段，就启动它的反传并让它流回去。调度在每个阶段上交替一次前向、一次反传。气泡仍是 N-1，但激活显存由流水线深度界定，而不是微批次数。生产流水线都用 1F1B（Megatron、PipeDream）。本课先实现更简单的 GPipe，1F1B 作为练习。

### 为什么每阶段计算量相等更重要

> **【中文解读】** 若阶段 0 花 50 ms、阶段 1 花 100 ms，每个周期都被阶段 1 卡住，其他阶段每周期白等 50 ms。参数量是错误的轴——Transformer 每层计算由注意力 + MLP 主导，嵌入层参数多、计算少。划分应均衡每阶段的 FLOPs。

如果阶段 0 花 50 ms 而阶段 1 花 100 ms，每个周期都卡在阶段 1 上。其他阶段每周期空等 50 ms 等阶段 1 放行。参数量相等是错误的轴：Transformer 的计算由每层的注意力 + MLP 主导，而嵌入层参数很多但计算很少。阶段分配应该均衡每阶段的 FLOPs，而不是每阶段的权重。

### 微批次与 batch

> **【中文解读】** 流水线一次跑 M 个大小为 B 的微批次：有效 batch 是 M*B（优化器看到的梯度），气泡分数只取决于 M。调 M 就是在气泡与每微批次激活显存之间做交易。

流水线一次跑 M 个大小为 B 的微批次。有效 batch 大小是 M*B。流水线步结束时的梯度是合并的 M*B 个样本上的梯度。气泡分数取决于 M；优化器看到的是 M*B。调 M 意味着在气泡（M 大则低）与每微批次显存（GPipe 下 M 大则激活显存高）之间做交易。

```figure
cd-pipeline-bubble
```

## 动手构建

> **【中文解读】** 代码分两层：纯调度层（gpipe_schedule + 甘特图 + 实测气泡对照闭式解）和真实通信层（双 rank gloo 进程，激活值 send/recv 接力、梯度回流）。demo 还打印 M=1..64 的气泡对照表，直观呈现"M 越大气泡越小"的饱和曲线。

`code/main.py` 实现：

- `PipelineStage`：一个小的 `nn.Module`，持有一个阶段的参数，暴露 `forward(activation)`。
- `Pipeline(stages, num_microbatches)`：在模拟阶段上编排 GPipe 调度，使用每阶段模拟墙钟。
- `bubble_fraction(num_stages, num_microbatches)`：闭式解 (N-1)/(M+N-1)。
- 一个 4 阶段 demo：打印每微批次 trace 和实测气泡分数。

运行：

```bash
python3 code/main.py
```

输出：一张阶段×微批次的甘特图，以及实测气泡百分比与闭式解预测的对照。

## 生产中的实战模式

> **【中文解读】** 激活值检查点与流水线是天生一对（GPipe 激活显存是单微批次的 M 倍，检查点用重算换显存）；阶段均衡要实测不要假设（先 profiling 再划分）；send/recv 顺序必须防死锁（偶数 rank 先发后收、奇数 rank 先收后发）。

三条模式让流水线并行足以投入生产。

**激活值检查点与流水线配对。** GPipe 上同时有 M 个微批次在飞时，激活显存是单个微批次的 M 倍。激活值检查点在反传时重算前向，用计算换显存；两者结合才让长序列的流水线变得可行。

**阶段均衡靠实测，不靠假设。** 生产团队先跑一遍 profiling，在目标硬件上量出每层真实计算量（FLOPs 和墙钟时间），再按测量结果划分。Megatron-LM 的 `--num-layers-per-stage` 标志接受一个列表，允许在每层成本不同时使用不均等的层数。

**send/recv 调度必须避免死锁。** 每个阶段都先发后收的流水线会在网络上死锁。标准修法是交错：偶数 rank 的阶段先发后收，奇数 rank 的阶段先收后发。本课显式编排 rank 顺序，让这个模式看得见。

## 用框架实现

> **【中文解读】** Megatron-LM 是参考实现（1F1B + 3D 并行）；DeepSpeed Pipeline 与 ZeRO 集成；PyTorch Pipe 是原生封装。手工实现过调度之后，这些框架的 stage 配置不再是黑盒。

生产模式：

- **Megatron-LM**——大规模流水线并行的参考实现，使用 1F1B 并支持张量 + 流水线 + 数据并行组合。
- **DeepSpeed Pipeline**——与 ZeRO 集成；ZeRO-1 + 流水线是最大开源模型的常见组合。
- **PyTorch Pipe**——PyTorch 原生流水线封装，构建在 `torch.distributed.pipeline.sync.Pipe` 之上。

## 产出物

Lesson 80 把每阶段的参数分片存进分片检查点。Lesson 81 在端到端 demo 上组合 DDP + ZeRO + 流水线（取其精神；demo 为控制运行时保留流水线的模拟形态）。

## 练习题

1. 实现 1F1B，验证气泡分数与 GPipe 相同但激活显存有界。
2. 在更深的模型上剖析每阶段真实耗时，并按实测墙钟时间重新均衡阶段。
3. 跨流水线微批次加梯度累积，检查梯度等于等价全 batch 前向的梯度。
4. 给流水线配上激活值检查点，测量显存下降与计算代价的对比。
5. 把流水线与 DDP 组合（每个流水线 rank 在数据并行组内再复制一份），并推理这个二维调度的行为。

## 术语速查表

| 英文 | 人们常说的 | 实际含义 |
|------|----------------|------------------------|
| Pipeline（流水线） | "沿深度切模型" | 每 rank 一个阶段，激活值逐段流动 |
| Bubble（气泡） | "流水线空闲时间" | 开头 + 结尾各 (N-1) 步里部分阶段无工可做 |
| Microbatch（微批次） | "batch 的切片" | 一个前向/反传单元；M 越大气泡越小 |
| GPipe | "先填后排" | 全部 M 个前向之后才开始反传；激活显存高 |
| 1F1B | "交错调度" | 每阶段一前一后交替；激活显存有界 |

## 延伸阅读

- [Huang et al, GPipe: Efficient Training of Giant Neural Networks](https://arxiv.org/abs/1811.06965) — GPipe 论文原典：先填后排调度与气泡推导
- [Narayanan et al, PipeDream: Generalized Pipeline Parallelism for DNN Training](https://arxiv.org/abs/1806.03377) — PipeDream 论文：1F1B 类交错调度的来源
- [Megatron-LM pipeline parallel docs](https://github.com/NVIDIA/Megatron-LM) — Megatron-LM 流水线并行文档：生产级 1F1B 与 3D 并行
- Phase 19 Lesson 76 — 调度使用的 send/recv 原语
- Phase 19 Lesson 78 — ZeRO 与流水线正交且经常组合使用
