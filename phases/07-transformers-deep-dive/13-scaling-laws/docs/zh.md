# Scaling Laws | 缩放定律

> 2020 年的 Kaplan 论文说：更大的模型，更低的损失。2022 年的 Hoffmann 论文说：你训练得不够。计算分到两个桶——参数和 token——而分配比例并不明显。

> **【中文解读】** Chinchilla 定律揭示了模型大小、数据量、计算量的最优关系。理解缩放定律 = 理解为什么 LLM 需要那么多数据。

**类型：** 学习
**语言：** Python
**前置条件：** 阶段 7 · 05（完整 Transformer），阶段 7 · 07（GPT）
**时长：** 约 45 分钟

## 问题引入

当你有 C FLOP 的训练计算预算并想要最好的模型时，你面对两个旋钮：

1. **多少参数 (N)？** 更大的模型，更高的容量。
2. **多少训练 token (D)？** 更多数据，更好地利用容量。

FLOP 约按 `6 × N × D` 缩放。你可以推高 N 降低 D，或推高 D 降低 N。哪个更好？

2022 年之前，答案是"猛推 N"。GPT-3（2020）是 175B 参数，在约 300B token 上训练。比例约为每参数 1.7 个 token。Kaplan 缩放定律支持了这一点。

Hoffmann 等人（2022），训练了一个名为 Chinchilla 的小型模型家族，发现了不同的东西：最优比例接近**每参数 20 个 token**。GPT-3 被欠训练了 10 倍。Chinchilla（70B 参数，1.4T token）在每个基准测试上击败了 GPT-3（175B，300B token），推理成本低 2.5 倍。

2026 年是 Chinchilla 的世界——但有一个重要的转折。Llama 3 8B 在 15 万亿 token 上训练，比例为每参数 1,875 个 token。超过 Chinchilla 最优的 94 倍。对于将大规模使用的模型，推理成本比训练成本更重要，所以过度训练（超过 Chinchilla）以获得更小的可部署足迹是 2026 年的默认。

> **【中文解读】** 缩放定律的核心洞察：FLOPs ≈ 6 × N × D（参数量 × token 数）。Kaplan（2020）倾向于增大 N，但 Chinchilla（2022）证明最优比例约为 20 token/参数。2026 年的实践更进一步：Llama 3 8B 用了 1,875 token/参数训练——远超 Chinchilla 最优，因为推理成本比训练成本更重要，过度训练小模型以降低部署成本已成为行业标准。

> **【拓展：过度训练策略的经济逻辑】** Llama 3 8B 用 15T token 训练（远超 Chinchilla 最优的 160B token），推理成本却大幅降低。这是因为推理时每个 token 的计算量与参数量成正比，8B 参数的推理成本仅为 70B 模型的约 1/9。对于部署量大的模型（如 API 服务），推理成本的节约远超额外训练成本。这解释了为什么 Phi-3-mini（3.8B）和 Qwen2-1.5B 等小模型都被过度训练。

## 核心概念

![Chinchilla 曲线：不同 N/D 比率下损失 vs 计算](../assets/scaling-laws.svg)

### Hoffmann 定律

来自 Chinchilla 论文，损失遵循：

```
L(N, D) = A / N^α + B / D^β + E
```

- `N` = 参数量（非嵌入）。
- `D` = 训练 token 数。
- `α ≈ 0.34`，`β ≈ 0.28`（大致对称）。
- `E ≈ 1.69`，不可约损失上限。
- `A ≈ 406`，`B ≈ 411`。

两项在缩放时相互权衡。在固定计算（C = 6ND）下对 `N` 求导并求解：

```
N_opt ≈ 0.6 × (C/6)^0.5
D_opt ≈ 0.6 × (C/6)^0.5
D_opt / N_opt ≈ 20
```

计算最优：每参数 20 个 token。

### 为什么仍然过度训练

Chinchilla 最优最小化每训练 FLOP 的训练损失。但训练成本只付一次；推理成本永远。

对于一个每月服务万亿 token 的聊天机器人，推理主导总成本。Llama 的方法：训练更小的，训练更久。8B 在 15T token 上是深度推理优化的：

- 适配消费级 GPU。
- 延迟是 70B Chinchilla 最优的一小部分。
- 质量对大多数任务足够接近。

DeepMind 2024 年的论文（"Over-training is the new optimal"）形式化了这一点。对于推理主导的工作负载，正确的比例更接近每参数 100-500 个 token，取决于服务量。

### 涌现 vs 平滑性

声称：某些能力（算术、多步推理、思维链跟随）在某个规模"突然涌现"。

Schaeffer 等人（2023）认为这是测量伪影：涌现指标使用不连续评分（精确匹配、阈值准确率），隐藏了底层 logits 的平滑改善。连续指标（交叉熵）显示平滑曲线。

2026 年的共识是：通过连续损失预测是可靠的。基准测试的跳变通常是评分器的伪影。根据连续指标规划预算。

> **【中文解读】** "涌现能力"（emergence）在 2023 年引发了大量讨论——某些能力似乎在特定规模突然出现。但 Schaeffer 等人证明这可能是度量伪影：不连续的评分标准（如精确匹配）隐藏了底层 logits 的平滑改善。2026 年的共识是：用连续损失（如交叉熵）预测是可靠的，基准测试的跳变往往是评分标准的问题。

> **【拓展：数据质量比数据量更重要】** 2026 年缩放定律的新变量是数据质量。Microsoft 的 Phi 系列证明，精心筛选的"高质量" token 可以将有效计算量提升 2 倍以上。Llama 3 使用了数据配比优化和合成数据增强。MoE 架构则进一步解耦了总参数量和活跃计算量。这些因素使得传统的 Chinchilla 曲线需要重新校准。

### 2026 年图景

缩放定律仍然有效，但：

| 因素 | 如何变化 |
|------|---------|
| 数据质量 | 策划"好" token（Phi 风格）将曲线移动超过 2 倍有效计算 |
| MoE | 总参数与活跃 FLOP 解耦；按活跃 FLOP 的缩放定律 |
| 后训练 | 某些能力（指令遵循、代码）随 SFT+RLHF 变化大于预训练 |
| 多模态 | 图像 + 文本 token 一起缩放；每种模态的独立曲线 |
| 合成数据 | 模型生成训练数据；有效计算可以复合增长 |

> **【拓展：合成数据与缩放定律的未来】** 2026 年缩放定律面临数据墙问题——高质量人类文本数据可能在未来几年耗尽。合成数据（模型生成的训练数据）是潜在的解决方案。Microsoft 的 Phi 系列使用 GPT-4 生成的"教科书质量"合成数据进行训练，NVIDIA 的 Nemotron 使用合成数据增强。如果合成数据有效，缩放定律的"有效计算"可以持续增长。

Muon 优化器（Kimi Moonlight，2024）在匹配数据下显示了约 2 倍的有效计算收益。一些 2026 年的训练运行默认使用 Muon。它改变了缩放定律中的绝对常数，而非形状。

## 动手实现

参见 `code/main.py`。我们实现 Chinchilla 损失方程并在多个计算预算下求解计算最优的 `(N, D)`。

### 步骤 1：Chinchilla 损失

```python
def chinchilla_loss(N, D, A=406.4, B=410.7, alpha=0.34, beta=0.28, E=1.69):
    return A / N ** alpha + B / D ** beta + E
```

在固定 `C = 6ND` 下绘制 `L` 作为 `(N, D)` 的等高线。找到最小值。

### 步骤 2：计算最优前沿

对于从 `1e17` 到 `1e25` FLOP 的计算预算，找到在 `6ND = C` 约束下最小化损失的 `(N, D)`。验证比例 `D/N ≈ 20`。

### 步骤 3：过度训练成本

计算为了训练一个 10 倍更小的模型（1/10 最优 N，10 倍最优 D）所支付的额外损失。同时报告推理 FLOP 节省（与 N 成正比）。

### 步骤 4：与真实模型比较

输入 GPT-3、Chinchilla、Llama 3 8B、DeepSeek-V3（活跃参数）的已知 `(N, D)` 对，比较预测与报告的损失。

## 用框架实现

你不太可能自己训练前沿模型。但缩放定律告诉你：

1. **你的微调是否有足够数据。** 如果你的任务特定数据低于基础模型每参数 20 个 token，预期在某个损失下饱和。
2. **是否选择更大的基础模型。** 如果你将所有预算花在推理上，优先选择更小的、训练更久的模型。
3. **收益在哪里递减。** 超过 1000 倍 Chinchilla 最优后，log-loss 变化变成噪声。

**2026 年的研究轨迹：**

- **数据受限机制。** 网络上高质量 token 数量有限（过滤后约 5-10 万亿英文 token）。前沿预训练正在接近这个上限。合成数据、多语言、多模态和 RLHF 缩放的微调是下一个杠杆。
- **计算倍增技巧。** Muon 优化器、MoE、更好的数据策划——每项都移动绝对常数，而非渐近线。
- **RL 的缩放定律。** 开放问题。早期证据表明 RL 样本的幂律但指数与预训练非常不同。

## 产出物

参见 `outputs/skill-training-budget-estimator.md`。该技能为新训练运行选择 `(N, D, 小时数, GPU)`，给定计算预算、部署约束和目标损失。

## 练习题

1. **简单。** 运行 `code/main.py`。打印计算预算 `1e20`、`1e22`、`1e24` 的 Chinchilla 最优 `(N, D)`。与真实模型表比较。
2. **中等。** 实现 Hoffmann 损失随计算变化的曲线。绘制计算最优前沿的损失 vs `log10(C)`。确定定律预测何时需要 `>10^28` FLOP 才能使交叉熵再降低 0.1。
3. **困难。** 在同一数据集上训练的 5 个微型模型（100K 到 10M 参数）上拟合你自己的缩放定律。估计 `α` 和 `E`。你的指数与已发表的匹配得如何？

## 术语速查表

| 术语 | 人们的说法 | 实际含义 |
|------|-----------|---------|
| 参数 (Parameters, N) | "模型大小" | 非嵌入权重计数；决定容量。 |
| Token (D) | "训练数据" | 看到的训练 token 数；决定参数被利用的程度。 |
| 计算 (Compute, C) | "花费的 FLOP" | 标准 Transformer 约为 `6 × N × D`。 |
| Chinchilla 最优 | "D/N ≈ 20" | 最小化预训练每 FLOP 损失的比例。 |
| 过度训练 | "超过 Chinchilla" | 花费额外训练 FLOP 来节省推理 FLOP；D/N >> 20。 |
| 不可约损失 | "下限" | 缩放定律中的 `E` 项；数据本身的熵。 |
| 涌现能力 | "规模上的突然跳变" | 通常是评分器伪影；连续损失是平滑的。 |
| 有效计算 | "训练效率乘数" | 更好的数据/优化器/架构乘以每个 FLOP 的效果。 |

## 延伸阅读

- [Kaplan 等人（2020）。Scaling Laws for Neural Language Models](https://arxiv.org/abs/2001.08361) —— 第一篇缩放定律论文；欠训练的。
- [Hoffmann 等人（2022）。Training Compute-Optimal Large Language Models](https://arxiv.org/abs/2203.15556) —— Chinchilla。
- [Schaeffer 等人（2023）。Are Emergent Abilities of Large Language Models a Mirage?](https://arxiv.org/abs/2304.15004) —— 涌现作为测量伪影。
- [Sardana, Frankle（2024）。Beyond Chinchilla-Optimal: Accounting for Inference in Language Model Scaling Laws](https://arxiv.org/abs/2401.00448) —— 为什么 Llama 的过度训练对其工作负载是正确的。
- [Jordan 等人（2024）。Muon: An optimizer for hidden layers in neural networks](https://kellerjordan.github.io/posts/muon/) —— 2 倍计算乘数。
