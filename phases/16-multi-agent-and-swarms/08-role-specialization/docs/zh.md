# 角色专业化 — 规划者、批评者、执行者、验证者 | 专业化 批评者 计划 角色

> 2026 年最常见的多 Agent 分解：一个 Agent 规划、一个执行、一个批评或验证。MetaGPT（arXiv:2308.00352）将此形式化为编码到角色提示词中的 SOP——产品经理、架构师、项目经理、工程师、QA 工程师——遵循 `Code = SOP(Team)`。ChatDev（arXiv:2307.07924）通过"聊天链"将设计师、程序员、审查员、测试员链式连接，带有"交际去幻觉"（Agent 显式请求缺失细节）。验证者是承重的：Cemri 等人（MAST，arXiv:2503.13657）显示每个多 Agent 失败都可以追溯到缺失或损坏的验证。PwC 报告 CrewAI 中结构化验证循环带来 7 倍准确率提升（10% → 70%）。

> **【中文解读】** 本节介绍了角色专业化——为每个 Agent 分配明确的角色和专长，提升整体团队效率。

> **【拓展：role specialization→具体应用】** 角色专业化是 CrewAI 的核心理念——每个 Agent 有角色（Role）、目标（Goal）和背景故事（Backstory）。实践表明，明确的角色定义能显著提高多 Agent 协作的效率。但过度专业化也有风险——角色边界过于严格会导致 Agent 拒绝做'不是自己职责'但必要的操作。


**类型：** 学习 + 构建
**语言：** Python（标准库）
**前置条件：** 第 16 阶段 · 04（原始模型），第 16 阶段 · 05（监督者）
**时间：** ~60 分钟

## 问题引入

通用的多 Agent 系统产生通用输出。群聊中的三个编码者编写三种风味的同样平庸代码。你可以添加更多 Agent、更多轮次，仍然无法跨越质量门槛。

修复不是更多 Agent——而是*不同*的 Agent。分配不同的角色。给批评者规划者没有的工具。给验证者一个客观的测试套件。现在系统有了内部分歧和有根据的纠正，而不只是并行猜测。

## 核心概念

### 四个规范角色

**规划者 (Planner)。** 阅读目标，产生步骤列表或规格。工具：知识检索、文档。输出：结构化计划。

**执行者 (Executor)。** 一次阅读一个计划步骤，产生产物。工具：实际工作工具（代码编译器、shell、API 客户端）。输出：产物。

**批评者 (Critic)。** 对照规划者意图阅读执行者的输出。工具：对产物的只读访问、静态分析。输出：接受/拒绝及原因。

**验证者 (Verifier)。** 阅读产物并运行确定性检查。工具：测试运行器、类型检查器、模式验证器。输出：通过/失败及证据。

批评者是主观的、有主见的，通常基于 LLM。验证者是客观的、确定性的，通常基于代码。它们不是同一个角色。

### MetaGPT 的 SOP 模式

MetaGPT（arXiv:2308.00352）将软件工程 SOP 编码为角色提示词：

- **产品经理** 编写 PRD。
- **架构师** 产生系统设计。
- **项目经理** 拆分任务。
- **工程师** 实现。
- **QA 工程师** 运行测试。

每个角色有严格的输入/输出模式。角色提示词说明角色*是什么*以及它*必须产生什么*。`Code = SOP(Team)` 的表述——确定性 SOP 将 LLM 团队变成可预测的流水线。

### ChatDev 的交际去幻觉

ChatDev 添加了一个关键动作：当执行者需要计划中没有的具体细节时，它在继续之前显式地询问设计师。这防止了经典的 LLM 失败——合理地发明细节。

实现：角色提示词包括"当你需要没有被给出的具体信息时，在产生输出之前按名称询问相关角色。"

### 为什么验证者最重要

Cemri 等人（MAST）追踪了 1642 个多 Agent 执行失败。21.3% 是验证缺口——系统发布了没有人检查过的答案。其余 79% 通常可以追溯到"有一个检查静默失败或从未运行"。验证是承重的角色。

PwC 报告（CrewAI 部署，2025），添加结构化验证循环将准确率从 10% 提升到 70%。一个角色带来 7 倍收益。

### 批评者 vs 验证者

- 批评者是审查产物质量的 LLM。主观的。可能被合理的散文欺骗。
- 验证者是在产物上运行的确定性程序。客观的。给出带证据的通过/失败。

两者都用。批评者捕获验证者无法表达的品味问题。验证者捕获批评者看不到的 bug，因为它们只在运行时出现。

### 反模式

你系统中的每个角色都是 LLM，每个角色的输出都是"我觉得不错。"经典的 MAST 失败模式。至少添加一个验证者，其通过/失败由代码决定，而非 LLM。

### 框架映射

- **CrewAI** — `Agent(role, goal, backstory)` 是教科书专业化表面。
- **LangGraph** — 节点可以有专业化的提示词；边强制流水线。
- **AutoGen** — GroupChat 中具有单词名称的角色特定 ConversableAgent。
- **OpenAI Agents SDK** — 角色专业化 Agent 之间的交接工具。

## 动手构建

`code/main.py` 实现了构建简单 Python 函数的 4 角色流水线：

- **规划者** 产生规格。
- **执行者** 生成代码字符串。
- **批评者**（LLM 模拟）标记明显问题。
- **验证者** 在沙箱（`exec`）中针对测试用例运行生成的代码。

演示运行两次：一次执行者产生正确代码（批评者 + 验证者都通过），一次执行者产生不符合规格的代码（批评者错过 bug 因为它看起来合理，验证者捕获它因为测试失败）。

运行：

```
python3 code/main.py
```

## 用框架实现

`outputs/skill-role-designer.md` 接收任务并产生角色名册（3-5 个角色）、每个角色的输入/输出模式和验证者检查。在将 Agent 接入框架之前使用此技能。

## 产出物

检查清单：

- **至少一个确定性验证者。** 永远不要全部是 LLM。
- **每个角色的显式 I/O 模式。** 规划者返回规格，而非散文；执行者读取该模式。
- **交际去幻觉。** 执行者在信息缺失时必须询问规划者；永远不要发明它。
- **批评者/验证者排序。** 先运行批评者（便宜，捕获设计问题），再运行验证者（慢，捕获 bug）。
- **循环预算。** 最多 2 轮批评者-执行者修订循环，然后升级给人类。

## 练习题

1. 运行 `code/main.py`，观察验证者如何捕获批评者错过的 bug。添加一个静态分析检查（计算 `return` 的出现次数）作为额外验证者。它捕获什么运行时测试错过的？
2. 添加第 5 个角色："需求分析师"，将用户愿望翻译为规划者可用的规格。什么交际去幻觉请求应该向上流向它？
3. 阅读 MetaGPT 第 3 节（"Agent"）。列出 MetaGPT 5 个角色中每个的输入/输出模式。
4. 阅读 ChatDev 的聊天链图（arXiv:2307.07924 图 3）。识别交际去幻觉在哪里打破了一个否则会无限循环的循环。
5. PwC 的 7 倍准确率提升来自验证循环。假设三个添加验证者不会帮助的任务——确定性正确性检查不可能或过于昂贵的地方。

## 术语速查表

| 术语 | 人们常说的 | 实际含义 |
|------|-----------|---------|
| 角色专业化 | "不同 Agent，不同工作" | 为规划者/执行者/批评者/验证者角色调优的独特系统提示词。 |
| SOP 模式 | "编码的标准操作程序" | MetaGPT 的框架：严格的 I/O 模式将团队变成流水线。 |
| 交际去幻觉 | "先问再发明" | ChatDev 模式：执行者在细节缺失时询问规划者，而非自己编造。 |
| 批评者 | "LLM 审查员" | 主观的、有主见的审查员。捕获品味问题。可能被合理的散文欺骗。 |
| 验证者 | "确定性检查" | 基于代码的通过/失败。测试运行器、类型检查器、模式验证器。无法被欺骗。 |
| 验证缺口 | "没有人检查" | MAST 失败的 21.3%。答案在没有能捕获 bug 的检查的情况下发布。 |
| 修订循环 | "批评者打回" | 批评者拒绝触发带反馈的执行者重新运行。需要预算。 |
| 全 LLM 反模式 | "我觉得不错" | 每个角色都是 LLM，没有确定性检查。经典的 MAST 失败。 |

## 延伸阅读

- [Hong 等人 — MetaGPT：多 Agent 协作的元编程](https://arxiv.org/abs/2308.00352) — SOP-即-角色-提示词的参考论文
- [Qian 等人 — 软件开发的交际 Agent (ChatDev)](https://arxiv.org/abs/2307.07924) — 聊天链 + 交际去幻觉
- [Cemri 等人 — 为什么多 Agent LLM 系统会失败？](https://arxiv.org/abs/2503.13657) — MAST 分类法；验证缺口占失败的 21.3%
- [CrewAI 文档 — Agent 角色](https://docs.crewai.com/en/introduction) — 生产角色规范表面
